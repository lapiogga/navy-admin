---
phase: 00-project-foundation
plan: 03
type: execute
wave: 3
depends_on: ["00-01", "00-02"]
files_modified:
  - src/shared/ui/DataTable/DataTable.tsx
  - src/shared/ui/DataTable/DataTable.test.tsx
  - src/shared/ui/DataTable/index.ts
  - src/shared/ui/CrudForm/CrudForm.tsx
  - src/shared/ui/CrudForm/index.ts
  - src/shared/ui/DetailModal/DetailModal.tsx
  - src/shared/ui/DetailModal/index.ts
  - src/shared/ui/SearchForm/SearchForm.tsx
  - src/shared/ui/SearchForm/index.ts
  - src/shared/ui/StatusBadge/StatusBadge.tsx
  - src/shared/ui/StatusBadge/StatusBadge.test.tsx
  - src/shared/ui/StatusBadge/index.ts
  - src/shared/ui/ConfirmDialog/ConfirmDialog.tsx
  - src/shared/ui/ConfirmDialog/index.ts
  - src/shared/ui/index.ts
  - src/shared/api/mocks/handlers/demo.ts
  - src/shared/api/mocks/handlers/index.ts
  - src/pages/portal/DemoPage.tsx
  - src/app/router.tsx
  - docs/URL-CONVENTION.md
autonomous: false
requirements:
  - BASE-07
  - BASE-09

must_haves:
  truths:
    - "shared/ui에서 DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, ConfirmDialog를 import할 수 있다"
    - "DataTable에 columns와 request를 전달하면 페이지네이션이 있는 테이블이 렌더링된다"
    - "CrudForm에 필드 정의를 전달하면 폼이 렌더링되고 onFinish로 값이 전달된다"
    - "DetailModal에 visible과 data를 전달하면 읽기 전용 상세 모달이 표시된다"
    - "SearchForm에 필드와 onSearch를 전달하면 검색 조건 폼이 렌더링된다"
    - "데모 페이지에서 전체 공통 컴포넌트가 MSW Mock 데이터와 함께 동작한다"
    - "URL 컨벤션 문서가 docs/URL-CONVENTION.md에 존재한다"
    - "공통 컴포넌트의 단위 테스트가 통과한다"
  artifacts:
    - path: "src/shared/ui/DataTable/DataTable.tsx"
      provides: "ProTable 래퍼 -- 페이지네이션, 정렬 내장 테이블"
      exports: ["DataTable"]
    - path: "src/shared/ui/DataTable/DataTable.test.tsx"
      provides: "DataTable 단위 테스트"
      contains: "describe.*DataTable"
    - path: "src/shared/ui/CrudForm/CrudForm.tsx"
      provides: "ProForm 래퍼 -- CRUD 폼"
      exports: ["CrudForm"]
    - path: "src/shared/ui/DetailModal/DetailModal.tsx"
      provides: "읽기 전용 상세 모달"
      exports: ["DetailModal"]
    - path: "src/shared/ui/SearchForm/SearchForm.tsx"
      provides: "검색 조건 입력 폼"
      exports: ["SearchForm"]
    - path: "src/shared/ui/StatusBadge/StatusBadge.tsx"
      provides: "상태값 배지 컴포넌트"
      exports: ["StatusBadge"]
    - path: "src/shared/ui/StatusBadge/StatusBadge.test.tsx"
      provides: "StatusBadge 단위 테스트"
      contains: "describe.*StatusBadge"
    - path: "src/shared/ui/ConfirmDialog/ConfirmDialog.tsx"
      provides: "확인 다이얼로그 유틸"
      exports: ["showConfirmDialog"]
    - path: "src/shared/ui/index.ts"
      provides: "공통 UI 배럴 export"
      exports: ["DataTable", "CrudForm", "DetailModal", "SearchForm", "StatusBadge", "showConfirmDialog"]
    - path: "docs/URL-CONVENTION.md"
      provides: "URL 경로 규칙 문서"
      contains: "/sys{번호}"
  key_links:
    - from: "src/shared/ui/DataTable/DataTable.tsx"
      to: "@ant-design/pro-components"
      via: "ProTable import"
      pattern: "import.*ProTable.*from.*@ant-design/pro-components"
    - from: "src/shared/ui/CrudForm/CrudForm.tsx"
      to: "@ant-design/pro-components"
      via: "ProForm import"
      pattern: "import.*ProForm.*from.*@ant-design/pro-components"
    - from: "src/pages/portal/DemoPage.tsx"
      to: "src/shared/ui/index.ts"
      via: "공통 컴포넌트 import"
      pattern: "import.*from.*@/shared/ui"
    - from: "src/pages/portal/DemoPage.tsx"
      to: "src/shared/api/mocks/handlers/demo.ts"
      via: "MSW Mock 데이터 fetch"
      pattern: "/api/demo"
---

<objective>
Phase 0에서 동결하는 6개 공통 UI 컴포넌트(DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, ConfirmDialog)를 구현하고, 데모 페이지에서 전체 컴포넌트가 MSW Mock 데이터와 함께 동작하는 것을 확인한다. URL 컨벤션 문서를 작성한다. 핵심 컴포넌트의 단위 테스트를 함께 작성한다.

Purpose: 이후 845개 화면이 이 6개 컴포넌트를 재사용하여 개발된다. Phase 0 이후 이 인터페이스는 변경 금지(frozen contract).
Output: 6개 공통 UI 컴포넌트, 단위 테스트, MSW 연동 데모 페이지, URL 컨벤션 문서
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-project-foundation/00-CONTEXT.md
@.planning/phases/00-project-foundation/00-RESEARCH.md
@.planning/phases/00-project-foundation/00-01-SUMMARY.md
@.planning/phases/00-project-foundation/00-02-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 핵심 타입 -->

From src/shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number; sort?: string[] }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string; errorCode?: string }
export interface ListParams extends PageRequest { keyword?: string; [key: string]: unknown }
```

From src/shared/api/client.ts:
```typescript
export const apiClient: AxiosInstance  // baseURL: '/api'
```

From src/shared/lib/ime.ts:
```typescript
export function useEnterSubmit(onSubmit: () => void): (e: React.KeyboardEvent) => void
```

<!-- Plan 02에서 생성된 핵심 exports -->

From src/shared/config/routes.ts:
```typescript
export const ROUTES: { LOGIN, PORTAL, COMMON: { ROOT, AUTH_GROUP, ... }, SYS01~SYS18: { ROOT } }
```

From src/entities/subsystem/config.ts:
```typescript
export const SUBSYSTEM_META: Record<string, SubsystemMeta>  // 18개 서브시스템 메타
```

From src/shared/api/mocks/handlers/index.ts:
```typescript
export const handlers = [...authHandlers]  // auth 핸들러만 포함
```

From src/test/setup.ts:
```typescript
// @testing-library/jest-dom matchers 자동 등록
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공통 UI 컴포넌트 6개 구현 + 단위 테스트 (DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, ConfirmDialog)</name>
  <files>
    src/shared/ui/DataTable/DataTable.tsx, src/shared/ui/DataTable/DataTable.test.tsx, src/shared/ui/DataTable/index.ts,
    src/shared/ui/CrudForm/CrudForm.tsx, src/shared/ui/CrudForm/index.ts,
    src/shared/ui/DetailModal/DetailModal.tsx, src/shared/ui/DetailModal/index.ts,
    src/shared/ui/SearchForm/SearchForm.tsx, src/shared/ui/SearchForm/index.ts,
    src/shared/ui/StatusBadge/StatusBadge.tsx, src/shared/ui/StatusBadge/StatusBadge.test.tsx, src/shared/ui/StatusBadge/index.ts,
    src/shared/ui/ConfirmDialog/ConfirmDialog.tsx, src/shared/ui/ConfirmDialog/index.ts,
    src/shared/ui/index.ts
  </files>
  <read_first>
    src/shared/api/types.ts (PageRequest, PageResponse 타입 -- DataTable의 request 파라미터에 사용)
    src/shared/lib/ime.ts (useEnterSubmit -- SearchForm에서 사용)
    src/app/styles/antd-theme.ts (테마 토큰 확인 -- StatusBadge 색상 참조)
    .planning/phases/00-project-foundation/00-RESEARCH.md (Code Examples 섹션의 ProTable 래퍼 패턴)
  </read_first>
  <action>
1. src/shared/ui/DataTable/DataTable.tsx 작성 (per D-11, ProTable 래퍼):
   - 제네릭 타입 `T extends Record<string, unknown>`으로 컬럼/데이터 타입 주입
   - Props: `columns` (ProColumns<T>[]), `request` (async (params: PageRequest) => PageResponse<T>), `rowKey` (keyof T & string), `toolBarRender?`, `rowSelection?`, `headerTitle?`
   - ProTable의 `request` prop에서 `params.current`(1-based)를 `page`(0-based)로 변환
   - `search={false}` (별도 SearchForm 사용), `dateFormatter="string"`, `pagination={{ showSizeChanger: true, defaultPageSize: 10 }}`
   - 구체적 코드:
     ```typescript
     import type { ProColumns, ProTableProps } from '@ant-design/pro-components'
     import { ProTable } from '@ant-design/pro-components'
     import type { PageRequest, PageResponse } from '@/shared/api/types'

     export interface DataTableProps<T extends Record<string, unknown>> {
       columns: ProColumns<T>[]
       request: (params: PageRequest) => Promise<PageResponse<T>>
       rowKey: keyof T & string
       toolBarRender?: ProTableProps<T, PageRequest>['toolBarRender']
       rowSelection?: ProTableProps<T, PageRequest>['rowSelection']
       headerTitle?: string
     }

     export function DataTable<T extends Record<string, unknown>>({
       columns, request, rowKey, toolBarRender, rowSelection, headerTitle,
     }: DataTableProps<T>) {
       return (
         <ProTable<T, PageRequest>
           columns={columns}
           request={async (params) => {
             const res = await request({
               page: (params.current ?? 1) - 1,
               size: params.pageSize ?? 10,
             })
             return { data: res.content, success: true, total: res.totalElements }
           }}
           rowKey={rowKey}
           toolBarRender={toolBarRender}
           rowSelection={rowSelection}
           headerTitle={headerTitle}
           pagination={{ showSizeChanger: true, defaultPageSize: 10 }}
           search={false}
           dateFormatter="string"
         />
       )
     }
     ```

2. src/shared/ui/CrudForm/CrudForm.tsx 작성 (per D-11, ProForm 래퍼):
   - 제네릭 타입 `T extends Record<string, unknown>`
   - Props: `fields` (CrudFormField[]), `onFinish` (async (values: T) => boolean), `initialValues?`, `loading?`, `mode` ('create' | 'edit' | 'view')
   - CrudFormField 타입: `{ name: string; label: string; type: 'text' | 'textarea' | 'select' | 'date' | 'number'; required?: boolean; options?: { label: string; value: string | number }[]; disabled?: boolean; rules?: Rule[] }`
   - mode='view'일 때 모든 필드 readonly
   - 구체적 코드:
     ```typescript
     import { ProForm, ProFormText, ProFormTextArea, ProFormSelect, ProFormDatePicker, ProFormDigit } from '@ant-design/pro-components'
     import type { Rule } from 'antd/es/form'

     export interface CrudFormField {
       name: string
       label: string
       type: 'text' | 'textarea' | 'select' | 'date' | 'number'
       required?: boolean
       options?: { label: string; value: string | number }[]
       disabled?: boolean
       rules?: Rule[]
       placeholder?: string
       colSpan?: number
     }

     export interface CrudFormProps<T extends Record<string, unknown>> {
       fields: CrudFormField[]
       onFinish: (values: T) => Promise<boolean>
       initialValues?: Partial<T>
       loading?: boolean
       mode?: 'create' | 'edit' | 'view'
     }

     export function CrudForm<T extends Record<string, unknown>>({
       fields, onFinish, initialValues, loading, mode = 'create',
     }: CrudFormProps<T>) {
       const isView = mode === 'view'

       const renderField = (field: CrudFormField) => {
         const commonProps = {
           key: field.name,
           name: field.name,
           label: field.label,
           placeholder: field.placeholder,
           disabled: isView || field.disabled,
           rules: field.required ? [{ required: true, message: `${field.label}을(를) 입력하세요` }, ...(field.rules ?? [])] : field.rules,
         }
         switch (field.type) {
           case 'textarea': return <ProFormTextArea {...commonProps} />
           case 'select': return <ProFormSelect {...commonProps} options={field.options} />
           case 'date': return <ProFormDatePicker {...commonProps} />
           case 'number': return <ProFormDigit {...commonProps} />
           default: return <ProFormText {...commonProps} />
         }
       }

       return (
         <ProForm<T>
           onFinish={onFinish}
           initialValues={initialValues as T}
           loading={loading}
           submitter={isView ? false : undefined}
         >
           {fields.map(renderField)}
         </ProForm>
       )
     }
     ```

3. src/shared/ui/DetailModal/DetailModal.tsx 작성 (per D-11):
   - Props: `open` (boolean), `onClose` (() => void), `title` (string), `data` (Record<string, unknown> | null), `fields` (DetailField[])
   - DetailField: `{ key: string; label: string; render?: (value: unknown) => React.ReactNode }`
   - antd Modal + Descriptions 컴포넌트 사용
   - 구체적 코드:
     ```typescript
     import { Modal, Descriptions } from 'antd'

     export interface DetailField {
       key: string
       label: string
       render?: (value: unknown) => React.ReactNode
     }

     export interface DetailModalProps {
       open: boolean
       onClose: () => void
       title: string
       data: Record<string, unknown> | null
       fields: DetailField[]
       width?: number
     }

     export function DetailModal({ open, onClose, title, data, fields, width = 640 }: DetailModalProps) {
       return (
         <Modal open={open} onCancel={onClose} title={title} footer={null} width={width}>
           {data && (
             <Descriptions column={1} bordered size="small">
               {fields.map((field) => (
                 <Descriptions.Item key={field.key} label={field.label}>
                   {field.render ? field.render(data[field.key]) : String(data[field.key] ?? '-')}
                 </Descriptions.Item>
               ))}
             </Descriptions>
           )}
         </Modal>
       )
     }
     ```

4. src/shared/ui/SearchForm/SearchForm.tsx 작성 (per D-11, D-24):
   - Props: `fields` (SearchField[]), `onSearch` ((values: Record<string, unknown>) => void), `onReset?` (() => void)
   - SearchField: `{ name: string; label: string; type: 'text' | 'select' | 'date' | 'dateRange'; options?: { label: string; value: string | number }[] }`
   - Enter 핸들링에 `useEnterSubmit` 훅 적용 (per D-24)
   - antd Form + Row/Col 레이아웃, 검색/초기화 버튼
   - 구체적 코드:
     ```typescript
     import { Form, Input, Select, DatePicker, Button, Row, Col, Space } from 'antd'
     import { SearchOutlined, ReloadOutlined } from '@ant-design/icons'
     import { useEnterSubmit } from '@/shared/lib/ime'

     const { RangePicker } = DatePicker

     export interface SearchField {
       name: string
       label: string
       type: 'text' | 'select' | 'date' | 'dateRange'
       options?: { label: string; value: string | number }[]
       placeholder?: string
     }

     export interface SearchFormProps {
       fields: SearchField[]
       onSearch: (values: Record<string, unknown>) => void
       onReset?: () => void
     }

     export function SearchForm({ fields, onSearch, onReset }: SearchFormProps) {
       const [form] = Form.useForm()
       const handleEnter = useEnterSubmit(() => form.submit())

       const handleReset = () => {
         form.resetFields()
         onReset?.()
       }

       const renderField = (field: SearchField) => {
         switch (field.type) {
           case 'select':
             return <Select options={field.options} placeholder={field.placeholder ?? '선택'} allowClear />
           case 'date':
             return <DatePicker className="w-full" />
           case 'dateRange':
             return <RangePicker className="w-full" />
           default:
             return <Input placeholder={field.placeholder ?? '검색어 입력'} onKeyDown={handleEnter} />
         }
       }

       return (
         <Form form={form} onFinish={onSearch} layout="inline" className="mb-4">
           <Row gutter={[16, 16]} className="w-full">
             {fields.map((field) => (
               <Col key={field.name} xs={24} sm={12} md={8} lg={6}>
                 <Form.Item name={field.name} label={field.label}>
                   {renderField(field)}
                 </Form.Item>
               </Col>
             ))}
             <Col>
               <Space>
                 <Button type="primary" htmlType="submit" icon={<SearchOutlined />}>검색</Button>
                 <Button onClick={handleReset} icon={<ReloadOutlined />}>초기화</Button>
               </Space>
             </Col>
           </Row>
         </Form>
       )
     }
     ```

5. src/shared/ui/StatusBadge/StatusBadge.tsx 작성 (per D-11):
   - Props: `status` (string), `colorMap?` (Record<string, string>), `labelMap?` (Record<string, string>)
   - 기본 colorMap: `{ 승인: 'green', 반려: 'red', 대기: 'orange', 완료: 'blue', 진행: 'cyan' }`
   - antd Tag 컴포넌트 사용
   - 구체적 코드:
     ```typescript
     import { Tag } from 'antd'

     const DEFAULT_COLOR_MAP: Record<string, string> = {
       승인: 'green', 완료: 'blue', 진행: 'cyan',
       대기: 'orange', 반려: 'red', 삭제: 'default',
     }

     export interface StatusBadgeProps {
       status: string
       colorMap?: Record<string, string>
       labelMap?: Record<string, string>
     }

     export function StatusBadge({ status, colorMap, labelMap }: StatusBadgeProps) {
       const mergedColors = { ...DEFAULT_COLOR_MAP, ...colorMap }
       const label = labelMap?.[status] ?? status
       const color = mergedColors[status] ?? 'default'
       return <Tag color={color}>{label}</Tag>
     }
     ```

6. src/shared/ui/ConfirmDialog/ConfirmDialog.tsx 작성 (per D-11):
   - `showConfirmDialog` 함수형 API (antd Modal.confirm 래퍼)
   - Props: `title`, `content`, `onConfirm` (async () => void), `danger?` (boolean)
   - 구체적 코드:
     ```typescript
     import { Modal } from 'antd'
     import { ExclamationCircleOutlined } from '@ant-design/icons'

     export interface ConfirmDialogOptions {
       title: string
       content: string
       onConfirm: () => Promise<void> | void
       danger?: boolean
     }

     export function showConfirmDialog({ title, content, onConfirm, danger = false }: ConfirmDialogOptions) {
       Modal.confirm({
         title,
         icon: <ExclamationCircleOutlined />,
         content,
         okText: '확인',
         cancelText: '취소',
         okButtonProps: { danger },
         onOk: onConfirm,
       })
     }
     ```

7. 각 컴포넌트 폴더에 index.ts 배럴 export 생성:
   ```typescript
   // src/shared/ui/DataTable/index.ts
   export { DataTable } from './DataTable'
   export type { DataTableProps } from './DataTable'
   ```
   (다른 5개 컴포넌트도 동일 패턴)

8. src/shared/ui/index.ts 통합 배럴 export:
   ```typescript
   export { DataTable } from './DataTable'
   export type { DataTableProps } from './DataTable'
   export { CrudForm } from './CrudForm'
   export type { CrudFormProps, CrudFormField } from './CrudForm'
   export { DetailModal } from './DetailModal'
   export type { DetailModalProps, DetailField } from './DetailModal'
   export { SearchForm } from './SearchForm'
   export type { SearchFormProps, SearchField } from './SearchForm'
   export { StatusBadge } from './StatusBadge'
   export type { StatusBadgeProps } from './StatusBadge'
   export { showConfirmDialog } from './ConfirmDialog'
   export type { ConfirmDialogOptions } from './ConfirmDialog'
   ```

9. **단위 테스트 작성:**

   src/shared/ui/StatusBadge/StatusBadge.test.tsx:
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { render, screen } from '@testing-library/react'
   import { StatusBadge } from './StatusBadge'

   describe('StatusBadge', () => {
     it('기본 상태값 텍스트를 렌더링한다', () => {
       render(<StatusBadge status="승인" />)
       expect(screen.getByText('승인')).toBeInTheDocument()
     })

     it('커스텀 labelMap으로 라벨을 변경할 수 있다', () => {
       render(<StatusBadge status="승인" labelMap={{ 승인: 'Approved' }} />)
       expect(screen.getByText('Approved')).toBeInTheDocument()
     })

     it('알 수 없는 상태값도 default 색상으로 렌더링된다', () => {
       render(<StatusBadge status="기타" />)
       expect(screen.getByText('기타')).toBeInTheDocument()
     })

     it('5종 기본 상태값이 모두 렌더링된다', () => {
       const { container } = render(
         <>
           <StatusBadge status="승인" />
           <StatusBadge status="반려" />
           <StatusBadge status="대기" />
           <StatusBadge status="완료" />
           <StatusBadge status="진행" />
         </>
       )
       expect(container.querySelectorAll('.ant-tag')).toHaveLength(5)
     })
   })
   ```

   src/shared/ui/DataTable/DataTable.test.tsx:
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { DataTable } from './DataTable'

   describe('DataTable', () => {
     it('DataTable 컴포넌트가 export되어 있다', () => {
       expect(DataTable).toBeDefined()
       expect(typeof DataTable).toBe('function')
     })

     it('DataTableProps 타입이 올바른 인터페이스를 가진다', () => {
       // 타입 수준 검증 -- DataTable은 columns, request, rowKey를 필수 props로 받는다
       // 컴파일 타임 체크이므로 이 파일이 tsc --noEmit을 통과하면 증명됨
       expect(true).toBe(true)
     })
   })
   ```
  </action>
  <verify>
    <automated>cd marine-admin && npx tsc --noEmit 2>&1 | head -10 && npx vitest run src/shared/ui --reporter=dot 2>&1 | tail -10</automated>
    TypeScript 컴파일 오류 없음. StatusBadge.test.tsx, DataTable.test.tsx 테스트 통과.
    `ls src/shared/ui/*/index.ts | wc -l` 결과 6 (DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, ConfirmDialog).
  </verify>
  <acceptance_criteria>
    - src/shared/ui/DataTable/DataTable.tsx에 `export function DataTable<T extends Record<string, unknown>>` 패턴이 존재
    - src/shared/ui/DataTable/DataTable.tsx에 `ProTable` import와 `page: (params.current ?? 1) - 1` 변환이 존재
    - src/shared/ui/CrudForm/CrudForm.tsx에 `export function CrudForm<T extends Record<string, unknown>>` 패턴이 존재
    - src/shared/ui/CrudForm/CrudForm.tsx에 `ProForm`, `ProFormText`, `ProFormSelect` import가 존재
    - src/shared/ui/DetailModal/DetailModal.tsx에 `Modal`, `Descriptions` import와 `export function DetailModal`이 존재
    - src/shared/ui/SearchForm/SearchForm.tsx에 `useEnterSubmit` import가 존재 (D-24 적용 확인)
    - src/shared/ui/StatusBadge/StatusBadge.tsx에 DEFAULT_COLOR_MAP 객체와 `Tag` import가 존재
    - src/shared/ui/ConfirmDialog/ConfirmDialog.tsx에 `Modal.confirm` 패턴과 `export function showConfirmDialog`가 존재
    - src/shared/ui/index.ts에 6개 컴포넌트 모두 re-export되어 있음
    - src/shared/ui/StatusBadge/StatusBadge.test.tsx 테스트 파일이 존재하고 5종 상태값 테스트 포함
    - src/shared/ui/DataTable/DataTable.test.tsx 테스트 파일이 존재
    - `npx tsc --noEmit` 실행 시 에러 0개
    - `npx vitest run src/shared/ui` 실행 시 모든 테스트 통과
  </acceptance_criteria>
  <done>6개 공통 UI 컴포넌트가 shared/ui에 구현되고, 배럴 export를 통해 `import { DataTable, CrudForm, ... } from '@/shared/ui'`로 접근 가능하며, StatusBadge와 DataTable의 단위 테스트가 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: 데모 페이지 (공통 컴포넌트 통합 검증) + MSW 데모 핸들러 + URL 컨벤션 문서</name>
  <files>
    src/shared/api/mocks/handlers/demo.ts, src/shared/api/mocks/handlers/index.ts,
    src/pages/portal/DemoPage.tsx, src/app/router.tsx, docs/URL-CONVENTION.md
  </files>
  <read_first>
    src/shared/ui/index.ts (Task 1에서 생성, 사용 가능한 컴포넌트 확인)
    src/shared/ui/DataTable/DataTable.tsx (DataTableProps 인터페이스)
    src/shared/ui/CrudForm/CrudForm.tsx (CrudFormProps, CrudFormField 인터페이스)
    src/shared/ui/SearchForm/SearchForm.tsx (SearchFormProps, SearchField 인터페이스)
    src/shared/ui/DetailModal/DetailModal.tsx (DetailModalProps, DetailField 인터페이스)
    src/shared/ui/StatusBadge/StatusBadge.tsx (StatusBadgeProps 인터페이스)
    src/shared/api/types.ts (PageRequest, PageResponse 타입)
    src/shared/api/mocks/handlers/index.ts (기존 handlers 배열)
    src/shared/config/routes.ts (ROUTES 상수)
    src/entities/subsystem/config.ts (SUBSYSTEM_META)
    src/app/router.tsx (현재 라우터 구조)
  </read_first>
  <action>
1. src/shared/api/mocks/handlers/demo.ts 작성 -- 데모용 Mock 데이터 핸들러:
   ```typescript
   import { http, HttpResponse } from 'msw'
   import { faker } from '@faker-js/faker/locale/ko'
   import type { PageResponse, ApiResult } from '@/shared/api/types'

   interface DemoItem {
     id: string
     title: string
     author: string
     status: string
     createdAt: string
     unit: string
   }

   function generateDemoItems(count: number): DemoItem[] {
     return Array.from({ length: count }, () => ({
       id: faker.string.uuid(),
       title: faker.lorem.sentence(),
       author: faker.person.fullName(),
       status: faker.helpers.arrayElement(['승인', '반려', '대기', '완료', '진행']),
       createdAt: faker.date.recent({ days: 30 }).toISOString().split('T')[0],
       unit: faker.helpers.arrayElement(['해병대사령부', '1사단', '2사단', '교육훈련단']),
     }))
   }

   const allItems = generateDemoItems(87)

   export const demoHandlers = [
     http.get('/api/demo/items', ({ request }) => {
       const url = new URL(request.url)
       const page = Number(url.searchParams.get('page') ?? '0')
       const size = Number(url.searchParams.get('size') ?? '10')
       const keyword = url.searchParams.get('keyword') ?? ''

       let filtered = allItems
       if (keyword) {
         filtered = allItems.filter((item) =>
           item.title.includes(keyword) || item.author.includes(keyword),
         )
       }

       const start = page * size
       const content = filtered.slice(start, start + size)

       const result: ApiResult<PageResponse<DemoItem>> = {
         success: true,
         data: {
           content,
           totalElements: filtered.length,
           totalPages: Math.ceil(filtered.length / size),
           size,
           number: page,
         },
       }
       return HttpResponse.json(result)
     }),

     http.post('/api/demo/items', async ({ request }) => {
       const body = await request.json()
       const result: ApiResult<DemoItem> = {
         success: true,
         data: { id: faker.string.uuid(), ...(body as Omit<DemoItem, 'id'>) },
         message: '등록되었습니다',
       }
       return HttpResponse.json(result, { status: 201 })
     }),

     http.delete('/api/demo/items/:id', () => {
       const result: ApiResult = { success: true, data: undefined as never, message: '삭제되었습니다' }
       return HttpResponse.json(result)
     }),
   ]
   ```

2. src/shared/api/mocks/handlers/index.ts 업데이트:
   ```typescript
   import { authHandlers } from './auth'
   import { demoHandlers } from './demo'
   export const handlers = [...authHandlers, ...demoHandlers]
   ```

3. src/pages/portal/DemoPage.tsx 작성 -- 6개 공통 컴포넌트 통합 데모:
   ```typescript
   import { useState } from 'react'
   import { Button, Card, Space, Typography, Divider, message } from 'antd'
   import { PlusOutlined } from '@ant-design/icons'
   import {
     DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, showConfirmDialog,
   } from '@/shared/ui'
   import type { CrudFormField, SearchField, DetailField } from '@/shared/ui'
   import { apiClient } from '@/shared/api/client'
   import type { PageRequest, PageResponse, ApiResult } from '@/shared/api/types'

   const { Title } = Typography

   interface DemoItem extends Record<string, unknown> {
     id: string
     title: string
     author: string
     status: string
     createdAt: string
     unit: string
   }

   // SearchForm 필드 정의
   const searchFields: SearchField[] = [
     { name: 'keyword', label: '검색어', type: 'text', placeholder: '제목 또는 작성자' },
     { name: 'status', label: '상태', type: 'select', options: [
       { label: '승인', value: '승인' },
       { label: '반려', value: '반려' },
       { label: '대기', value: '대기' },
     ]},
     { name: 'dateRange', label: '기간', type: 'dateRange' },
   ]

   // CrudForm 필드 정의
   const formFields: CrudFormField[] = [
     { name: 'title', label: '제목', type: 'text', required: true, placeholder: '제목을 입력하세요' },
     { name: 'author', label: '작성자', type: 'text', required: true },
     { name: 'unit', label: '소속부대', type: 'select', options: [
       { label: '해병대사령부', value: '해병대사령부' },
       { label: '1사단', value: '1사단' },
       { label: '2사단', value: '2사단' },
     ]},
     { name: 'status', label: '상태', type: 'select', required: true, options: [
       { label: '대기', value: '대기' },
       { label: '진행', value: '진행' },
     ]},
   ]

   // DetailModal 필드 정의
   const detailFields: DetailField[] = [
     { key: 'id', label: 'ID' },
     { key: 'title', label: '제목' },
     { key: 'author', label: '작성자' },
     { key: 'unit', label: '소속부대' },
     { key: 'status', label: '상태', render: (v) => <StatusBadge status={String(v)} /> },
     { key: 'createdAt', label: '작성일' },
   ]

   export default function DemoPage() {
     const [searchParams, setSearchParams] = useState<Record<string, unknown>>({})
     const [detailOpen, setDetailOpen] = useState(false)
     const [detailData, setDetailData] = useState<DemoItem | null>(null)
     const [formOpen, setFormOpen] = useState(false)

     const fetchItems = async (params: PageRequest): Promise<PageResponse<DemoItem>> => {
       const res = await apiClient.get<unknown, ApiResult<PageResponse<DemoItem>>>('/demo/items', {
         params: { ...params, ...searchParams },
       })
       return res.data
     }

     const handleCreate = async (values: Record<string, unknown>) => {
       await apiClient.post('/demo/items', values)
       message.success('등록되었습니다')
       setFormOpen(false)
       return true
     }

     const handleDelete = (item: DemoItem) => {
       showConfirmDialog({
         title: '삭제 확인',
         content: `"${item.title}"을(를) 삭제하시겠습니까?`,
         danger: true,
         onConfirm: async () => {
           await apiClient.delete(`/demo/items/${item.id}`)
           message.success('삭제되었습니다')
         },
       })
     }

     return (
       <div>
         <Title level={4}>공통 컴포넌트 데모 (Phase 0 Frozen Contract)</Title>

         <Card title="1. SearchForm -- 검색 폼" className="mb-4">
           <SearchForm
             fields={searchFields}
             onSearch={setSearchParams}
             onReset={() => setSearchParams({})}
           />
         </Card>

         <Card title="2. DataTable -- 데이터 테이블" className="mb-4">
           <DataTable<DemoItem>
             columns={[
               { title: '제목', dataIndex: 'title', ellipsis: true },
               { title: '작성자', dataIndex: 'author', width: 100 },
               { title: '소속부대', dataIndex: 'unit', width: 120 },
               { title: '상태', dataIndex: 'status', width: 80, render: (status: string) => <StatusBadge status={status} /> },
               { title: '작성일', dataIndex: 'createdAt', width: 110 },
               {
                 title: '관리',
                 width: 120,
                 render: (_, record) => (
                   <Space>
                     <a onClick={() => { setDetailData(record); setDetailOpen(true) }}>상세</a>
                     <a onClick={() => handleDelete(record)} className="text-red-500">삭제</a>
                   </Space>
                 ),
               },
             ]}
             request={fetchItems}
             rowKey="id"
             headerTitle="데모 목록"
             toolBarRender={() => [
               <Button key="create" type="primary" icon={<PlusOutlined />} onClick={() => setFormOpen(true)}>
                 등록
               </Button>,
             ]}
           />
         </Card>

         <Card title="3. StatusBadge -- 상태 배지" className="mb-4">
           <Space>
             <StatusBadge status="승인" />
             <StatusBadge status="반려" />
             <StatusBadge status="대기" />
             <StatusBadge status="완료" />
             <StatusBadge status="진행" />
           </Space>
         </Card>

         <Divider />

         {/* 4. DetailModal */}
         <DetailModal
           open={detailOpen}
           onClose={() => setDetailOpen(false)}
           title="상세 정보"
           data={detailData}
           fields={detailFields}
         />

         {/* 5. CrudForm (모달로 표시) */}
         {formOpen && (
           <Card title="6. CrudForm -- 등록 폼" className="mb-4">
             <CrudForm<Record<string, unknown>>
               fields={formFields}
               onFinish={handleCreate}
               mode="create"
             />
             <Button onClick={() => setFormOpen(false)} className="mt-2">닫기</Button>
           </Card>
         )}
       </div>
     )
   }
   ```

4. src/app/router.tsx 업데이트 -- 데모 페이지 라우트 추가:
   - 기존 children 배열의 portal index 이후에 데모 라우트 추가:
     ```typescript
     // 기존 { index: true, element: <PortalPage /> } 다음에 추가
     { path: 'demo', element: withSuspense(lazy(() => import('@/pages/portal/DemoPage'))) },
     ```
   - PortalPage에서 "공통 컴포넌트 데모" 링크를 추가하거나, 직접 /demo URL로 접근 가능하게.

5. docs/URL-CONVENTION.md 작성 (per BASE-09):
   ```markdown
   # URL Convention (해병대 행정포탈)

   ## 기본 경로

   | 경로 | 설명 |
   |------|------|
   | `/login` | 로그인 페이지 |
   | `/` | 메인 포탈 대시보드 |
   | `/demo` | 공통 컴포넌트 데모 (Phase 0) |

   ## 서브시스템 경로 패턴

   `/sys{번호}/{대메뉴슬러그}/{소메뉴슬러그}`

   예시:
   - `/sys01/application/write` -- 초과근무 신청서 작성
   - `/sys01/application/list` -- 초과근무 신청 목록
   - `/sys02/survey/create` -- 설문 생성

   ## 서브시스템 번호 대응표

   | 번호 | 시스템 | URL prefix | pages/ 폴더 |
   |------|--------|-----------|-------------|
   | 01 | 초과근무관리체계 | /sys01 | sys01-overtime |
   | 02 | 설문종합관리체계 | /sys02 | sys02-survey |
   | 03 | 성과관리체계 | /sys03 | sys03-performance |
   | 04 | 인증서발급신청체계 | /sys04 | sys04-certificate |
   | 05 | 행정규칙포탈체계 | /sys05 | sys05-admin-rules |
   | 06 | 해병대규정관리체계 | /sys06 | sys06-regulations |
   | 07 | 군사자료관리체계 | /sys07 | sys07-mil-data |
   | 08 | 부대계보관리체계 | /sys08 | sys08-unit-lineage |
   | 09 | 영현보훈체계 | /sys09 | sys09-memorial |
   | 10 | 주말버스예약관리체계 | /sys10 | sys10-weekend-bus |
   | 11 | 연구자료종합관리체계 | /sys11 | sys11-research |
   | 12 | 지시건의사항관리체계 | /sys12 | sys12-directives |
   | 13 | 지식관리체계 | /sys13 | sys13-knowledge |
   | 14 | 나의 제언 | /sys14 | sys14-suggestion |
   | 15 | 보안일일결산체계 | /sys15 | sys15-security |
   | 16 | 회의실예약관리체계 | /sys16 | sys16-meeting-room |
   | 17 | 검열결과관리체계 | /sys17 | sys17-inspection |
   | 18 | 직무기술서관리체계 | /sys18 | sys18-job-desc |

   ## 공통 기능 경로

   `/common/{기능슬러그}`

   | 경로 | 설명 |
   |------|------|
   | /common/auth-group | 권한관리 |
   | /common/code-mgmt | 코드관리 |
   | /common/board | 공통게시판 |
   | /common/approval | 결재관리 |
   | /common/system-mgr | 시스템관리 |
   | /common/menu-mgmt | 메뉴관리 |

   ## 경로 상수 참조

   모든 경로는 `src/shared/config/routes.ts`의 `ROUTES` 상수를 사용한다.
   URL을 하드코딩하지 않는다.

   ```typescript
   import { ROUTES } from '@/shared/config/routes'
   navigate(ROUTES.SYS01.ROOT)  // '/sys01'
   navigate(ROUTES.COMMON.AUTH_GROUP)  // '/common/auth-group'
   ```

   ## 서브시스템 메타데이터

   `src/entities/subsystem/config.ts`의 `SUBSYSTEM_META` 객체에서 서브시스템 정보를 조회한다.

   ## Phase 0 이후 변경 금지 항목

   - URL prefix 패턴 (`/sys{번호}`)
   - 서브시스템 번호 매핑
   - 공통 기능 경로 패턴 (`/common/{슬러그}`)
   - ROUTES 상수 키 이름
   ```
  </action>
  <verify>
    <automated>cd marine-admin && npx tsc --noEmit 2>&1 | head -10 && npx vitest run --reporter=dot 2>&1 | tail -10</automated>
    TypeScript 컴파일 오류 없음. 전체 테스트 스위트 통과.
    `npm run dev` 실행 후 브라우저에서:
    - admin/1234 로그인 후 /demo 접근 시 데모 페이지 표시
    - DataTable에 Mock 데이터 87건이 페이지네이션으로 표시
    - SearchForm에서 검색 시 테이블 필터링
    - 상세 클릭 시 DetailModal 표시
    - 등록 클릭 시 CrudForm 표시
    - StatusBadge 5종 색상 표시
  </verify>
  <acceptance_criteria>
    - src/shared/api/mocks/handlers/demo.ts에 `http.get('/api/demo/items'` 핸들러가 존재
    - src/shared/api/mocks/handlers/index.ts에 `demoHandlers`가 spread되어 있음
    - src/pages/portal/DemoPage.tsx에 `import { DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, showConfirmDialog } from '@/shared/ui'` import가 존재
    - src/pages/portal/DemoPage.tsx에 `<DataTable`, `<CrudForm`, `<DetailModal`, `<SearchForm`, `<StatusBadge` JSX 태그가 존재
    - src/pages/portal/DemoPage.tsx에 `showConfirmDialog` 호출이 존재
    - src/app/router.tsx에 `path: 'demo'` 라우트가 존재
    - docs/URL-CONVENTION.md 파일이 존재하고 `/sys{번호}` 패턴 설명이 포함
    - docs/URL-CONVENTION.md에 18개 서브시스템 URL 대응표가 포함
    - `npx tsc --noEmit` 실행 시 에러 0개
    - `npx vitest run` 실행 시 모든 테스트 통과
  </acceptance_criteria>
  <done>데모 페이지에서 6개 공통 컴포넌트(DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, ConfirmDialog)가 MSW Mock 데이터와 함께 동작하고, URL 컨벤션이 문서화된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 전체 통합 검증</name>
  <files>없음 (검증 전용 체크포인트)</files>
  <action>
Phase 0 전체 산출물을 사용자가 브라우저에서 직접 확인한다.

검증 대상: FSD 프로젝트 구조, Tailwind+antd 테마, Vitest 테스트 인프라, Zustand 상태관리, MSW Mock API, React Router 18개 서브시스템 라우팅, ProLayout 레이아웃, ProtectedRoute 인증 + 세션 자동 체크, 6개 공통 컴포넌트(DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, ConfirmDialog), 단위 테스트, 데모 페이지, URL 컨벤션 문서.

사용자 검증 절차:
1. npm run dev로 앱 시작
2. 브라우저에서 http://localhost:5173 접속 -- /login으로 리다이렉트 확인
3. admin / 1234 입력 후 로그인 -- 대시보드 이동 확인
4. ProLayout 사이드바에 18개 서브시스템 메뉴 표시 확인
5. 헤더에 사용자 정보(계급, 이름, 소속부대) 표시 확인
6. 아무 서브시스템 카드(예: 초과근무관리체계) 클릭 -- /sys01 이동, placeholder 표시 확인
7. /demo URL로 이동 -- 데모 페이지 표시 확인
8. DataTable에 Mock 데이터(87건)가 페이지네이션과 함께 표시 확인
9. SearchForm에서 검색어 입력 후 검색 버튼 클릭 -- 테이블 필터링 확인
10. 테이블 행의 상세 클릭 -- DetailModal 팝업 확인
11. 등록 버튼 클릭 -- CrudForm 표시 확인
12. StatusBadge 5종(승인/반려/대기/완료/진행) 색상 확인
13. 테이블 행의 삭제 클릭 -- ConfirmDialog 팝업 확인
14. 로그아웃 -- /login 이동 확인
15. 브라우저 DevTools > Network 탭에서 MSW 인터셉트 확인

모든 항목이 확인되면 "approved" 입력. 문제가 있으면 구체적으로 기술.
  </action>
  <verify>
    <automated>cd marine-admin && npx vitest run --reporter=dot 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>사용자가 Phase 0 전체 산출물을 브라우저에서 검증하고 approved를 입력한다.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` 타입 체크 통과
2. `npx vitest run` 전체 테스트 스위트 통과
3. 6개 공통 컴포넌트가 `@/shared/ui`에서 import 가능
4. 데모 페이지(/demo)에서 전 컴포넌트가 MSW 데이터와 함께 동작
5. URL 컨벤션 문서(docs/URL-CONVENTION.md) 존재
6. Phase 0 ROADMAP Success Criteria 5개 항목 모두 충족:
   - npm run dev로 FSD 구조 프로젝트 실행 가능
   - shared/ui 컴포넌트 import하여 샘플 화면 생성 가능
   - MSW Mock 응답이 네트워크 탭에 보임
   - /sys01/ ~ /sys18/ URL lazy-loading 동작
   - authStore Mock 사용자 설정 시 ProtectedRoute 동작
</verification>

<success_criteria>
- 6개 공통 UI 컴포넌트 모두 shared/ui에서 단일 import로 사용 가능
- 데모 페이지에서 DataTable이 MSW 데이터를 페이지네이션으로 렌더링
- SearchForm 검색 시 DataTable 결과 필터링
- DetailModal, CrudForm, ConfirmDialog가 사용자 인터랙션에 올바르게 동작
- URL 컨벤션 문서가 18개 서브시스템 + 공통 기능 경로를 포함
- 전체 테스트 스위트(date, ime, client, authStore, auth handlers, router, StatusBadge, DataTable)가 통과
</success_criteria>

<output>
After completion, create `.planning/phases/00-project-foundation/00-03-SUMMARY.md`
</output>
