---
phase: 01-99
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/common/index.tsx
  - navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx
  - navy-admin/src/pages/common/code-mgmt/CodeListPanel.tsx
  - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx
  - navy-admin/src/entities/code/types.ts
  - navy-admin/src/entities/code/api.ts
  - navy-admin/src/entities/code/index.ts
  - navy-admin/src/features/common/hooks/useCodeOptions.ts
  - navy-admin/src/shared/api/mocks/handlers/common/code.ts
  - navy-admin/src/shared/api/mocks/handlers/common/index.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/shared/config/routes.ts
  - navy-admin/src/__tests__/common/code.test.ts
  - navy-admin/src/__tests__/common/useCodeOptions.test.ts
autonomous: true
requirements: [COM-07, COM-08]

must_haves:
  truths:
    - "코드그룹 목록이 DataTable에 표시되고, 등록/수정/삭제가 동작한다"
    - "코드그룹을 선택하면 우측 패널에 해당 그룹의 코드 목록이 표시된다"
    - "코드를 등록/수정/삭제하면 우측 코드 목록에 즉시 반영된다"
    - "useCodeOptions 훅으로 groupCode를 전달하면 해당 코드 옵션이 5분 캐싱된다"
    - "/common/code-mgmt URL로 접근하면 코드관리 화면이 표시된다"
  artifacts:
    - path: "navy-admin/src/entities/code/types.ts"
      provides: "CodeGroup, Code 타입 정의"
      contains: "interface CodeGroup"
    - path: "navy-admin/src/entities/code/api.ts"
      provides: "코드그룹/코드 CRUD API 함수"
      exports: ["codeGroupApi", "codeApi"]
    - path: "navy-admin/src/shared/api/mocks/handlers/common/code.ts"
      provides: "코드 관련 MSW 핸들러"
      exports: ["codeHandlers"]
    - path: "navy-admin/src/features/common/hooks/useCodeOptions.ts"
      provides: "코드 옵션 전역 캐싱 훅"
      exports: ["useCodeOptions"]
    - path: "navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx"
      provides: "코드관리 2패널 페이지"
      exports: ["CodeManagementPage"]
  key_links:
    - from: "navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx"
      to: "/api/common/code-groups"
      via: "codeGroupApi.list -> DataTable request prop"
      pattern: "codeGroupApi\\.list"
    - from: "navy-admin/src/features/common/hooks/useCodeOptions.ts"
      to: "/api/common/codes/options/:groupCode"
      via: "TanStack Query useQuery with staleTime 300000"
      pattern: "staleTime.*5.*60.*1000"
    - from: "navy-admin/src/shared/api/mocks/handlers/index.ts"
      to: "navy-admin/src/shared/api/mocks/handlers/common/index.ts"
      via: "spread import"
      pattern: "commonHandlers"
---

<objective>
코드관리(COM-07, COM-08)를 구현하고 공통 기능 라우팅 인프라를 확립한다.

Purpose: 코드관리는 18개 서브시스템 모든 Select/Radio 옵션의 소스이다. Phase 1의 다른 모든 화면이 useCodeOptions 훅을 통해 코드 옵션을 참조하므로 반드시 선행 구현되어야 한다. 또한 pages/common/index.tsx의 서브경로 라우팅을 구성하여 이후 플랜들이 페이지만 추가하면 되는 구조를 확립한다.

Output: 코드그룹/코드 CRUD 화면, MSW 핸들러, useCodeOptions 훅, 공통 기능 라우팅 인프라
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-99/01-RESEARCH.md
@.planning/phases/01-99/01-UI-SPEC.md

<interfaces>
<!-- Phase 0 동결 인터페이스 -- 변경 금지, 활용만 가능 -->

From navy-admin/src/shared/api/types.ts:
```typescript
export interface PageRequest {
  page: number
  size: number
  sort?: string[]
}
export interface PageResponse<T> {
  content: T[]
  totalElements: number
  totalPages: number
  size: number
  number: number
}
export interface ApiResult<T = void> {
  success: boolean
  data: T
  message?: string
  errorCode?: string
}
export interface ListParams extends PageRequest {
  keyword?: string
  [key: string]: unknown
}
```

From navy-admin/src/shared/ui/index.ts:
```typescript
export { DataTable } from './DataTable'
export type { DataTableProps } from './DataTable'
export { CrudForm } from './CrudForm'
export type { CrudFormProps, CrudFormField } from './CrudForm'
export { DetailModal } from './DetailModal'
export type { DetailModalProps, DetailField } from './DetailModal'
export { SearchForm } from './SearchForm'
export type { SearchFormProps, SearchField } from './SearchForm'
export { StatusBadge } from './StatusBadge'
export type { StatusBadgeProps } from './StatusBadge'
export { showConfirmDialog } from './ConfirmDialog'
export type { ConfirmDialogOptions } from './ConfirmDialog'
export { SubsystemPage } from './SubsystemPage'
```

From navy-admin/src/shared/api/client.ts:
```typescript
import axios from 'axios'
export const apiClient = axios.create({
  baseURL: '/api',
  timeout: 10000,
  headers: { 'Content-Type': 'application/json' },
})
// interceptor: response.data 자동 추출, 에러 메시지 래핑
```

From navy-admin/src/shared/config/routes.ts:
```typescript
export const ROUTES = {
  LOGIN: '/login',
  PORTAL: '/',
  COMMON: {
    ROOT: '/common',
    AUTH_GROUP: '/common/auth-group',
    CODE_MGMT: '/common/code-mgmt',
    BOARD: '/common/board',
    MENU_MGMT: '/common/menu-mgmt',
    APPROVAL: '/common/approval',
    SYSTEM_MGR: '/common/system-mgr',
  },
  // ... SYS01~18
} as const
```

From navy-admin/src/shared/api/mocks/handlers/index.ts:
```typescript
import { authHandlers } from './auth'
import { demoHandlers } from './demo'
export const handlers = [...authHandlers, ...demoHandlers]
```

From navy-admin/src/shared/api/mocks/handlers/demo.ts (MSW 핸들러 패턴 참조):
- faker.js 한국어 로케일 사용
- PageResponse + ApiResult 래핑
- URL searchParams에서 page/size/keyword 추출
- `let mockData: T[]` 배열에서 CRUD 처리

From navy-admin/src/pages/common/index.tsx (현재 상태):
```typescript
import { SubsystemPage } from '@/shared/ui/SubsystemPage'
export default function Page() {
  return <SubsystemPage />
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 코드 entity/API, MSW 핸들러, useCodeOptions 훅, 공통 라우팅 인프라, 테스트 스캐폴드</name>
  <files>
    navy-admin/src/entities/code/types.ts,
    navy-admin/src/entities/code/api.ts,
    navy-admin/src/entities/code/index.ts,
    navy-admin/src/features/common/hooks/useCodeOptions.ts,
    navy-admin/src/shared/api/mocks/handlers/common/code.ts,
    navy-admin/src/shared/api/mocks/handlers/common/index.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/config/routes.ts,
    navy-admin/src/pages/common/index.tsx,
    navy-admin/src/__tests__/common/code.test.ts,
    navy-admin/src/__tests__/common/useCodeOptions.test.ts
  </files>
  <read_first>
    .planning/phases/01-99/01-UI-SPEC.md,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/shared/api/client.ts,
    navy-admin/src/shared/api/mocks/handlers/demo.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/config/routes.ts,
    navy-admin/src/pages/common/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts,
    navy-admin/src/app/router.tsx
  </read_first>
  <action>
**1) entities/code/types.ts 생성:**
```typescript
export interface CodeGroup {
  id: string
  groupCode: string        // 예: 'APPROVAL_STATUS', 'USER_RANK'
  groupName: string        // 예: '결재상태', '계급'
  description: string
  useYn: string            // 'Y' | 'N'
  createdAt: string
  updatedAt: string
}

export interface Code {
  id: string
  groupId: string          // CodeGroup.id 참조
  groupCode: string        // CodeGroup.groupCode (조회 편의)
  codeValue: string        // 실제 값 (예: 'APPROVED')
  codeName: string         // 표시 이름 (예: '승인')
  sortOrder: number        // 정렬 순서
  useYn: string            // 'Y' | 'N'
  description: string
  createdAt: string
}

export interface CodeOption {
  label: string   // codeName
  value: string   // codeValue
}
```

**2) entities/code/api.ts 생성:**
- `codeGroupApi` 객체: `list(params: PageRequest): Promise<PageResponse<CodeGroup>>`, `create(data)`, `update(id, data)`, `delete(id)` -- 모두 apiClient 사용, baseURL이 `/api`이므로 경로는 `/common/code-groups`
- `codeApi` 객체: `listByGroup(groupId: string, params: PageRequest)`, `create(data)`, `update(id, data)`, `delete(id)`, `getOptions(groupCode: string): Promise<CodeOption[]>`
- API 경로:
  - GET/POST `/common/code-groups`
  - PUT/DELETE `/common/code-groups/:id`
  - GET/POST `/common/codes?groupId={groupId}`
  - PUT/DELETE `/common/codes/:id`
  - GET `/common/codes/options/:groupCode`

**3) entities/code/index.ts 배럴 export:**
```typescript
export * from './types'
export * from './api'
```

**4) features/common/hooks/useCodeOptions.ts 생성:**
```typescript
import { useQuery } from '@tanstack/react-query'
import { codeApi } from '@/entities/code/api'

export function useCodeOptions(groupCode: string) {
  return useQuery({
    queryKey: ['code-options', groupCode],
    queryFn: () => codeApi.getOptions(groupCode),
    staleTime: 5 * 60 * 1000,  // 5분 캐시
    enabled: !!groupCode,
  })
}
```
이 훅은 Select/Radio 등에서 `const { data: options } = useCodeOptions('APPROVAL_STATUS')` 형태로 사용.

**5) shared/api/mocks/handlers/common/code.ts 생성:**
- demo.ts 패턴을 따라 MSW 핸들러 구현
- `faker.js` 한국어 로케일로 Mock 데이터 생성
- 코드그룹 Mock: 10개 생성 (groupCode 예: 'APPROVAL_STATUS', 'USER_RANK', 'UNIT_TYPE', 'BOARD_TYPE', 'FILE_TYPE', 'PERMISSION_TYPE', 'MSG_TYPE', 'LOG_LEVEL', 'SYSTEM_TYPE', 'CERT_TYPE')
- 각 코드그룹별 코드 Mock: 그룹당 3~8개 코드 생성
- 엔드포인트 핸들러:
  - `http.get('/api/common/code-groups', ...)` -- page/size/keyword 파라미터로 필터링+페이지네이션
  - `http.post('/api/common/code-groups', ...)` -- body에서 groupCode/groupName/description 수신, mockGroups 배열에 추가
  - `http.put('/api/common/code-groups/:id', ...)` -- id로 찾아 업데이트
  - `http.delete('/api/common/code-groups/:id', ...)` -- id로 찾아 삭제
  - `http.get('/api/common/codes', ...)` -- `groupId` query param으로 필터링 + page/size
  - `http.post('/api/common/codes', ...)` -- 코드 추가
  - `http.put('/api/common/codes/:id', ...)` -- 코드 수정
  - `http.delete('/api/common/codes/:id', ...)` -- 코드 삭제
  - `http.get('/api/common/codes/options/:groupCode', ...)` -- groupCode로 해당 그룹의 코드를 `CodeOption[]`(label/value)로 반환
- 모든 응답은 `ApiResult<T>` 래핑

**6) shared/api/mocks/handlers/common/index.ts 생성:**
```typescript
import { codeHandlers } from './code'
export const commonHandlers = [...codeHandlers]
```
이후 Plan 02~04에서 permissionHandlers, approvalHandlers 등을 이 파일에 spread 추가.

**7) shared/api/mocks/handlers/index.ts 수정:**
```typescript
import { authHandlers } from './auth'
import { demoHandlers } from './demo'
import { commonHandlers } from './common'
export const handlers = [...authHandlers, ...demoHandlers, ...commonHandlers]
```

**8) shared/config/routes.ts 수정:**
ROUTES.COMMON에 서브경로 추가 (기존 구조 유지하면서):
```typescript
COMMON: {
  ROOT: '/common',
  AUTH_GROUP: '/common/auth-group',
  CODE_MGMT: '/common/code-mgmt',
  BOARD: '/common/board',
  MENU_MGMT: '/common/menu-mgmt',
  APPROVAL: '/common/approval',
  SYSTEM_MGR: '/common/system-mgr',
  ACCESS_LOG: '/common/access-log',
  ERROR_LOG: '/common/error-log',
  MESSAGE_MGMT: '/common/message-mgmt',
},
```
기존 6개 경로에 ACCESS_LOG, ERROR_LOG, MESSAGE_MGMT 3개 추가. (이후 Plan에서 사용)

**9) pages/common/index.tsx 수정:**
SubsystemPage 플레이스홀더를 제거하고, React Router `Routes`/`Route`로 서브경로 분기:
```typescript
import { Routes, Route, Navigate } from 'react-router-dom'
import { lazy, Suspense } from 'react'
import { PageSpinner } from '@/app/components/PageSpinner'

const CodeManagementPage = lazy(() => import('./code-mgmt/CodeManagementPage'))

// 아직 구현되지 않은 페이지들은 임시 컴포넌트로 표시
function NotImplemented() {
  return <div style={{ padding: 24, textAlign: 'center', color: '#999' }}>준비 중인 기능입니다.</div>
}

export default function Page() {
  return (
    <Routes>
      <Route path="code-mgmt" element={<Suspense fallback={<PageSpinner />}><CodeManagementPage /></Suspense>} />
      <Route path="auth-group" element={<NotImplemented />} />
      <Route path="board" element={<NotImplemented />} />
      <Route path="approval" element={<NotImplemented />} />
      <Route path="system-mgr" element={<NotImplemented />} />
      <Route path="menu-mgmt" element={<NotImplemented />} />
      <Route path="access-log" element={<NotImplemented />} />
      <Route path="error-log" element={<NotImplemented />} />
      <Route path="message-mgmt" element={<NotImplemented />} />
      <Route path="*" element={<Navigate to="code-mgmt" replace />} />
    </Routes>
  )
}
```
이후 Plan 02에서 auth-group, Plan 03에서 approval/system-mgr/menu-mgmt/access-log/error-log/message-mgmt, Plan 04에서 board를 실제 컴포넌트로 교체.

**10) src/__tests__/common/code.test.ts 생성 (테스트 스캐폴드):**
```typescript
import { describe, it, expect } from 'vitest'
import type { CodeGroup, Code, CodeOption } from '@/entities/code/types'

describe('코드관리 타입', () => {
  it('CodeGroup 타입이 정의되어 있다', () => {
    const group: CodeGroup = {
      id: '1', groupCode: 'APPROVAL_STATUS', groupName: '결재상태',
      description: '', useYn: 'Y', createdAt: '', updatedAt: ''
    }
    expect(group.groupCode).toBe('APPROVAL_STATUS')
  })

  it('CodeOption 타입이 정의되어 있다', () => {
    const opt: CodeOption = { label: '승인', value: 'APPROVED' }
    expect(opt.value).toBe('APPROVED')
  })
})

describe('codeHandlers MSW 핸들러', () => {
  it('codeHandlers가 export된다', async () => {
    const { codeHandlers } = await import('@/shared/api/mocks/handlers/common/code')
    expect(Array.isArray(codeHandlers)).toBe(true)
    expect(codeHandlers.length).toBeGreaterThan(0)
  })
})

describe('commonHandlers에 codeHandlers 포함', () => {
  it('commonHandlers 배열이 비어있지 않다', async () => {
    const { commonHandlers } = await import('@/shared/api/mocks/handlers/common/index')
    expect(commonHandlers.length).toBeGreaterThan(0)
  })
})
```

**11) src/__tests__/common/useCodeOptions.test.ts 생성 (테스트 스캐폴드):**
```typescript
import { describe, it, expect } from 'vitest'

describe('useCodeOptions 훅', () => {
  it('useCodeOptions가 export된다', async () => {
    const { useCodeOptions } = await import('@/features/common/hooks/useCodeOptions')
    expect(typeof useCodeOptions).toBe('function')
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose src/__tests__/common/code.test.ts src/__tests__/common/useCodeOptions.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/entities/code/types.ts contains `export interface CodeGroup`
    - navy-admin/src/entities/code/types.ts contains `export interface Code`
    - navy-admin/src/entities/code/types.ts contains `export interface CodeOption`
    - navy-admin/src/entities/code/api.ts contains `export const codeGroupApi`
    - navy-admin/src/entities/code/api.ts contains `export const codeApi`
    - navy-admin/src/entities/code/api.ts contains `getOptions`
    - navy-admin/src/features/common/hooks/useCodeOptions.ts contains `staleTime: 5 * 60 * 1000`
    - navy-admin/src/features/common/hooks/useCodeOptions.ts contains `export function useCodeOptions`
    - navy-admin/src/shared/api/mocks/handlers/common/code.ts contains `export const codeHandlers`
    - navy-admin/src/shared/api/mocks/handlers/common/code.ts contains `http.get('/api/common/code-groups'`
    - navy-admin/src/shared/api/mocks/handlers/common/code.ts contains `http.get('/api/common/codes/options/:groupCode'`
    - navy-admin/src/shared/api/mocks/handlers/common/index.ts contains `commonHandlers`
    - navy-admin/src/shared/api/mocks/handlers/index.ts contains `commonHandlers`
    - navy-admin/src/pages/common/index.tsx contains `Route path="code-mgmt"`
    - navy-admin/src/pages/common/index.tsx does NOT contain `SubsystemPage`
    - navy-admin/src/__tests__/common/code.test.ts exists
    - navy-admin/src/__tests__/common/useCodeOptions.test.ts exists
    - TypeScript noEmit: 0 errors
    - code.test.ts: all tests pass
    - useCodeOptions.test.ts: all tests pass
  </acceptance_criteria>
  <done>코드 entity 타입/API, MSW 핸들러(코드그룹 CRUD + 코드 CRUD + 옵션 조회), useCodeOptions 훅(5분 캐시), 공통 기능 라우팅 인프라, 기본 테스트 스캐폴드 완성. TypeScript 컴파일/테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 코드관리 2패널 페이지 (코드그룹 목록 + 코드 목록)</name>
  <files>
    navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx,
    navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx,
    navy-admin/src/pages/common/code-mgmt/CodeListPanel.tsx
  </files>
  <read_first>
    .planning/phases/01-99/01-UI-SPEC.md,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/DetailModal/DetailModal.tsx,
    navy-admin/src/entities/code/types.ts,
    navy-admin/src/entities/code/api.ts,
    navy-admin/src/shared/api/mocks/handlers/common/code.ts,
    navy-admin/src/pages/portal/DemoPage.tsx
  </read_first>
  <action>
**UI-SPEC 디자인 계약 적용 (01-UI-SPEC.md 참조):**
- 마스터-디테일 2패널: `Row gutter={16}` (md=16px 간격), `Col span={10}` (좌측), `Col span={14}` (우측)
- 빈 상태 heading: "코드그룹을 선택하세요" / body: "좌측 목록에서 코드그룹을 선택하면 해당 코드 목록이 표시됩니다"
- Primary 버튼(등록): `type="primary"` (accent #1E3A5F가 antd-theme.ts colorPrimary로 자동 적용)
- 삭제 확인: `showConfirmDialog` title="코드그룹 삭제" content="하위 코드가 모두 삭제됩니다. 계속하시겠습니까?" (destructive #ff4d4f)
- 성공 메시지: `message.success('저장되었습니다')` (2초), 실패: `message.error('저장에 실패했습니다. 입력 내용을 확인하고 다시 시도하세요')` (4초)
- 삭제 성공: `message.success('삭제되었습니다')`
- 테이블 빈 상태: heading="데이터가 없습니다" body="등록 버튼을 눌러 첫 번째 항목을 추가하세요"
- Pattern A (표준 CRUD) 인터랙션 계약 준수: 페이지 진입 시 자동 로드, 등록 -> CrudForm 모달, 행 클릭 -> 상세, 수정 버튼 -> CrudForm(edit), 삭제 -> ConfirmDialog -> DELETE -> invalidateQueries

**1) pages/common/code-mgmt/CodeManagementPage.tsx 생성:**
마스터-디테일 2패널 레이아웃 (UI-SPEC Pattern B):
```typescript
import { useState } from 'react'
import { Row, Col } from 'antd'
import { CodeGroupPage } from './CodeGroupPage'
import { CodeListPanel } from './CodeListPanel'

export default function CodeManagementPage() {
  const [selectedGroupId, setSelectedGroupId] = useState<string | null>(null)
  const [selectedGroupCode, setSelectedGroupCode] = useState<string | null>(null)

  return (
    <Row gutter={16} style={{ height: '100%' }}>
      <Col span={10}>
        <CodeGroupPage
          onSelectGroup={(groupId, groupCode) => {
            setSelectedGroupId(groupId)
            setSelectedGroupCode(groupCode)
          }}
          selectedGroupId={selectedGroupId}
        />
      </Col>
      <Col span={14}>
        {selectedGroupId && selectedGroupCode ? (
          <CodeListPanel groupId={selectedGroupId} groupCode={selectedGroupCode} />
        ) : (
          <div style={{
            padding: 48,
            textAlign: 'center',
            color: '#999',
            background: '#F0F2F5',
            borderRadius: 8,
            marginTop: 16,
          }}>
            <div style={{ fontSize: 16, fontWeight: 600, marginBottom: 8 }}>
              코드그룹을 선택하세요
            </div>
            <div style={{ fontSize: 14 }}>
              좌측 목록에서 코드그룹을 선택하면 해당 코드 목록이 표시됩니다
            </div>
          </div>
        )}
      </Col>
    </Row>
  )
}
```
export default로 내보내야 lazy import 동작.

**2) pages/common/code-mgmt/CodeGroupPage.tsx 생성:**
DataTable + CrudForm 패턴 (UI-SPEC Pattern A 적용):
- `DataTable<CodeGroup>` 사용, columns: groupCode, groupName, useYn, createdAt
- `toolBarRender`에 '등록' 버튼 (`type="primary"`, accent #1E3A5F 자동) -> Modal + CrudForm 열기
- 행 클릭 시 `onSelectGroup(record.id, record.groupCode)` 콜백 호출
- 선택된 행 하이라이트 (rowClassName으로 selectedGroupId 비교)
- CrudForm fields: groupCode(text, required), groupName(text, required), description(textarea), useYn(select, options: [{label:'사용',value:'Y'},{label:'미사용',value:'N'}])
- '수정' 버튼: 행의 액션 컬럼에 수정/삭제 버튼 배치
- '삭제' 시 showConfirmDialog 호출 (title: "코드그룹 삭제", content: "하위 코드가 모두 삭제됩니다. 계속하시겠습니까?", okButtonProps에 danger) -> codeGroupApi.delete
- 폼 제출 성공 -> `message.success('저장되었습니다')` + 모달 닫힘 + invalidateQueries
- 폼 제출 실패 -> `message.error('저장에 실패했습니다. 입력 내용을 확인하고 다시 시도하세요')` + 모달 유지
- 삭제 성공 -> `message.success('삭제되었습니다')`
- TanStack Query: `useQuery` for list, `useMutation` for create/update/delete, `onSuccess`에서 `queryClient.invalidateQueries({ queryKey: ['code-groups'] })`
- request 함수: `(params: PageRequest) => codeGroupApi.list(params).then(r => r.data)` -- apiClient interceptor가 response.data를 반환하므로 ApiResult.data 접근 필요

**3) pages/common/code-mgmt/CodeListPanel.tsx 생성:**
선택된 코드그룹의 코드 CRUD:
- props: `{ groupId: string; groupCode: string }`
- `DataTable<Code>` 사용, columns: codeValue, codeName, sortOrder, useYn
- 코드그룹이 변경되면 테이블 자동 리로드 (key={groupId}로 DataTable 리마운트)
- '등록' 버튼 (`type="primary"`) -> Modal + CrudForm (codeValue, codeName, sortOrder(number), useYn(select), description(textarea))
- 수정/삭제 액션 컬럼
- 삭제 시 showConfirmDialog (title: "삭제 확인", content: "선택한 항목을 삭제하시겠습니까? 삭제된 데이터는 복구할 수 없습니다")
- 성공/실패 메시지는 UI-SPEC Copywriting Contract 준수 (저장되었습니다 / 삭제되었습니다 / 에러 메시지)
- TanStack Query: queryKey에 groupId 포함하여 그룹별 캐시 분리
- 코드 CRUD 성공 시 `invalidateQueries({ queryKey: ['codes', groupId] })` AND `invalidateQueries({ queryKey: ['code-options'] })` (useCodeOptions 캐시도 무효화)
- request 함수: `(params: PageRequest) => codeApi.listByGroup(groupId, params).then(r => r.data)`
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx contains `export default function CodeManagementPage`
    - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx contains `Row gutter={16}`
    - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx contains `Col span={10}`
    - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx contains `Col span={14}`
    - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx contains `코드그룹을 선택하세요`
    - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx contains `#F0F2F5`
    - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx contains `CodeGroupPage`
    - navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx contains `CodeListPanel`
    - navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx contains `DataTable<CodeGroup>`
    - navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx contains `CrudForm`
    - navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx contains `codeGroupApi`
    - navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx contains `invalidateQueries`
    - navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx contains `저장되었습니다`
    - navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx contains `하위 코드가 모두 삭제됩니다`
    - navy-admin/src/pages/common/code-mgmt/CodeListPanel.tsx contains `DataTable<Code>`
    - navy-admin/src/pages/common/code-mgmt/CodeListPanel.tsx contains `codeApi`
    - navy-admin/src/pages/common/code-mgmt/CodeListPanel.tsx contains `code-options`
    - navy-admin/src/pages/common/code-mgmt/CodeListPanel.tsx contains `삭제된 데이터는 복구할 수 없습니다`
    - TypeScript noEmit: 0 errors
    - All tests pass (including code.test.ts, useCodeOptions.test.ts)
  </acceptance_criteria>
  <done>코드관리 2패널 화면 완성: 좌측 코드그룹 DataTable(등록/수정/삭제), 우측 선택 그룹의 코드 DataTable(등록/수정/삭제), Row gutter={16}/Col span={10/14} 레이아웃, UI-SPEC 빈 상태 copy/배경색 #F0F2F5, 삭제 확인 dialog copy 적용, TanStack Query 캐시 무효화 동작, /common/code-mgmt URL 접속 시 화면 표시</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` -- 0 errors
2. `cd navy-admin && npx vitest run --reporter=verbose` -- 모든 테스트 통과 (code.test.ts, useCodeOptions.test.ts 포함)
3. `cd navy-admin && npm run dev` 후 브라우저에서:
   - /common 접속 시 /common/code-mgmt로 리다이렉트
   - 코드그룹 목록이 좌측 DataTable에 표시
   - 코드그룹 미선택 시 우측에 #F0F2F5 배경의 빈 상태 ("코드그룹을 선택하세요")
   - 코드그룹 선택 시 우측에 코드 목록 표시
   - 코드그룹/코드 등록/수정/삭제 동작
   - 삭제 시 UI-SPEC 확인 dialog 표시
</verification>

<success_criteria>
- /common/code-mgmt URL로 접근하면 2패널 코드관리 화면이 표시된다
- 코드그룹 CRUD(등록/수정/삭제)가 MSW Mock API와 연동되어 동작한다
- 코드그룹 선택 시 우측 패널에 해당 그룹의 코드 목록이 표시된다
- 코드 CRUD가 MSW Mock API와 연동되어 동작한다
- useCodeOptions('GROUP_CODE') 호출 시 5분 staleTime 캐싱이 적용된다
- UI-SPEC 디자인 계약 준수: Row gutter=16, Col 10/14, 빈 상태 #F0F2F5 배경, Copywriting Contract 적용
- TypeScript 컴파일 에러 없음, 기존 + 신규 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/01-99/01-01-SUMMARY.md`
</output>
