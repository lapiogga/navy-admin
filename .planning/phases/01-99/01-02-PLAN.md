---
phase: 01-99
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - navy-admin/src/entities/permission/types.ts
  - navy-admin/src/entities/permission/api.ts
  - navy-admin/src/entities/permission/index.ts
  - navy-admin/src/features/common/hooks/usePermission.ts
  - navy-admin/src/shared/api/mocks/handlers/common/permission.ts
  - navy-admin/src/shared/api/mocks/handlers/common/index.ts
  - navy-admin/src/pages/common/auth-group/PermissionGroupPage.tsx
  - navy-admin/src/pages/common/auth-group/MenuPermissionPage.tsx
  - navy-admin/src/pages/common/auth-group/GroupMenuPage.tsx
  - navy-admin/src/pages/common/auth-group/GroupUserPage.tsx
  - navy-admin/src/pages/common/auth-group/GroupUnitPage.tsx
  - navy-admin/src/pages/common/auth-group/index.tsx
  - navy-admin/src/pages/common/index.tsx
  - navy-admin/src/__tests__/common/permission.test.ts
autonomous: true
requirements: [COM-10, COM-11, COM-12, COM-13, COM-14]

must_haves:
  truths:
    - "권한그룹 목록이 DataTable에 표시되고, 등록/수정/삭제가 동작한다"
    - "메뉴별 권한그룹 등록 화면에서 메뉴 Tree를 체크하여 권한그룹에 메뉴를 배정할 수 있다"
    - "권한그룹별 메뉴 등록 화면에서 권한그룹을 선택하면 해당 그룹의 접근 가능 메뉴가 Tree에 체크 표시된다"
    - "권한그룹별 사용자 등록에서 사용자를 검색하고 권한그룹에 배정/해제할 수 있다"
    - "권한그룹별 사용부대 등록에서 부대를 추가/제거할 수 있다"
    - "permissionHandlers가 common/index.ts에 등록되어 MSW 핸들러 체인에 포함된다"
  artifacts:
    - path: "navy-admin/src/entities/permission/types.ts"
      provides: "PermissionGroup, MenuPermission, GroupUser, GroupUnit 타입"
      contains: "interface PermissionGroup"
    - path: "navy-admin/src/entities/permission/api.ts"
      provides: "권한관리 CRUD API"
      exports: ["permissionGroupApi", "menuPermissionApi", "groupUserApi", "groupUnitApi"]
    - path: "navy-admin/src/shared/api/mocks/handlers/common/permission.ts"
      provides: "권한 관련 MSW 핸들러"
      exports: ["permissionHandlers"]
    - path: "navy-admin/src/shared/api/mocks/handlers/common/index.ts"
      provides: "commonHandlers에 permissionHandlers 포함"
      contains: "permissionHandlers"
    - path: "navy-admin/src/features/common/hooks/usePermission.ts"
      provides: "권한 체크 훅 인터페이스 (Phase 2에서 실 적용)"
      exports: ["usePermission"]
    - path: "navy-admin/src/pages/common/auth-group/index.tsx"
      provides: "권한관리 탭 페이지"
  key_links:
    - from: "navy-admin/src/pages/common/auth-group/MenuPermissionPage.tsx"
      to: "navy-admin/src/entities/subsystem/menus.ts"
      via: "SUBSYSTEM_MENUS를 antd Tree 데이터로 변환"
      pattern: "SUBSYSTEM_MENUS"
    - from: "navy-admin/src/pages/common/auth-group/GroupUserPage.tsx"
      to: "/api/common/permission-groups/:id/users"
      via: "permissionGroupApi"
      pattern: "permission-groups.*users"
    - from: "navy-admin/src/shared/api/mocks/handlers/common/index.ts"
      to: "navy-admin/src/shared/api/mocks/handlers/common/permission.ts"
      via: "permissionHandlers spread import"
      pattern: "permissionHandlers"
---

<objective>
권한관리(COM-10~14)를 구현한다: 권한그룹 CRUD, 메뉴별/권한그룹별 메뉴 배정(Tree + Checkbox), 사용자/부대 배정.

Purpose: 권한그룹은 18개 서브시스템 845개 화면의 접근 제어 기반이다. Phase 1에서는 관리 화면(CRUD)을 구현하고, usePermission 훅 인터페이스를 정의해둔다. 실제 메뉴 접근 제어 적용은 Phase 2에서 통합.

Output: 권한그룹/메뉴배정/사용자배정/부대배정 화면, MSW 핸들러, usePermission 훅 인터페이스
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-99/01-RESEARCH.md
@.planning/phases/01-99/01-01-SUMMARY.md

<interfaces>
<!-- Phase 0 동결 인터페이스 -->

From navy-admin/src/shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number; sort?: string[] }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string; errorCode?: string }
```

From navy-admin/src/shared/ui/index.ts:
```typescript
export { DataTable } from './DataTable'
export type { DataTableProps } from './DataTable'
export { CrudForm } from './CrudForm'
export type { CrudFormProps, CrudFormField } from './CrudForm'
export { DetailModal } from './DetailModal'
export { showConfirmDialog } from './ConfirmDialog'
```

From navy-admin/src/entities/user/types.ts:
```typescript
export interface User {
  id: string; name: string; rank: string; unit: string; username: string; roles: string[]
}
```

From navy-admin/src/entities/subsystem/menus.ts:
```typescript
import type { MenuDataItem } from '@ant-design/pro-components'
export const SUBSYSTEM_MENUS: Record<string, MenuDataItem[]> = {
  sys01: [...], sys02: [...], ..., common: [...]
}
// 각 메뉴: { name: string, path: string, children?: MenuDataItem[] }
```

From navy-admin/src/entities/subsystem/config.ts:
```typescript
export const SUBSYSTEM_META: Record<string, SubsystemMeta> = {
  sys01: { code: 'SYS01', name: '초과근무관리체계', path: '/sys01', ... }, ...
}
```

From navy-admin/src/shared/api/client.ts:
```typescript
export const apiClient = axios.create({ baseURL: '/api', ... })
// interceptor: response.data 자동 추출
```

From navy-admin/src/shared/api/mocks/handlers/demo.ts (MSW 패턴):
- `let mockData: T[]` 배열 사용
- `http.get(...)`, `http.post(...)`, `http.put(...)`, `http.delete(...)`
- URL searchParams에서 page/size 추출
- `ApiResult<PageResponse<T>>` 래핑

From navy-admin/src/shared/api/mocks/handlers/common/index.ts (Plan 01-01이 생성):
```typescript
import { codeHandlers } from './code'
export const commonHandlers = [...codeHandlers]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 권한 entity/API, MSW 핸들러, usePermission 훅 인터페이스, 테스트 스캐폴드</name>
  <files>
    navy-admin/src/entities/permission/types.ts,
    navy-admin/src/entities/permission/api.ts,
    navy-admin/src/entities/permission/index.ts,
    navy-admin/src/features/common/hooks/usePermission.ts,
    navy-admin/src/shared/api/mocks/handlers/common/permission.ts,
    navy-admin/src/shared/api/mocks/handlers/common/index.ts,
    navy-admin/src/__tests__/common/permission.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/shared/api/client.ts,
    navy-admin/src/shared/api/mocks/handlers/demo.ts,
    navy-admin/src/shared/api/mocks/handlers/common/index.ts,
    navy-admin/src/entities/user/types.ts,
    navy-admin/src/entities/subsystem/menus.ts,
    navy-admin/src/entities/subsystem/config.ts,
    navy-admin/src/features/auth/store/authStore.ts
  </read_first>
  <action>
**1) entities/permission/types.ts 생성:**
```typescript
export interface PermissionGroup {
  id: string
  groupCode: string       // 예: 'ADMIN', 'COMMON_USER', 'SYS01_MANAGER'
  groupName: string       // 예: '관리자', '일반사용자'
  description: string
  createdAt: string
  updatedAt: string
}

export interface MenuPermission {
  id: string
  groupId: string
  menuPath: string        // 예: '/sys01/1/1'
  menuName: string        // 예: '신청서 작성'
  subsystemCode: string   // 예: 'sys01'
}

export interface GroupUser {
  id: string
  groupId: string
  userId: string
  userName: string
  userRank: string
  userUnit: string
  assignedAt: string
}

export interface GroupUnit {
  id: string
  groupId: string
  unitCode: string
  unitName: string
  assignedAt: string
}
```

**2) entities/permission/api.ts 생성:**
- `permissionGroupApi`: list(PageRequest), create, update(id), delete(id)
  - 경로: `/common/permission-groups`, `/common/permission-groups/:id`
- `menuPermissionApi`:
  - `getByMenu(menuPath: string): Promise<ApiResult<MenuPermission[]>>` -- 메뉴별 권한그룹 조회
  - `getByGroup(groupId: string): Promise<ApiResult<MenuPermission[]>>` -- 권한그룹별 메뉴 조회
  - `assign(groupId: string, menuPaths: string[]): Promise<ApiResult>` -- 메뉴 배정
  - `remove(groupId: string, menuPaths: string[]): Promise<ApiResult>` -- 메뉴 해제
  - 경로: `/common/menu-permissions`, `/common/permission-groups/:id/menus`
- `groupUserApi`:
  - `list(groupId: string, params: PageRequest): Promise<PageResponse<GroupUser>>`
  - `assign(groupId: string, userIds: string[]): Promise<ApiResult>`
  - `remove(groupId: string, userId: string): Promise<ApiResult>`
  - `detail(groupId: string, userId: string): Promise<ApiResult<GroupUser>>`
  - 경로: `/common/permission-groups/:id/users`, `/common/permission-groups/:id/users/:uid`
- `groupUnitApi`:
  - `list(groupId: string, params: PageRequest): Promise<PageResponse<GroupUnit>>`
  - `assign(groupId: string, units: {unitCode:string, unitName:string}[]): Promise<ApiResult>`
  - `remove(groupId: string, unitId: string): Promise<ApiResult>`
  - 경로: `/common/permission-groups/:id/units`

**3) entities/permission/index.ts 배럴 export.**

**4) features/common/hooks/usePermission.ts 생성:**
Phase 1에서는 인터페이스만 정의, Phase 2에서 실 적용.
```typescript
import { useAuthStore } from '@/features/auth/store/authStore'

/** 현재 사용자의 권한을 체크하는 훅. Phase 1: Mock 구현 (항상 true 반환). Phase 2에서 실 권한 체크 연결. */
export function usePermission() {
  const user = useAuthStore((s) => s.user)
  const roles = user?.roles ?? []

  return {
    /** 특정 메뉴 경로에 접근 가능한지 확인 */
    hasMenuAccess: (_menuPath: string): boolean => {
      // Phase 1: 항상 true (관리 화면만 구현, 접근 제어는 Phase 2)
      return true
    },
    /** 특정 권한그룹 코드를 보유하고 있는지 확인 */
    hasRole: (roleCode: string): boolean => {
      return roles.includes(roleCode)
    },
    /** 현재 사용자의 권한그룹 코드 목록 */
    roles,
  }
}
```

**5) shared/api/mocks/handlers/common/permission.ts 생성:**
faker.js로 Mock 데이터 생성:
- `mockGroups: PermissionGroup[]` -- 20개 (관리자, 일반사용자, 조회전용, 시스템관리자, 각 서브시스템별 매니저 등)
- `mockMenuPermissions: MenuPermission[]` -- 그룹별로 SUBSYSTEM_MENUS에서 랜덤 메뉴 배정
- `mockGroupUsers: GroupUser[]` -- 그룹별 3~10명 사용자 배정
- `mockGroupUnits: GroupUnit[]` -- 그룹별 2~5개 부대 배정
- `mockAllUsers` -- 50명 Mock 사용자 목록 (사용자 검색/배정용)

MSW 핸들러:
- `http.get('/api/common/permission-groups', ...)` -- 페이지네이션
- `http.post('/api/common/permission-groups', ...)`
- `http.put('/api/common/permission-groups/:id', ...)`
- `http.delete('/api/common/permission-groups/:id', ...)`
- `http.get('/api/common/menu-permissions', ...)` -- ?menuPath=xxx or ?groupId=xxx 파라미터로 필터
- `http.post('/api/common/menu-permissions', ...)` -- { groupId, menuPaths } body
- `http.delete('/api/common/menu-permissions', ...)` -- { groupId, menuPaths } body
- `http.get('/api/common/permission-groups/:id/menus', ...)` -- 그룹의 배정된 메뉴 조회
- `http.post('/api/common/permission-groups/:id/menus', ...)` -- 메뉴 배정
- `http.delete('/api/common/permission-groups/:id/menus', ...)` -- 메뉴 해제
- `http.get('/api/common/permission-groups/:id/users', ...)` -- 그룹 사용자 목록
- `http.post('/api/common/permission-groups/:id/users', ...)` -- 사용자 배정
- `http.delete('/api/common/permission-groups/:id/users/:uid', ...)` -- 사용자 해제
- `http.get('/api/common/permission-groups/:id/users/:uid', ...)` -- 사용자 상세
- `http.get('/api/common/permission-groups/:id/units', ...)` -- 부대 목록
- `http.post('/api/common/permission-groups/:id/units', ...)` -- 부대 배정
- `http.delete('/api/common/permission-groups/:id/units/:unitId', ...)` -- 부대 해제
- `http.get('/api/common/users', ...)` -- 전체 사용자 검색 (배정용)

모든 응답 ApiResult<T> 래핑.

**6) shared/api/mocks/handlers/common/index.ts 수정 (CRITICAL -- BLOCKER FIX):**
Plan 01-01이 생성한 기존 common/index.ts에 permissionHandlers를 추가 등록한다:
```typescript
import { codeHandlers } from './code'
import { permissionHandlers } from './permission'
export const commonHandlers = [...codeHandlers, ...permissionHandlers]
```
**반드시** 기존 codeHandlers import를 유지하면서 permissionHandlers를 spread로 추가할 것. 이 파일을 수정하지 않으면 권한관리 MSW 핸들러 전체가 런타임에 로드되지 않는다.

**7) src/__tests__/common/permission.test.ts 생성 (테스트 스캐폴드):**
기본 단위 테스트 파일 생성:
```typescript
import { describe, it, expect } from 'vitest'
// 테스트 대상 import
import type { PermissionGroup, MenuPermission, GroupUser, GroupUnit } from '@/entities/permission/types'

describe('권한관리 타입', () => {
  it('PermissionGroup 타입이 정의되어 있다', () => {
    const group: PermissionGroup = {
      id: '1', groupCode: 'ADMIN', groupName: '관리자',
      description: '', createdAt: '', updatedAt: ''
    }
    expect(group.groupCode).toBe('ADMIN')
  })

  it('MenuPermission 타입이 정의되어 있다', () => {
    const perm: MenuPermission = {
      id: '1', groupId: '1', menuPath: '/sys01/1', menuName: '메뉴1', subsystemCode: 'sys01'
    }
    expect(perm.menuPath).toBe('/sys01/1')
  })
})

describe('permissionHandlers MSW 핸들러', () => {
  it('permissionHandlers가 export된다', async () => {
    const { permissionHandlers } = await import('@/shared/api/mocks/handlers/common/permission')
    expect(Array.isArray(permissionHandlers)).toBe(true)
    expect(permissionHandlers.length).toBeGreaterThan(0)
  })
})

describe('commonHandlers에 permissionHandlers 포함', () => {
  it('commonHandlers 배열에 permission 핸들러가 포함된다', async () => {
    const { commonHandlers } = await import('@/shared/api/mocks/handlers/common/index')
    expect(commonHandlers.length).toBeGreaterThan(0)
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose src/__tests__/common/permission.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/entities/permission/types.ts contains `export interface PermissionGroup`
    - navy-admin/src/entities/permission/types.ts contains `export interface MenuPermission`
    - navy-admin/src/entities/permission/types.ts contains `export interface GroupUser`
    - navy-admin/src/entities/permission/types.ts contains `export interface GroupUnit`
    - navy-admin/src/entities/permission/api.ts contains `export const permissionGroupApi`
    - navy-admin/src/entities/permission/api.ts contains `export const menuPermissionApi`
    - navy-admin/src/entities/permission/api.ts contains `export const groupUserApi`
    - navy-admin/src/entities/permission/api.ts contains `export const groupUnitApi`
    - navy-admin/src/features/common/hooks/usePermission.ts contains `export function usePermission`
    - navy-admin/src/features/common/hooks/usePermission.ts contains `hasMenuAccess`
    - navy-admin/src/features/common/hooks/usePermission.ts contains `hasRole`
    - navy-admin/src/shared/api/mocks/handlers/common/permission.ts contains `export const permissionHandlers`
    - navy-admin/src/shared/api/mocks/handlers/common/permission.ts contains `http.get('/api/common/permission-groups'`
    - navy-admin/src/shared/api/mocks/handlers/common/permission.ts contains `/api/common/users`
    - navy-admin/src/shared/api/mocks/handlers/common/index.ts contains `permissionHandlers`
    - navy-admin/src/shared/api/mocks/handlers/common/index.ts contains `codeHandlers`
    - navy-admin/src/__tests__/common/permission.test.ts exists
    - TypeScript noEmit: 0 errors
    - permission.test.ts: all tests pass
  </acceptance_criteria>
  <done>권한 entity 타입/API, MSW 핸들러(그룹 CRUD + 메뉴배정 + 사용자배정 + 부대배정 + 사용자검색), usePermission 훅 인터페이스 완성. permissionHandlers가 common/index.ts에 등록됨. 기본 테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 권한관리 5개 탭 페이지 (그룹CRUD + 메뉴배정 + 사용자/부대배정)</name>
  <files>
    navy-admin/src/pages/common/auth-group/index.tsx,
    navy-admin/src/pages/common/auth-group/PermissionGroupPage.tsx,
    navy-admin/src/pages/common/auth-group/MenuPermissionPage.tsx,
    navy-admin/src/pages/common/auth-group/GroupMenuPage.tsx,
    navy-admin/src/pages/common/auth-group/GroupUserPage.tsx,
    navy-admin/src/pages/common/auth-group/GroupUnitPage.tsx,
    navy-admin/src/pages/common/index.tsx
  </files>
  <read_first>
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/entities/permission/types.ts,
    navy-admin/src/entities/permission/api.ts,
    navy-admin/src/entities/subsystem/menus.ts,
    navy-admin/src/entities/subsystem/config.ts,
    navy-admin/src/pages/common/index.tsx,
    navy-admin/src/pages/portal/DemoPage.tsx
  </read_first>
  <action>
**1) pages/common/auth-group/index.tsx 생성:**
antd Tabs로 5개 탭 구성:
```typescript
import { Tabs } from 'antd'
import { PermissionGroupPage } from './PermissionGroupPage'
import { MenuPermissionPage } from './MenuPermissionPage'
import { GroupMenuPage } from './GroupMenuPage'
import { GroupUserPage } from './GroupUserPage'
import { GroupUnitPage } from './GroupUnitPage'

export default function AuthGroupIndex() {
  return (
    <Tabs
      defaultActiveKey="group"
      items={[
        { key: 'group', label: '권한그룹 등록', children: <PermissionGroupPage /> },
        { key: 'menu-perm', label: '메뉴별 권한그룹', children: <MenuPermissionPage /> },
        { key: 'group-menu', label: '권한그룹별 메뉴', children: <GroupMenuPage /> },
        { key: 'group-user', label: '권한그룹별 사용자', children: <GroupUserPage /> },
        { key: 'group-unit', label: '권한그룹별 사용부대', children: <GroupUnitPage /> },
      ]}
    />
  )
}
```

**2) pages/common/auth-group/PermissionGroupPage.tsx (COM-10):**
DataTable + CrudForm 패턴:
- DataTable<PermissionGroup> columns: groupCode, groupName, description, createdAt, 액션(수정/삭제)
- toolBarRender: '등록' 버튼 -> Modal + CrudForm
- CrudForm fields: groupCode(text, required), groupName(text, required), description(textarea)
- TanStack Query useQuery/useMutation, invalidateQueries onSuccess

**3) pages/common/auth-group/MenuPermissionPage.tsx (COM-11):**
좌측: 권한그룹 Select -> 우측: 메뉴 Tree (checkable)
- antd Select로 권한그룹 목록 표시 (permissionGroupApi.list 전체 조회)
- 권한그룹 선택 시 menuPermissionApi.getByGroup(groupId)로 배정된 메뉴 조회
- SUBSYSTEM_MENUS를 antd `Tree`의 `treeData` (TreeDataNode[])로 변환:
  ```
  Object.entries(SUBSYSTEM_MENUS).map(([sysCode, menus]) => ({
    key: sysCode,
    title: SUBSYSTEM_META[sysCode]?.name ?? sysCode,
    children: menus.map(menu => ({
      key: menu.path,
      title: menu.name,
      children: menu.children?.map(child => ({
        key: child.path,
        title: child.name,
      }))
    }))
  }))
  ```
- Tree `checkable` prop 사용, `checkedKeys`는 배정된 메뉴 경로 배열
- '저장' 버튼: 변경된 checkedKeys를 menuPermissionApi.assign으로 전송
- RESEARCH Pitfall 6 주의: `checkStrictly={true}` 또는 onCheck에서 `checked` 배열만 사용

**4) pages/common/auth-group/GroupMenuPage.tsx (COM-12):**
COM-11의 역방향 뷰 -- 구조 거의 동일:
- 좌측: 권한그룹 Select
- 우측: 선택된 그룹의 접근 가능 메뉴를 Tree에 체크 표시 (읽기 전용 또는 편집 가능)
- permissionGroupApi에서 `/:id/menus` 경로로 조회
- 저장 시 `/:id/menus` POST로 업데이트
- MenuPermissionPage와의 차이: 이 화면은 "권한그룹을 선택하고 메뉴를 보는" 관점, COM-11은 동일 기능이지만 연구 설계에서 두 방향 뷰를 요구

**5) pages/common/auth-group/GroupUserPage.tsx (COM-14):**
권한그룹 선택 -> 배정된 사용자 DataTable + 사용자 추가 모달:
- 상단: 권한그룹 Select
- DataTable<GroupUser> columns: userName, userRank, userUnit, assignedAt, 액션(해제)
- '사용자 추가' 버튼 -> Modal:
  - 사용자 검색 테이블 (/api/common/users 호출), keyword 검색 가능
  - 체크박스 rowSelection으로 복수 사용자 선택
  - '배정' 버튼 -> groupUserApi.assign(groupId, selectedUserIds)
- 사용자 해제: showConfirmDialog -> groupUserApi.remove(groupId, userId)
- 행 클릭 시 DetailModal로 사용자 상세 표시

**6) pages/common/auth-group/GroupUnitPage.tsx (COM-13):**
권한그룹 선택 -> 배정된 부대 DataTable + 부대 추가 모달:
- 상단: 권한그룹 Select
- DataTable<GroupUnit> columns: unitCode, unitName, assignedAt, 액션(해제)
- '부대 추가' 버튼 -> Modal + CrudForm (unitCode(text, required), unitName(text, required))
- 부대 해제: showConfirmDialog -> groupUnitApi.remove

**7) pages/common/index.tsx 수정:**
Plan 01-01이 생성한 pages/common/index.tsx에서 `auth-group` Route의 element를 NotImplemented에서 실제 컴포넌트로 교체:
```typescript
const AuthGroupIndex = lazy(() => import('./auth-group'))
// ...
<Route path="auth-group" element={<Suspense fallback={<PageSpinner />}><AuthGroupIndex /></Suspense>} />
```
주의: Plan 01-01이 이미 이 파일에 Routes 구조를 만들어두었으므로, auth-group Route만 교체. 다른 Route는 건드리지 않을 것.
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/common/auth-group/index.tsx contains `export default function AuthGroupIndex`
    - navy-admin/src/pages/common/auth-group/index.tsx contains `Tabs`
    - navy-admin/src/pages/common/auth-group/PermissionGroupPage.tsx contains `DataTable<PermissionGroup>`
    - navy-admin/src/pages/common/auth-group/PermissionGroupPage.tsx contains `CrudForm`
    - navy-admin/src/pages/common/auth-group/MenuPermissionPage.tsx contains `Tree`
    - navy-admin/src/pages/common/auth-group/MenuPermissionPage.tsx contains `SUBSYSTEM_MENUS`
    - navy-admin/src/pages/common/auth-group/MenuPermissionPage.tsx contains `checkable`
    - navy-admin/src/pages/common/auth-group/GroupMenuPage.tsx contains `Tree`
    - navy-admin/src/pages/common/auth-group/GroupUserPage.tsx contains `DataTable<GroupUser>`
    - navy-admin/src/pages/common/auth-group/GroupUserPage.tsx contains `groupUserApi`
    - navy-admin/src/pages/common/auth-group/GroupUnitPage.tsx contains `DataTable<GroupUnit>`
    - navy-admin/src/pages/common/index.tsx contains `AuthGroupIndex`
    - navy-admin/src/pages/common/index.tsx does NOT contain `auth-group" element={<NotImplemented`
    - TypeScript noEmit: 0 errors
    - All existing tests pass
  </acceptance_criteria>
  <done>권한관리 5개 탭(그룹CRUD, 메뉴별권한그룹, 권한그룹별메뉴, 사용자배정, 부대배정) 화면 완성, /common/auth-group URL 접속 시 탭 화면 표시, 모든 CRUD 동작</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` -- 0 errors
2. `cd navy-admin && npx vitest run --reporter=verbose` -- 모든 테스트 통과 (permission.test.ts 포함)
3. 브라우저에서:
   - /common/auth-group 접속 시 5개 탭 화면
   - 권한그룹 등록/수정/삭제 동작
   - 메뉴별 권한그룹: 그룹 선택 -> Tree 체크 -> 저장
   - 사용자 배정: 그룹 선택 -> 사용자 검색 -> 배정
   - 부대 배정: 그룹 선택 -> 부대 추가 -> 배정
</verification>

<success_criteria>
- /common/auth-group URL로 접근하면 5개 탭 권한관리 화면이 표시된다
- 권한그룹 CRUD가 MSW Mock과 연동되어 동작한다
- permissionHandlers가 common/index.ts에 등록되어 MSW 핸들러 체인에 포함된다
- 메뉴별 권한그룹 화면에서 Tree 체크로 메뉴를 배정/해제할 수 있다
- 사용자/부대 배정이 동작한다
- usePermission 훅이 export되어 Phase 2에서 사용 가능하다
- TypeScript 컴파일 에러 없음, 기존 + 신규 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/01-99/01-02-SUMMARY.md`
</output>
