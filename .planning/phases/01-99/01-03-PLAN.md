---
phase: 01-99
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - navy-admin/src/entities/approval/types.ts
  - navy-admin/src/entities/approval/api.ts
  - navy-admin/src/entities/approval/index.ts
  - navy-admin/src/shared/api/mocks/handlers/common/approval.ts
  - navy-admin/src/shared/api/mocks/handlers/common/system.ts
  - navy-admin/src/shared/api/mocks/handlers/common/index.ts
  - navy-admin/src/shared/lib/csv.ts
  - navy-admin/src/pages/common/approval/ApprovalLinePage.tsx
  - navy-admin/src/pages/common/system-mgr/SystemManagerPage.tsx
  - navy-admin/src/pages/common/system-mgr/MenuManagementPage.tsx
  - navy-admin/src/pages/common/system-mgr/MessageManagementPage.tsx
  - navy-admin/src/pages/common/system-mgr/AccessLogPage.tsx
  - navy-admin/src/pages/common/system-mgr/ErrorLogPage.tsx
  - navy-admin/src/pages/common/system-mgr/index.tsx
  - navy-admin/src/pages/common/index.tsx
autonomous: true
requirements: [COM-01, COM-02, COM-03, COM-04, COM-05, COM-06]

must_haves:
  truths:
    - "결재선을 등록하면 Transfer로 결재자를 선택하고 순서를 지정할 수 있다"
    - "결재선 목록에서 수정/삭제가 즉시 반영된다"
    - "체계담당자 목록이 DataTable에 표시되고 CRUD가 동작한다"
    - "메뉴관리에서 메뉴 트리를 조회하고 등록/수정이 동작한다"
    - "메시지관리에서 메시지 코드/내용 CRUD가 동작한다"
    - "접속로그 조회에서 목록을 조회하고 CSV 저장이 동작한다"
    - "장애로그 조회에서 읽기 전용 목록이 표시된다"
  artifacts:
    - path: "navy-admin/src/entities/approval/types.ts"
      provides: "ApprovalLine, Approver 타입"
      contains: "interface ApprovalLine"
    - path: "navy-admin/src/entities/approval/api.ts"
      provides: "결재선 CRUD API"
      exports: ["approvalLineApi"]
    - path: "navy-admin/src/shared/lib/csv.ts"
      provides: "CSV 다운로드 유틸"
      exports: ["downloadCsv"]
    - path: "navy-admin/src/pages/common/approval/ApprovalLinePage.tsx"
      provides: "결재선 관리 페이지"
      contains: "Transfer"
    - path: "navy-admin/src/pages/common/system-mgr/AccessLogPage.tsx"
      provides: "접속로그 조회 + CSV 내보내기"
      contains: "downloadCsv"
  key_links:
    - from: "navy-admin/src/pages/common/approval/ApprovalLinePage.tsx"
      to: "/api/common/approval-lines"
      via: "approvalLineApi CRUD"
      pattern: "approvalLineApi"
    - from: "navy-admin/src/pages/common/system-mgr/AccessLogPage.tsx"
      to: "navy-admin/src/shared/lib/csv.ts"
      via: "downloadCsv 함수 import"
      pattern: "downloadCsv"
---

<objective>
결재선 관리(COM-06)와 시스템관리(COM-01~05)를 구현한다.

Purpose: 결재선은 이후 서브시스템의 결재 워크플로우에서 재사용된다. 시스템관리(체계담당자, 메뉴, 메시지, 로그)는 관리자 전용 기능이며, CSV 내보내기 유틸(downloadCsv)은 다른 화면에서도 재사용 가능하다.

Output: 결재선 관리 화면(Transfer 패턴), 시스템관리 5개 화면, CSV 유틸, MSW 핸들러
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-99/01-RESEARCH.md
@.planning/phases/01-99/01-01-SUMMARY.md

<interfaces>
From navy-admin/src/shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number; sort?: string[] }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string; errorCode?: string }
```

From navy-admin/src/shared/ui/index.ts:
```typescript
export { DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, showConfirmDialog, SubsystemPage }
```

From navy-admin/src/shared/api/client.ts:
```typescript
export const apiClient = axios.create({ baseURL: '/api', ... })
```

From navy-admin/src/entities/user/types.ts:
```typescript
export interface User { id: string; name: string; rank: string; unit: string; username: string; roles: string[] }
```

From navy-admin/src/entities/subsystem/menus.ts:
```typescript
export const SUBSYSTEM_MENUS: Record<string, MenuDataItem[]> = { ... }
```

From navy-admin/src/shared/api/mocks/handlers/demo.ts (MSW 패턴):
- faker.js 한국어, ApiResult 래핑, URL searchParams page/size 추출
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 결재선 entity/API, MSW 핸들러, CSV 유틸, 시스템관리 MSW 핸들러</name>
  <files>
    navy-admin/src/entities/approval/types.ts,
    navy-admin/src/entities/approval/api.ts,
    navy-admin/src/entities/approval/index.ts,
    navy-admin/src/shared/api/mocks/handlers/common/approval.ts,
    navy-admin/src/shared/api/mocks/handlers/common/system.ts,
    navy-admin/src/shared/api/mocks/handlers/common/index.ts,
    navy-admin/src/shared/lib/csv.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/shared/api/client.ts,
    navy-admin/src/shared/api/mocks/handlers/demo.ts,
    navy-admin/src/shared/api/mocks/handlers/common/index.ts,
    navy-admin/src/shared/lib/date.ts,
    navy-admin/src/entities/user/types.ts,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**1) entities/approval/types.ts 생성:**
```typescript
export interface Approver {
  userId: string
  userName: string
  userRank: string
  userUnit: string
  order: number          // 결재 순서 (1, 2, 3...)
}

export interface ApprovalLine {
  id: string
  lineName: string       // 결재선 이름 (예: '기본 결재선', '부대장 직속 결재선')
  description: string
  approvers: Approver[]  // 순서 있는 결재자 배열
  createdBy: string      // 등록자 ID
  createdAt: string
  updatedAt: string
}
```

**2) entities/approval/api.ts 생성:**
- `approvalLineApi`:
  - `list(params: PageRequest): Promise<PageResponse<ApprovalLine>>`
  - `detail(id: string): Promise<ApiResult<ApprovalLine>>`
  - `create(data: { lineName: string; description: string; approvers: { userId: string; order: number }[] })`
  - `update(id: string, data: ...)`
  - `delete(id: string)`
- 경로: `/common/approval-lines`, `/common/approval-lines/:id`

**3) entities/approval/index.ts 배럴 export.**

**4) shared/lib/csv.ts 생성:**
RESEARCH Pattern 6 구현:
```typescript
/** CSV 파일을 생성하여 브라우저 다운로드 실행 */
export function downloadCsv(
  filename: string,
  rows: Record<string, unknown>[],
  headers: { key: string; label: string }[]
) {
  const headerRow = headers.map(h => h.label).join(',')
  const dataRows = rows.map(row =>
    headers.map(h => `"${String(row[h.key] ?? '').replace(/"/g, '""')}"`).join(',')
  )
  const csv = [headerRow, ...dataRows].join('\n')
  const blob = new Blob(['\uFEFF' + csv], { type: 'text/csv;charset=utf-8;' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = filename
  a.click()
  URL.revokeObjectURL(url)
}
```
BOM(\uFEFF) 포함하여 엑셀 한글 깨짐 방지.

**5) shared/api/mocks/handlers/common/approval.ts 생성:**
- `mockLines: ApprovalLine[]` -- 15개 Mock 결재선 생성
  - 각 결재선에 2~5명의 결재자 포함
  - faker.js: lineName 예 `faker.helpers.arrayElement(['기본결재선', '부대장직속', '팀장결재선', ...])`
  - approvers: faker.person.fullName(), rank, unit 조합
- MSW 핸들러: GET(목록+상세), POST, PUT, DELETE
- `http.get('/api/common/users', ...)` -- 50명 Mock 사용자 목록 (결재자 선택용, 이미 permission.ts에 있다면 공유 또는 여기서도 정의)

**6) shared/api/mocks/handlers/common/system.ts 생성:**
시스템관리 5개 도메인의 MSW 핸들러:

**체계담당자 (COM-01):**
- `mockManagers` -- 25명 Mock
- GET/POST `/api/common/system-managers` (페이지네이션+키워드)
- GET/PUT/DELETE `/api/common/system-managers/:id`

**메뉴관리 (COM-02):**
- `mockMenus` -- SUBSYSTEM_MENUS의 실데이터를 변환하여 Mock (id, menuName, menuPath, parentId, subsystemCode, sortOrder, useYn)
- GET `/api/common/menus` -- 트리 형태 반환 (계층 구조)
- POST `/api/common/menus` -- 메뉴 추가
- PUT/DELETE `/api/common/menus/:id`

**메시지관리 (COM-03):**
- `mockMessages` -- 30개 Mock (messageCode, messageContent, messageType, useYn)
- GET/POST `/api/common/messages` (페이지네이션)
- PUT/DELETE `/api/common/messages/:id`

**접속로그 (COM-04):**
- `mockAccessLogs` -- 200개 Mock (userId, userName, loginTime, logoutTime, ipAddress, userAgent, subsystemCode)
- GET `/api/common/access-logs` (페이지네이션+날짜 필터)
- GET `/api/common/access-logs/:id` (상세)

**장애로그 (COM-05):**
- `mockErrorLogs` -- 50개 Mock (errorCode, errorMessage, errorLevel, occurredAt, resolvedAt, stackTrace)
- GET `/api/common/error-logs` (페이지네이션)

**7) shared/api/mocks/handlers/common/index.ts 수정:**
```typescript
import { codeHandlers } from './code'
import { approvalHandlers } from './approval'
import { systemHandlers } from './system'
export const commonHandlers = [...codeHandlers, ...approvalHandlers, ...systemHandlers]
```
주의: permission.ts는 Plan 02에서 추가되므로 이 시점에서는 Plan 01의 codeHandlers + 이 플랜의 approvalHandlers + systemHandlers만 포함. Plan 02가 병렬 실행되면 common/index.ts에 permissionHandlers도 추가해야 하므로, 이 파일을 수정할 때 기존 import를 유지해야 한다. 만약 Plan 02가 먼저 완료되었다면 permissionHandlers import도 유지할 것.
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/entities/approval/types.ts contains `export interface ApprovalLine`
    - navy-admin/src/entities/approval/types.ts contains `export interface Approver`
    - navy-admin/src/entities/approval/api.ts contains `export const approvalLineApi`
    - navy-admin/src/shared/lib/csv.ts contains `export function downloadCsv`
    - navy-admin/src/shared/lib/csv.ts contains `\uFEFF`
    - navy-admin/src/shared/api/mocks/handlers/common/approval.ts contains `export const approvalHandlers`
    - navy-admin/src/shared/api/mocks/handlers/common/approval.ts contains `/api/common/approval-lines`
    - navy-admin/src/shared/api/mocks/handlers/common/system.ts contains `export const systemHandlers`
    - navy-admin/src/shared/api/mocks/handlers/common/system.ts contains `/api/common/system-managers`
    - navy-admin/src/shared/api/mocks/handlers/common/system.ts contains `/api/common/menus`
    - navy-admin/src/shared/api/mocks/handlers/common/system.ts contains `/api/common/messages`
    - navy-admin/src/shared/api/mocks/handlers/common/system.ts contains `/api/common/access-logs`
    - navy-admin/src/shared/api/mocks/handlers/common/system.ts contains `/api/common/error-logs`
    - navy-admin/src/shared/api/mocks/handlers/common/index.ts contains `approvalHandlers`
    - navy-admin/src/shared/api/mocks/handlers/common/index.ts contains `systemHandlers`
    - TypeScript noEmit: 0 errors
  </acceptance_criteria>
  <done>결재선 entity/API, CSV 유틸, 시스템관리 5개 도메인 MSW 핸들러, common/index.ts에 핸들러 등록 완성</done>
</task>

<task type="auto">
  <name>Task 2: 결재선 관리 + 시스템관리 5개 페이지 구현</name>
  <files>
    navy-admin/src/pages/common/approval/ApprovalLinePage.tsx,
    navy-admin/src/pages/common/system-mgr/index.tsx,
    navy-admin/src/pages/common/system-mgr/SystemManagerPage.tsx,
    navy-admin/src/pages/common/system-mgr/MenuManagementPage.tsx,
    navy-admin/src/pages/common/system-mgr/MessageManagementPage.tsx,
    navy-admin/src/pages/common/system-mgr/AccessLogPage.tsx,
    navy-admin/src/pages/common/system-mgr/ErrorLogPage.tsx,
    navy-admin/src/pages/common/index.tsx
  </files>
  <read_first>
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/SearchForm/SearchForm.tsx,
    navy-admin/src/shared/ui/DetailModal/DetailModal.tsx,
    navy-admin/src/entities/approval/types.ts,
    navy-admin/src/entities/approval/api.ts,
    navy-admin/src/shared/lib/csv.ts,
    navy-admin/src/shared/api/mocks/handlers/common/system.ts,
    navy-admin/src/pages/common/index.tsx,
    navy-admin/src/pages/portal/DemoPage.tsx,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**1) pages/common/approval/ApprovalLinePage.tsx (COM-06):**
RESEARCH Pattern 3 적용 -- Transfer 컴포넌트로 결재자 선택:
- DataTable<ApprovalLine> 목록: lineName, approvers 수, createdAt, 액션(수정/삭제/상세)
- '등록' 버튼 → Modal:
  - lineName(text), description(textarea) 입력 필드
  - antd `Transfer` 컴포넌트로 결재자 선택:
    - dataSource: `/api/common/users`에서 가져온 전체 사용자 목록, key=userId, title=`${rank} ${name} (${unit})`
    - targetKeys: 선택된 결재자 userId 배열
    - showSearch, filter: item.title.includes(inputValue)
  - Transfer 아래에 선택된 결재자 순서 표시 (List + 위/아래 버튼):
    - RESEARCH Pitfall 3 대응: Transfer의 targetKeys와 별도로 `orderedApproverIds: string[]` 상태 관리
    - 순서 변경 버튼: 선택된 항목을 위/아래로 이동
  - 저장 시: `approvalLineApi.create({ lineName, description, approvers: orderedApproverIds.map((uid, i) => ({ userId: uid, order: i + 1 })) })`
- '수정' 시: 기존 결재선 데이터로 Transfer/순서 초기화
- '상세' 시: DetailModal로 결재자 순서 표시
- '삭제': showConfirmDialog 사용

**2) pages/common/system-mgr/index.tsx:**
antd Tabs로 5개 탭 구성:
```typescript
import { Tabs } from 'antd'
import { SystemManagerPage } from './SystemManagerPage'
import { MenuManagementPage } from './MenuManagementPage'
import { MessageManagementPage } from './MessageManagementPage'
import { AccessLogPage } from './AccessLogPage'
import { ErrorLogPage } from './ErrorLogPage'

export default function SystemMgrIndex() {
  return (
    <Tabs
      defaultActiveKey="manager"
      items={[
        { key: 'manager', label: '체계담당자', children: <SystemManagerPage /> },
        { key: 'menu', label: '메뉴관리', children: <MenuManagementPage /> },
        { key: 'message', label: '메시지 관리', children: <MessageManagementPage /> },
        { key: 'access-log', label: '접속로그 조회', children: <AccessLogPage /> },
        { key: 'error-log', label: '장애로그 조회', children: <ErrorLogPage /> },
      ]}
    />
  )
}
```

**3) pages/common/system-mgr/SystemManagerPage.tsx (COM-01):**
DataTable + CrudForm 표준 CRUD 패턴:
- columns: managerName, managerRank, subsystemCode, contactInfo, createdAt, 액션(수정/삭제)
- CrudForm fields: managerName(text), managerRank(text), subsystemCode(select, options: SUBSYSTEM_META에서 추출), contactInfo(text), description(textarea)
- TanStack Query CRUD

**4) pages/common/system-mgr/MenuManagementPage.tsx (COM-02):**
antd Tree로 메뉴 계층 표시 + 등록/수정:
- 좌측: antd Tree로 전체 메뉴 계층 (SUBSYSTEM_MENUS 기반 MSW 데이터)
  - Tree `selectedKeys`로 메뉴 선택
- 우측: 선택된 메뉴의 상세 정보 + 수정 폼
  - menuName, menuPath, sortOrder, useYn 필드
  - '저장' 버튼으로 수정
- 상단: '메뉴 추가' 버튼 → Modal (parentId select + 메뉴 정보 입력)

**5) pages/common/system-mgr/MessageManagementPage.tsx (COM-03):**
DataTable + CrudForm 표준 CRUD:
- columns: messageCode, messageContent, messageType, useYn, 액션
- CrudForm fields: messageCode(text), messageContent(textarea), messageType(select, options: [{label:'정보',value:'INFO'},{label:'경고',value:'WARN'},{label:'오류',value:'ERROR'}]), useYn(select)

**6) pages/common/system-mgr/AccessLogPage.tsx (COM-04):**
읽기 전용 DataTable + SearchForm + CSV 내보내기:
- SearchForm: keyword(검색어), dateRange(날짜 범위 -- antd DatePicker.RangePicker 사용 또는 두 개의 date 필드)
- DataTable columns: userName, loginTime, logoutTime, ipAddress, subsystemCode
- 행 클릭 시 DetailModal로 상세 (userAgent 포함)
- toolBarRender에 'CSV 저장' 버튼:
  ```typescript
  import { downloadCsv } from '@/shared/lib/csv'
  // 현재 페이지 데이터가 아닌 전체 데이터를 CSV로 내보내려면 별도 API 호출 또는
  // 현재 조회 결과를 CSV로 변환 (MVP에서는 현재 페이지 데이터만 내보내기)
  onClick={() => downloadCsv('access-log.csv', tableData, [
    { key: 'userName', label: '사용자명' },
    { key: 'loginTime', label: '접속시간' },
    { key: 'logoutTime', label: '종료시간' },
    { key: 'ipAddress', label: 'IP주소' },
    { key: 'subsystemCode', label: '접속체계' },
  ])
  ```

**7) pages/common/system-mgr/ErrorLogPage.tsx (COM-05):**
읽기 전용 DataTable (단일 화면):
- columns: errorCode, errorMessage, errorLevel, occurredAt, resolvedAt
- 검색 필드 없음 (단순 목록 조회)
- toolBarRender 없음 (CRUD 없는 읽기 전용)

**8) pages/common/index.tsx 수정:**
approval, system-mgr Route의 element를 실제 컴포넌트로 교체:
```typescript
const ApprovalLinePage = lazy(() => import('./approval/ApprovalLinePage'))
const SystemMgrIndex = lazy(() => import('./system-mgr'))
// ...
<Route path="approval" element={<Suspense fallback={<PageSpinner />}><ApprovalLinePage /></Suspense>} />
<Route path="system-mgr" element={<Suspense fallback={<PageSpinner />}><SystemMgrIndex /></Suspense>} />
```
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20 && npm run test -- --run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/common/approval/ApprovalLinePage.tsx contains `Transfer`
    - navy-admin/src/pages/common/approval/ApprovalLinePage.tsx contains `approvalLineApi`
    - navy-admin/src/pages/common/approval/ApprovalLinePage.tsx contains `orderedApproverIds` or `orderedApprovers`
    - navy-admin/src/pages/common/system-mgr/index.tsx contains `Tabs`
    - navy-admin/src/pages/common/system-mgr/SystemManagerPage.tsx contains `DataTable`
    - navy-admin/src/pages/common/system-mgr/MenuManagementPage.tsx contains `Tree`
    - navy-admin/src/pages/common/system-mgr/MessageManagementPage.tsx contains `DataTable`
    - navy-admin/src/pages/common/system-mgr/AccessLogPage.tsx contains `downloadCsv`
    - navy-admin/src/pages/common/system-mgr/AccessLogPage.tsx contains `CSV`
    - navy-admin/src/pages/common/system-mgr/ErrorLogPage.tsx contains `DataTable` or `ProTable`
    - navy-admin/src/pages/common/index.tsx contains `ApprovalLinePage`
    - navy-admin/src/pages/common/index.tsx contains `SystemMgrIndex`
    - TypeScript noEmit: 0 errors
    - All existing tests pass
  </acceptance_criteria>
  <done>결재선 관리(Transfer 결재자 선택+순서 지정), 시스템관리 5개 탭(체계담당자/메뉴/메시지/접속로그CSV/장애로그) 완성, /common/approval과 /common/system-mgr URL 동작</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` -- 0 errors
2. `cd navy-admin && npm run test -- --run` -- 기존 테스트 모두 통과
3. 브라우저에서:
   - /common/approval: 결재선 목록, Transfer로 결재자 선택, 순서 변경
   - /common/system-mgr: 5개 탭 동작
   - 접속로그 'CSV 저장' 클릭 시 파일 다운로드
</verification>

<success_criteria>
- /common/approval URL에서 결재선 CRUD + Transfer 결재자 선택이 동작한다
- /common/system-mgr URL에서 5개 탭(체계담당자/메뉴/메시지/접속로그/장애로그)이 동작한다
- 접속로그 CSV 내보내기가 동작하고, BOM 포함으로 한글 깨짐 없다
- TypeScript 컴파일 에러 없음, 기존 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/01-99/01-03-SUMMARY.md`
</output>
