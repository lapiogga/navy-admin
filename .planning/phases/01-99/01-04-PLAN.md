---
phase: 01-99
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - navy-admin/src/entities/board/types.ts
  - navy-admin/src/entities/board/api.ts
  - navy-admin/src/entities/board/index.ts
  - navy-admin/src/shared/api/mocks/handlers/common/board.ts
  - navy-admin/src/shared/api/mocks/handlers/common/index.ts
  - navy-admin/src/pages/common/board/BoardConfigPage.tsx
  - navy-admin/src/pages/common/board/BoardListPage.tsx
  - navy-admin/src/pages/common/board/BoardPostPage.tsx
  - navy-admin/src/pages/common/board/BoardCategoryPage.tsx
  - navy-admin/src/pages/common/board/index.tsx
  - navy-admin/src/pages/common/index.tsx
  - navy-admin/src/__tests__/common/board.test.ts
autonomous: true
requirements: [COM-09]

must_haves:
  truths:
    - "게시판 설정에서 게시판을 생성/수정/삭제하면 목록에 즉시 반영된다"
    - "게시판을 선택하면 해당 게시판의 게시글 목록이 표시된다"
    - "게시글을 작성하면 목록에 나타나고, 첨부파일 Upload UI가 동작한다"
    - "게시글 상세에서 댓글을 작성/삭제할 수 있다"
    - "카테고리 관리에서 게시판별 카테고리 CRUD가 동작한다"
  artifacts:
    - path: "navy-admin/src/entities/board/types.ts"
      provides: "BoardConfig, BoardPost, BoardComment, BoardAttachment, BoardCategory 타입"
      contains: "interface BoardConfig"
    - path: "navy-admin/src/entities/board/api.ts"
      provides: "게시판 관련 전체 API"
      exports: ["boardConfigApi", "boardPostApi", "boardCommentApi", "boardCategoryApi"]
    - path: "navy-admin/src/shared/api/mocks/handlers/common/board.ts"
      provides: "게시판 관련 MSW 핸들러"
      exports: ["boardHandlers"]
    - path: "navy-admin/src/pages/common/board/index.tsx"
      provides: "게시판 관리 진입점"
  key_links:
    - from: "navy-admin/src/pages/common/board/BoardPostPage.tsx"
      to: "/api/common/boards/:boardId/posts"
      via: "boardPostApi.list"
      pattern: "boardPostApi"
    - from: "navy-admin/src/pages/common/board/BoardPostPage.tsx"
      to: "antd Upload"
      via: "Upload action='/api/common/board/attachments'"
      pattern: "Upload"
    - from: "navy-admin/src/pages/common/board/index.tsx"
      to: "navy-admin/src/pages/common/board/BoardCategoryPage.tsx"
      via: "selectedBoardId prop 전달"
      pattern: "selectedBoardId"
    - from: "navy-admin/src/pages/common/board/index.tsx"
      to: "navy-admin/src/pages/common/board/BoardListPage.tsx"
      via: "selectedBoardId prop 전달"
      pattern: "selectedBoardId"
---

<objective>
공통게시판(COM-09)을 구현한다: 게시판 설정, 카테고리, 게시글 CRUD, 댓글, 첨부파일 Mock 업로드.

Purpose: 공통게시판은 32개 프로세스를 포함하는 Phase 1 최대 복잡도 모듈이다. 이후 서브시스템들이 boardId를 기반으로 게시판 기능을 재사용하므로 entities/board/ API를 재사용 가능하게 설계한다.

Output: 게시판 설정/카테고리/게시글/댓글/첨부파일 5개 서브도메인 화면과 MSW 핸들러
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-99/01-RESEARCH.md
@.planning/phases/01-99/01-01-SUMMARY.md

<interfaces>
From navy-admin/src/shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number; sort?: string[] }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string; errorCode?: string }
```

From navy-admin/src/shared/ui/index.ts:
```typescript
export { DataTable, CrudForm, DetailModal, SearchForm, StatusBadge, showConfirmDialog, SubsystemPage }
```

From navy-admin/src/shared/api/client.ts:
```typescript
export const apiClient = axios.create({ baseURL: '/api', ... })
```

From navy-admin/src/features/common/hooks/useCodeOptions.ts (Plan 01에서 생성):
```typescript
export function useCodeOptions(groupCode: string) {
  return useQuery({
    queryKey: ['code-options', groupCode],
    queryFn: () => codeApi.getOptions(groupCode),
    staleTime: 5 * 60 * 1000,
    enabled: !!groupCode,
  })
}
```

From navy-admin/src/shared/api/mocks/handlers/demo.ts (MSW 패턴 참조):
- faker.js, ApiResult 래핑, page/size URL params
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 게시판 entity/API, MSW 핸들러 (5개 서브도메인), 테스트 스캐폴드</name>
  <files>
    navy-admin/src/entities/board/types.ts,
    navy-admin/src/entities/board/api.ts,
    navy-admin/src/entities/board/index.ts,
    navy-admin/src/shared/api/mocks/handlers/common/board.ts,
    navy-admin/src/shared/api/mocks/handlers/common/index.ts,
    navy-admin/src/__tests__/common/board.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/shared/api/client.ts,
    navy-admin/src/shared/api/mocks/handlers/demo.ts,
    navy-admin/src/shared/api/mocks/handlers/common/index.ts,
    navy-admin/src/entities/code/types.ts
  </read_first>
  <action>
**1) entities/board/types.ts 생성:**
5개 서브도메인 타입 정의:
```typescript
/** 게시판 설정 (메타) */
export interface BoardConfig {
  id: string
  boardName: string           // 게시판 이름 (예: '공지사항', '질의응답')
  boardType: string           // 'NOTICE' | 'QNA' | 'FREE' | 'DATA'
  subsystemCode: string       // 예: 'common', 'sys01'
  description: string
  useCategory: boolean        // 카테고리 사용 여부
  useAttachment: boolean      // 첨부파일 허용 여부
  useComment: boolean         // 댓글 허용 여부
  maxAttachSize: number       // MB 단위 최대 첨부 크기
  createdAt: string
  updatedAt: string
}

/** 게시판 카테고리 */
export interface BoardCategory {
  id: string
  boardId: string
  categoryName: string
  sortOrder: number
  useYn: string
}

/** 게시글 */
export interface BoardPost {
  id: string
  boardId: string
  categoryId?: string
  title: string
  content: string
  authorId: string
  authorName: string
  authorUnit: string
  viewCount: number
  isPinned: boolean           // 공지 고정 여부
  status: string              // 'ACTIVE' | 'DELETED' | 'HIDDEN'
  attachments: BoardAttachment[]
  createdAt: string
  updatedAt: string
}

/** 첨부파일 */
export interface BoardAttachment {
  id: string
  postId: string
  fileName: string
  fileSize: number            // bytes
  mimeType: string
  uploadedAt: string
}

/** 댓글 */
export interface BoardComment {
  id: string
  postId: string
  content: string
  authorId: string
  authorName: string
  createdAt: string
}
```

**2) entities/board/api.ts 생성:**
4개 API 객체:

- `boardConfigApi`:
  - `list(params: PageRequest): Promise<PageResponse<BoardConfig>>`
  - `detail(id: string): Promise<ApiResult<BoardConfig>>`
  - `create(data: Omit<BoardConfig, 'id' | 'createdAt' | 'updatedAt'>)`
  - `update(id, data)`, `delete(id)`
  - 경로: `/common/board-configs`, `/common/board-configs/:id`

- `boardCategoryApi`:
  - `list(boardId: string): Promise<ApiResult<BoardCategory[]>>` (비페이지, 전체 반환)
  - `create(boardId, data)`, `update(boardId, id, data)`, `delete(boardId, id)`
  - 경로: `/common/boards/:boardId/categories`

- `boardPostApi`:
  - `list(boardId: string, params: PageRequest & { categoryId?: string; keyword?: string }): Promise<PageResponse<BoardPost>>`
  - `detail(boardId: string, id: string)`
  - `create(boardId, data)`, `update(boardId, id, data)`, `delete(boardId, id)`
  - 경로: `/common/boards/:boardId/posts`, `/common/boards/:boardId/posts/:id`

- `boardCommentApi`:
  - `list(postId: string): Promise<ApiResult<BoardComment[]>>` (비페이지, 전체 반환)
  - `create(postId, data: { content: string })`, `delete(postId, commentId)`
  - 경로: `/common/posts/:postId/comments`

- 첨부파일은 antd Upload의 action으로 직접 호출되므로 별도 API 객체 불필요. 삭제만:
  - `boardAttachmentApi.delete(id)`
  - 경로: DELETE `/common/board/attachments/:id`

**3) entities/board/index.ts 배럴 export.**

**4) shared/api/mocks/handlers/common/board.ts 생성:**
faker.js로 Mock 데이터:
- `mockBoardConfigs: BoardConfig[]` -- 5개 (공지사항, 질의응답, 자료실, 자유게시판, 건의사항)
- `mockCategories: BoardCategory[]` -- 각 게시판별 2~4개 카테고리
- `mockPosts: BoardPost[]` -- 각 게시판별 15~30개 게시글 (faker.lorem + 한국어 이름)
- `mockComments: BoardComment[]` -- 게시글당 0~5개 댓글
- `mockAttachments: BoardAttachment[]` -- 일부 게시글에 1~3개 첨부파일

MSW 핸들러:
- 게시판 설정: GET/POST `/api/common/board-configs`, GET/PUT/DELETE `/api/common/board-configs/:id`
- 카테고리: GET/POST `/api/common/boards/:boardId/categories`, PUT/DELETE `/api/common/boards/:boardId/categories/:id`
- 게시글: GET/POST `/api/common/boards/:boardId/posts`, GET/PUT/DELETE `/api/common/boards/:boardId/posts/:id`
  - GET 목록: page/size/keyword/categoryId 파라미터 처리
- 댓글: GET/POST `/api/common/posts/:postId/comments`, DELETE `/api/common/posts/:postId/comments/:id`
- 첨부파일:
  - `http.post('/api/common/board/attachments', ...)` -- Mock: 파일 저장 없이 id/fileName/fileSize만 반환
  - `http.delete('/api/common/board/attachments/:id', ...)`

RESEARCH Pitfall 5 대응: 첨부 Upload의 onPreview/onDownload는 페이지에서 처리 (MSW는 메타데이터만)

**5) shared/api/mocks/handlers/common/index.ts 수정:**
boardHandlers 추가:
```typescript
import { codeHandlers } from './code'
import { approvalHandlers } from './approval'
import { systemHandlers } from './system'
import { boardHandlers } from './board'
// permissionHandlers도 있으면 유지
export const commonHandlers = [...codeHandlers, ...approvalHandlers, ...systemHandlers, ...boardHandlers, ...]
```

**6) src/__tests__/common/board.test.ts 생성 (테스트 스캐폴드):**
```typescript
import { describe, it, expect } from 'vitest'
import type { BoardConfig, BoardPost, BoardComment, BoardCategory } from '@/entities/board/types'

describe('게시판 타입', () => {
  it('BoardConfig 타입이 정의되어 있다', () => {
    const config: BoardConfig = {
      id: '1', boardName: '공지사항', boardType: 'NOTICE',
      subsystemCode: 'common', description: '', useCategory: true,
      useAttachment: true, useComment: true, maxAttachSize: 10,
      createdAt: '', updatedAt: ''
    }
    expect(config.boardName).toBe('공지사항')
  })

  it('BoardPost 타입이 정의되어 있다', () => {
    const post: BoardPost = {
      id: '1', boardId: '1', title: '테스트', content: '',
      authorId: '1', authorName: '홍길동', authorUnit: '1사단',
      viewCount: 0, isPinned: false, status: 'ACTIVE',
      attachments: [], createdAt: '', updatedAt: ''
    }
    expect(post.title).toBe('테스트')
  })
})

describe('boardHandlers MSW 핸들러', () => {
  it('boardHandlers가 export된다', async () => {
    const { boardHandlers } = await import('@/shared/api/mocks/handlers/common/board')
    expect(Array.isArray(boardHandlers)).toBe(true)
    expect(boardHandlers.length).toBeGreaterThan(0)
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose src/__tests__/common/board.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/entities/board/types.ts contains `export interface BoardConfig`
    - navy-admin/src/entities/board/types.ts contains `export interface BoardPost`
    - navy-admin/src/entities/board/types.ts contains `export interface BoardComment`
    - navy-admin/src/entities/board/types.ts contains `export interface BoardAttachment`
    - navy-admin/src/entities/board/types.ts contains `export interface BoardCategory`
    - navy-admin/src/entities/board/api.ts contains `export const boardConfigApi`
    - navy-admin/src/entities/board/api.ts contains `export const boardPostApi`
    - navy-admin/src/entities/board/api.ts contains `export const boardCommentApi`
    - navy-admin/src/entities/board/api.ts contains `export const boardCategoryApi`
    - navy-admin/src/shared/api/mocks/handlers/common/board.ts contains `export const boardHandlers`
    - navy-admin/src/shared/api/mocks/handlers/common/board.ts contains `/api/common/board-configs`
    - navy-admin/src/shared/api/mocks/handlers/common/board.ts contains `/api/common/boards/:boardId/posts`
    - navy-admin/src/shared/api/mocks/handlers/common/board.ts contains `/api/common/posts/:postId/comments`
    - navy-admin/src/shared/api/mocks/handlers/common/board.ts contains `/api/common/board/attachments`
    - navy-admin/src/shared/api/mocks/handlers/common/index.ts contains `boardHandlers`
    - navy-admin/src/__tests__/common/board.test.ts exists
    - TypeScript noEmit: 0 errors
    - board.test.ts: all tests pass
  </acceptance_criteria>
  <done>게시판 5개 서브도메인(설정/카테고리/게시글/댓글/첨부) entity 타입, API, MSW 핸들러, 기본 테스트 스캐폴드 완성</done>
</task>

<task type="auto">
  <name>Task 2: 게시판 관리 페이지 구현 (설정/카테고리/게시글/댓글/첨부)</name>
  <files>
    navy-admin/src/pages/common/board/index.tsx,
    navy-admin/src/pages/common/board/BoardConfigPage.tsx,
    navy-admin/src/pages/common/board/BoardCategoryPage.tsx,
    navy-admin/src/pages/common/board/BoardListPage.tsx,
    navy-admin/src/pages/common/board/BoardPostPage.tsx,
    navy-admin/src/pages/common/index.tsx
  </files>
  <read_first>
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/DetailModal/DetailModal.tsx,
    navy-admin/src/shared/ui/SearchForm/SearchForm.tsx,
    navy-admin/src/entities/board/types.ts,
    navy-admin/src/entities/board/api.ts,
    navy-admin/src/shared/api/mocks/handlers/common/board.ts,
    navy-admin/src/pages/common/index.tsx,
    navy-admin/src/features/common/hooks/useCodeOptions.ts
  </read_first>
  <action>
**1) pages/common/board/index.tsx 생성:**
게시판 관리 진입점 -- 탭으로 구성. selectedBoardId를 BoardCategoryPage/BoardListPage에 prop으로 전달:
```typescript
import { useState } from 'react'
import { Tabs } from 'antd'
import { BoardConfigPage } from './BoardConfigPage'
import { BoardCategoryPage } from './BoardCategoryPage'
import { BoardListPage } from './BoardListPage'

export default function BoardIndex() {
  const [selectedBoardId, setSelectedBoardId] = useState<string | null>(null)

  return (
    <Tabs
      defaultActiveKey="config"
      items={[
        { key: 'config', label: '게시판 설정', children: <BoardConfigPage onSelectBoard={setSelectedBoardId} /> },
        { key: 'category', label: '카테고리 관리', children: <BoardCategoryPage boardId={selectedBoardId} /> },
        { key: 'posts', label: '게시글 관리', children: <BoardListPage boardId={selectedBoardId} /> },
      ]}
    />
  )
}
```
게시판 설정 탭에서 게시판을 선택하면 selectedBoardId가 설정되고, 다른 탭에서 해당 게시판의 카테고리/게시글을 관리.

**2) pages/common/board/BoardConfigPage.tsx:**
게시판 메타 CRUD:
- DataTable<BoardConfig> columns: boardName, boardType, subsystemCode, useCategory(boolean), useAttachment(boolean), createdAt, 액션(수정/삭제)
- 행 클릭 시 onSelectBoard(record.id) 콜백
- toolBarRender: '게시판 생성' 버튼 -> Modal + CrudForm
- CrudForm fields:
  - boardName(text, required)
  - boardType(select, options: [{label:'공지사항',value:'NOTICE'},{label:'질의응답',value:'QNA'},{label:'자유게시판',value:'FREE'},{label:'자료실',value:'DATA'}])
  - subsystemCode(select, options: SUBSYSTEM_META에서 추출 + 'common')
  - description(textarea)
  - useCategory/useAttachment/useComment -- antd Switch로 구현하되, CrudForm에 없는 필드이므로 Modal 내부에서 직접 antd Form을 사용하거나 CrudForm의 select type으로 'Y'/'N' 대체:
    - useCategory(select, options: [{label:'사용',value:true},{label:'미사용',value:false}])
    - useAttachment(select, options: 동일)
    - useComment(select, options: 동일)
  - maxAttachSize(number)

**3) pages/common/board/BoardCategoryPage.tsx:**
선택된 게시판의 카테고리 CRUD:
- boardId가 null이면 "게시판 설정 탭에서 게시판을 먼저 선택하세요" 메시지 표시
- DataTable<BoardCategory> columns: categoryName, sortOrder, useYn, 액션(수정/삭제)
- '카테고리 추가' 버튼 -> Modal + CrudForm (categoryName, sortOrder(number), useYn(select))
- TanStack Query: queryKey에 boardId 포함

**4) pages/common/board/BoardListPage.tsx:**
선택된 게시판의 게시글 목록 + 작성/상세:
- boardId가 null이면 "게시판을 선택하세요" 메시지
- SearchForm: keyword 검색
- DataTable<BoardPost> columns: title, authorName, authorUnit, viewCount, isPinned(StatusBadge), createdAt
- '게시글 작성' 버튼 -> BoardPostPage(모달 또는 별도 패널)로 이동
- 행 클릭 -> BoardPostPage(상세 뷰)

**5) pages/common/board/BoardPostPage.tsx:**
게시글 작성/수정/상세 모달:
- 모드: create / edit / view (CrudForm mode 활용)
- 필드: title(text), content(textarea), categoryId(select, useCodeOptions 아닌 boardCategoryApi.list에서 동적 로드), isPinned(select: Y/N)
- 첨부파일 섹션 (boardConfig.useAttachment가 true인 경우):
  - antd `Upload` 컴포넌트:
    ```typescript
    <Upload
      action="/api/common/board/attachments"
      onChange={({ fileList }) => setFileList(fileList)}
      fileList={fileList}
      onPreview={() => message.info('Mock 환경에서는 파일 미리보기를 지원하지 않습니다')}
    >
      <Button icon={<UploadOutlined />}>파일 첨부</Button>
    </Upload>
    ```
  - RESEARCH Pitfall 5: onPreview를 커스텀 구현하여 Mock에서는 안내 메시지
- 댓글 섹션 (boardConfig.useComment가 true인 경우):
  - 댓글 목록: boardCommentApi.list(postId) 조회 -> List 컴포넌트로 표시
  - 댓글 입력: TextArea + '등록' 버튼 -> boardCommentApi.create(postId, { content })
  - 댓글 삭제: showConfirmDialog -> boardCommentApi.delete
- 상세 뷰(view 모드)에서는 조회수 표시, 첨부파일 목록(다운로드 버튼 -- Mock에서는 message.info), 댓글

**6) pages/common/index.tsx 수정:**
board Route의 element를 실제 컴포넌트로 교체:
```typescript
const BoardIndex = lazy(() => import('./board'))
// ...
<Route path="board" element={<Suspense fallback={<PageSpinner />}><BoardIndex /></Suspense>} />
```
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/common/board/index.tsx contains `export default function BoardIndex`
    - navy-admin/src/pages/common/board/index.tsx contains `Tabs`
    - navy-admin/src/pages/common/board/index.tsx contains `selectedBoardId`
    - navy-admin/src/pages/common/board/index.tsx contains `BoardCategoryPage boardId={selectedBoardId}`
    - navy-admin/src/pages/common/board/index.tsx contains `BoardListPage boardId={selectedBoardId}`
    - navy-admin/src/pages/common/board/BoardConfigPage.tsx contains `DataTable<BoardConfig>`
    - navy-admin/src/pages/common/board/BoardConfigPage.tsx contains `boardConfigApi`
    - navy-admin/src/pages/common/board/BoardCategoryPage.tsx contains `DataTable<BoardCategory>`
    - navy-admin/src/pages/common/board/BoardCategoryPage.tsx contains `boardCategoryApi`
    - navy-admin/src/pages/common/board/BoardListPage.tsx contains `DataTable<BoardPost>`
    - navy-admin/src/pages/common/board/BoardListPage.tsx contains `boardPostApi`
    - navy-admin/src/pages/common/board/BoardPostPage.tsx contains `Upload`
    - navy-admin/src/pages/common/board/BoardPostPage.tsx contains `boardCommentApi`
    - navy-admin/src/pages/common/board/BoardPostPage.tsx contains `onPreview`
    - navy-admin/src/pages/common/index.tsx contains `BoardIndex`
    - TypeScript noEmit: 0 errors
    - All tests pass (including board.test.ts)
  </acceptance_criteria>
  <done>게시판 전체 기능 완성: 설정 CRUD + 카테고리 CRUD + 게시글 CRUD(검색/고정/상세) + 댓글 CRUD + 첨부파일 Upload Mock, selectedBoardId prop wiring 동작, /common/board URL 동작</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` -- 0 errors
2. `cd navy-admin && npx vitest run --reporter=verbose` -- 모든 테스트 통과 (board.test.ts 포함)
3. 브라우저에서:
   - /common/board 접속 시 3개 탭 표시
   - 게시판 설정: CRUD 동작, 게시판 선택 시 다른 탭 연동 (selectedBoardId prop 전달)
   - 카테고리 관리: 선택된 게시판의 카테고리 CRUD
   - 게시글 관리: 목록 조회, 작성(첨부파일 Upload 포함), 상세(댓글 작성/삭제)
</verification>

<success_criteria>
- /common/board URL에서 게시판 설정/카테고리/게시글 3개 탭이 동작한다
- 게시판을 선택하면 카테고리와 게시글 탭이 해당 게시판의 데이터를 표시한다 (selectedBoardId prop wiring)
- 게시글 작성 시 antd Upload로 첨부파일을 추가할 수 있다 (Mock 응답)
- 게시글 상세에서 댓글 작성/삭제가 동작한다
- TypeScript 컴파일 에러 없음, 기존 + 신규 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/01-99/01-04-SUMMARY.md`
</output>
