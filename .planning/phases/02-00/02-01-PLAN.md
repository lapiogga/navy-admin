---
phase: 02-00
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/features/announcements/types.ts
  - navy-admin/src/features/announcements/hooks/useAnnouncements.ts
  - navy-admin/src/features/announcements/components/AnnouncementSection.tsx
  - navy-admin/src/shared/api/mocks/handlers/announcements.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/pages/portal/index.tsx
  - navy-admin/src/pages/login/index.tsx
  - navy-admin/src/app/layouts/MainPortalLayout.tsx
  - navy-admin/src/app/layouts/PortalLayout.tsx
  - navy-admin/src/__tests__/portal/announcement.test.ts
  - navy-admin/src/__tests__/portal/header.test.ts
  - navy-admin/src/shared/api/mocks/handlers/announcements.test.ts
autonomous: true
requirements:
  - PTL-01
  - PTL-02
  - PTL-03
  - PTL-04
  - PTL-05

must_haves:
  truths:
    - "대시보드 상단에 공지사항 최신 3건이 표시된다"
    - "긴급 공지사항은 antd Alert type=warning으로 별도 표시된다"
    - "서브시스템 카드 그리드가 18개 표시되고 클릭 시 새 창이 열린다"
    - "로그아웃 시 message.success('로그아웃 되었습니다')가 표시된다"
    - "헤더에 계급 이름 (소속부대) 형식으로 사용자 정보가 표시된다"
    - "서브시스템 헤더에 '메인포탈로 돌아가기' 버튼이 있고 클릭 시 window.close() + opener.focus() 동작한다"
  artifacts:
    - path: "navy-admin/src/features/announcements/types.ts"
      provides: "Announcement 타입 정의"
      contains: "export interface Announcement"
    - path: "navy-admin/src/features/announcements/components/AnnouncementSection.tsx"
      provides: "공지사항 섹션 컴포넌트"
      exports: ["AnnouncementSection"]
      min_lines: 25
    - path: "navy-admin/src/shared/api/mocks/handlers/announcements.ts"
      provides: "공지사항 MSW 핸들러"
      exports: ["announcementHandlers"]
    - path: "navy-admin/src/pages/portal/index.tsx"
      provides: "대시보드 페이지 (공지사항 + 카드 그리드)"
      contains: "AnnouncementSection"
    - path: "navy-admin/src/app/layouts/PortalLayout.tsx"
      provides: "서브시스템 레이아웃 — 메인포탈로 돌아가기 버튼 포함"
      contains: "메인포탈로 돌아가기"
  key_links:
    - from: "navy-admin/src/features/announcements/components/AnnouncementSection.tsx"
      to: "/api/announcements"
      via: "useQuery + apiClient"
      pattern: "queryKey.*announcements"
    - from: "navy-admin/src/pages/portal/index.tsx"
      to: "navy-admin/src/features/announcements/components/AnnouncementSection.tsx"
      via: "import AnnouncementSection"
      pattern: "import.*AnnouncementSection"
    - from: "navy-admin/src/shared/api/mocks/handlers/index.ts"
      to: "navy-admin/src/shared/api/mocks/handlers/announcements.ts"
      via: "spread announcementHandlers"
      pattern: "announcementHandlers"
    - from: "navy-admin/src/app/layouts/PortalLayout.tsx"
      to: "window.opener.focus()"
      via: "handleGoPortal 함수"
      pattern: "opener\\.focus.*close"
---

<objective>
대시보드에 공지사항 배너를 추가하고, 로그인/로그아웃 메시지를 보강하며, 서브시스템 헤더의 '메인포탈로 돌아가기' 버튼 동작을 검증하여 PTL-01~PTL-05 요구사항을 충족한다.

Purpose: 메인 포탈 진입 후 사용자가 공지사항을 확인하고 서브시스템에 접근하는 핵심 플로우를 완성한다.
Output: 공지사항 feature slice(타입/훅/컴포넌트/MSW), 대시보드 보강, 로그인/로그아웃 메시지 개선, 서브시스템 복귀 버튼 검증, 테스트
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-00/02-CONTEXT.md
@.planning/phases/02-00/02-RESEARCH.md
@.planning/phases/02-00/02-UI-SPEC.md
@.planning/phases/02-00/02-VALIDATION.md

<interfaces>
<!-- 기존 코드 인터페이스 — executor는 이 코드를 기반으로 작업 -->

From navy-admin/src/shared/api/types.ts:
```typescript
export interface ApiResult<T = void> {
  success: boolean
  data: T
  message?: string
  errorCode?: string
}
```

From navy-admin/src/shared/api/client.ts:
```typescript
export const apiClient = axios.create({ baseURL: '/api', ... })
// interceptor가 response.data를 반환하므로 apiClient.get() 결과는 ApiResult 자체
```

From navy-admin/src/entities/user/types.ts:
```typescript
export interface User {
  id: string; name: string; rank: string; unit: string; username: string; roles: string[]
}
```

From navy-admin/src/shared/config/routes.ts:
```typescript
export const ROUTES = { LOGIN: '/login', PORTAL: '/', ... } as const
```

From navy-admin/src/entities/subsystem/config.ts:
```typescript
export const SUBSYSTEM_META: Record<string, SubsystemMeta> = { sys01: {...}, ... } // 18개
```

From navy-admin/src/app/layouts/PortalLayout.tsx (SubsystemProLayout):
```typescript
// 이미 구현된 '메인포탈로 돌아가기' 로직 (D-08):
const handleGoPortal = () => {
  if (window.opener) {
    window.opener.focus()
    window.close()
  } else {
    navigate(ROUTES.PORTAL)
  }
}
// Dropdown 메뉴에 { key: 'portal', label: '메인포탈로 돌아가기', onClick: handleGoPortal } 포함
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공지사항 feature slice + MSW 핸들러 생성</name>
  <files>
    navy-admin/src/features/announcements/types.ts,
    navy-admin/src/features/announcements/hooks/useAnnouncements.ts,
    navy-admin/src/features/announcements/components/AnnouncementSection.tsx,
    navy-admin/src/shared/api/mocks/handlers/announcements.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/shared/api/client.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/api/mocks/handlers/auth.ts
  </read_first>
  <action>
**1. `navy-admin/src/features/announcements/types.ts` 생성:**
```typescript
export interface Announcement {
  id: string
  title: string
  content: string
  createdAt: string
  isUrgent: boolean
}
```

**2. `navy-admin/src/shared/api/mocks/handlers/announcements.ts` 생성:**
- auth.ts 핸들러 패턴을 따름 (http.get, HttpResponse.json, ApiResult 래퍼)
- `http.get('/api/announcements', ...)` — 3건의 Mock 공지 반환
- Mock 데이터:
  - id '1', title '시스템 점검 안내', isUrgent: true, createdAt '2026-04-05'
  - id '2', title '해군 행정포탈 v1.0 오픈', isUrgent: false, createdAt '2026-04-04'
  - id '3', title '사용자 매뉴얼 배포', isUrgent: false, createdAt '2026-04-03'
- `export const announcementHandlers = [...]`

**3. `navy-admin/src/shared/api/mocks/handlers/index.ts` 수정:**
- `import { announcementHandlers } from './announcements'` 추가
- handlers 배열에 `...announcementHandlers` spread 추가

**4. `navy-admin/src/features/announcements/hooks/useAnnouncements.ts` 생성:**
```typescript
import { useQuery } from '@tanstack/react-query'
import { apiClient } from '@/shared/api/client'
import type { ApiResult } from '@/shared/api/types'
import type { Announcement } from '../types'

export function useAnnouncements() {
  return useQuery({
    queryKey: ['announcements'],
    queryFn: () =>
      apiClient.get<unknown, ApiResult<Announcement[]>>('/announcements')
        .then((r) => (r as ApiResult<Announcement[]>).data ?? (r as unknown as Announcement[])),
  })
}
```
주의: apiClient interceptor가 response.data를 반환하므로 이중 래핑 방어 패턴 적용 (Phase 1 결정: `(res as ApiResult).data ?? res`)

**5. `navy-admin/src/features/announcements/components/AnnouncementSection.tsx` 생성:**
- per D-01: 대시보드 상단에 공지사항 배너 배치 (최신 3건 표시, antd List/Alert 활용)
- `import { List, Alert, Typography } from 'antd'`
- `import { useAnnouncements } from '../hooks/useAnnouncements'`
- `import type { Announcement } from '../types'`
- `const { Title, Text } = Typography`
- `export function AnnouncementSection()`
- useAnnouncements() 호출 -> data, isLoading 추출
- urgentItem = data?.find((a) => a.isUrgent)
- items = data?.slice(0, 3) ?? []
- 렌더링:
  - `<div className="mb-12">` (2xl=48px 하단 여백)
  - `<Title level={5}>공지사항</Title>` (UI-SPEC 카피)
  - urgentItem 있으면 `<Alert type="warning" message={urgentItem.title} closable className="mb-2" />`
  - `<List size="small" bordered={false} loading={isLoading} dataSource={items} locale={{ emptyText: '등록된 공지사항이 없습니다' }}>`
  - renderItem: `<List.Item>` + `<List.Item.Meta title={item.title} description={<Text type="secondary">{item.createdAt}</Text>} />`
  </action>
  <verify>
    <automated>cd navy-admin && grep -r "announcementHandlers" src/shared/api/mocks/handlers/index.ts && grep -r "export function AnnouncementSection" src/features/announcements/components/AnnouncementSection.tsx && grep -r "export interface Announcement" src/features/announcements/types.ts && echo "--- All files verified ---"</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/features/announcements/types.ts contains `export interface Announcement`
    - navy-admin/src/features/announcements/types.ts contains `isUrgent: boolean`
    - navy-admin/src/shared/api/mocks/handlers/announcements.ts contains `export const announcementHandlers`
    - navy-admin/src/shared/api/mocks/handlers/announcements.ts contains `http.get('/api/announcements'`
    - navy-admin/src/shared/api/mocks/handlers/announcements.ts contains `시스템 점검 안내`
    - navy-admin/src/shared/api/mocks/handlers/index.ts contains `announcementHandlers`
    - navy-admin/src/features/announcements/hooks/useAnnouncements.ts contains `queryKey: ['announcements']`
    - navy-admin/src/features/announcements/components/AnnouncementSection.tsx contains `export function AnnouncementSection`
    - navy-admin/src/features/announcements/components/AnnouncementSection.tsx contains `Alert type="warning"`
    - navy-admin/src/features/announcements/components/AnnouncementSection.tsx contains `등록된 공지사항이 없습니다`
  </acceptance_criteria>
  <done>공지사항 feature slice(타입/훅/컴포넌트) + MSW 핸들러가 생성되고, handlers/index.ts에 등록됨</done>
</task>

<task type="auto">
  <name>Task 2: 대시보드 보강 + 로그인/로그아웃 메시지 + D-08 검증 + 테스트</name>
  <files>
    navy-admin/src/pages/portal/index.tsx,
    navy-admin/src/pages/login/index.tsx,
    navy-admin/src/app/layouts/MainPortalLayout.tsx,
    navy-admin/src/app/layouts/PortalLayout.tsx,
    navy-admin/src/__tests__/portal/announcement.test.ts,
    navy-admin/src/__tests__/portal/header.test.ts,
    navy-admin/src/shared/api/mocks/handlers/announcements.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/portal/index.tsx,
    navy-admin/src/pages/login/index.tsx,
    navy-admin/src/app/layouts/MainPortalLayout.tsx,
    navy-admin/src/app/layouts/PortalLayout.tsx,
    navy-admin/src/features/announcements/components/AnnouncementSection.tsx,
    navy-admin/src/entities/subsystem/config.ts
  </read_first>
  <action>
**1. `navy-admin/src/pages/portal/index.tsx` 수정 (per D-01, D-02):**
- `import { AnnouncementSection } from '@/features/announcements/components/AnnouncementSection'` 추가
- 렌더 구조 변경:
  ```tsx
  return (
    <div>
      <AnnouncementSection />
      <Title level={4}>서브시스템 바로가기</Title>
      <Row gutter={[16, 16]}>
        {subsystems.map((sys) => (
          <Col key={sys.code} xs={24} sm={12} md={8} lg={6}>
            <Card hoverable onClick={() => handleOpen(sys.path)} className="text-center">
              <Title level={5}>{sys.name}</Title>
              <Text type="secondary">{sys.processCount}개 프로세스</Text>
            </Card>
          </Col>
        ))}
      </Row>
    </div>
  )
  ```
- 기존 '컴포넌트 데모' 카드는 제거 (Phase 0 임시 카드, Phase 2에서 불필요)

**2. `navy-admin/src/app/layouts/MainPortalLayout.tsx` 수정 (per PTL-03, Pattern E):**
- `import { message } from 'antd'` 추가 (기존 import에 합침)
- handleLogout 함수 수정:
  ```typescript
  const handleLogout = () => {
    logout()
    navigate(ROUTES.LOGIN)
    message.success('로그아웃 되었습니다')
  }
  ```
- 헤더 시스템명 fontSize를 UI-SPEC Display 토큰(16px)으로 유지 (현재 18px -> 16px 변경):
  `<Text strong style={{ color: '#fff', fontSize: 16 }}>`

**3. `navy-admin/src/pages/login/index.tsx` 확인:**
- 이미 `message.success('로그인 성공')` 포함 — 변경 불필요
- 이미 Card width 400px + shadow-lg + bg-gray-100 레이아웃 — UI-SPEC Pattern A 충족
- PTL-01은 기존 코드로 충족됨. 변경 없음.

**4. `navy-admin/src/app/layouts/PortalLayout.tsx` 검증 (per D-08):**
- SubsystemProLayout에 이미 구현된 '메인포탈로 돌아가기' 기능을 검증한다.
- 확인 항목:
  - handleGoPortal 함수: `window.opener.focus()` -> `window.close()` 순서 (D-08 준수)
  - opener 없을 경우 `navigate(ROUTES.PORTAL)` fallback (D-08 안전장치)
  - Dropdown 메뉴에 `key: 'portal'`, `label: '메인포탈로 돌아가기'` 항목 존재
- 이미 Phase 0에서 올바르게 구현됨. 코드 변경 불필요. 테스트로 검증.

**5. `navy-admin/src/__tests__/portal/announcement.test.ts` 생성 (Wave 0 Nyquist):**
- readFileSync 기반 파일 내용 검증 (Phase 1 heavy antd 패턴):
  ```typescript
  import { describe, it, expect } from 'vitest'
  import { readFileSync } from 'fs'
  import { resolve } from 'path'

  describe('AnnouncementSection', () => {
    const content = readFileSync(
      resolve(__dirname, '../../features/announcements/components/AnnouncementSection.tsx'),
      'utf-8'
    )
    it('should use antd List with size="small"', () => {
      expect(content).toContain('size="small"')
    })
    it('should render Alert for urgent announcements', () => {
      expect(content).toContain('Alert')
      expect(content).toContain('type="warning"')
    })
    it('should show empty text when no announcements', () => {
      expect(content).toContain('등록된 공지사항이 없습니다')
    })
    it('should use useAnnouncements hook', () => {
      expect(content).toContain('useAnnouncements')
    })
  })

  describe('AnnouncementHandlers', () => {
    const handlerContent = readFileSync(
      resolve(__dirname, '../../shared/api/mocks/handlers/announcements.ts'),
      'utf-8'
    )
    it('should handle GET /api/announcements', () => {
      expect(handlerContent).toContain("'/api/announcements'")
    })
    it('should include 3 mock announcements', () => {
      expect(handlerContent).toContain('시스템 점검 안내')
      expect(handlerContent).toContain('해군 행정포탈 v1.0 오픈')
      expect(handlerContent).toContain('사용자 매뉴얼 배포')
    })
  })
  ```

**6. `navy-admin/src/__tests__/portal/header.test.ts` 생성 (Wave 0 Nyquist):**
- readFileSync 기반 MainPortalLayout 파일 내용 검증:
  ```typescript
  import { describe, it, expect } from 'vitest'
  import { readFileSync } from 'fs'
  import { resolve } from 'path'

  describe('MainPortalLayout header', () => {
    const content = readFileSync(
      resolve(__dirname, '../../app/layouts/MainPortalLayout.tsx'),
      'utf-8'
    )
    it('should display system name "해군 행정포탈"', () => {
      expect(content).toContain('해군 행정포탈')
    })
    it('should display user rank, name, and unit', () => {
      expect(content).toContain('user.rank')
      expect(content).toContain('user.name')
      expect(content).toContain('user.unit')
    })
    it('should show logout success message', () => {
      expect(content).toContain("message.success('로그아웃 되었습니다')")
    })
    it('should use background color #001529 for header', () => {
      expect(content).toContain('#001529')
    })
  })
  ```

**7. `navy-admin/src/shared/api/mocks/handlers/announcements.test.ts` 생성 (Wave 0 Nyquist — VALIDATION.md 요구):**
- readFileSync 기반 MSW 핸들러 구조 검증:
  ```typescript
  import { describe, it, expect } from 'vitest'
  import { readFileSync } from 'fs'
  import { resolve } from 'path'

  describe('announcements MSW handler', () => {
    const content = readFileSync(
      resolve(__dirname, './announcements.ts'),
      'utf-8'
    )
    it('should export announcementHandlers array', () => {
      expect(content).toContain('export const announcementHandlers')
    })
    it('should use http.get for /api/announcements', () => {
      expect(content).toContain("http.get")
      expect(content).toContain("/api/announcements")
    })
    it('should return ApiResult format with success flag', () => {
      expect(content).toContain('success: true')
    })
    it('should include isUrgent field in mock data', () => {
      expect(content).toContain('isUrgent')
    })
  })
  ```

**8. SubsystemProLayout '메인포탈로 돌아가기' 테스트 추가 (D-08 검증):**
- `navy-admin/src/__tests__/portal/header.test.ts`에 아래 describe 블록 추가:
  ```typescript
  describe('SubsystemProLayout - 메인포탈 복귀 (D-08)', () => {
    const layoutContent = readFileSync(
      resolve(__dirname, '../../app/layouts/PortalLayout.tsx'),
      'utf-8'
    )
    it('should have handleGoPortal function', () => {
      expect(layoutContent).toContain('handleGoPortal')
    })
    it('should call window.opener.focus() before window.close()', () => {
      const focusIdx = layoutContent.indexOf('opener.focus()')
      const closeIdx = layoutContent.indexOf('window.close()')
      expect(focusIdx).toBeGreaterThan(-1)
      expect(closeIdx).toBeGreaterThan(-1)
      expect(focusIdx).toBeLessThan(closeIdx)
    })
    it('should fallback to navigate(ROUTES.PORTAL) when no opener', () => {
      expect(layoutContent).toContain('navigate(ROUTES.PORTAL)')
    })
    it('should have "메인포탈로 돌아가기" menu item', () => {
      expect(layoutContent).toContain('메인포탈로 돌아가기')
    })
  })
  ```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/portal/ src/shared/api/mocks/handlers/announcements.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/portal/index.tsx contains `import { AnnouncementSection }`
    - navy-admin/src/pages/portal/index.tsx contains `<AnnouncementSection`
    - navy-admin/src/pages/portal/index.tsx does NOT contain `컴포넌트 데모`
    - navy-admin/src/app/layouts/MainPortalLayout.tsx contains `message.success('로그아웃 되었습니다')`
    - navy-admin/src/app/layouts/MainPortalLayout.tsx contains `fontSize: 16`
    - navy-admin/src/__tests__/portal/announcement.test.ts exists and contains `describe('AnnouncementSection'`
    - navy-admin/src/__tests__/portal/header.test.ts exists and contains `describe('MainPortalLayout header'`
    - navy-admin/src/__tests__/portal/header.test.ts contains `describe('SubsystemProLayout - 메인포탈 복귀 (D-08)'`
    - navy-admin/src/shared/api/mocks/handlers/announcements.test.ts exists and contains `describe('announcements MSW handler'`
    - `cd navy-admin && npx vitest run src/__tests__/portal/ src/shared/api/mocks/handlers/announcements.test.ts` exits 0
  </acceptance_criteria>
  <done>대시보드에 AnnouncementSection이 통합되고, 로그아웃 메시지가 추가되며, 헤더 fontSize가 16px로 조정됨. D-08 '메인포탈로 돌아가기' 동작 검증 테스트 통과. MSW 핸들러 테스트 통과.</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx vitest run src/__tests__/portal/ src/shared/api/mocks/handlers/announcements.test.ts` — 공지사항/헤더/D-08/MSW핸들러 테스트 통과
2. `cd navy-admin && npx vitest run` — 전체 테스트 스위트 통과 (기존 테스트 깨지지 않음)
3. grep 검증:
   - `grep "AnnouncementSection" navy-admin/src/pages/portal/index.tsx` — import 확인
   - `grep "announcementHandlers" navy-admin/src/shared/api/mocks/handlers/index.ts` — MSW 등록 확인
   - `grep "로그아웃 되었습니다" navy-admin/src/app/layouts/MainPortalLayout.tsx` — 메시지 확인
   - `grep "메인포탈로 돌아가기" navy-admin/src/app/layouts/PortalLayout.tsx` — D-08 버튼 확인
   - `grep "opener.focus" navy-admin/src/app/layouts/PortalLayout.tsx` — D-08 로직 확인
</verification>

<success_criteria>
- 공지사항 feature slice 완성 (타입, 훅, 컴포넌트, MSW 핸들러)
- 대시보드에 공지사항 섹션이 서브시스템 카드 그리드 위에 표시됨
- 로그아웃 시 message.success 표시됨
- 헤더에 사용자 정보(계급 이름 소속부대)가 표시됨
- 서브시스템 헤더에 '메인포탈로 돌아가기' 버튼이 있고 D-08 동작(opener.focus -> close, fallback navigate) 검증됨
- 모든 테스트 통과 (announcement, header, D-08, MSW handler)
</success_criteria>

<output>
After completion, create `.planning/phases/02-00/02-01-SUMMARY.md`
</output>
