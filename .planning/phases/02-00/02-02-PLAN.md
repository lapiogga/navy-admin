---
phase: 02-00
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/features/auth/hooks/useSessionCheck.ts
  - navy-admin/src/features/auth/components/SessionWarningModal.tsx
  - navy-admin/src/app/components/RequireAuth.tsx
  - navy-admin/src/features/auth/hooks/useSessionCheck.test.ts
autonomous: true
requirements:
  - PTL-04

must_haves:
  truths:
    - "사용자가 60초간 활동하지 않으면 자동 로그아웃되고 로그인 화면으로 이동한다"
    - "만료 30초 전에 세션 만료 경고 Modal이 표시되고 카운트다운이 진행된다"
    - "세션 연장 버튼 클릭 시 Modal이 닫히고 idle 타이머가 리셋된다"
    - "지금 로그아웃 버튼 클릭 시 즉시 로그아웃되고 로그인 화면으로 이동한다"
    - "마우스/키보드/클릭/스크롤/터치 활동 시 idle 타이머가 리셋된다"
  artifacts:
    - path: "navy-admin/src/features/auth/hooks/useSessionCheck.ts"
      provides: "Idle 이중 타이머 기반 세션 체크 훅"
      contains: "IDLE_TIMEOUT_MS"
      min_lines: 40
    - path: "navy-admin/src/features/auth/components/SessionWarningModal.tsx"
      provides: "세션 만료 경고 Modal 컴포넌트"
      exports: ["SessionWarningModal"]
      min_lines: 20
    - path: "navy-admin/src/app/components/RequireAuth.tsx"
      provides: "인증 가드 + SessionWarningModal 렌더링"
      contains: "SessionWarningModal"
    - path: "navy-admin/src/features/auth/hooks/useSessionCheck.test.ts"
      provides: "useSessionCheck 타이머 동작 테스트"
      contains: "IDLE_TIMEOUT_MS"
  key_links:
    - from: "navy-admin/src/features/auth/hooks/useSessionCheck.ts"
      to: "navy-admin/src/features/auth/store/authStore.ts"
      via: "useAuthStore((s) => s.logout)"
      pattern: "useAuthStore.*logout"
    - from: "navy-admin/src/app/components/RequireAuth.tsx"
      to: "navy-admin/src/features/auth/components/SessionWarningModal.tsx"
      via: "import SessionWarningModal"
      pattern: "SessionWarningModal"
    - from: "navy-admin/src/app/components/RequireAuth.tsx"
      to: "navy-admin/src/features/auth/hooks/useSessionCheck.ts"
      via: "useSessionCheck() destructured return"
      pattern: "isWarningVisible.*countdown.*extendSession"
---

<objective>
useSessionCheck 훅을 Idle 이중 타이머 패턴으로 리팩토링하고 SessionWarningModal을 RequireAuth에 연동하여 PTL-04(세션 관리) 요구사항을 충족한다.

Purpose: 사용자 활동 기반 세션 만료 감지로 MVP 테스트 용이성(1분 idle)과 UX(30초 전 경고)를 동시에 확보한다.
Output: useSessionCheck(Idle 이중 타이머), SessionWarningModal, RequireAuth 연동, 테스트
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-00/02-CONTEXT.md
@.planning/phases/02-00/02-RESEARCH.md
@.planning/phases/02-00/02-UI-SPEC.md
@.planning/phases/02-00/02-VALIDATION.md

<interfaces>
<!-- 기존 코드 인터페이스 — executor는 이 코드를 기반으로 작업 -->

From navy-admin/src/features/auth/store/authStore.ts:
```typescript
interface AuthState {
  user: User | null
  token: string | null
  isAuthenticated: boolean
  sessionExpiry: number | null
  login: (credentials: LoginCredentials) => Promise<void>
  logout: () => void
  checkSession: () => boolean
}
export const useAuthStore = create<AuthState>()(persist(...))
```

From navy-admin/src/features/auth/hooks/useSessionCheck.ts (현재 — 이 파일을 전면 리팩토링):
```typescript
// 현재: setInterval 60초 기반 단순 체크
// 목표: Idle 이벤트 + 이중 타이머 (warnTimer + idleTimer)
export function useSessionCheck() { ... }
```

From navy-admin/src/app/components/RequireAuth.tsx (현재):
```typescript
export function RequireAuth({ children }: RequireAuthProps) {
  // useSessionCheck() 호출 (반환값 없음 → destructured return으로 변경 필요)
  // storage 이벤트 크로스탭 로그아웃 동기화 (유지)
  // isAuthenticated false → Navigate to LOGIN (유지)
  // children 렌더링 (SessionWarningModal 추가 필요)
}
```

From navy-admin/src/shared/config/routes.ts:
```typescript
export const ROUTES = { LOGIN: '/login', PORTAL: '/', ... } as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useSessionCheck Idle 이중 타이머 리팩토링 + SessionWarningModal 생성</name>
  <files>
    navy-admin/src/features/auth/hooks/useSessionCheck.ts,
    navy-admin/src/features/auth/components/SessionWarningModal.tsx
  </files>
  <read_first>
    navy-admin/src/features/auth/hooks/useSessionCheck.ts,
    navy-admin/src/features/auth/store/authStore.ts,
    navy-admin/src/shared/config/routes.ts,
    navy-admin/src/app/components/RequireAuth.tsx
  </read_first>
  <action>
**1. `navy-admin/src/features/auth/hooks/useSessionCheck.ts` 전면 리팩토링:**

기존 setInterval 기반 코드를 완전히 교체한다. 아래 구현을 정확히 따른다.

```typescript
import { useState, useEffect, useCallback, useRef } from 'react'
import { useNavigate } from 'react-router-dom'
import { message } from 'antd'
import { useAuthStore } from '@/features/auth/store/authStore'
import { ROUTES } from '@/shared/config/routes'

export const IDLE_TIMEOUT_MS = 60_000   // 1분 (D-04: MVP 테스트 용이성)
export const WARN_BEFORE_MS = 30_000    // 만료 30초 전 경고 (D-05)

/**
 * Idle 기반 세션 만료 감지 훅.
 * - 5가지 사용자 활동 이벤트를 감지하여 idle 타이머를 리셋
 * - 만료 30초 전: 경고 Modal 표시 + 카운트다운 시작
 * - 만료 시: authStore.logout() + /login 이동 + message.warning
 * - 반환: { isWarningVisible, countdown, extendSession }
 */
export function useSessionCheck() {
  const logout = useAuthStore((s) => s.logout)
  const isAuthenticated = useAuthStore((s) => s.isAuthenticated)
  const navigate = useNavigate()
  const [isWarningVisible, setIsWarningVisible] = useState(false)
  const [countdown, setCountdown] = useState(30)

  const warnTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)
  const idleTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)
  const countdownRef = useRef<ReturnType<typeof setInterval> | null>(null)

  const clearAllTimers = useCallback(() => {
    if (warnTimerRef.current) { clearTimeout(warnTimerRef.current); warnTimerRef.current = null }
    if (idleTimerRef.current) { clearTimeout(idleTimerRef.current); idleTimerRef.current = null }
    if (countdownRef.current) { clearInterval(countdownRef.current); countdownRef.current = null }
  }, [])

  const resetTimers = useCallback(() => {
    clearAllTimers()
    setIsWarningVisible(false)
    setCountdown(30)

    // 경고 타이머: 만료 30초 전에 Modal 표시
    warnTimerRef.current = setTimeout(() => {
      setIsWarningVisible(true)
      let cnt = 30
      countdownRef.current = setInterval(() => {
        cnt -= 1
        setCountdown(cnt)
        if (cnt <= 0 && countdownRef.current) {
          clearInterval(countdownRef.current)
          countdownRef.current = null
        }
      }, 1000)
    }, IDLE_TIMEOUT_MS - WARN_BEFORE_MS)

    // Idle 타이머: 만료 시 강제 로그아웃
    idleTimerRef.current = setTimeout(() => {
      clearAllTimers()
      logout()
      navigate(ROUTES.LOGIN, { replace: true })
      message.warning('세션이 만료되었습니다. 다시 로그인하세요')
    }, IDLE_TIMEOUT_MS)
  }, [logout, navigate, clearAllTimers])

  useEffect(() => {
    if (!isAuthenticated) {
      clearAllTimers()
      return
    }

    const events: string[] = ['mousemove', 'keydown', 'click', 'scroll', 'touchstart']
    const handleActivity = () => {
      // 경고 Modal이 표시된 상태에서는 일반 활동으로 리셋하지 않음
      // (버튼 클릭으로만 연장/로그아웃 가능)
      // 단, 경고 전에는 모든 활동이 타이머를 리셋
      if (!warnTimerRef.current && !idleTimerRef.current) return
      // 경고 Modal 표시 중이 아닐 때만 리셋
      resetTimers()
    }

    events.forEach((e) => window.addEventListener(e, handleActivity, { passive: true }))
    resetTimers() // 초기 타이머 시작

    return () => {
      clearAllTimers()
      events.forEach((e) => window.removeEventListener(e, handleActivity))
    }
  }, [isAuthenticated, resetTimers, clearAllTimers])

  const extendSession = useCallback(() => {
    resetTimers()
  }, [resetTimers])

  return { isWarningVisible, countdown, extendSession }
}
```

주의사항 (Pitfall 대응):
- resetTimers를 useCallback으로 감싸서 이벤트 리스너 참조 안정성 확보 (Pitfall 1)
- clearAllTimers에서 warnTimer, idleTimer, countdown interval 3개 모두 정리 (Pitfall 2)
- logout() 다음 navigate() 순서 (Pitfall 3 — Zustand set()은 동기)
- IDLE_TIMEOUT_MS, WARN_BEFORE_MS를 export하여 테스트에서 사용 가능

**2. `navy-admin/src/features/auth/components/SessionWarningModal.tsx` 생성:**

```typescript
import { Modal, Button, Typography } from 'antd'

const { Text } = Typography

interface SessionWarningModalProps {
  visible: boolean
  countdown: number
  onExtend: () => void
  onLogout: () => void
}

/**
 * 세션 만료 경고 Modal (D-05).
 * - closable=false, maskClosable=false — 사용자가 Modal을 회피할 수 없음
 * - 카운트다운 숫자: 20px semibold colorError (#ff4d4f)
 * - footer: [세션 연장 (primary)] [지금 로그아웃 (default)]
 */
export function SessionWarningModal({ visible, countdown, onExtend, onLogout }: SessionWarningModalProps) {
  return (
    <Modal
      title="세션 만료 경고"
      open={visible}
      closable={false}
      maskClosable={false}
      footer={[
        <Button key="extend" type="primary" onClick={onExtend}>
          세션 연장
        </Button>,
        <Button key="logout" onClick={onLogout}>
          지금 로그아웃
        </Button>,
      ]}
    >
      <div style={{ textAlign: 'center' }}>
        <Text>{countdown}초 후 자동으로 로그아웃됩니다.</Text>
        <br />
        <Text style={{ fontSize: 20, fontWeight: 600, color: '#ff4d4f' }}>
          {countdown}
        </Text>
      </div>
    </Modal>
  )
}
```
  </action>
  <verify>
    <automated>cd navy-admin && grep -n "IDLE_TIMEOUT_MS" src/features/auth/hooks/useSessionCheck.ts && grep -n "SessionWarningModal" src/features/auth/components/SessionWarningModal.tsx | head -5 && npx tsc --noEmit src/features/auth/hooks/useSessionCheck.ts src/features/auth/components/SessionWarningModal.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `export const IDLE_TIMEOUT_MS = 60_000`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `export const WARN_BEFORE_MS = 30_000`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `mousemove`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `keydown`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `click`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `scroll`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `touchstart`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `return { isWarningVisible, countdown, extendSession }`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `message.warning('세션이 만료되었습니다. 다시 로그인하세요')`
    - navy-admin/src/features/auth/hooks/useSessionCheck.ts contains `useCallback`
    - navy-admin/src/features/auth/components/SessionWarningModal.tsx contains `export function SessionWarningModal`
    - navy-admin/src/features/auth/components/SessionWarningModal.tsx contains `closable={false}`
    - navy-admin/src/features/auth/components/SessionWarningModal.tsx contains `maskClosable={false}`
    - navy-admin/src/features/auth/components/SessionWarningModal.tsx contains `세션 연장`
    - navy-admin/src/features/auth/components/SessionWarningModal.tsx contains `지금 로그아웃`
    - navy-admin/src/features/auth/components/SessionWarningModal.tsx contains `fontSize: 20`
    - navy-admin/src/features/auth/components/SessionWarningModal.tsx contains `#ff4d4f`
  </acceptance_criteria>
  <done>useSessionCheck이 Idle 이중 타이머로 리팩토링되고, SessionWarningModal이 생성됨</done>
</task>

<task type="auto">
  <name>Task 2: RequireAuth에 SessionWarningModal 연동 + 테스트 생성</name>
  <files>
    navy-admin/src/app/components/RequireAuth.tsx,
    navy-admin/src/features/auth/hooks/useSessionCheck.test.ts
  </files>
  <read_first>
    navy-admin/src/app/components/RequireAuth.tsx,
    navy-admin/src/features/auth/hooks/useSessionCheck.ts,
    navy-admin/src/features/auth/components/SessionWarningModal.tsx,
    navy-admin/src/features/auth/store/authStore.ts,
    navy-admin/src/features/auth/store/authStore.test.ts
  </read_first>
  <action>
**1. `navy-admin/src/app/components/RequireAuth.tsx` 수정:**

useSessionCheck의 반환값을 destructure하고 SessionWarningModal을 children 아래에 렌더링한다.

```typescript
import { useEffect } from 'react'
import { Navigate, useLocation, useNavigate } from 'react-router-dom'
import { useAuthStore } from '@/features/auth/store/authStore'
import { useSessionCheck } from '@/features/auth/hooks/useSessionCheck'
import { SessionWarningModal } from '@/features/auth/components/SessionWarningModal'
import { ROUTES } from '@/shared/config/routes'

interface RequireAuthProps {
  children: React.ReactNode
}

/**
 * 인증 보호 라우트 래퍼.
 * - 미인증 시 /login으로 리다이렉트
 * - Idle 기반 세션 만료 감지 + 경고 Modal (useSessionCheck)
 * - 다른 탭에서 로그아웃 시 storage 이벤트로 감지하여 동기화
 */
export function RequireAuth({ children }: RequireAuthProps) {
  const isAuthenticated = useAuthStore((s) => s.isAuthenticated)
  const logout = useAuthStore((s) => s.logout)
  const navigate = useNavigate()
  const location = useLocation()

  // Idle 기반 세션 만료 체크 (PTL-04)
  const { isWarningVisible, countdown, extendSession } = useSessionCheck()

  // 다른 탭(서브시스템)에서 로그아웃 시 현재 탭도 로그아웃 동기화
  useEffect(() => {
    const handleStorage = (e: StorageEvent) => {
      if (e.key === 'auth-storage') {
        const stored = e.newValue ? JSON.parse(e.newValue) : null
        if (!stored?.state?.isAuthenticated) {
          useAuthStore.getState().logout()
        }
      }
    }
    window.addEventListener('storage', handleStorage)
    return () => window.removeEventListener('storage', handleStorage)
  }, [])

  if (!isAuthenticated) {
    return <Navigate to={ROUTES.LOGIN} state={{ from: location }} replace />
  }

  return (
    <>
      {children}
      <SessionWarningModal
        visible={isWarningVisible}
        countdown={countdown}
        onExtend={extendSession}
        onLogout={() => {
          logout()
          navigate(ROUTES.LOGIN)
        }}
      />
    </>
  )
}
```

**2. `navy-admin/src/features/auth/hooks/useSessionCheck.test.ts` 생성:**

readFileSync 기반 파일 내용 검증 + vi.useFakeTimers 기반 타이머 로직 검증:

```typescript
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'fs'
import { resolve } from 'path'

const hookPath = resolve(__dirname, './useSessionCheck.ts')
const hookContent = readFileSync(hookPath, 'utf-8')

describe('useSessionCheck - 코드 구조 검증', () => {
  it('should export IDLE_TIMEOUT_MS as 60000', () => {
    expect(hookContent).toContain('IDLE_TIMEOUT_MS = 60_000')
  })

  it('should export WARN_BEFORE_MS as 30000', () => {
    expect(hookContent).toContain('WARN_BEFORE_MS = 30_000')
  })

  it('should listen to 5 idle events: mousemove, keydown, click, scroll, touchstart', () => {
    expect(hookContent).toContain('mousemove')
    expect(hookContent).toContain('keydown')
    expect(hookContent).toContain('click')
    expect(hookContent).toContain('scroll')
    expect(hookContent).toContain('touchstart')
  })

  it('should use useCallback for resetTimers to prevent infinite re-renders', () => {
    expect(hookContent).toContain('useCallback')
  })

  it('should clear all 3 timers: warnTimer, idleTimer, countdownInterval', () => {
    expect(hookContent).toContain('warnTimerRef')
    expect(hookContent).toContain('idleTimerRef')
    expect(hookContent).toContain('countdownRef')
    expect(hookContent).toContain('clearTimeout')
    expect(hookContent).toContain('clearInterval')
  })

  it('should return isWarningVisible, countdown, extendSession', () => {
    expect(hookContent).toContain('return { isWarningVisible, countdown, extendSession }')
  })

  it('should call logout and navigate to LOGIN on timeout', () => {
    expect(hookContent).toContain('logout()')
    expect(hookContent).toContain('ROUTES.LOGIN')
  })

  it('should show session expired message', () => {
    expect(hookContent).toContain('세션이 만료되었습니다. 다시 로그인하세요')
  })

  it('should use passive event listeners', () => {
    expect(hookContent).toContain('passive: true')
  })

  it('should clean up event listeners on unmount', () => {
    expect(hookContent).toContain('removeEventListener')
  })
})

describe('RequireAuth - SessionWarningModal 연동 검증', () => {
  const requireAuthContent = readFileSync(
    resolve(__dirname, '../../../app/components/RequireAuth.tsx'),
    'utf-8'
  )

  it('should import SessionWarningModal', () => {
    expect(requireAuthContent).toContain('SessionWarningModal')
  })

  it('should destructure useSessionCheck return values', () => {
    expect(requireAuthContent).toContain('isWarningVisible')
    expect(requireAuthContent).toContain('countdown')
    expect(requireAuthContent).toContain('extendSession')
  })

  it('should render SessionWarningModal with correct props', () => {
    expect(requireAuthContent).toContain('visible={isWarningVisible}')
    expect(requireAuthContent).toContain('countdown={countdown}')
    expect(requireAuthContent).toContain('onExtend={extendSession}')
    expect(requireAuthContent).toContain('onLogout=')
  })

  it('should maintain cross-tab logout sync', () => {
    expect(requireAuthContent).toContain('auth-storage')
    expect(requireAuthContent).toContain('handleStorage')
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/features/auth/hooks/useSessionCheck.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/app/components/RequireAuth.tsx contains `import { SessionWarningModal }`
    - navy-admin/src/app/components/RequireAuth.tsx contains `const { isWarningVisible, countdown, extendSession } = useSessionCheck()`
    - navy-admin/src/app/components/RequireAuth.tsx contains `<SessionWarningModal`
    - navy-admin/src/app/components/RequireAuth.tsx contains `visible={isWarningVisible}`
    - navy-admin/src/app/components/RequireAuth.tsx contains `countdown={countdown}`
    - navy-admin/src/app/components/RequireAuth.tsx contains `onExtend={extendSession}`
    - navy-admin/src/app/components/RequireAuth.tsx contains `auth-storage` (크로스탭 동기화 유지)
    - navy-admin/src/features/auth/hooks/useSessionCheck.test.ts contains `describe('useSessionCheck`
    - navy-admin/src/features/auth/hooks/useSessionCheck.test.ts contains `describe('RequireAuth`
    - `cd navy-admin && npx vitest run src/features/auth/hooks/useSessionCheck.test.ts` exits 0
  </acceptance_criteria>
  <done>RequireAuth가 SessionWarningModal을 렌더링하고, useSessionCheck 반환값을 연동함. 크로스탭 로그아웃 동기화 유지. 테스트 통과.</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx vitest run src/features/auth/hooks/useSessionCheck.test.ts` — 세션 체크 테스트 통과
2. `cd navy-admin && npx vitest run` — 전체 테스트 스위트 통과
3. grep 검증:
   - `grep "IDLE_TIMEOUT_MS = 60_000" navy-admin/src/features/auth/hooks/useSessionCheck.ts` — 상수 확인
   - `grep "SessionWarningModal" navy-admin/src/app/components/RequireAuth.tsx` — Modal 연동 확인
   - `grep "closable={false}" navy-admin/src/features/auth/components/SessionWarningModal.tsx` — Modal 속성 확인
4. TypeScript 에러 없음: `cd navy-admin && npx tsc --noEmit 2>&1 | grep -c error` — 0
</verification>

<success_criteria>
- useSessionCheck이 Idle 이중 타이머 패턴으로 동작 (5개 이벤트 감지, 30초 전 경고, 60초 만료)
- SessionWarningModal이 closable=false, maskClosable=false로 경고 표시
- RequireAuth에서 SessionWarningModal을 children 아래에 렌더링
- 세션 연장/로그아웃 버튼이 정상 동작
- 모든 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-00/02-02-SUMMARY.md`
</output>
