---
phase: 03-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys04-certificate/index.tsx
  - navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx
  - navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx
  - navy-admin/src/pages/sys04-certificate/CertificateRegisterPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys04.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys04/certificate.test.ts
autonomous: true
requirements: [CERT-01, CERT-02, CERT-03, CERT-04, CERT-05, CERT-06]

must_haves:
  truths:
    - "인증서 신청 목록에서 신청서를 작성/수정/삭제할 수 있다"
    - "관리자가 승인 대기 목록에서 인증서를 승인/반려할 수 있다"
    - "등록대장에서 전체 인증서를 목록/상세 조회할 수 있다"
    - "게시판/코드관리/권한관리 메뉴가 Phase 1 공통 페이지로 연결된다"
  artifacts:
    - path: "navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx"
      provides: "인증서 신청 CRUD 화면"
      contains: "DataTable"
    - path: "navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx"
      provides: "인증서 승인/반려 화면"
      contains: "Popconfirm"
    - path: "navy-admin/src/pages/sys04-certificate/CertificateRegisterPage.tsx"
      provides: "인증서 등록대장 화면"
      contains: "DetailModal"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys04.ts"
      provides: "인증서 MSW 핸들러"
      exports: ["sys04Handlers"]
  key_links:
    - from: "pages/sys04-certificate/index.tsx"
      to: "pages/common/board, pages/common/code-mgmt, pages/common/auth-group"
      via: "React Router Routes + lazy import"
      pattern: "Route.*path.*element"
    - from: "shared/api/mocks/handlers/index.ts"
      to: "sys04Handlers"
      via: "spread into handlers array"
      pattern: "sys04Handlers"
---

<objective>
04_인증서발급신청체계 전체 구현 (CERT-01~06, 14개 프로세스)

Purpose: 인증서 신청 -> 승인/반려 -> 등록대장 워크플로우를 DataTable+CrudForm+StatusBadge 패턴으로 구현하고, 공통 기능(게시판/코드관리/권한관리)을 Phase 1 페이지 재사용으로 연결한다.
Output: sys04 서브시스템 3개 고유 페이지 + MSW 핸들러 + 공통 기능 라우트 연결
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-5/03-CONTEXT.md
@.planning/phases/03-5/03-RESEARCH.md

<interfaces>
<!-- 공통 컴포넌트 인터페이스 (Phase 0 frozen contract) -->

From shared/ui/DataTable/DataTable.tsx:
```typescript
export interface DataTableProps<T extends Record<string, unknown>> {
  columns: ProColumns<T>[]
  request: (params: PageRequest) => Promise<PageResponse<T>>
  rowKey: keyof T & string
  toolBarRender?: ProTableProps<T, PageRequest>['toolBarRender']
  rowSelection?: ProTableProps<T, PageRequest>['rowSelection']
  headerTitle?: string
  onRow?: (record: T) => { onClick?: () => void; style?: React.CSSProperties }
}
```

From shared/ui/CrudForm/CrudForm.tsx:
```typescript
export interface CrudFormField {
  name: string; label: string
  type: 'text' | 'textarea' | 'select' | 'date' | 'number'
  required?: boolean; options?: { label: string; value: string | number }[]
  disabled?: boolean; rules?: Rule[]; placeholder?: string; colSpan?: number
}
export interface CrudFormProps<T extends Record<string, unknown>> {
  fields: CrudFormField[]
  onFinish: (values: T) => Promise<boolean>
  initialValues?: Partial<T>; loading?: boolean; mode?: 'create' | 'edit' | 'view'
}
```

From shared/ui/StatusBadge/StatusBadge.tsx:
```typescript
export interface StatusBadgeProps {
  status: string; colorMap?: Record<string, string>; labelMap?: Record<string, string>
}
```

From shared/ui/DetailModal/DetailModal.tsx:
```typescript
export interface DetailField { key: string; label: string; render?: (value: unknown) => React.ReactNode }
export interface DetailModalProps {
  open: boolean; onClose: () => void; title: string
  data: Record<string, unknown> | null; fields: DetailField[]; width?: number
}
```

From shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number; sort?: string[] }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string; errorCode?: string }
```

From shared/api/mocks/handlers/index.ts:
```typescript
import { authHandlers } from './auth'
import { demoHandlers } from './demo'
import { commonHandlers } from './common'
import { announcementHandlers } from './announcements'
export const handlers = [...authHandlers, ...demoHandlers, ...commonHandlers, ...announcementHandlers]
```

메뉴 구조 (entities/subsystem/menus.ts):
```typescript
sys04: [
  { name: '인증서발급신청', path: '/sys04/1', children: [
    { name: '게시판', path: '/sys04/1/1' },
    { name: '인증서 신청', path: '/sys04/1/2' },
    { name: '인증서 승인/관리', path: '/sys04/1/3' },
    { name: '인증서 등록대장', path: '/sys04/1/4' },
  ]},
  { name: '시스템', path: '/sys04/2', children: [
    { name: '공통코드관리', path: '/sys04/2/1' },
    { name: '사용자별권한등록', path: '/sys04/2/2' },
  ]},
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sys04 MSW 핸들러 + 서브시스템 index.tsx 라우팅</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys04.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys04-certificate/index.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/api/mocks/handlers/announcements.ts,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys04-certificate/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
    1. **sys04.ts MSW 핸들러 생성** (`navy-admin/src/shared/api/mocks/handlers/sys04.ts`):
       - `CertStatus` 타입 정의: `'pending' | 'approved' | 'rejected'`
       - `CertApplication` 인터페이스: `{ id: string, applicantName: string, applicantUnit: string, certType: string, purpose: string, status: CertStatus, appliedAt: string, processedAt?: string }`
       - `faker` ko locale로 20건 Mock 데이터 생성 (certType: '재직증명서'|'경력증명서'|'복무증명서', unit: '1사단'|'2사단'|'해병대사령부')
       - 엔드포인트 구현:
         - `GET /api/sys04/certificates` — 페이지네이션 + status 필터. `ApiResult<PageResponse<CertApplication>>` 형식 반환
         - `POST /api/sys04/certificates` — 새 신청 추가. request body에서 certType, purpose 받아 status='pending'으로 생성
         - `PUT /api/sys04/certificates/:id` — 신청 수정. body 머지
         - `DELETE /api/sys04/certificates/:id` — 신청 삭제
         - `PATCH /api/sys04/certificates/:id/approve` — 승인. status='approved', processedAt=today
         - `PATCH /api/sys04/certificates/:id/reject` — 반려. status='rejected', processedAt=today
       - `export const sys04Handlers = [...]`

    2. **handlers/index.ts 수정**: `import { sys04Handlers } from './sys04'` 추가하고 handlers 배열에 `...sys04Handlers` 추가

    3. **index.tsx 라우팅** (`navy-admin/src/pages/sys04-certificate/index.tsx`):
       - `SubsystemPage` import 제거
       - `Routes`, `Route`, `Navigate` from 'react-router-dom' import
       - `lazy` from 'react' import
       - 공통 기능 lazy import (per D-01, D-02, D-03):
         - `const BoardIndex = lazy(() => import('@/pages/common/board'))` for CERT-04
         - `const CodeMgmtIndex = lazy(() => import('@/pages/common/code-mgmt'))` for CERT-05
         - `const AuthGroupIndex = lazy(() => import('@/pages/common/auth-group'))` for CERT-06
       - 고유 페이지 import (동기):
         - `import CertificateApplyPage from './CertificateApplyPage'`
         - `import CertificateApprovalPage from './CertificateApprovalPage'`
         - `import CertificateRegisterPage from './CertificateRegisterPage'`
       - Routes 구조:
         ```
         <Route index element={<Navigate to="/sys04/1/2" replace />} />
         <Route path="1/1" element={<Suspense fallback={<PageSpinner />}><BoardIndex /></Suspense>} />
         <Route path="1/2" element={<CertificateApplyPage />} />
         <Route path="1/3" element={<CertificateApprovalPage />} />
         <Route path="1/4" element={<CertificateRegisterPage />} />
         <Route path="2/1" element={<Suspense fallback={<PageSpinner />}><CodeMgmtIndex /></Suspense>} />
         <Route path="2/2" element={<Suspense fallback={<PageSpinner />}><AuthGroupIndex /></Suspense>} />
         ```
       - 기본 경로는 `/sys04/1/2` (인증서 신청)로 리다이렉트 (per D-15)
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/shared/api/mocks/handlers/sys04.ts 파일 존재
    - sys04.ts에 `export const sys04Handlers` 포함
    - sys04.ts에 `http.get('/api/sys04/certificates'` 포함
    - sys04.ts에 `http.patch('/api/sys04/certificates/:id/approve'` 포함
    - sys04.ts에 `http.patch('/api/sys04/certificates/:id/reject'` 포함
    - handlers/index.ts에 `sys04Handlers` 포함
    - index.tsx에 `Navigate to="/sys04/1/2"` 포함
    - index.tsx에 `BoardIndex` 포함 (CERT-04 게시판 재사용)
    - index.tsx에 `CodeMgmtIndex` 포함 (CERT-05 코드관리 재사용)
    - index.tsx에 `AuthGroupIndex` 포함 (CERT-06 권한관리 재사용)
    - TypeScript 컴파일 에러 0건
  </acceptance_criteria>
  <done>sys04 MSW 핸들러가 6개 엔드포인트를 제공하고, index.tsx가 고유 3개 + 공통 3개 라우트를 매핑</done>
</task>

<task type="auto">
  <name>Task 2: sys04 고유 페이지 3개 + 테스트</name>
  <files>
    navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx,
    navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx,
    navy-admin/src/pages/sys04-certificate/CertificateRegisterPage.tsx,
    navy-admin/src/__tests__/sys04/certificate.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys04.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx,
    navy-admin/src/shared/ui/DetailModal/DetailModal.tsx,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys04-certificate/index.tsx
  </read_first>
  <action>
    1. **CertificateApplyPage.tsx** (CERT-01 — 신청서 작성/수정/삭제):
       - `PageContainer` (antd pro) 타이틀 "인증서 신청"
       - `DataTable` 컬럼: 번호(id), 인증서종류(certType), 신청목적(purpose), 상태(status - StatusBadge), 신청일(appliedAt), 액션(수정/삭제 버튼)
       - StatusBadge colorMap: `{ pending: 'orange', approved: 'green', rejected: 'red' }`, labelMap: `{ pending: '대기', approved: '승인', rejected: '반려' }` (per D-06)
       - 상단 toolBarRender에 "신청서 작성" 버튼 → antd Modal 내 CrudForm 표시
       - CrudForm fields: certType (select, options: 재직증명서/경력증명서/복무증명서), purpose (textarea, required)
       - request 함수: `GET /api/sys04/certificates` 호출, apiClient 사용
       - 등록 mutation: `POST /api/sys04/certificates`, 수정: `PUT /api/sys04/certificates/:id`, 삭제: `DELETE /api/sys04/certificates/:id`
       - useMutation + useQueryClient.invalidateQueries로 목록 갱신

    2. **CertificateApprovalPage.tsx** (CERT-02 — 승인/관리):
       - `PageContainer` 타이틀 "인증서 승인/관리"
       - `DataTable` 컬럼: 번호, 신청자(applicantName), 소속(applicantUnit), 인증서종류, 신청목적, 상태, 신청일, 처리일(processedAt), 처리 액션
       - 처리 액션 컬럼 (per D-07): status==='pending'일 때만 승인/반려 버튼 표시
         - 승인: `<Popconfirm title="승인하시겠습니까?" onConfirm={() => approveMutation.mutate(record.id)}>`
         - 반려: `<Popconfirm title="반려하시겠습니까?" onConfirm={() => rejectMutation.mutate(record.id)}>`
       - status!=='pending'이면 StatusBadge만 표시
       - approveMutation: `PATCH /api/sys04/certificates/:id/approve`
       - rejectMutation: `PATCH /api/sys04/certificates/:id/reject`
       - 성공 시 `message.success('처리 완료')` + invalidateQueries

    3. **CertificateRegisterPage.tsx** (CERT-03 — 등록대장):
       - `PageContainer` 타이틀 "인증서 등록대장"
       - `DataTable` 컬럼: 번호, 신청자, 소속, 인증서종류, 신청목적, 상태(StatusBadge), 신청일, 처리일
       - 행 클릭 시 `DetailModal` 표시 (onRow prop 사용)
       - DetailModal fields: 신청자, 소속, 인증서종류, 신청목적, 상태, 신청일, 처리일
       - toolBarRender에 "저장" 버튼 → `message.success('저장되었습니다')` Mock (per D-13)

    4. **certificate.test.ts** (Nyquist 테스트):
       - readFileSync 기반 검증 (Phase 1 확립 패턴):
         - CertificateApplyPage.tsx 파일 존재 + 'DataTable' 포함 + 'CrudForm' 포함
         - CertificateApprovalPage.tsx 파일 존재 + 'Popconfirm' 포함 + 'StatusBadge' 포함
         - CertificateRegisterPage.tsx 파일 존재 + 'DetailModal' 포함
         - sys04.ts MSW 핸들러에 'sys04Handlers' export 포함
         - handlers/index.ts에 'sys04Handlers' 포함
         - index.tsx에 7개 Route 엘리먼트 포함 (BoardIndex, CodeMgmtIndex, AuthGroupIndex, 3개 고유 + Navigate)
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys04/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CertificateApplyPage.tsx에 `DataTable` 포함
    - CertificateApplyPage.tsx에 `CrudForm` 포함
    - CertificateApplyPage.tsx에 `StatusBadge` 포함
    - CertificateApprovalPage.tsx에 `Popconfirm` 포함
    - CertificateApprovalPage.tsx에 `approveMutation` 또는 `approve` 포함
    - CertificateRegisterPage.tsx에 `DetailModal` 포함
    - certificate.test.ts 전체 통과
    - `npx tsc --noEmit` 에러 0건
  </acceptance_criteria>
  <done>인증서 신청/승인/등록대장 3개 페이지가 DataTable+CrudForm+StatusBadge+DetailModal 패턴으로 완전 구현되고 테스트 통과</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx tsc --noEmit` — TypeScript 에러 0건
- `cd navy-admin && npx vitest run src/__tests__/sys04/` — 전체 테스트 통과
- `cd navy-admin && npx vitest run` — 기존 테스트 regression 없음
</verification>

<success_criteria>
1. /sys04/1/2 에서 인증서 신청 목록이 표시되고 신청서 작성/수정/삭제 가능
2. /sys04/1/3 에서 승인 대기 목록이 표시되고 승인/반려 Popconfirm 동작
3. /sys04/1/4 에서 등록대장 목록이 표시되고 행 클릭 시 상세 모달 표시
4. /sys04/1/1 게시판, /sys04/2/1 코드관리, /sys04/2/2 권한관리가 Phase 1 공통 페이지를 렌더링
5. TypeScript 0 errors, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-01-SUMMARY.md`
</output>
