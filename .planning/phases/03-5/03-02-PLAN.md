---
phase: 03-5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys05-admin-rules/index.tsx
  - navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx
  - navy-admin/src/pages/sys05-admin-rules/PrecedentHQPage.tsx
  - navy-admin/src/pages/sys05-admin-rules/PrecedentUnitPage.tsx
  - navy-admin/src/pages/sys05-admin-rules/DirectiveListPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys05.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys05/admin-rules.test.ts
autonomous: true
requirements: [AREG-01, AREG-02, AREG-03, AREG-04]

must_haves:
  truths:
    - "현행규정 목록을 조회하고 상세 내용을 볼 수 있다"
    - "현행규정에 즐겨찾기를 추가/해제할 수 있고 새로고침 후에도 유지된다"
    - "다운로드/프린트 버튼을 클릭하면 Mock 알림이 표시된다"
    - "예규(해군본부/예하부대)와 지시문서를 각각 목록/상세 조회할 수 있다"
  artifacts:
    - path: "navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx"
      provides: "현행규정 조회 화면"
      contains: "useFavorites"
    - path: "navy-admin/src/pages/sys05-admin-rules/PrecedentHQPage.tsx"
      provides: "예규(해군본부) 조회 화면"
      contains: "DataTable"
    - path: "navy-admin/src/pages/sys05-admin-rules/DirectiveListPage.tsx"
      provides: "지시문서 조회 화면"
      contains: "DataTable"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys05.ts"
      provides: "행정규칙 MSW 핸들러"
      exports: ["sys05Handlers"]
  key_links:
    - from: "pages/sys05-admin-rules/RegulationListPage.tsx"
      to: "localStorage"
      via: "useFavorites 훅"
      pattern: "useFavorites|localStorage"
    - from: "shared/api/mocks/handlers/index.ts"
      to: "sys05Handlers"
      via: "spread into handlers array"
      pattern: "sys05Handlers"
---

<objective>
05_행정규칙포탈체계 전체 구현 (AREG-01~04, 15개 프로세스)

Purpose: 현행규정/예규/지시문서의 목록+상세 조회 화면을 DataTable+DetailModal 패턴으로 구현하고, 즐겨찾기(localStorage)와 다운로드 Mock을 추가한다.
Output: sys05 서브시스템 4개 고유 페이지 + MSW 핸들러 + 즐겨찾기 훅
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-5/03-CONTEXT.md
@.planning/phases/03-5/03-RESEARCH.md

<interfaces>
From shared/ui/DataTable/DataTable.tsx:
```typescript
export interface DataTableProps<T extends Record<string, unknown>> {
  columns: ProColumns<T>[]
  request: (params: PageRequest) => Promise<PageResponse<T>>
  rowKey: keyof T & string
  toolBarRender?: ProTableProps<T, PageRequest>['toolBarRender']
  headerTitle?: string
  onRow?: (record: T) => { onClick?: () => void; style?: React.CSSProperties }
}
```

From shared/ui/DetailModal/DetailModal.tsx:
```typescript
export interface DetailField { key: string; label: string; render?: (value: unknown) => React.ReactNode }
export interface DetailModalProps {
  open: boolean; onClose: () => void; title: string
  data: Record<string, unknown> | null; fields: DetailField[]; width?: number
}
```

From shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number; sort?: string[] }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string }
```

메뉴 구조:
```typescript
sys05: [
  { name: '해군규정', path: '/sys05/1', children: [
    { name: '현행규정', path: '/sys05/1/1' },
  ]},
  { name: '예규', path: '/sys05/2', children: [
    { name: '해군본부', path: '/sys05/2/1' },
    { name: '예하부대', path: '/sys05/2/2' },
  ]},
  { name: '지시', path: '/sys05/3', children: [
    { name: '지시문서', path: '/sys05/3/1' },
  ]},
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sys05 MSW 핸들러 + 즐겨찾기 훅 + index.tsx 라우팅</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys05.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys05-admin-rules/useFavorites.ts,
    navy-admin/src/pages/sys05-admin-rules/index.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys05-admin-rules/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
    1. **sys05.ts MSW 핸들러 생성** (`navy-admin/src/shared/api/mocks/handlers/sys05.ts`):
       - `Regulation` 인터페이스: `{ id: string, title: string, docNumber: string, category: string, department: string, effectiveDate: string, content: string, fileUrl?: string }`
       - category별 Mock 데이터: regulations(현행규정 15건), precedentsHQ(해군본부 예규 12건), precedentsUnit(예하부대 예규 10건), directives(지시문서 8건)
       - faker ko locale로 title(규정명), docNumber(`REG-${i}`), department(기획처/인사처/정보처), content(lorem)
       - 엔드포인트:
         - `GET /api/sys05/regulations` — keyword 필터 + 페이지네이션
         - `GET /api/sys05/regulations/:id` — 상세 조회 (단건 반환)
         - `GET /api/sys05/precedents/hq` — 해군본부 예규 목록
         - `GET /api/sys05/precedents/unit` — 예하부대 예규 목록
         - `GET /api/sys05/directives` — 지시문서 목록
         - `GET /api/sys05/directives/:id` — 지시문서 상세
       - `export const sys05Handlers = [...]`

    2. **handlers/index.ts 수정**: `import { sys05Handlers } from './sys05'` 추가, handlers 배열에 `...sys05Handlers` spread

    3. **useFavorites.ts 훅** (`navy-admin/src/pages/sys05-admin-rules/useFavorites.ts`) (per D-14):
       - `useFavorites(storageKey: string)` 커스텀 훅
       - `useState<string[]>` 초기값: `JSON.parse(localStorage.getItem(storageKey) ?? '[]')`
       - `toggle(id: string)`: favorites에 있으면 제거, 없으면 추가 → `setFavorites(next)` + `localStorage.setItem(storageKey, JSON.stringify(next))`
       - `isFavorite(id: string)`: `favorites.includes(id)`
       - 반환: `{ favorites, toggle, isFavorite }`

    4. **index.tsx 라우팅** (`navy-admin/src/pages/sys05-admin-rules/index.tsx`):
       - `SubsystemPage` import 제거
       - Routes 구조:
         ```
         <Route index element={<Navigate to="/sys05/1/1" replace />} />
         <Route path="1/1" element={<RegulationListPage />} />
         <Route path="2/1" element={<PrecedentHQPage />} />
         <Route path="2/2" element={<PrecedentUnitPage />} />
         <Route path="3/1" element={<DirectiveListPage />} />
         ```
       - sys05에는 공통 기능(게시판/코드관리/권한관리) 메뉴가 없으므로 고유 페이지만 라우팅
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/shared/api/mocks/handlers/sys05.ts 파일 존재
    - sys05.ts에 `export const sys05Handlers` 포함
    - sys05.ts에 `http.get('/api/sys05/regulations'` 포함
    - handlers/index.ts에 `sys05Handlers` 포함
    - useFavorites.ts에 `localStorage.setItem` 포함
    - useFavorites.ts에 `localStorage.getItem` 포함
    - index.tsx에 `Navigate to="/sys05/1/1"` 포함
    - index.tsx에 `RegulationListPage` 포함
    - TypeScript 컴파일 에러 0건
  </acceptance_criteria>
  <done>sys05 MSW 핸들러 6개 엔드포인트, 즐겨찾기 훅, 4개 라우트 매핑 완료</done>
</task>

<task type="auto">
  <name>Task 2: sys05 고유 페이지 4개 + 테스트</name>
  <files>
    navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx,
    navy-admin/src/pages/sys05-admin-rules/PrecedentHQPage.tsx,
    navy-admin/src/pages/sys05-admin-rules/PrecedentUnitPage.tsx,
    navy-admin/src/pages/sys05-admin-rules/DirectiveListPage.tsx,
    navy-admin/src/__tests__/sys05/admin-rules.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys05.ts,
    navy-admin/src/pages/sys05-admin-rules/useFavorites.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/DetailModal/DetailModal.tsx,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys05-admin-rules/index.tsx
  </read_first>
  <action>
    1. **RegulationListPage.tsx** (AREG-01 — 현행규정 6개 프로세스):
       - `PageContainer` 타이틀 "현행규정"
       - `useFavorites('sys05-regulation-favorites')` 훅 사용
       - `DataTable` 컬럼: 번호(id), 문서번호(docNumber), 규정명(title), 담당부서(department), 시행일(effectiveDate), 즐겨찾기(StarOutlined/StarFilled 아이콘 토글)
       - 즐겨찾기 컬럼: `isFavorite(record.id)` → StarFilled(노란색) / StarOutlined, onClick → `toggle(record.id)`
       - 행 클릭 시 `DetailModal` 표시: fields = [문서번호, 규정명, 담당부서, 시행일, 내용(content)]
       - DetailModal 하단에 "다운로드" 버튼, "프린트" 버튼: 클릭 시 `message.success('다운로드 시작')`, `message.success('인쇄를 시작합니다')` (per D-13)
       - SearchForm 또는 DataTable search props로 keyword 검색 지원

    2. **PrecedentHQPage.tsx** (AREG-02 — 예규 해군본부 4개 프로세스):
       - `PageContainer` 타이틀 "예규 - 해군본부"
       - `DataTable` 컬럼: 번호, 문서번호, 예규명(title), 담당부서, 시행일
       - request: `GET /api/sys05/precedents/hq`
       - 행 클릭 시 DetailModal (문서번호, 예규명, 담당부서, 시행일, 내용)
       - 다운로드 버튼: `message.success('다운로드 시작')` (per D-13)

    3. **PrecedentUnitPage.tsx** (AREG-03 — 예규 예하부대 1개 프로세스):
       - PrecedentHQPage와 동일 패턴, API 경로만 `/api/sys05/precedents/unit`으로 변경
       - `PageContainer` 타이틀 "예규 - 예하부대"

    4. **DirectiveListPage.tsx** (AREG-04 — 지시문서 4개 프로세스):
       - `PageContainer` 타이틀 "지시문서"
       - `DataTable` 컬럼: 번호, 문서번호, 지시명(title), 발령부서(department), 발령일(effectiveDate)
       - request: `GET /api/sys05/directives`
       - 행 클릭 시 DetailModal, 다운로드 버튼: Mock 알림 (per D-13)

    5. **admin-rules.test.ts** (Nyquist 테스트):
       - readFileSync 기반:
         - RegulationListPage.tsx에 'useFavorites' + 'DataTable' + 'DetailModal' + 'message.success' 포함
         - PrecedentHQPage.tsx에 'DataTable' 포함
         - PrecedentUnitPage.tsx에 'DataTable' 포함
         - DirectiveListPage.tsx에 'DataTable' + 'DetailModal' 포함
         - sys05.ts에 'sys05Handlers' 포함
         - index.tsx에 4개 Route path 포함 ('1/1', '2/1', '2/2', '3/1')
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys05/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RegulationListPage.tsx에 `useFavorites` 포함
    - RegulationListPage.tsx에 `StarFilled` 또는 `StarOutlined` 포함
    - RegulationListPage.tsx에 `message.success` 포함
    - PrecedentHQPage.tsx에 `DataTable` 포함
    - PrecedentUnitPage.tsx에 `DataTable` 포함
    - DirectiveListPage.tsx에 `DataTable` 포함
    - admin-rules.test.ts 전체 통과
    - `npx tsc --noEmit` 에러 0건
  </acceptance_criteria>
  <done>현행규정(즐겨찾기+다운로드Mock), 예규(해군본부/예하부대), 지시문서 4개 페이지가 완전 구현되고 테스트 통과</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx tsc --noEmit` — TypeScript 에러 0건
- `cd navy-admin && npx vitest run src/__tests__/sys05/` — 전체 테스트 통과
- `cd navy-admin && npx vitest run` — 기존 테스트 regression 없음
</verification>

<success_criteria>
1. /sys05/1/1 에서 현행규정 목록이 표시되고 즐겨찾기 토글 + 상세 모달 + 다운로드 Mock 동작
2. /sys05/2/1, /sys05/2/2 에서 예규 목록/상세 조회 동작
3. /sys05/3/1 에서 지시문서 목록/상세 조회 + 다운로드 Mock 동작
4. 즐겨찾기가 localStorage에 저장되어 새로고침 후에도 유지
5. TypeScript 0 errors, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-02-SUMMARY.md`
</output>
