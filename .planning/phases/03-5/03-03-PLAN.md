---
phase: 03-5
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys14-suggestion/index.tsx
  - navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx
  - navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx
  - navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys14.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys14/suggestion.test.ts
autonomous: true
requirements: [SUGST-01, SUGST-02, SUGST-03, SUGST-04, SUGST-05]

must_haves:
  truths:
    - "메인화면에 제언 현황 통계(총건수/이번달/답변완료/처리대기)와 최신 5건이 표시된다"
    - "제언 목록에서 작성/수정/삭제가 가능하고 상세에서 추천/신고 카운트가 동작한다"
    - "관리자 답변이 제언 상세 페이지 하단에 표시된다"
    - "공지사항과 권한관리 메뉴가 Phase 1 공통 페이지로 연결된다"
  artifacts:
    - path: "navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx"
      provides: "제언 메인화면 (통계+최신목록)"
      contains: "Statistic"
    - path: "navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx"
      provides: "제언확인 화면 (CRUD+추천/신고)"
      contains: "DataTable"
    - path: "navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx"
      provides: "관리자 제언관리 화면"
      contains: "DataTable"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys14.ts"
      provides: "제언 MSW 핸들러"
      exports: ["sys14Handlers"]
  key_links:
    - from: "pages/sys14-suggestion/SuggestionListPage.tsx"
      to: "/api/sys14/suggestions/:id/recommend"
      via: "useMutation"
      pattern: "recommend|recommend"
    - from: "pages/sys14-suggestion/index.tsx"
      to: "pages/common/board, pages/common/auth-group"
      via: "React Router lazy import"
      pattern: "BoardIndex|AuthGroupIndex"
---

<objective>
14_나의제언 전체 구현 (SUGST-01~05, 16개 프로세스)

Purpose: 제언 메인화면(통계+최신목록) + 제언확인(CRUD+추천/신고/비공개) + 관리자(답변/관리) + 공통 기능 연결을 구현한다. 추천/신고는 독립 API, 답변은 상세 페이지 내 하단 영역으로 구현한다.
Output: sys14 서브시스템 3개 고유 페이지 + MSW 핸들러 + 공통 기능 라우트 연결
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-5/03-CONTEXT.md
@.planning/phases/03-5/03-RESEARCH.md

<interfaces>
From shared/ui/DataTable/DataTable.tsx:
```typescript
export interface DataTableProps<T extends Record<string, unknown>> {
  columns: ProColumns<T>[]; request: (params: PageRequest) => Promise<PageResponse<T>>
  rowKey: keyof T & string; toolBarRender?: ...; headerTitle?: string; onRow?: ...
}
```

From shared/ui/CrudForm/CrudForm.tsx:
```typescript
export interface CrudFormField { name: string; label: string; type: 'text'|'textarea'|'select'|'date'|'number'; required?: boolean; options?: ... }
export interface CrudFormProps<T> { fields: CrudFormField[]; onFinish: (values: T) => Promise<boolean>; initialValues?: Partial<T>; mode?: 'create'|'edit'|'view' }
```

From shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string }
```

메뉴 구조:
```typescript
sys14: [
  { name: '나의제언', path: '/sys14/1', children: [
    { name: '메인화면', path: '/sys14/1/1' },
    { name: '공지사항', path: '/sys14/1/2' },
    { name: '제언확인', path: '/sys14/1/3' },
    { name: '관리자', path: '/sys14/1/4' },
  ]},
  { name: '시스템', path: '/sys14/2', children: [
    { name: '사용자별권한등록', path: '/sys14/2/1' },
  ]},
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sys14 MSW 핸들러 + index.tsx 라우팅</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys14.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys14-suggestion/index.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys14-suggestion/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
    1. **sys14.ts MSW 핸들러 생성** (`navy-admin/src/shared/api/mocks/handlers/sys14.ts`):
       - `Suggestion` 인터페이스:
         ```
         { id: string, title: string, content: string, authorName: string, authorUnit: string,
           status: 'open' | 'answered' | 'closed', isPrivate: boolean,
           recommendCount: number, reportCount: number,
           answer?: string, answeredAt?: string, answeredBy?: string,
           createdAt: string, updatedAt: string }
         ```
       - faker ko locale로 25건 Mock 데이터. status 분포: open 60%, answered 30%, closed 10%
       - 엔드포인트:
         - `GET /api/sys14/suggestions` — keyword/status 필터 + 페이지네이션. isPrivate=true인 항목은 관리자가 아닌 경우 제외 (Mock에서는 전체 반환)
         - `GET /api/sys14/suggestions/:id` — 상세 단건 (answer 포함)
         - `POST /api/sys14/suggestions` — 새 제언 등록 (title, content 필수)
         - `PUT /api/sys14/suggestions/:id` — 제언 수정
         - `DELETE /api/sys14/suggestions/:id` — 제언 삭제
         - `POST /api/sys14/suggestions/:id/recommend` — 추천 +1 (per D-17, 독립 API)
         - `POST /api/sys14/suggestions/:id/report` — 신고 +1 (per D-17, 독립 API)
         - `PATCH /api/sys14/suggestions/:id/private` — 비공개 처리 (isPrivate=true)
         - `PATCH /api/sys14/suggestions/:id/answer` — 관리자 답변 등록 (body: { answer: string }) (per D-18)
         - `GET /api/sys14/suggestions/stats` — 통계 반환: `{ total, thisMonth, answered, pending }`
         - `GET /api/sys14/suggestions/recent` — 최신 5건 (PageResponse 형식, size=5)
       - `export const sys14Handlers = [...]`

    2. **handlers/index.ts 수정**: `import { sys14Handlers } from './sys14'` 추가, `...sys14Handlers` spread

    3. **index.tsx 라우팅** (`navy-admin/src/pages/sys14-suggestion/index.tsx`):
       - `SubsystemPage` import 제거
       - 공통 기능 lazy import (per D-01, D-03):
         - `const BoardIndex = lazy(() => import('@/pages/common/board'))` for SUGST-03
         - `const AuthGroupIndex = lazy(() => import('@/pages/common/auth-group'))` for SUGST-05
       - 고유 페이지 import:
         - `import SuggestionMainPage from './SuggestionMainPage'`
         - `import SuggestionListPage from './SuggestionListPage'`
         - `import SuggestionAdminPage from './SuggestionAdminPage'`
       - Routes 구조:
         ```
         <Route index element={<Navigate to="/sys14/1/1" replace />} />
         <Route path="1/1" element={<SuggestionMainPage />} />
         <Route path="1/2" element={<Suspense fallback={<PageSpinner />}><BoardIndex /></Suspense>} />
         <Route path="1/3" element={<SuggestionListPage />} />
         <Route path="1/4" element={<SuggestionAdminPage />} />
         <Route path="2/1" element={<Suspense fallback={<PageSpinner />}><AuthGroupIndex /></Suspense>} />
         ```
       - 기본 경로 `/sys14/1/1` (메인화면) (per D-15, 메인화면 있는 경우)
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - sys14.ts에 `export const sys14Handlers` 포함
    - sys14.ts에 `http.post('/api/sys14/suggestions/:id/recommend'` 포함
    - sys14.ts에 `http.post('/api/sys14/suggestions/:id/report'` 포함
    - sys14.ts에 `http.patch('/api/sys14/suggestions/:id/answer'` 포함
    - sys14.ts에 `http.get('/api/sys14/suggestions/stats'` 포함
    - handlers/index.ts에 `sys14Handlers` 포함
    - index.tsx에 `Navigate to="/sys14/1/1"` 포함
    - index.tsx에 `BoardIndex` 포함 (SUGST-03)
    - index.tsx에 `AuthGroupIndex` 포함 (SUGST-05)
    - TypeScript 컴파일 에러 0건
  </acceptance_criteria>
  <done>sys14 MSW 핸들러 11개 엔드포인트(CRUD+추천+신고+비공개+답변+통계+최신), index.tsx 5개 라우트 매핑</done>
</task>

<task type="auto">
  <name>Task 2: sys14 고유 페이지 3개 + 테스트</name>
  <files>
    navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx,
    navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx,
    navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx,
    navy-admin/src/__tests__/sys14/suggestion.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys14.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys14-suggestion/index.tsx
  </read_first>
  <action>
    1. **SuggestionMainPage.tsx** (SUGST-01 — 메인화면, per D-08/D-09):
       - `PageContainer` 타이틀 "나의 제언"
       - 상단: `Row gutter={16}` + 4개 `Col span={6}` + `Card` + `Statistic`
         - "전체 제언" (total), "이번달 등록" (thisMonth), "답변 완료" (answered), "처리 대기" (pending)
       - useQuery로 `GET /api/sys14/suggestions/stats` 호출하여 통계 데이터 표시
       - 하단: `Card title="최신 제언" extra={<Button type="link" onClick={() => navigate('/sys14/1/3')}>전체보기</Button>}`
         - 내부에 최신 5건 antd `List` 또는 간단한 `DataTable` (request: `GET /api/sys14/suggestions/recent`)
         - 각 항목: 제목, 작성자, 작성일, 상태(StatusBadge)

    2. **SuggestionListPage.tsx** (SUGST-02 — 제언확인, 11개 프로세스):
       - `PageContainer` 타이틀 "제언확인"
       - `DataTable` 컬럼: 번호(id), 제목(title), 작성자(authorName), 소속(authorUnit), 상태(StatusBadge: open=대기/cyan, answered=답변완료/green, closed=종료/default), 추천(recommendCount), 작성일(createdAt)
       - StatusBadge colorMap: `{ open: 'cyan', answered: 'green', closed: 'default' }`, labelMap: `{ open: '대기', answered: '답변완료', closed: '종료' }`
       - toolBarRender: "제언 작성" 버튼 → antd Modal 내 CrudForm
       - CrudForm fields: title (text, required), content (textarea, required)
       - 행 클릭 → 상세 페이지 영역(같은 페이지 내 하단 또는 Drawer/Modal):
         - useQuery `GET /api/sys14/suggestions/:id`로 상세 조회
         - 상세 내용 + 추천/신고 버튼:
           - "추천" 버튼(LikeOutlined + count): `POST /api/sys14/suggestions/:id/recommend` 호출 (per D-17)
           - "신고" 버튼(WarningOutlined + count): `POST /api/sys14/suggestions/:id/report` 호출 (per D-17)
         - 하단 답변 영역 (per D-18): 답변이 있으면 표시 (answeredBy, answeredAt, answer 내용)
       - CRUD mutations: POST(등록), PUT(수정), DELETE(삭제) + invalidateQueries

    3. **SuggestionAdminPage.tsx** (SUGST-04 — 관리자 제언관리):
       - `PageContainer` 타이틀 "제언관리 (관리자)"
       - `DataTable` 컬럼: 번호, 제목, 작성자, 상태, 비공개여부(isPrivate → Tag), 추천수, 신고수, 작성일, 액션
       - 액션 컬럼:
         - "비공개" 버튼 (isPrivate=false인 경우): Popconfirm → `PATCH /api/sys14/suggestions/:id/private` (per D-17)
         - "답변" 버튼: Modal 열기 → antd Input.TextArea로 답변 입력 → `PATCH /api/sys14/suggestions/:id/answer` 호출 (per D-18)
       - 답변 등록 성공 시 `message.success('답변이 등록되었습니다')` + invalidateQueries

    4. **suggestion.test.ts** (Nyquist 테스트):
       - readFileSync 기반:
         - SuggestionMainPage.tsx에 'Statistic' + 'Card' + 'navigate' 포함
         - SuggestionListPage.tsx에 'DataTable' + 'CrudForm' + 'recommend' + 'report' 포함
         - SuggestionAdminPage.tsx에 'DataTable' + 'answer' + 'private' 포함
         - sys14.ts에 'sys14Handlers' 포함
         - index.tsx에 'SuggestionMainPage' + 'BoardIndex' + 'AuthGroupIndex' 포함
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys14/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SuggestionMainPage.tsx에 `Statistic` 포함
    - SuggestionMainPage.tsx에 `전체보기` 포함
    - SuggestionListPage.tsx에 `recommend` 포함
    - SuggestionListPage.tsx에 `report` 포함
    - SuggestionListPage.tsx에 `CrudForm` 포함
    - SuggestionAdminPage.tsx에 `answer` 포함
    - SuggestionAdminPage.tsx에 `private` 또는 `isPrivate` 포함
    - suggestion.test.ts 전체 통과
    - `npx tsc --noEmit` 에러 0건
  </acceptance_criteria>
  <done>제언 메인화면(통계+최신목록), 제언확인(CRUD+추천/신고+답변표시), 관리자(비공개+답변등록) 3개 페이지 완전 구현 및 테스트 통과</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx tsc --noEmit` — TypeScript 에러 0건
- `cd navy-admin && npx vitest run src/__tests__/sys14/` — 전체 테스트 통과
- `cd navy-admin && npx vitest run` — 기존 테스트 regression 없음
</verification>

<success_criteria>
1. /sys14/1/1 에서 통계 카드 4개 + 최신 제언 5건 목록 표시
2. /sys14/1/3 에서 제언 목록 CRUD + 상세에서 추천/신고 카운트 + 답변 표시
3. /sys14/1/4 에서 관리자가 비공개 처리 + 답변 등록 가능
4. /sys14/1/2 공지사항, /sys14/2/1 권한관리가 Phase 1 공통 페이지 렌더링
5. TypeScript 0 errors, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-03-SUMMARY.md`
</output>
