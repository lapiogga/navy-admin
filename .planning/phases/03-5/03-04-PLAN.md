---
phase: 03-5
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys11-research/index.tsx
  - navy-admin/src/pages/sys11-research/ResearchMainPage.tsx
  - navy-admin/src/pages/sys11-research/ResearchListPage.tsx
  - navy-admin/src/pages/sys11-research/ResearchAdminPage.tsx
  - navy-admin/src/pages/sys11-research/ResearchFilePage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys11.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys11/research.test.ts
autonomous: true
requirements: [RSRC-01, RSRC-02, RSRC-03, RSRC-04, RSRC-05, RSRC-06]

must_haves:
  truths:
    - "메인화면에 최신자료/인기자료 통계와 최신 5건이 표시된다"
    - "연구자료 목록에서 등록/수정/삭제가 가능하고 다운로드 Mock 알림이 동작한다"
    - "관리자 페이지에서 자료관리/카테고리관리/통계를 탭으로 전환할 수 있다"
    - "공지사항과 권한관리 메뉴가 Phase 1 공통 페이지로 연결된다"
  artifacts:
    - path: "navy-admin/src/pages/sys11-research/ResearchMainPage.tsx"
      provides: "연구자료 메인화면 (통계+최신목록)"
      contains: "Statistic"
    - path: "navy-admin/src/pages/sys11-research/ResearchListPage.tsx"
      provides: "연구자료 CRUD 화면"
      contains: "DataTable"
    - path: "navy-admin/src/pages/sys11-research/ResearchAdminPage.tsx"
      provides: "관리자 화면 (Tabs)"
      contains: "Tabs"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys11.ts"
      provides: "연구자료 MSW 핸들러"
      exports: ["sys11Handlers"]
  key_links:
    - from: "pages/sys11-research/index.tsx"
      to: "pages/common/board, pages/common/auth-group"
      via: "React Router lazy import"
      pattern: "BoardIndex|AuthGroupIndex"
    - from: "shared/api/mocks/handlers/index.ts"
      to: "sys11Handlers"
      via: "spread into handlers array"
      pattern: "sys11Handlers"
---

<objective>
11_연구자료종합관리체계 전체 구현 (RSRC-01~06, 19개 프로세스)

Purpose: 연구자료 메인화면(통계) + 자료 CRUD + 관리자(Tabs: 자료관리/카테고리/통계) + 자료실 + 공통 기능 연결을 구현한다.
Output: sys11 서브시스템 4개 고유 페이지 + MSW 핸들러 + 공통 기능 라우트 연결
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-5/03-CONTEXT.md
@.planning/phases/03-5/03-RESEARCH.md

<interfaces>
From shared/ui/DataTable/DataTable.tsx:
```typescript
export interface DataTableProps<T extends Record<string, unknown>> {
  columns: ProColumns<T>[]; request: (params: PageRequest) => Promise<PageResponse<T>>
  rowKey: keyof T & string; toolBarRender?: ...; headerTitle?: string; onRow?: ...
}
```

From shared/ui/CrudForm/CrudForm.tsx:
```typescript
export interface CrudFormField { name: string; label: string; type: 'text'|'textarea'|'select'|'date'|'number'; required?: boolean; options?: ... }
export interface CrudFormProps<T> { fields: CrudFormField[]; onFinish: (values: T) => Promise<boolean>; initialValues?: Partial<T>; mode?: 'create'|'edit'|'view' }
```

From shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string }
```

메뉴 구조:
```typescript
sys11: [
  { name: '연구자료종합관리', path: '/sys11/1', children: [
    { name: '메인화면', path: '/sys11/1/1' },
    { name: '연구자료', path: '/sys11/1/2' },
    { name: '자료실', path: '/sys11/1/3' },
    { name: '공지사항', path: '/sys11/1/4' },
    { name: '관리자', path: '/sys11/1/5' },
  ]},
  { name: '시스템', path: '/sys11/2', children: [
    { name: '사용자별권한등록', path: '/sys11/2/1' },
  ]},
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sys11 MSW 핸들러 + index.tsx 라우팅</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys11.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys11-research/index.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys11-research/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
    1. **sys11.ts MSW 핸들러 생성** (`navy-admin/src/shared/api/mocks/handlers/sys11.ts`):
       - `ResearchItem` 인터페이스:
         ```
         { id: string, title: string, author: string, department: string,
           category: string, description: string, fileUrl: string, fileName: string,
           downloadCount: number, viewCount: number, createdAt: string, updatedAt: string }
         ```
       - `ResearchCategory` 인터페이스: `{ id: string, name: string, parentId?: string, sortOrder: number }`
       - faker ko locale로 30건 연구자료 Mock + 6개 카테고리 (전략연구, 작전연구, 교육훈련, 인사관리, 군수지원, 기타)
       - 엔드포인트:
         - `GET /api/sys11/research` — keyword/category 필터 + 페이지네이션
         - `GET /api/sys11/research/:id` — 상세 단건
         - `POST /api/sys11/research` — 등록
         - `PUT /api/sys11/research/:id` — 수정
         - `DELETE /api/sys11/research/:id` — 삭제
         - `GET /api/sys11/research/stats` — 통계: `{ total, thisMonth, topCategory, totalDownloads }`
         - `GET /api/sys11/research/recent` — 최신 5건 (PageResponse)
         - `GET /api/sys11/research/popular` — 인기 5건 (downloadCount 내림차순)
         - `GET /api/sys11/categories` — 카테고리 목록 (전체)
         - `POST /api/sys11/categories` — 카테고리 등록
         - `DELETE /api/sys11/categories/:id` — 카테고리 삭제
         - `GET /api/sys11/files` — 자료실 목록 (research와 동일 데이터, 파일 다운로드 중심 뷰)
       - `export const sys11Handlers = [...]`

    2. **handlers/index.ts 수정**: `import { sys11Handlers } from './sys11'` 추가, `...sys11Handlers` spread

    3. **index.tsx 라우팅** (`navy-admin/src/pages/sys11-research/index.tsx`):
       - `SubsystemPage` import 제거
       - 공통 기능 lazy import (per D-01, D-03):
         - `const BoardIndex = lazy(() => import('@/pages/common/board'))` for RSRC-04
         - `const AuthGroupIndex = lazy(() => import('@/pages/common/auth-group'))` for RSRC-06
       - 고유 페이지 import:
         - `import ResearchMainPage from './ResearchMainPage'`
         - `import ResearchListPage from './ResearchListPage'`
         - `import ResearchFilePage from './ResearchFilePage'`
         - `import ResearchAdminPage from './ResearchAdminPage'`
       - Routes 구조:
         ```
         <Route index element={<Navigate to="/sys11/1/1" replace />} />
         <Route path="1/1" element={<ResearchMainPage />} />
         <Route path="1/2" element={<ResearchListPage />} />
         <Route path="1/3" element={<ResearchFilePage />} />
         <Route path="1/4" element={<Suspense fallback={<PageSpinner />}><BoardIndex /></Suspense>} />
         <Route path="1/5" element={<ResearchAdminPage />} />
         <Route path="2/1" element={<Suspense fallback={<PageSpinner />}><AuthGroupIndex /></Suspense>} />
         ```
       - 기본 경로 `/sys11/1/1` (메인화면) (per D-15)
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - sys11.ts에 `export const sys11Handlers` 포함
    - sys11.ts에 `http.get('/api/sys11/research'` 포함
    - sys11.ts에 `http.get('/api/sys11/categories'` 포함
    - sys11.ts에 `http.get('/api/sys11/research/stats'` 포함
    - handlers/index.ts에 `sys11Handlers` 포함
    - index.tsx에 `Navigate to="/sys11/1/1"` 포함
    - index.tsx에 `BoardIndex` 포함 (RSRC-04)
    - index.tsx에 `AuthGroupIndex` 포함 (RSRC-06)
    - TypeScript 컴파일 에러 0건
  </acceptance_criteria>
  <done>sys11 MSW 핸들러 12개 엔드포인트, index.tsx 6개 라우트 매핑 완료</done>
</task>

<task type="auto">
  <name>Task 2: sys11 고유 페이지 4개 + 테스트</name>
  <files>
    navy-admin/src/pages/sys11-research/ResearchMainPage.tsx,
    navy-admin/src/pages/sys11-research/ResearchListPage.tsx,
    navy-admin/src/pages/sys11-research/ResearchFilePage.tsx,
    navy-admin/src/pages/sys11-research/ResearchAdminPage.tsx,
    navy-admin/src/__tests__/sys11/research.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys11.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/DetailModal/DetailModal.tsx,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys11-research/index.tsx
  </read_first>
  <action>
    1. **ResearchMainPage.tsx** (RSRC-01 — 메인화면, per D-08/D-09):
       - `PageContainer` 타이틀 "연구자료종합관리"
       - 상단: `Row gutter={16}` + 4개 `Col span={6}` + `Card` + `Statistic`
         - "전체 자료" (total), "이번달 등록" (thisMonth), "인기 분야" (topCategory), "총 다운로드" (totalDownloads)
       - useQuery `GET /api/sys11/research/stats`
       - 하단: 2개 Card 병렬 (`Row gutter={16}`)
         - "최신 자료" Card: useQuery `GET /api/sys11/research/recent` → 5건 List (제목, 분야, 작성일)
         - "인기 자료" Card: useQuery `GET /api/sys11/research/popular` → 5건 List (제목, 다운로드수)
       - 각 Card에 `extra={<Button type="link" onClick={() => navigate('/sys11/1/2')}>전체보기</Button>}`

    2. **ResearchListPage.tsx** (RSRC-02 — 연구자료 8개 프로세스):
       - `PageContainer` 타이틀 "연구자료"
       - `DataTable` 컬럼: 번호(id), 제목(title), 저자(author), 분야(category), 부서(department), 다운로드(downloadCount), 조회수(viewCount), 등록일(createdAt)
       - toolBarRender: "자료 등록" 버튼 → Modal + CrudForm
       - CrudForm fields: title (text, required), category (select, 6개 카테고리 options), author (text, required), department (text), description (textarea, required)
       - 행 클릭 → DetailModal: 제목, 저자, 분야, 부서, 설명, 파일명, 다운로드수, 조회수, 등록일
       - DetailModal 하단 "다운로드" 버튼: `message.success('다운로드 시작')` (per D-13)
       - CRUD mutations: POST/PUT/DELETE + invalidateQueries

    3. **ResearchFilePage.tsx** (RSRC-05 — 자료실):
       - `PageContainer` 타이틀 "자료실"
       - `DataTable` 컬럼: 번호, 제목, 파일명(fileName), 파일크기(Mock), 다운로드수, 등록일
       - request: `GET /api/sys11/files`
       - 각 행에 "다운로드" 버튼: `message.success('다운로드 시작')` (per D-13)
       - 검색: keyword 필터

    4. **ResearchAdminPage.tsx** (RSRC-03 — 관리자 6개 프로세스):
       - `PageContainer` 타이틀 "관리자"
       - antd `Tabs` 컴포넌트로 6개 기능 분리:
         - Tab 1 "자료관리": DataTable (연구자료 목록 + 수정/삭제 액션)
         - Tab 2 "카테고리관리": DataTable (카테고리 목록) + CrudForm Modal (카테고리 추가), 삭제 Popconfirm
         - Tab 3 "통계": antd Statistic 4개 카드 (총 자료수, 카테고리별 건수, 이번달 등록, 총 다운로드)
         - Tab 4 "사용자관리": Mock 테이블 (사용자명, 소속, 역할)
         - Tab 5 "삭제관리": Mock 테이블 (삭제된 자료 목록)
         - Tab 6 "권한관리": Phase 1 auth-group 페이지와 동일하므로 Navigate to /sys11/2/1

    5. **research.test.ts** (Nyquist 테스트):
       - readFileSync 기반:
         - ResearchMainPage.tsx에 'Statistic' + '전체보기' 포함
         - ResearchListPage.tsx에 'DataTable' + 'CrudForm' + 'DetailModal' 포함
         - ResearchFilePage.tsx에 'DataTable' + 'message.success' 포함
         - ResearchAdminPage.tsx에 'Tabs' + 'DataTable' 포함
         - sys11.ts에 'sys11Handlers' 포함
         - index.tsx에 'BoardIndex' + 'AuthGroupIndex' 포함
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys11/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ResearchMainPage.tsx에 `Statistic` 포함
    - ResearchMainPage.tsx에 `전체보기` 포함
    - ResearchListPage.tsx에 `DataTable` 포함
    - ResearchListPage.tsx에 `CrudForm` 포함
    - ResearchFilePage.tsx에 `message.success` 포함
    - ResearchAdminPage.tsx에 `Tabs` 포함
    - research.test.ts 전체 통과
    - `npx tsc --noEmit` 에러 0건
  </acceptance_criteria>
  <done>연구자료 메인(통계+최신/인기), 자료CRUD, 자료실, 관리자(6탭) 4개 페이지 완전 구현 및 테스트 통과</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx tsc --noEmit` — TypeScript 에러 0건
- `cd navy-admin && npx vitest run src/__tests__/sys11/` — 전체 테스트 통과
- `cd navy-admin && npx vitest run` — 기존 테스트 regression 없음
</verification>

<success_criteria>
1. /sys11/1/1 에서 통계 카드 4개 + 최신/인기 자료 각 5건 표시
2. /sys11/1/2 에서 연구자료 CRUD + 상세 모달 + 다운로드 Mock 동작
3. /sys11/1/3 에서 자료실 목록 + 다운로드 Mock 동작
4. /sys11/1/5 에서 관리자 6개 탭 전환 동작
5. /sys11/1/4 공지사항, /sys11/2/1 권한관리가 Phase 1 공통 페이지 렌더링
6. TypeScript 0 errors, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-04-SUMMARY.md`
</output>
