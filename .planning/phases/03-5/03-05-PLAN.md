---
phase: 03-5
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys16-meeting-room/index.tsx
  - navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx
  - navy-admin/src/pages/sys16-meeting-room/MyReservationPage.tsx
  - navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx
  - navy-admin/src/pages/sys16-meeting-room/MeetingRoomMgmtPage.tsx
  - navy-admin/src/pages/sys16-meeting-room/MeetingStatusPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys16.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys16/meeting-room.test.ts
autonomous: true
requirements: [ROOM-01, ROOM-02, ROOM-03, ROOM-04, ROOM-05, ROOM-06, ROOM-07]

must_haves:
  truths:
    - "사용자가 회의실을 선택하고 날짜/시간/목적을 입력하여 예약을 신청할 수 있다"
    - "내예약확인에서 나의 예약 목록을 조회/수정/삭제할 수 있다"
    - "관리자가 예약 목록에서 승인/반려할 수 있다"
    - "회의실 CRUD + 시간대 설정 + 장비 관리 + 사진 업로드가 동작한다"
    - "회의현황에서 전체 예약 현황을 조회할 수 있다"
    - "공지사항과 코드관리 메뉴가 Phase 1 공통 페이지로 연결된다"
  artifacts:
    - path: "navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx"
      provides: "회의예약신청 화면"
      contains: "CrudForm"
    - path: "navy-admin/src/pages/sys16-meeting-room/MyReservationPage.tsx"
      provides: "내예약확인 화면"
      contains: "DataTable"
    - path: "navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx"
      provides: "회의예약관리(승인/반려) 화면"
      contains: "Popconfirm"
    - path: "navy-admin/src/pages/sys16-meeting-room/MeetingRoomMgmtPage.tsx"
      provides: "회의실관리 화면 (CRUD+시간대+장비+사진)"
      contains: "Tabs"
    - path: "navy-admin/src/pages/sys16-meeting-room/MeetingStatusPage.tsx"
      provides: "회의현황 화면"
      contains: "DataTable"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys16.ts"
      provides: "회의실 MSW 핸들러"
      exports: ["sys16Handlers"]
  key_links:
    - from: "pages/sys16-meeting-room/ReservationMgmtPage.tsx"
      to: "/api/sys16/reservations/:id/approve"
      via: "useMutation Popconfirm"
      pattern: "approve|reject"
    - from: "pages/sys16-meeting-room/MeetingRoomMgmtPage.tsx"
      to: "/api/sys16/meeting-rooms/:id/photos"
      via: "antd Upload action"
      pattern: "Upload.*action"
    - from: "pages/sys16-meeting-room/index.tsx"
      to: "pages/common/board, pages/common/code-mgmt"
      via: "React Router lazy import"
      pattern: "BoardIndex|CodeMgmtIndex"
---

<objective>
16_회의실예약관리체계 전체 구현 (ROOM-01~07, 21개 프로세스)

Purpose: 회의예약신청 -> 관리자승인/반려 -> 내예약확인 워크플로우 + 회의실 CRUD(시간대/장비/사진 특수 UI) + 회의현황 + 공통 기능 연결. Phase 3에서 가장 복잡한 서브시스템.
Output: sys16 서브시스템 5개 고유 페이지 + MSW 핸들러 + 공통 기능 라우트 연결
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-5/03-CONTEXT.md
@.planning/phases/03-5/03-RESEARCH.md

<interfaces>
From shared/ui/DataTable/DataTable.tsx:
```typescript
export interface DataTableProps<T extends Record<string, unknown>> {
  columns: ProColumns<T>[]; request: (params: PageRequest) => Promise<PageResponse<T>>
  rowKey: keyof T & string; toolBarRender?: ...; headerTitle?: string; onRow?: ...
}
```

From shared/ui/CrudForm/CrudForm.tsx:
```typescript
export interface CrudFormField { name: string; label: string; type: 'text'|'textarea'|'select'|'date'|'number'; required?: boolean; options?: ... }
export interface CrudFormProps<T> { fields: CrudFormField[]; onFinish: (values: T) => Promise<boolean>; initialValues?: Partial<T>; mode?: 'create'|'edit'|'view' }
```

From shared/ui/StatusBadge/StatusBadge.tsx:
```typescript
export interface StatusBadgeProps { status: string; colorMap?: Record<string, string>; labelMap?: Record<string, string> }
```

From shared/api/types.ts:
```typescript
export interface PageRequest { page: number; size: number }
export interface PageResponse<T> { content: T[]; totalElements: number; totalPages: number; size: number; number: number }
export interface ApiResult<T = void> { success: boolean; data: T; message?: string }
```

메뉴 구조:
```typescript
sys16: [
  { name: '회의실 예약관리', path: '/sys16/1', children: [
    { name: '공지사항', path: '/sys16/1/1' },
    { name: '회의예약신청', path: '/sys16/1/2' },
    { name: '내예약확인', path: '/sys16/1/3' },
    { name: '회의현황', path: '/sys16/1/4' },
    { name: '회의예약관리', path: '/sys16/1/5' },
    { name: '회의실 관리', path: '/sys16/1/6' },
  ]},
  { name: '시스템', path: '/sys16/2', children: [
    { name: '공통코드관리', path: '/sys16/2/1' },
  ]},
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sys16 MSW 핸들러 + index.tsx 라우팅</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys16.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys16-meeting-room/index.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys16-meeting-room/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
    1. **sys16.ts MSW 핸들러 생성** (`navy-admin/src/shared/api/mocks/handlers/sys16.ts`):
       - 타입 정의:
         ```typescript
         type ReservationStatus = 'pending' | 'approved' | 'rejected'
         interface MeetingRoom {
           id: string; name: string; location: string; capacity: number
           description: string; photos: { id: string; url: string }[]
           equipment: { id: string; name: string; quantity: number }[]
           schedule: { day: string; enabled: boolean; startTime: string; endTime: string }[]
         }
         interface Reservation {
           id: string; roomId: string; roomName: string
           applicant: string; unit: string; purpose: string
           date: string; startTime: string; endTime: string
           status: ReservationStatus; createdAt: string; processedAt?: string
         }
         ```
       - faker ko locale로 Mock 데이터: 5개 회의실 + 25건 예약
       - 회의실별 schedule: 월~금 enabled=true (09:00-18:00), 토일 enabled=false
       - 회의실별 equipment: 빔프로젝터, 화이트보드, 마이크 등 2-4개
       - 회의실별 photos: picsum.photos URL 1-3개

       - 엔드포인트:
         **예약 관련:**
         - `POST /api/sys16/reservations` — 예약 신청 (roomId, date, startTime, endTime, purpose)
         - `GET /api/sys16/reservations/my` — 내 예약 목록 (페이지네이션)
         - `PUT /api/sys16/reservations/:id` — 예약 수정
         - `DELETE /api/sys16/reservations/:id` — 예약 삭제
         - `GET /api/sys16/reservations/:id` — 예약 상세
         - `GET /api/sys16/reservations` — 전체 예약 목록 (관리자, 페이지네이션 + status 필터)
         - `PATCH /api/sys16/reservations/:id/approve` — 예약 승인 (per D-05)
         - `PATCH /api/sys16/reservations/:id/reject` — 예약 반려 (per D-05)
         - `GET /api/sys16/reservations/status` — 회의현황 (전체 예약 달력/목록 뷰)

         **회의실 관련:**
         - `GET /api/sys16/meeting-rooms` — 회의실 목록
         - `GET /api/sys16/meeting-rooms/:id` — 회의실 상세
         - `POST /api/sys16/meeting-rooms` — 회의실 등록
         - `PUT /api/sys16/meeting-rooms/:id` — 회의실 수정
         - `DELETE /api/sys16/meeting-rooms/:id` — 회의실 삭제
         - `PUT /api/sys16/meeting-rooms/:id/schedule` — 시간대 설정 (per D-10)
         - `GET /api/sys16/meeting-rooms/:id/equipment` — 장비 목록
         - `POST /api/sys16/meeting-rooms/:id/equipment` — 장비 추가 (per D-11)
         - `DELETE /api/sys16/meeting-rooms/:id/equipment/:eqId` — 장비 삭제
         - `POST /api/sys16/meeting-rooms/:id/photos` — 사진 업로드 Mock (per D-12, `{ id, url }` 반환)
         - `DELETE /api/sys16/meeting-rooms/:id/photos/:photoId` — 사진 삭제
       - `export const sys16Handlers = [...]`

    2. **handlers/index.ts 수정**: `import { sys16Handlers } from './sys16'` 추가, `...sys16Handlers` spread

    3. **index.tsx 라우팅** (`navy-admin/src/pages/sys16-meeting-room/index.tsx`):
       - `SubsystemPage` import 제거
       - 공통 기능 lazy import (per D-01, D-02):
         - `const BoardIndex = lazy(() => import('@/pages/common/board'))` for ROOM-06
         - `const CodeMgmtIndex = lazy(() => import('@/pages/common/code-mgmt'))` for ROOM-07
       - 고유 페이지 import:
         - `import MeetingReservePage from './MeetingReservePage'`
         - `import MyReservationPage from './MyReservationPage'`
         - `import MeetingStatusPage from './MeetingStatusPage'`
         - `import ReservationMgmtPage from './ReservationMgmtPage'`
         - `import MeetingRoomMgmtPage from './MeetingRoomMgmtPage'`
       - Routes 구조:
         ```
         <Route index element={<Navigate to="/sys16/1/2" replace />} />
         <Route path="1/1" element={<Suspense fallback={<PageSpinner />}><BoardIndex /></Suspense>} />
         <Route path="1/2" element={<MeetingReservePage />} />
         <Route path="1/3" element={<MyReservationPage />} />
         <Route path="1/4" element={<MeetingStatusPage />} />
         <Route path="1/5" element={<ReservationMgmtPage />} />
         <Route path="1/6" element={<MeetingRoomMgmtPage />} />
         <Route path="2/1" element={<Suspense fallback={<PageSpinner />}><CodeMgmtIndex /></Suspense>} />
         ```
       - 기본 경로 `/sys16/1/2` (회의예약신청) (per D-15)
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - sys16.ts에 `export const sys16Handlers` 포함
    - sys16.ts에 `http.post('/api/sys16/reservations'` 포함
    - sys16.ts에 `http.patch('/api/sys16/reservations/:id/approve'` 포함
    - sys16.ts에 `http.put('/api/sys16/meeting-rooms/:id/schedule'` 포함
    - sys16.ts에 `http.post('/api/sys16/meeting-rooms/:id/photos'` 포함
    - handlers/index.ts에 `sys16Handlers` 포함
    - index.tsx에 `Navigate to="/sys16/1/2"` 포함
    - index.tsx에 `BoardIndex` 포함 (ROOM-06)
    - index.tsx에 `CodeMgmtIndex` 포함 (ROOM-07)
    - TypeScript 컴파일 에러 0건
  </acceptance_criteria>
  <done>sys16 MSW 핸들러 21개 엔드포인트, index.tsx 7개 라우트 매핑 완료</done>
</task>

<task type="auto">
  <name>Task 2: sys16 예약 페이지 3개 (신청/내예약/예약관리)</name>
  <files>
    navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx,
    navy-admin/src/pages/sys16-meeting-room/MyReservationPage.tsx,
    navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx,
    navy-admin/src/pages/sys16-meeting-room/MeetingStatusPage.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys16.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys16-meeting-room/index.tsx
  </read_first>
  <action>
    1. **MeetingReservePage.tsx** (ROOM-01 — 회의예약신청):
       - `PageContainer` 타이틀 "회의예약신청"
       - antd `Form` (또는 CrudForm 커스텀) 구조:
         - 회의실 선택: `Select` (useQuery `GET /api/sys16/meeting-rooms` 목록에서 id/name)
         - 예약일: `DatePicker`
         - 시작시간/종료시간: `TimePicker` format="HH:mm" minuteStep={30}
         - 회의 목적: `Input.TextArea`
       - 제출: `POST /api/sys16/reservations` mutation
       - 성공 시 `message.success('예약이 신청되었습니다')` + 내예약확인(`/sys16/1/3`)으로 navigate

    2. **MyReservationPage.tsx** (ROOM-02 — 내예약확인 4개 프로세스):
       - `PageContainer` 타이틀 "내예약확인"
       - `DataTable` 컬럼: 번호(id), 회의실(roomName), 예약일(date), 시간(startTime~endTime), 목적(purpose), 상태(StatusBadge), 신청일(createdAt), 액션(수정/삭제)
       - StatusBadge colorMap: `{ pending: 'orange', approved: 'green', rejected: 'red' }`, labelMap: `{ pending: '대기', approved: '승인', rejected: '반려' }` (per D-06)
       - 수정: status==='pending'일 때만 수정 버튼 → Modal + 기존 값으로 폼 표시 → `PUT /api/sys16/reservations/:id`
       - 삭제: status==='pending'일 때만 삭제 버튼 → Popconfirm → `DELETE /api/sys16/reservations/:id`
       - 행 클릭 시 DetailModal: 회의실, 예약일, 시간, 목적, 상태, 신청일, 처리일

    3. **ReservationMgmtPage.tsx** (ROOM-03 — 회의예약관리 4개 프로세스):
       - `PageContainer` 타이틀 "회의예약관리"
       - `DataTable` 컬럼: 번호, 신청자(applicant), 소속(unit), 회의실, 예약일, 시간, 목적, 상태, 처리 액션
       - 처리 액션 (per D-05, D-07): status==='pending'일 때 승인/반려 버튼
         - 승인: `<Popconfirm title="승인하시겠습니까?" onConfirm={() => approveMutation.mutate(record.id)}>`
         - 반려: `<Popconfirm title="반려하시겠습니까?" onConfirm={() => rejectMutation.mutate(record.id)}>`
       - status!=='pending'이면 StatusBadge 표시
       - approveMutation: `PATCH /api/sys16/reservations/:id/approve`
       - rejectMutation: `PATCH /api/sys16/reservations/:id/reject`
       - request: `GET /api/sys16/reservations` (status 필터 지원)

    4. **MeetingStatusPage.tsx** (ROOM-05 — 회의현황):
       - `PageContainer` 타이틀 "회의현황"
       - `DataTable` 컬럼: 번호, 회의실, 신청자, 예약일, 시간, 목적, 상태(StatusBadge)
       - request: `GET /api/sys16/reservations/status`
       - 검색: 날짜 범위 필터 (DatePicker.RangePicker) + 회의실 Select 필터
  </action>
  <verify>
    <automated>cd navy-admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - MeetingReservePage.tsx에 `Select` 또는 `select` 포함 (회의실 선택)
    - MeetingReservePage.tsx에 `DatePicker` 포함
    - MeetingReservePage.tsx에 `TimePicker` 포함
    - MyReservationPage.tsx에 `DataTable` 포함
    - MyReservationPage.tsx에 `StatusBadge` 포함
    - ReservationMgmtPage.tsx에 `Popconfirm` 포함
    - ReservationMgmtPage.tsx에 `approve` 포함
    - MeetingStatusPage.tsx에 `DataTable` 포함
    - TypeScript 컴파일 에러 0건
  </acceptance_criteria>
  <done>예약신청(DatePicker+TimePicker), 내예약(CRUD+StatusBadge), 예약관리(승인/반려), 회의현황 4개 페이지 완전 구현</done>
</task>

<task type="auto">
  <name>Task 3: sys16 회의실관리 페이지 (특수 UI) + 테스트</name>
  <files>
    navy-admin/src/pages/sys16-meeting-room/MeetingRoomMgmtPage.tsx,
    navy-admin/src/__tests__/sys16/meeting-room.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys16.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/api/types.ts,
    navy-admin/src/pages/sys16-meeting-room/index.tsx,
    navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx,
    navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx
  </read_first>
  <action>
    1. **MeetingRoomMgmtPage.tsx** (ROOM-04 — 회의실관리 8개 프로세스):
       - `PageContainer` 타이틀 "회의실 관리"
       - 좌측: 회의실 목록 DataTable (이름, 위치, 수용인원, 액션: 수정/삭제)
         - toolBarRender: "회의실 등록" 버튼 → Modal + CrudForm (name, location, capacity, description)
         - 행 선택 시 우측 상세 영역 표시

       - 우측: 선택된 회의실의 상세 관리 (antd `Tabs`):
         - **Tab 1 "기본정보"**: 회의실 정보 Descriptions (이름, 위치, 수용인원, 설명) + "수정" 버튼
         - **Tab 2 "시간대 설정"** (per D-10):
           - antd `Table` (직접 사용, DataTable이 아닌 antd 기본 Table) 7행(월~일) x 3열(요일, 운영여부 Switch, 운영시간 TimePicker.RangePicker)
           - 요일별 운영 여부 `Switch`, 운영시간 `TimePicker.RangePicker` format="HH:mm" minuteStep={30}
           - "저장" 버튼: `PUT /api/sys16/meeting-rooms/:id/schedule` mutation
           - 초기값: 선택 회의실의 schedule 데이터
         - **Tab 3 "장비 관리"** (per D-11):
           - `DataTable` (장비명, 수량, 삭제 버튼) + "장비 추가" 버튼 → Modal + CrudForm (name: text, quantity: number)
           - 추가: `POST /api/sys16/meeting-rooms/:id/equipment`
           - 삭제: `DELETE /api/sys16/meeting-rooms/:id/equipment/:eqId` (Popconfirm)
         - **Tab 4 "사진 관리"** (per D-12):
           - antd `Upload` listType="picture-card" maxCount={5} accept="image/*"
           - action: `/api/sys16/meeting-rooms/${roomId}/photos`
           - MSW가 `{ success: true, data: { id, url } }` 반환
           - 업로드 성공 시 `message.success('사진이 등록되었습니다')`
           - 기존 사진 표시: 회의실 photos 배열에서 `fileList` 변환
           - 삭제: `DELETE /api/sys16/meeting-rooms/:id/photos/:photoId`

       - 레이아웃: antd `Row` + `Col` (좌 span={8} 목록, 우 span={16} 상세) 또는 ProLayout 좌우 분할

    2. **meeting-room.test.ts** (Nyquist 테스트):
       - readFileSync 기반:
         - MeetingReservePage.tsx에 'DatePicker' + 'TimePicker' 포함
         - MyReservationPage.tsx에 'DataTable' + 'StatusBadge' 포함
         - ReservationMgmtPage.tsx에 'Popconfirm' + 'approve' 포함
         - MeetingRoomMgmtPage.tsx에 'Tabs' + 'TimePicker.RangePicker' + 'Upload' + 'Switch' 포함
         - MeetingStatusPage.tsx에 'DataTable' 포함
         - sys16.ts에 'sys16Handlers' 포함
         - index.tsx에 'BoardIndex' + 'CodeMgmtIndex' 포함 (ROOM-06, ROOM-07 확인)
         - handlers/index.ts에 'sys16Handlers' 포함
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys16/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MeetingRoomMgmtPage.tsx에 `Tabs` 포함
    - MeetingRoomMgmtPage.tsx에 `TimePicker.RangePicker` 또는 `TimePicker` 포함
    - MeetingRoomMgmtPage.tsx에 `Upload` 포함
    - MeetingRoomMgmtPage.tsx에 `Switch` 포함
    - MeetingRoomMgmtPage.tsx에 `schedule` 포함
    - MeetingRoomMgmtPage.tsx에 `equipment` 포함
    - meeting-room.test.ts 전체 통과
    - `npx tsc --noEmit` 에러 0건
  </acceptance_criteria>
  <done>회의실관리(기본정보+시간대Switch/TimePicker+장비CRUD+사진Upload) 페이지 + 전체 서브시스템 Nyquist 테스트 완성</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx tsc --noEmit` — TypeScript 에러 0건
- `cd navy-admin && npx vitest run src/__tests__/sys16/` — 전체 테스트 통과
- `cd navy-admin && npx vitest run` — 기존 테스트 regression 없음
</verification>

<success_criteria>
1. /sys16/1/2 에서 회의실 선택 + 날짜/시간 입력 + 예약 신청 동작
2. /sys16/1/3 에서 내 예약 목록 조회/수정/삭제 + 상태 뱃지 표시
3. /sys16/1/5 에서 관리자 승인/반려 Popconfirm 동작
4. /sys16/1/6 에서 회의실 CRUD + 시간대 Switch/TimePicker + 장비 CRUD + 사진 Upload 동작
5. /sys16/1/4 에서 회의현황 목록 표시
6. /sys16/1/1 공지사항, /sys16/2/1 코드관리가 Phase 1 공통 페이지 렌더링
7. TypeScript 0 errors, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-05-SUMMARY.md`
</output>
