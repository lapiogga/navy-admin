---
phase: 03-5
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx
  - navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx
  - navy-admin/src/pages/sys04-certificate/CertificateRegisterPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys04.ts
  - navy-admin/src/__tests__/sys04/certificate.test.ts
autonomous: true
gap_closure: true
requirements:
  - CERT-01
  - CERT-02
  - CERT-03

must_haves:
  truths:
    - "인증서 신청폼에 신청구분(신규발급/재발급/갱신), 사유, 군번, 이메일, 전화번호 필드가 모두 표시된다"
    - "대기 상태 신청건에 회수 버튼이 있고 클릭하면 withdrawn 상태로 변경된다"
    - "반려 시 반송의견 입력 모달이 표시되고 사유를 입력해야 반려가 완료된다"
    - "승인대기 목록에 신청구분, 국방전자서명인증센터 상태 컬럼이 표시된다"
    - "등록대장에 엑셀 다운로드 버튼과 발급 통계 탭이 있다"
  artifacts:
    - path: "navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx"
      provides: "확장된 신청폼(7필드) + 회수 버튼"
      contains: "requestType"
    - path: "navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx"
      provides: "반려사유 모달 + 확장 컬럼"
      contains: "rejectReason"
    - path: "navy-admin/src/pages/sys04-certificate/CertificateRegisterPage.tsx"
      provides: "엑셀 다운로드 + 통계"
      contains: "exportToExcel"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys04.ts"
      provides: "확장 필드 + withdraw/reject-reason API"
      contains: "withdrawn"
  key_links:
    - from: "CertificateApplyPage.tsx"
      to: "/api/sys04/certificates/:id/withdraw"
      via: "useMutation"
      pattern: "withdraw"
    - from: "CertificateApprovalPage.tsx"
      to: "/api/sys04/certificates/:id/reject"
      via: "rejectReason body"
      pattern: "rejectReason"
---

<objective>
SYS04 인증서발급신청체계 GAP 패치 (G01-G05)

Purpose: req_analysis.txt 원본 대비 누락된 신청폼 필드, 회수 기능, 반려사유, 등록대장 엑셀/통계를 보완한다.
Output: 5개 GAP 해소된 SYS04 페이지 3개 + MSW 핸들러 + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-5/03-UI-SPEC.md

<interfaces>
<!-- CertApplication 타입 (sys04.ts MSW handler에서 export) -->
export type CertStatus = 'pending' | 'approved' | 'rejected'

export interface CertApplication {
  id: string
  applicantName: string
  applicantUnit: string
  certType: string
  purpose: string
  status: CertStatus
  appliedAt: string
  processedAt?: string
}

<!-- DataTable, CrudForm, StatusBadge 인터페이스는 shared/ui에서 import -->
<!-- CrudForm fields 패턴: { name, label, type, required, options?, placeholder? } -->
<!-- DataTable ProColumns 패턴: { title, dataIndex, width, render? } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SYS04 페이지 3개 GAP 패치 (G01-G05)</name>
  <read_first>
    - navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx
    - navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx
    - navy-admin/src/pages/sys04-certificate/CertificateRegisterPage.tsx
    - navy-admin/src/shared/api/mocks/handlers/sys04.ts
  </read_first>
  <files>
    navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx,
    navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx,
    navy-admin/src/pages/sys04-certificate/CertificateRegisterPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys04.ts
  </files>
  <action>
**G01 — CertificateApplyPage.tsx 신청폼 필드 확장:**
기존 `formFields` 배열(현재 certType, purpose 2개)에 다음 필드를 추가:
- `{ name: 'requestType', label: '신청구분', type: 'select', required: true, options: [{label:'신규발급',value:'신규발급'},{label:'재발급',value:'재발급'},{label:'갱신',value:'갱신'}] }` — certType 바로 다음에 삽입
- `{ name: 'reason', label: '사유', type: 'textarea', required: true, placeholder: '사유를 입력하세요' }` — purpose 다음에 삽입
- `{ name: 'militaryId', label: '군번', type: 'text', required: true, disabled: true, initialValue: 'M-20250001' }` — readonly 군번 (로그인 정보 자동입력 Mock)
- `{ name: 'email', label: '이메일', type: 'text', required: true, placeholder: 'example@mil.kr' }`
- `{ name: 'phone', label: '전화번호', type: 'text', required: true, placeholder: '010-0000-0000' }`

기존 `FormValues` 타입을 확장: `{ certType, requestType, purpose, reason, militaryId, email, phone }`

columns 배열에 `requestType` 컬럼 추가 (certType 바로 다음, width: 100, title: '신청구분')

**G02 — CertificateApplyPage.tsx 회수 기능:**
액션 컬럼에 "수정" 버튼 옆에 "회수" 버튼 추가 (`record.status === 'pending'`일 때만 활성):
```tsx
<Popconfirm title="회수하시겠습니까?" onConfirm={() => withdrawMutation.mutate(record.id)} okText="회수" cancelText="취소">
  <Button type="link" size="small" disabled={record.status !== 'pending'}>회수</Button>
</Popconfirm>
```
withdrawMutation 추가: `apiClient.patch(/sys04/certificates/${id}/withdraw)`
STATUS_COLOR_MAP에 `withdrawn: 'default'` 추가, STATUS_LABEL_MAP에 `withdrawn: '회수'` 추가

**G03 — CertificateApprovalPage.tsx 반려사유 입력:**
반려 버튼 클릭 시 바로 API 호출하는 대신, Modal을 열고 Input.TextArea로 반려사유(rejectReason)를 입력받는다:
- `const [rejectModalOpen, setRejectModalOpen] = useState(false)` + `const [rejectTargetId, setRejectTargetId] = useState<string | null>(null)` + `const [rejectReason, setRejectReason] = useState('')`
- 반려 버튼 onClick: `setRejectTargetId(record.id); setRejectModalOpen(true)`
- Modal 확인 시: `apiClient.patch(/sys04/certificates/${rejectTargetId}/reject, { rejectReason })`
- Modal: `<Modal title="반려사유 입력" open={rejectModalOpen} ...><Input.TextArea value={rejectReason} onChange={e => setRejectReason(e.target.value)} rows={4} placeholder="반려사유를 입력하세요" /></Modal>`

**G05 — CertificateApprovalPage.tsx 컬럼 추가:**
columns에 다음 추가:
- `{ title: '신청구분', dataIndex: 'requestType', width: 100 }` — certType 다음
- `{ title: '국방전자서명인증센터', dataIndex: 'ndscaStatus', width: 120, render: (_, record) => <StatusBadge status={record.ndscaStatus || 'pending'} colorMap={...} labelMap={...} /> }` — 신청일시 다음

**G04 — CertificateRegisterPage.tsx 엑셀/통계:**
- 현재 toolBarRender에 "저장" 버튼이 있으면 그 옆에 "엑셀 다운로드" 버튼 추가 (`onClick={() => exportToExcel()`)
- `exportToExcel` 함수: 현재 테이블 데이터를 CSV 문자열로 변환 후 Blob 다운로드 (shared/lib/csvExport 패턴 — 기존 Phase 1의 CSV 유틸 확인 후 재사용하거나 동일 패턴 구현)
- 컬럼에 `requestType` 추가 (certType 다음)
- 페이지 하단에 antd Tabs 추가: "목록" 탭(기존 DataTable) + "통계" 탭(antd Row + Col + Statistic 4개: 전체건수, 승인건수, 반려건수, 대기건수)

**MSW sys04.ts 확장:**
CertApplication 인터페이스에 추가:
- `requestType: string` (신규발급/재발급/갱신)
- `reason: string`
- `militaryId: string`
- `email: string`
- `phone: string`
- `ndscaStatus: 'pending' | 'approved' | 'rejected'`
- `rejectReason?: string`

CertStatus 타입에 `'withdrawn'` 추가

Mock 데이터 생성에 새 필드 포함:
- `requestType: faker.helpers.arrayElement(['신규발급', '재발급', '갱신'])`
- `reason: faker.lorem.sentence()`
- `militaryId: 'M-' + faker.string.numeric(8)`
- `email: faker.internet.email()`
- `phone: faker.phone.number()`
- `ndscaStatus: faker.helpers.arrayElement(['pending', 'approved', 'rejected'])`

새 핸들러 추가:
- `http.patch('/api/sys04/certificates/:id/withdraw', ...)` — status를 'withdrawn'으로 변경
- 기존 reject 핸들러 수정: body에서 `rejectReason`을 읽어 저장

POST 핸들러 수정: body에서 requestType, reason, militaryId, email, phone 읽어서 저장
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys04/certificate.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CertificateApplyPage.tsx에 'requestType', 'reason', 'militaryId', 'email', 'phone' 문자열이 모두 존재
    - CertificateApplyPage.tsx에 'withdraw' 문자열이 존재
    - CertificateApprovalPage.tsx에 'rejectReason' 및 'rejectModalOpen' 문자열이 존재
    - CertificateApprovalPage.tsx에 'ndscaStatus' 문자열이 존재
    - CertificateRegisterPage.tsx에 'exportToExcel' 또는 'csv' 문자열이 존재
    - CertificateRegisterPage.tsx에 'Statistic' 또는 '통계' 문자열이 존재
    - sys04.ts MSW 핸들러에 'withdrawn', 'rejectReason', 'requestType', 'ndscaStatus' 문자열이 모두 존재
    - 빌드 에러 없음: npx tsc --noEmit 통과
  </acceptance_criteria>
  <done>G01-G05 모든 GAP 해소. 신청폼 7필드, 회수 기능, 반려사유 모달, 승인목록 확장 컬럼, 등록대장 엑셀/통계가 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: SYS04 테스트 업데이트</name>
  <read_first>
    - navy-admin/src/__tests__/sys04/certificate.test.ts
    - navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx
    - navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx
    - navy-admin/src/pages/sys04-certificate/CertificateRegisterPage.tsx
  </read_first>
  <files>navy-admin/src/__tests__/sys04/certificate.test.ts</files>
  <action>
기존 readFileSync 기반 테스트 패턴을 따라 GAP 패치 검증 테스트를 추가한다.

기존 describe 블록들 뒤에 새 describe 블록들 추가:

```typescript
describe('CertificateApplyPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'CertificateApplyPage.tsx'), 'utf-8')

  it('G01: 신청구분 필드가 존재한다', () => {
    expect(content).toContain('requestType')
    expect(content).toContain('신청구분')
  })

  it('G01: 사유 필드가 존재한다', () => {
    expect(content).toContain("name: 'reason'")
  })

  it('G01: 군번 필드가 존재한다', () => {
    expect(content).toContain('militaryId')
  })

  it('G01: 이메일 필드가 존재한다', () => {
    expect(content).toContain("name: 'email'")
  })

  it('G01: 전화번호 필드가 존재한다', () => {
    expect(content).toContain("name: 'phone'")
  })

  it('G02: 회수 기능이 존재한다', () => {
    expect(content).toContain('withdraw')
    expect(content).toContain('회수')
  })
})

describe('CertificateApprovalPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'CertificateApprovalPage.tsx'), 'utf-8')

  it('G03: 반려사유 입력 모달이 존재한다', () => {
    expect(content).toContain('rejectReason')
    expect(content).toContain('반려사유')
  })

  it('G05: 신청구분 컬럼이 존재한다', () => {
    expect(content).toContain('requestType')
  })

  it('G05: 국방전자서명인증센터 컬럼이 존재한다', () => {
    expect(content).toContain('ndscaStatus')
  })
})

describe('CertificateRegisterPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'CertificateRegisterPage.tsx'), 'utf-8')

  it('G04: 엑셀 다운로드 기능이 존재한다', () => {
    expect(content).toMatch(/excel|csv|엑셀/i)
  })

  it('G04: 발급 통계가 존재한다', () => {
    expect(content).toMatch(/Statistic|통계/)
  })

  it('G04: 신청구분 컬럼이 존재한다', () => {
    expect(content).toContain('requestType')
  })
})

describe('sys04 MSW 핸들러 GAP 패치', () => {
  const content = readFileSync(resolve(HANDLERS_BASE, 'sys04.ts'), 'utf-8')

  it('withdrawn 상태가 정의되어 있다', () => {
    expect(content).toContain('withdrawn')
  })

  it('rejectReason 필드가 존재한다', () => {
    expect(content).toContain('rejectReason')
  })

  it('ndscaStatus 필드가 존재한다', () => {
    expect(content).toContain('ndscaStatus')
  })

  it('withdraw 핸들러가 존재한다', () => {
    expect(content).toContain('/withdraw')
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys04/certificate.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 모든 GAP 패치 테스트(G01~G05)가 PASS
    - 기존 테스트도 여전히 PASS
    - 테스트 파일에 'GAP 패치' describe 블록이 4개 존재
  </acceptance_criteria>
  <done>SYS04 GAP 패치 테스트 전체 통과. G01-G05 검증 완료.</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` — 타입 에러 없음
2. `cd navy-admin && npx vitest run src/__tests__/sys04/ --reporter=verbose` — 전체 통과
3. grep으로 각 GAP 키워드 확인:
   - `grep -c 'requestType' navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx` >= 2
   - `grep -c 'withdraw' navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx` >= 1
   - `grep -c 'rejectReason' navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx` >= 2
   - `grep -c 'ndscaStatus' navy-admin/src/shared/api/mocks/handlers/sys04.ts` >= 1
</verification>

<success_criteria>
- G01: 신청폼에 requestType, reason, militaryId, email, phone 5개 필드 추가됨
- G02: pending 상태 건에 회수 버튼 있고 withdrawn 상태로 변경됨
- G03: 반려 시 반송의견 입력 모달이 표시됨
- G04: 등록대장에 엑셀 다운로드 버튼 + 통계 탭 존재
- G05: 승인대기 목록에 신청구분 + 국방전자서명인증센터 컬럼 표시
- 빌드 에러 없음, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-06-SUMMARY.md`
</output>
