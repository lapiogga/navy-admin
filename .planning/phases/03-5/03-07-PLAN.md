---
phase: 03-5
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx
  - navy-admin/src/pages/sys05-admin-rules/PrecedentHQPage.tsx
  - navy-admin/src/pages/sys05-admin-rules/PrecedentUnitPage.tsx
  - navy-admin/src/pages/sys05-admin-rules/DirectiveListPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys05.ts
  - navy-admin/src/__tests__/sys05/admin-rules.test.ts
autonomous: true
gap_closure: true
requirements:
  - AREG-01
  - AREG-02
  - AREG-03
  - AREG-04

must_haves:
  truths:
    - "현행규정을 등록/수정/삭제할 수 있다 (등록 폼: 규정명, 문서번호, 분류, 소관부서, 시행일, 내용, 첨부파일)"
    - "해군본부 예규를 등록/수정/삭제할 수 있다"
    - "지시문서를 등록/수정/삭제할 수 있다"
    - "현행규정 목록에 분류 컬럼이 표시된다"
    - "조직도(부/실/단) 트리 또는 부대별 카드 레이아웃이 표시된다"
  artifacts:
    - path: "navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx"
      provides: "규정 CRUD + 분류 컬럼 + 조직도 사이드바"
      contains: "CrudForm"
    - path: "navy-admin/src/pages/sys05-admin-rules/PrecedentHQPage.tsx"
      provides: "예규 CRUD"
      contains: "CrudForm"
    - path: "navy-admin/src/pages/sys05-admin-rules/PrecedentUnitPage.tsx"
      provides: "부대별 카드 그리드"
      contains: "Card"
    - path: "navy-admin/src/pages/sys05-admin-rules/DirectiveListPage.tsx"
      provides: "지시문서 CRUD"
      contains: "CrudForm"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys05.ts"
      provides: "CRUD 핸들러 (POST/PUT/DELETE)"
      contains: "http.post"
  key_links:
    - from: "RegulationListPage.tsx"
      to: "/api/sys05/regulations"
      via: "POST/PUT/DELETE fetch"
      pattern: "method.*POST|PUT|DELETE"
    - from: "PrecedentHQPage.tsx"
      to: "/api/sys05/precedents-hq"
      via: "CRUD mutations"
      pattern: "method.*POST"
---

<objective>
SYS05 행정규칙포탈체계 GAP 패치 (G06-G10)

Purpose: 현재 조회/상세보기만 가능한 3개 페이지(현행규정/예규/지시문서)에 등록/수정/삭제 CRUD를 추가하고, 조직도 전시와 예하부대 카드 레이아웃을 구현한다.
Output: 4개 페이지 CRUD 확장 + MSW 핸들러 + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-5/03-UI-SPEC.md

<interfaces>
<!-- RegulationListPage 현재 구조: DataTable + 상세 Modal (조회만) -->
<!-- PrecedentHQPage 현재 구조: DataTable + 상세 Modal (조회만) -->
<!-- DirectiveListPage 현재 구조: DataTable + 상세 Modal (조회만) -->
<!-- PrecedentUnitPage 현재 구조: DataTable (해군본부 예규와 동일) -->
<!-- CrudForm 패턴: fields 배열로 정의, onFinish 콜백, mode='create'|'edit' -->
<!-- sys05 MSW: regulations, precedents-hq, precedents-unit, directives 각 GET 핸들러만 존재 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SYS05 페이지 4개 CRUD 추가 + 조직도/카드 (G06-G10)</name>
  <read_first>
    - navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx
    - navy-admin/src/pages/sys05-admin-rules/PrecedentHQPage.tsx
    - navy-admin/src/pages/sys05-admin-rules/PrecedentUnitPage.tsx
    - navy-admin/src/pages/sys05-admin-rules/DirectiveListPage.tsx
    - navy-admin/src/shared/api/mocks/handlers/sys05.ts
  </read_first>
  <files>
    navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx,
    navy-admin/src/pages/sys05-admin-rules/PrecedentHQPage.tsx,
    navy-admin/src/pages/sys05-admin-rules/PrecedentUnitPage.tsx,
    navy-admin/src/pages/sys05-admin-rules/DirectiveListPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys05.ts
  </files>
  <action>
**G06 — RegulationListPage.tsx 현행규정 CRUD 추가:**
현재 DataTable + 상세 Modal(조회만)에서 CRUD 기능을 추가:

1. 분류(`category`) 컬럼 추가: `{ title: '분류', dataIndex: 'category', width: 100 }` — "규정명" 다음에 삽입
2. "담당부서" 컬럼명을 "소관부서"로 변경
3. CrudForm 필드 정의 추가:
```typescript
const REGULATION_FIELDS = [
  { name: 'title', label: '규정명', type: 'text' as const, required: true },
  { name: 'docNumber', label: '문서번호', type: 'text' as const, required: true },
  { name: 'category', label: '분류', type: 'select' as const, required: true, options: [{label:'법령',value:'법령'},{label:'훈령',value:'훈령'},{label:'예규',value:'예규'},{label:'고시',value:'고시'}] },
  { name: 'department', label: '소관부서', type: 'select' as const, required: true, options: [{label:'기획부',value:'기획부'},{label:'인사부',value:'인사부'},{label:'작전부',value:'작전부'},{label:'군수부',value:'군수부'},{label:'정보부',value:'정보부'}] },
  { name: 'effectiveDate', label: '시행일', type: 'date' as const, required: true },
  { name: 'content', label: '내용', type: 'textarea' as const, required: true },
]
```
4. 등록/수정 Modal 추가 (기존 상세 Modal 패턴 참고):
   - `const [crudOpen, setCrudOpen] = useState(false)` + `const [editTarget, setEditTarget] = useState<Regulation | null>(null)`
   - 등록 버튼을 toolBarRender에 추가: `<Button type="primary" onClick={() => { setEditTarget(null); setCrudOpen(true) }}>규정 등록</Button>`
   - 목록 행의 수정/삭제 액션 컬럼 추가 (DataTable columns 끝에)
   - createMutation: `fetch('/api/sys05/regulations', { method: 'POST', ... })`
   - updateMutation: `fetch('/api/sys05/regulations/${id}', { method: 'PUT', ... })`
   - deleteMutation: `fetch('/api/sys05/regulations/${id}', { method: 'DELETE' })`
5. 조직도(G09): DataTable 좌측에 antd Tree 사이드바 추가:
```tsx
// 레이아웃을 Row + Col로 감싸기
<Row gutter={16}>
  <Col span={6}>
    <Card title="조직도" size="small">
      <Tree treeData={ORG_TREE_DATA} onSelect={handleOrgSelect} defaultExpandAll />
    </Card>
  </Col>
  <Col span={18}>
    {/* 기존 DataTable */}
  </Col>
</Row>
```
ORG_TREE_DATA는 부/실/단 구조:
```typescript
const ORG_TREE_DATA = [
  { title: '해병대사령부', key: 'hq', children: [
    { title: '기획부', key: '기획부' },
    { title: '인사부', key: '인사부' },
    { title: '작전부', key: '작전부' },
    { title: '군수부', key: '군수부' },
    { title: '정보부', key: '정보부' },
  ]},
]
```
조직도에서 부서 선택 시 해당 부서로 필터링 (fetch URL에 department 파라미터 추가)

**G07 — PrecedentHQPage.tsx 예규 CRUD 추가:**
현재 조회만 가능한 페이지에 CRUD 추가. RegulationListPage와 동일한 패턴:
1. CrudForm 필드: `docNumber, title, department, effectiveDate, content`
2. 등록 버튼 + 등록/수정 Modal
3. 행 액션에 수정/삭제 버튼 추가
4. create/update/delete mutations — URL: `/api/sys05/precedents-hq`

**G08 — DirectiveListPage.tsx 지시문서 CRUD 추가:**
동일 패턴:
1. CrudForm 필드: `docNumber, title, department(발령부서), effectiveDate(발령일), content`
2. 등록 버튼 + CRUD
3. mutations URL: `/api/sys05/directives`

**G10 — PrecedentUnitPage.tsx 카드 레이아웃 변환:**
현재 DataTable 구조를 Card 그리드로 변경:
```tsx
const UNIT_LINKS = [
  { name: '1사단', url: '#', description: '1사단 예규 페이지' },
  { name: '2사단', url: '#', description: '2사단 예규 페이지' },
  { name: '교육훈련단', url: '#', description: '교육훈련단 예규 페이지' },
  { name: '상륙기동단', url: '#', description: '상륙기동단 예규 페이지' },
  { name: '항공단', url: '#', description: '항공단 예규 페이지' },
]
// Row + Col(span=6) + Card 패턴으로 부대별 링크 카드
```

**MSW sys05.ts 확장:**
기존 GET 핸들러에 추가:
- regulations: POST, PUT(/:id), DELETE(/:id) + GET에 department 쿼리 필터
- precedents-hq: POST, PUT(/:id), DELETE(/:id)
- directives: POST, PUT(/:id), DELETE(/:id)

POST 핸들러는 body에서 필드를 읽어 새 항목 생성, PUT은 기존 항목 업데이트, DELETE는 배열에서 제거.
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys05/admin-rules.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RegulationListPage.tsx에 'CrudForm', 'category', 'Tree', 'ORG_TREE' 또는 'treeData' 문자열 존재
    - RegulationListPage.tsx에 'POST' 또는 'method' + 'create' 패턴 존재
    - PrecedentHQPage.tsx에 'CrudForm' 문자열 존재
    - DirectiveListPage.tsx에 'CrudForm' 문자열 존재
    - PrecedentUnitPage.tsx에 'Card' 문자열 존재, 'DataTable' 문자열 미존재 또는 최소화
    - sys05.ts MSW에 'http.post', 'http.put', 'http.delete' 문자열 존재
    - npx tsc --noEmit 통과
  </acceptance_criteria>
  <done>G06-G10 모든 GAP 해소. 규정/예규/지시문서 CRUD 동작, 조직도 트리, 예하부대 카드 레이아웃 완성.</done>
</task>

<task type="auto">
  <name>Task 2: SYS05 테스트 업데이트</name>
  <read_first>
    - navy-admin/src/__tests__/sys05/admin-rules.test.ts
    - navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx
    - navy-admin/src/pages/sys05-admin-rules/PrecedentHQPage.tsx
    - navy-admin/src/pages/sys05-admin-rules/DirectiveListPage.tsx
    - navy-admin/src/pages/sys05-admin-rules/PrecedentUnitPage.tsx
  </read_first>
  <files>navy-admin/src/__tests__/sys05/admin-rules.test.ts</files>
  <action>
기존 readFileSync 테스트 패턴에 GAP 패치 검증 추가:

```typescript
describe('RegulationListPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'RegulationListPage.tsx'), 'utf-8')

  it('G06: CrudForm으로 등록/수정 기능이 존재한다', () => {
    expect(content).toContain('CrudForm')
  })

  it('G06: 분류 컬럼이 존재한다', () => {
    expect(content).toContain('category')
  })

  it('G06: 등록 버튼이 존재한다', () => {
    expect(content).toMatch(/규정 등록|등록/)
  })

  it('G09: 조직도 트리가 존재한다', () => {
    expect(content).toContain('Tree')
  })
})

describe('PrecedentHQPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'PrecedentHQPage.tsx'), 'utf-8')

  it('G07: CrudForm으로 등록/수정 기능이 존재한다', () => {
    expect(content).toContain('CrudForm')
  })

  it('G07: 등록 버튼이 존재한다', () => {
    expect(content).toMatch(/예규 등록|등록/)
  })
})

describe('DirectiveListPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'DirectiveListPage.tsx'), 'utf-8')

  it('G08: CrudForm으로 등록/수정 기능이 존재한다', () => {
    expect(content).toContain('CrudForm')
  })
})

describe('PrecedentUnitPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'PrecedentUnitPage.tsx'), 'utf-8')

  it('G10: Card 레이아웃으로 변환되었다', () => {
    expect(content).toContain('Card')
  })

  it('G10: 부대 링크가 존재한다', () => {
    expect(content).toMatch(/1사단|2사단|교육훈련단/)
  })
})

describe('sys05 MSW 핸들러 GAP 패치', () => {
  const content = readFileSync(resolve(HANDLERS_BASE, 'sys05.ts'), 'utf-8')

  it('POST 핸들러가 존재한다', () => {
    expect(content).toContain('http.post')
  })

  it('PUT 핸들러가 존재한다', () => {
    expect(content).toContain('http.put')
  })

  it('DELETE 핸들러가 존재한다', () => {
    expect(content).toContain('http.delete')
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys05/admin-rules.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 모든 GAP 패치 테스트(G06~G10)가 PASS
    - 기존 테스트도 여전히 PASS
  </acceptance_criteria>
  <done>SYS05 GAP 패치 테스트 전체 통과. G06-G10 검증 완료.</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` — 타입 에러 없음
2. `cd navy-admin && npx vitest run src/__tests__/sys05/ --reporter=verbose` — 전체 통과
3. grep 확인: `grep -c 'CrudForm' navy-admin/src/pages/sys05-admin-rules/RegulationListPage.tsx` >= 1
4. grep 확인: `grep -c 'http.post' navy-admin/src/shared/api/mocks/handlers/sys05.ts` >= 1
</verification>

<success_criteria>
- G06: 현행규정 등록/수정/삭제 CRUD + 분류 컬럼 추가
- G07: 해군본부 예규 등록/수정/삭제 CRUD 추가
- G08: 지시문서 등록/수정/삭제 CRUD 추가
- G09: 현행규정에 조직도(부/실/단 트리) 사이드바 표시
- G10: 예하부대 예규가 부대별 카드 그리드로 전시
- 빌드 에러 없음, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-07-SUMMARY.md`
</output>
