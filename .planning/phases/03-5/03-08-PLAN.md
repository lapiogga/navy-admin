---
phase: 03-5
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx
  - navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx
  - navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx
  - navy-admin/src/pages/sys14-suggestion/index.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys14.ts
  - navy-admin/src/__tests__/sys14/suggestion.test.ts
autonomous: true
gap_closure: true
requirements:
  - SUGST-01
  - SUGST-02
  - SUGST-04

must_haves:
  truths:
    - "제언 목록에 담당부서, 조치일, 조치유형 컬럼이 표시된다"
    - "순번이 연도별 형식(2026-1, 2026-2)으로 표시된다"
    - "진행상태가 4단계(등록/접수/진행/완료)로 운영되고, 조치유형 선택 시 조치일 DatePicker가 연동된다"
    - "반려 시 반려사유 입력 모달이 표시된다"
    - "상세 화면에 직책, 전화번호, 첨부파일이 표시된다"
    - "등록폼에 첨부파일 Upload가 있고 로그인 정보가 자동 표시된다"
    - "제언 상세에서 댓글 CRUD가 동작한다 (목록/등록/수정/삭제)"
    - "관리자 서식관리 CRUD가 동작한다"
    - "엑셀 출력 버튼이 있다"
    - "메인화면에 공지사항 최신 5건이 표시된다"
  artifacts:
    - path: "navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx"
      provides: "확장 컬럼 + 4단계 상태 + 댓글 + 반려사유 + 엑셀"
      contains: "assignedDept"
    - path: "navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx"
      provides: "공지사항 5건 영역"
      contains: "공지사항"
    - path: "navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx"
      provides: "서식관리 탭"
      contains: "서식관리"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys14.ts"
      provides: "확장 필드 + 댓글/서식 API"
      contains: "comments"
  key_links:
    - from: "SuggestionListPage.tsx"
      to: "/api/sys14/suggestions/:id/comments"
      via: "fetch CRUD"
      pattern: "comments"
    - from: "SuggestionAdminPage.tsx"
      to: "/api/sys14/templates"
      via: "fetch CRUD"
      pattern: "templates"
---

<objective>
SYS14 나의제언 GAP 패치 (G17-G27)

Purpose: 11개 GAP 중 가장 많은 누락이 있는 SYS14를 패치한다. 목록 컬럼 보강, 상태 체계 변경, 댓글 CRUD 신규 구현, 서식관리 신규 구현, 엑셀 출력 등 포괄적 보완.
Output: 3개 페이지 확장 + 댓글/서식 신규 기능 + MSW + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-5/03-UI-SPEC.md

<interfaces>
<!-- SuggestionListPage 현재 구조 -->
interface Suggestion {
  id: string; title: string; content: string;
  authorName: string; authorUnit: string;
  status: 'open' | 'answered' | 'closed';
  isPrivate: boolean; recommendCount: number; reportCount: number;
  answer?: string; answeredAt?: string; answeredBy?: string;
  createdAt: string; updatedAt: string;
}
<!-- 상태 변경 필요: 'open'|'answered'|'closed' -> 'registered'|'received'|'processing'|'completed'|'rejected' -->
<!-- CRUD_FIELDS: 현재 title, content만 있음 -->
<!-- SuggestionAdminPage: 현재 관리자 답변 기능만 있음, 서식관리 없음 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SuggestionListPage 컬럼/상태/폼/댓글/엑셀 패치 (G17-G24, G26)</name>
  <read_first>
    - navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx
    - navy-admin/src/shared/api/mocks/handlers/sys14.ts
  </read_first>
  <files>
    navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys14.ts
  </files>
  <action>
**G19 — 진행상태 체계 변경 (가장 먼저 — 다른 GAP에 영향):**
Suggestion 인터페이스의 status 타입 변경:
```typescript
status: 'registered' | 'received' | 'processing' | 'completed' | 'rejected'
```
STATUS_COLOR_MAP 변경:
```typescript
const STATUS_COLOR_MAP = { registered: 'blue', received: 'cyan', processing: 'orange', completed: 'green', rejected: 'red' }
const STATUS_LABEL_MAP = { registered: '등록', received: '접수', processing: '진행', completed: '완료', rejected: '반려' }
```

**G17 — 목록 컬럼 추가:**
기존 columns 배열에 다음 추가 ('작성일' 컬럼 다음에):
```typescript
{ title: '진행상태', dataIndex: 'status', width: 100, render: (_, record) => <StatusBadge status={record.status} colorMap={STATUS_COLOR_MAP} labelMap={STATUS_LABEL_MAP} /> },
{ title: '담당부서', dataIndex: 'assignedDept', width: 100 },
{ title: '조치일', dataIndex: 'actionDate', width: 110 },
{ title: '조치유형', dataIndex: 'actionType', width: 100 },
```
기존 '상태' 컬럼(status)은 위의 '진행상태'로 대체. 기존 '소속', '추천' 컬럼은 유지.

**G18 — 순번 연도별 형식:**
기존 '번호' 컬럼을 변경:
```typescript
{
  title: '순번', width: 100,
  render: (_, record, index) => {
    const year = record.createdAt?.substring(0, 4) || new Date().getFullYear()
    return `${year}-${index + 1}`
  },
},
```

**G22 — 상세 모달 필드 추가:**
Descriptions에 다음 추가 ('소속' 다음에):
```tsx
<Descriptions.Item label="직책">{detail.authorPosition || '-'}</Descriptions.Item>
<Descriptions.Item label="전화번호">{detail.authorPhone || '-'}</Descriptions.Item>
```
'내용' 다음에:
```tsx
<Descriptions.Item label="첨부파일" span={2}>
  {detail.attachments?.length ? detail.attachments.map((f: {name:string}, i:number) => <div key={i}>{f.name}</div>) : '-'}
</Descriptions.Item>
```

**G23 — 등록폼 확장:**
CRUD_FIELDS를 확장:
```typescript
const CRUD_FIELDS = [
  { name: 'authorUnit', label: '소속', type: 'text' as const, disabled: true },
  { name: 'authorName', label: '제언자', type: 'text' as const, disabled: true },
  { name: 'authorPosition', label: '직책', type: 'text' as const, disabled: true },
  { name: 'authorPhone', label: '전화번호', type: 'text' as const, disabled: true },
  { name: 'title', label: '제목', type: 'text' as const, required: true },
  { name: 'content', label: '내용', type: 'textarea' as const, required: true },
]
```
CrudForm initialValues에 로그인 Mock 정보 추가: `{ authorUnit: '해병대사령부', authorName: '홍길동', authorPosition: '대위', authorPhone: '010-1234-5678' }`

등록폼 하단에 antd Upload 컴포넌트 추가 (CrudForm 외부, Modal 내):
```tsx
<Upload listType="text" beforeUpload={() => false} maxCount={3}>
  <Button icon={<UploadOutlined />}>첨부파일 선택</Button>
</Upload>
```

**G20 — 상태 변경 확장 (상세 모달 내 관리자 영역):**
상세 모달 하단(답변 영역 옆)에 상태 변경 패널 추가:
```tsx
<Divider />
<Space direction="vertical" style={{width:'100%'}}>
  <Text strong>상태 관리</Text>
  <Space>
    <Select value={newStatus} onChange={setNewStatus} style={{width:120}} options={[
      {label:'등록',value:'registered'},{label:'접수',value:'received'},
      {label:'진행',value:'processing'},{label:'완료',value:'completed'},
    ]} />
    {/* G20: 조치유형 + 조치일 연동 */}
    <Select value={actionType} onChange={setActionType} placeholder="조치유형" style={{width:120}} options={[
      {label:'시정',value:'시정'},{label:'개선',value:'개선'},{label:'검토완료',value:'검토완료'},{label:'반려',value:'반려'},
    ]} />
    {actionType && <DatePicker value={actionDateVal} onChange={setActionDateVal} placeholder="조치일" />}
    <Button type="primary" onClick={handleStatusChange}>변경</Button>
  </Space>
</Space>
```

**G21 — 반려사유 입력:**
조치유형에서 '반려' 선택 시 Input.TextArea 표시:
```tsx
{actionType === '반려' && (
  <Input.TextArea value={rejectReason} onChange={e => setRejectReason(e.target.value)} rows={3} placeholder="반려사유를 입력하세요" />
)}
```
상태 변경 API 호출 시 rejectReason을 body에 포함.

**G24 — 댓글 CRUD:**
상세 모달 내 하단에 댓글 섹션 추가:
```tsx
<Divider />
<Title level={5}>댓글</Title>
{/* 댓글 목록 */}
<List
  dataSource={comments}
  renderItem={(item) => (
    <List.Item actions={[
      item.authorName === '홍길동' && <Button type="link" size="small" onClick={() => handleEditComment(item)}>수정</Button>,
      item.authorName === '홍길동' && <Popconfirm title="삭제하시겠습니까?" onConfirm={() => deleteCommentMutation.mutate(item.id)}><Button type="link" size="small" danger>삭제</Button></Popconfirm>,
    ].filter(Boolean)}>
      <List.Item.Meta
        title={<><Text strong>{item.authorName}</Text> <Text type="secondary">{item.createdAt}</Text></>}
        description={item.content}
      />
    </List.Item>
  )}
/>
{/* 댓글 작성 */}
<Space.Compact style={{width:'100%', marginTop:8}}>
  <Input.TextArea value={commentText} onChange={e => setCommentText(e.target.value)} rows={2} placeholder="댓글을 입력하세요" style={{flex:1}} />
  <Button type="primary" onClick={handleAddComment}>등록</Button>
</Space.Compact>
```

댓글 관련 state/mutation:
- `const [comments, setComments] = useState([])` — detail이 열릴 때 fetch
- `useQuery({ queryKey: ['sys14','comments', selectedId], queryFn: () => fetch(/api/sys14/suggestions/${selectedId}/comments).then(...), enabled: !!selectedId && detailOpen })`
- createCommentMutation: POST `/api/sys14/suggestions/${selectedId}/comments`
- updateCommentMutation: PUT `/api/sys14/suggestions/${selectedId}/comments/${commentId}`
- deleteCommentMutation: DELETE `/api/sys14/suggestions/${selectedId}/comments/${commentId}`

**G26 — 엑셀 출력:**
toolBarRender에 "엑셀 출력" 버튼 추가:
```tsx
<Button key="excel" onClick={handleExcelExport}>엑셀 출력</Button>
```
handleExcelExport: 현재 테이블 데이터를 CSV로 변환 (제목, 내용, 제언자, 소속, 작성일, 진행상태, 담당부서, 조치일, 조치유형 컬럼) → Blob 다운로드

**MSW sys14.ts 확장:**
Suggestion 인터페이스에 추가:
- `assignedDept: string` (담당부서)
- `actionDate?: string` (조치일)
- `actionType?: string` (조치유형)
- `authorPosition?: string` (직책)
- `authorPhone?: string` (전화번호)
- `attachments?: {name:string, url:string}[]`
- `rejectReason?: string`

상태 타입 변경: `'registered' | 'received' | 'processing' | 'completed' | 'rejected'`

Mock 데이터에 새 필드 포함:
- `assignedDept: faker.helpers.arrayElement(['기획부','인사부','작전부'])`
- `actionDate: 조건부 생성`
- `actionType: faker.helpers.arrayElement(['시정','개선','검토완료'])`
- `authorPosition: faker.helpers.arrayElement(['대위','소령','중령'])`
- `authorPhone: faker.phone.number()`
- `attachments: []`

댓글 API 핸들러 추가:
```typescript
// 댓글 타입
interface SuggestionComment { id: string; suggestionId: string; authorName: string; content: string; createdAt: string; }
let commentStore: SuggestionComment[] = []

// GET /api/sys14/suggestions/:id/comments
// POST /api/sys14/suggestions/:id/comments
// PUT /api/sys14/suggestions/:id/comments/:commentId
// DELETE /api/sys14/suggestions/:id/comments/:commentId
```

상태 변경 API 수정: PATCH `/api/sys14/suggestions/:id/status` body에 `{ status, actionType, actionDate, rejectReason }` 지원
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys14/suggestion.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SuggestionListPage.tsx에 'assignedDept', 'actionDate', 'actionType' 문자열 존재
    - SuggestionListPage.tsx에 'registered', 'received', 'processing', 'completed' 문자열 존재
    - SuggestionListPage.tsx에 'comments' 문자열 존재
    - SuggestionListPage.tsx에 'rejectReason' 문자열 존재
    - SuggestionListPage.tsx에 'authorPosition', 'authorPhone' 문자열 존재
    - SuggestionListPage.tsx에 'Upload' 또는 'UploadOutlined' 문자열 존재
    - SuggestionListPage.tsx에 'excel' 또는 'csv' 또는 '엑셀' 문자열 존재 (대소문자 무관)
    - sys14.ts MSW에 'comments', 'assignedDept', 'actionType', 'rejectReason' 존재
    - npx tsc --noEmit 통과
  </acceptance_criteria>
  <done>G17-G24, G26 해소. 목록 컬럼, 상태 체계, 폼, 댓글, 엑셀 모두 동작.</done>
</task>

<task type="auto">
  <name>Task 2: SuggestionMainPage + SuggestionAdminPage + 라우트 패치 (G25, G27)</name>
  <read_first>
    - navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx
    - navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx
    - navy-admin/src/pages/sys14-suggestion/index.tsx
  </read_first>
  <files>
    navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx,
    navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx,
    navy-admin/src/pages/sys14-suggestion/index.tsx
  </files>
  <action>
**G27 — SuggestionMainPage.tsx 공지사항 5건 추가:**
현재 메인화면에 통계 카드 + 최신 제언 5건만 표시됨. 공지사항 영역 추가:

기존 레이아웃을 Row+Col로 확장 (현재 구조 파악 후):
```tsx
<Row gutter={[16, 16]}>
  {/* 기존 통계 카드 영역 */}
  <Col span={24}>{/* 기존 Row+Col Statistic 카드 */}</Col>

  {/* 공지사항 + 최신제언 양쪽 배치 */}
  <Col span={12}>
    <Card title="공지사항" extra={<Button type="link" onClick={() => navigate('/sys14/1/2')}>전체보기</Button>} size="small">
      <List
        dataSource={notices}
        renderItem={(item) => (
          <List.Item><Text ellipsis>{item.title}</Text> <Text type="secondary">{item.createdAt}</Text></List.Item>
        )}
        locale={{ emptyText: '공지사항이 없습니다' }}
      />
    </Card>
  </Col>
  <Col span={12}>
    <Card title="최신 제언" extra={<Button type="link" onClick={() => navigate('/sys14/1/3')}>전체보기</Button>} size="small">
      {/* 기존 최신 제언 목록 이동 */}
    </Card>
  </Col>
</Row>
```

공지사항 데이터: useQuery로 `/api/sys14/notices?size=5` 또는 기존 게시판 API 활용.
MSW에 간단한 notices Mock 핸들러 추가 (5건):
```typescript
http.get('/api/sys14/notices', () => {
  const notices = Array.from({length:5}, (_,i) => ({
    id: `notice-${i+1}`, title: faker.lorem.sentence(), createdAt: faker.date.recent({days:30}).toISOString().split('T')[0]
  }))
  return HttpResponse.json({ success: true, data: notices })
})
```

**G25 — SuggestionAdminPage.tsx 서식관리 추가:**
현재 SuggestionAdminPage에 관리자 답변 기능만 있을 경우, Tabs로 감싸서 "제언관리" + "서식관리" 탭 추가:

```tsx
<PageContainer title="관리자">
  <Tabs items={[
    { key: 'manage', label: '제언관리', children: /* 기존 관리자 컨텐츠 */ },
    { key: 'templates', label: '서식관리', children: <TemplateManager /> },
  ]} />
</PageContainer>
```

TemplateManager 컴포넌트 (같은 파일 내 또는 인라인):
```tsx
function TemplateManager() {
  // 서식 목록 + CRUD
  const [templates, setTemplates] = useState([])
  const [formOpen, setFormOpen] = useState(false)
  const [editTarget, setEditTarget] = useState(null)

  // useQuery: GET /api/sys14/templates
  // createMutation: POST /api/sys14/templates
  // updateMutation: PUT /api/sys14/templates/:id
  // deleteMutation: DELETE /api/sys14/templates/:id

  return (
    <>
      <DataTable columns={templateColumns} request={fetchTemplates} ... toolBarRender={() => [<Button type="primary" onClick={() => setFormOpen(true)}>서식 등록</Button>]} />
      <Modal ...><CrudForm fields={[
        { name: 'name', label: '서식명', type: 'text', required: true },
        { name: 'content', label: '서식 내용', type: 'textarea', required: true },
      ]} ... /></Modal>
    </>
  )
}
```

MSW에 서식 API 핸들러 추가:
```typescript
interface Template { id: string; name: string; content: string; createdAt: string; }
let templates: Template[] = [
  { id: 'tmpl-1', name: '기본 서식', content: '1. 제언 배경\n2. 제언 내용\n3. 기대 효과', createdAt: '2026-01-01' },
  { id: 'tmpl-2', name: '개선 제안 서식', content: '1. 현황\n2. 문제점\n3. 개선 방안\n4. 기대 효과', createdAt: '2026-02-01' },
]
// GET /api/sys14/templates (PageResponse)
// POST /api/sys14/templates
// PUT /api/sys14/templates/:id
// DELETE /api/sys14/templates/:id
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys14/suggestion.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SuggestionMainPage.tsx에 '공지사항' 문자열 존재
    - SuggestionMainPage.tsx에 'notices' 문자열 존재
    - SuggestionAdminPage.tsx에 '서식관리' 문자열 존재
    - SuggestionAdminPage.tsx에 'templates' 또는 'Template' 문자열 존재
    - sys14.ts MSW에 'templates' 핸들러 존재
    - npx tsc --noEmit 통과
  </acceptance_criteria>
  <done>G25, G27 해소. 메인 공지사항 표시, 서식관리 CRUD 동작.</done>
</task>

<task type="auto">
  <name>Task 3: SYS14 테스트 업데이트</name>
  <read_first>
    - navy-admin/src/__tests__/sys14/suggestion.test.ts
    - navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx
    - navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx
    - navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx
  </read_first>
  <files>navy-admin/src/__tests__/sys14/suggestion.test.ts</files>
  <action>
기존 readFileSync 테스트 패턴에 GAP 패치 검증 추가:

```typescript
describe('SuggestionListPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'SuggestionListPage.tsx'), 'utf-8')

  it('G17: 담당부서 컬럼이 존재한다', () => {
    expect(content).toContain('assignedDept')
  })

  it('G17: 조치일 컬럼이 존재한다', () => {
    expect(content).toContain('actionDate')
  })

  it('G17: 조치유형 컬럼이 존재한다', () => {
    expect(content).toContain('actionType')
  })

  it('G18: 연도별 순번 형식이 적용되었다', () => {
    expect(content).toMatch(/year.*index|getFullYear/)
  })

  it('G19: 4단계 진행상태가 정의되어 있다', () => {
    expect(content).toContain('registered')
    expect(content).toContain('received')
    expect(content).toContain('processing')
    expect(content).toContain('completed')
  })

  it('G20: 조치유형 Select가 존재한다', () => {
    expect(content).toMatch(/조치유형|actionType/)
  })

  it('G21: 반려사유 입력이 존재한다', () => {
    expect(content).toContain('rejectReason')
  })

  it('G22: 직책 필드가 존재한다', () => {
    expect(content).toContain('authorPosition')
  })

  it('G22: 전화번호 필드가 존재한다', () => {
    expect(content).toContain('authorPhone')
  })

  it('G23: Upload 컴포넌트가 존재한다', () => {
    expect(content).toContain('Upload')
  })

  it('G24: 댓글 기능이 존재한다', () => {
    expect(content).toContain('comments')
    expect(content).toContain('댓글')
  })

  it('G26: 엑셀 출력 기능이 존재한다', () => {
    expect(content).toMatch(/excel|csv|엑셀/i)
  })
})

describe('SuggestionMainPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'SuggestionMainPage.tsx'), 'utf-8')

  it('G27: 공지사항 영역이 존재한다', () => {
    expect(content).toContain('공지사항')
  })
})

describe('SuggestionAdminPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'SuggestionAdminPage.tsx'), 'utf-8')

  it('G25: 서식관리 탭/기능이 존재한다', () => {
    expect(content).toContain('서식관리')
  })

  it('G25: 서식 CRUD가 존재한다', () => {
    expect(content).toMatch(/template|서식/)
  })
})

describe('sys14 MSW 핸들러 GAP 패치', () => {
  const content = readFileSync(resolve(HANDLERS_BASE, 'sys14.ts'), 'utf-8')

  it('댓글 API가 존재한다', () => {
    expect(content).toContain('comments')
  })

  it('서식 API가 존재한다', () => {
    expect(content).toContain('templates')
  })

  it('확장 필드가 존재한다', () => {
    expect(content).toContain('assignedDept')
    expect(content).toContain('actionType')
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys14/suggestion.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - 모든 GAP 패치 테스트(G17~G27)가 PASS
    - 기존 테스트도 여전히 PASS
  </acceptance_criteria>
  <done>SYS14 GAP 패치 테스트 전체 통과. G17-G27 검증 완료.</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` — 타입 에러 없음
2. `cd navy-admin && npx vitest run src/__tests__/sys14/ --reporter=verbose` — 전체 통과
3. grep으로 핵심 GAP 확인:
   - `grep -c 'assignedDept' navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx` >= 1
   - `grep -c 'comments' navy-admin/src/pages/sys14-suggestion/SuggestionListPage.tsx` >= 2
   - `grep -c '서식관리' navy-admin/src/pages/sys14-suggestion/SuggestionAdminPage.tsx` >= 1
   - `grep -c '공지사항' navy-admin/src/pages/sys14-suggestion/SuggestionMainPage.tsx` >= 1
</verification>

<success_criteria>
- G17: 목록에 담당부서, 조치일, 조치유형 컬럼 표시
- G18: 순번이 2026-1 형식으로 표시
- G19: 4단계 진행상태 운영
- G20: 조치유형 선택 시 조치일 DatePicker 연동
- G21: 반려사유 입력 동작
- G22: 상세에 직책, 전화번호, 첨부파일 표시
- G23: 등록폼에 첨부파일 Upload + 로그인 정보 자동표시
- G24: 댓글 CRUD 동작
- G25: 서식관리 CRUD 동작
- G26: 엑셀 출력 동작
- G27: 메인화면 공지사항 5건 표시
- 빌드 에러 없음, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-08-SUMMARY.md`
</output>
