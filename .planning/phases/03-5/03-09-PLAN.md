---
phase: 03-5
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys11-research/ResearchMainPage.tsx
  - navy-admin/src/pages/sys11-research/ResearchListPage.tsx
  - navy-admin/src/pages/sys11-research/ResearchAdminPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys11.ts
  - navy-admin/src/__tests__/sys11/research.test.ts
autonomous: true
gap_closure: true
requirements:
  - RSRC-01
  - RSRC-02
  - RSRC-03

must_haves:
  truths:
    - "메인화면에 년도 선택 콤보박스, 카테고리별 통계표, 소개자료 카드가 표시된다"
    - "연구자료 목록에 연구년도, 연구예산 컬럼이 표시된다"
    - "연구자료 등록폼에 연구년도, 연구예산, 진행상황, 첨부파일 필드가 있다"
    - "검색조건에 진행상황 Select와 키워드 입력이 있다"
    - "엑셀 다운로드 버튼이 있다"
    - "관리자 탭에서 첨부파일 다운로드 이력을 조회할 수 있다"
  artifacts:
    - path: "navy-admin/src/pages/sys11-research/ResearchMainPage.tsx"
      provides: "년도 선택 + 카테고리 통계표 + 소개자료"
      contains: "yearSelect"
    - path: "navy-admin/src/pages/sys11-research/ResearchListPage.tsx"
      provides: "확장 컬럼 + 검색 + 엑셀 + 첨부파일"
      contains: "researchYear"
    - path: "navy-admin/src/pages/sys11-research/ResearchAdminPage.tsx"
      provides: "다운로드 이력 탭"
      contains: "downloadHistory"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys11.ts"
      provides: "확장 필드 + 다운로드 이력 API"
      contains: "researchYear"
  key_links:
    - from: "ResearchListPage.tsx"
      to: "/api/sys11/research"
      via: "확장 쿼리파라미터"
      pattern: "progressStatus|keyword"
    - from: "ResearchAdminPage.tsx"
      to: "/api/sys11/download-history"
      via: "useQuery"
      pattern: "download-history"
---

<objective>
SYS11 연구자료종합관리체계 GAP 패치 (G11-G16)

Purpose: 메인화면 통계 보강, 목록 컬럼/폼 필드 추가, 검색조건, 엑셀 다운로드, 다운로드 이력 기능을 보완한다.
Output: 3개 페이지 확장 + MSW 핸들러 + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-5/03-UI-SPEC.md

<interfaces>
<!-- ResearchItem 현재 인터페이스 -->
interface ResearchItem extends Record<string, unknown> {
  id: string; title: string; author: string; department: string;
  category: string; description: string; fileUrl: string; fileName: string;
  downloadCount: number; viewCount: number; createdAt: string; updatedAt: string;
}
<!-- CRUD_FIELDS: 현재 title, category, author, department, description -->
<!-- 추가 필요: researchYear, budget, progressStatus, file(Upload) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SYS11 페이지 3개 GAP 패치 + MSW (G11-G16)</name>
  <read_first>
    - navy-admin/src/pages/sys11-research/ResearchMainPage.tsx
    - navy-admin/src/pages/sys11-research/ResearchListPage.tsx
    - navy-admin/src/pages/sys11-research/ResearchAdminPage.tsx
    - navy-admin/src/shared/api/mocks/handlers/sys11.ts
  </read_first>
  <files>
    navy-admin/src/pages/sys11-research/ResearchMainPage.tsx,
    navy-admin/src/pages/sys11-research/ResearchListPage.tsx,
    navy-admin/src/pages/sys11-research/ResearchAdminPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys11.ts
  </files>
  <action>
**G11 — ResearchMainPage.tsx 메인화면 보강:**
현재 Statistic 4개 + 최신 목록만 있음. 다음 추가:

1. 년도 선택 콤보박스 (상단에):
```tsx
<Select value={selectedYear} onChange={setSelectedYear} style={{width:120, marginBottom:16}} options={[
  {label:'2026',value:2026},{label:'2025',value:2025},{label:'2024',value:2024},
]} />
```

2. 카테고리별 통계 테이블 (Statistic 카드 아래에):
```tsx
<Card title={`${selectedYear}년 카테고리별 현황`} size="small" style={{marginTop:16}}>
  <Table dataSource={categoryStats} pagination={false} columns={[
    { title: '카테고리', dataIndex: 'category' },
    { title: '총 과제수', dataIndex: 'totalCount' },
    { title: '총 예산액', dataIndex: 'totalBudget', render: (v) => `${v?.toLocaleString()}원` },
  ]} rowKey="category" size="small" />
</Card>
```

3. 소개자료 카드 그리드 (하단에):
```tsx
<Row gutter={[16,16]} style={{marginTop:16}}>
  {INTRO_DATA.map(item => (
    <Col span={8} key={item.category}>
      <Card title={item.category} size="small" hoverable onClick={() => navigate(`/sys11/1/2?category=${item.category}`)}>
        <Text>{item.description}</Text>
        <div style={{marginTop:8}}><Button type="link" size="small">자료 보기</Button></div>
      </Card>
    </Col>
  ))}
</Row>
```
INTRO_DATA: 카테고리별 소개 Mock 데이터 배열

MSW에 통계 API 추가: `GET /api/sys11/stats?year=2026` — 카테고리별 {category, totalCount, totalBudget} 배열 반환

**G12 — ResearchListPage.tsx 컬럼 추가:**
ResearchItem 인터페이스에 추가:
```typescript
researchYear: number  // 연구년도
budget: number        // 연구예산
progressStatus: string // 진행상황 (최초평가/중간평가/최종평가)
```

columns 배열에 추가 ('저자' 다음에):
```typescript
{ title: '연구년도', dataIndex: 'researchYear', width: 100 },
{ title: '연구예산', dataIndex: 'budget', width: 110, render: (v) => v ? `${Number(v).toLocaleString()}원` : '-' },
```
'저자' 컬럼 title을 '연구자'로 변경

**G13 — ResearchListPage.tsx 등록폼 필드 추가:**
기존 CRUD_FIELDS에 추가 (author 다음에):
```typescript
{ name: 'researchYear', label: '연구년도', type: 'number' as const, required: true },
{ name: 'budget', label: '연구예산', type: 'number' as const, required: true },
{ name: 'progressStatus', label: '진행상황', type: 'select' as const, required: true, options: [
  {label:'최초평가',value:'최초평가'},{label:'중간평가',value:'중간평가'},{label:'최종평가',value:'최종평가'}
] },
```

등록/수정 Modal 내에 CrudForm 아래에 Upload 추가:
```tsx
<Upload listType="text" beforeUpload={() => false} maxCount={1}>
  <Button icon={<UploadOutlined />}>첨부파일 선택</Button>
</Upload>
```

**G14 — ResearchListPage.tsx 검색조건 추가:**
DataTable 상단에 검색 영역 추가 (Space 또는 Form.Item 패턴):
```tsx
// DataTable 위에 검색 영역
<Card size="small" style={{marginBottom:16}}>
  <Space>
    <Select value={searchStatus} onChange={setSearchStatus} placeholder="진행상황" allowClear style={{width:140}} options={[
      {label:'전체',value:''},{label:'최초평가',value:'최초평가'},{label:'중간평가',value:'중간평가'},{label:'최종평가',value:'최종평가'}
    ]} />
    <Input.Search value={keyword} onChange={e => setKeyword(e.target.value)} placeholder="키워드 검색" onSearch={handleSearch} style={{width:200}} allowClear />
  </Space>
</Card>
```
fetchResearch에 progressStatus, keyword 쿼리 파라미터 추가.

**G15 — ResearchListPage.tsx 엑셀 다운로드:**
toolBarRender에 "엑셀 다운로드" 버튼 추가:
```tsx
<Button key="excel" onClick={handleExcelExport}>엑셀 다운로드</Button>
```
handleExcelExport: CSV 변환 + Blob 다운로드 (컬럼: 순번, 제목, 내용, 연구자, 연구년도, 연구예산)

**G16 — ResearchAdminPage.tsx 다운로드 이력 탭:**
현재 AdminPage 구조 확인 후 Tabs 추가:
- 기존 관리자 컨텐츠를 "관리자" 탭으로
- "다운로드 이력" 탭 추가:
```tsx
{ key: 'download-history', label: '다운로드 이력', children: <DownloadHistoryTab /> }
```

DownloadHistoryTab 컴포넌트:
```tsx
function DownloadHistoryTab() {
  // 검색조건: 게시판 Select
  // DataTable: 번호, 다운로더, 소속, 파일명, 다운로드일시, 게시판
  // GET /api/sys11/download-history
}
```

검색조건:
```tsx
<Select value={boardFilter} onChange={setBoardFilter} style={{width:200, marginBottom:16}} options={[
  {label:'전체',value:''},{label:'연구자료',value:'research'},{label:'자료실',value:'library'},
]} />
```

테이블 컬럼:
```typescript
[
  { title: '번호', dataIndex: 'id', width: 80 },
  { title: '다운로더', dataIndex: 'downloaderName', width: 100 },
  { title: '소속', dataIndex: 'downloaderUnit', width: 120 },
  { title: '파일명', dataIndex: 'fileName', ellipsis: true },
  { title: '다운로드일시', dataIndex: 'downloadedAt', width: 150 },
  { title: '게시판', dataIndex: 'boardType', width: 100 },
]
```

**MSW sys11.ts 확장:**
ResearchItem Mock 데이터에 추가:
- `researchYear: faker.number.int({min:2020, max:2026})`
- `budget: faker.number.int({min:1000000, max:100000000})`
- `progressStatus: faker.helpers.arrayElement(['최초평가','중간평가','최종평가'])`

GET 핸들러에 progressStatus, keyword 쿼리 필터 추가.

통계 API 추가:
```typescript
http.get('/api/sys11/stats', ({ request }) => {
  const url = new URL(request.url)
  const year = url.searchParams.get('year') || '2026'
  const stats = CATEGORY_OPTIONS.map(cat => ({
    category: cat, totalCount: faker.number.int({min:5,max:30}), totalBudget: faker.number.int({min:10000000, max:500000000})
  }))
  return HttpResponse.json({ success: true, data: stats })
})
```

다운로드 이력 API 추가:
```typescript
http.get('/api/sys11/download-history', ({ request }) => {
  const url = new URL(request.url)
  const page = parseInt(url.searchParams.get('page') || '0')
  const size = parseInt(url.searchParams.get('size') || '10')
  const items = Array.from({length:30}, (_,i) => ({
    id: `dl-${i+1}`, downloaderName: faker.person.fullName(), downloaderUnit: faker.helpers.arrayElement(['1사단','2사단','해병대사령부']),
    fileName: faker.system.fileName(), downloadedAt: faker.date.recent({days:60}).toISOString(), boardType: faker.helpers.arrayElement(['연구자료','자료실']),
  }))
  return HttpResponse.json({ success: true, data: paginate(items, page, size) })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys11/research.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ResearchMainPage.tsx에 'selectedYear' 또는 'yearSelect', 'categoryStats', 'INTRO_DATA' 또는 '소개자료' 문자열 존재
    - ResearchListPage.tsx에 'researchYear', 'budget' 문자열 존재
    - ResearchListPage.tsx에 'progressStatus', 'keyword' 문자열 존재
    - ResearchListPage.tsx에 'Upload' 문자열 존재
    - ResearchListPage.tsx에 'excel' 또는 'csv' 또는 '엑셀' 문자열 존재
    - ResearchAdminPage.tsx에 'downloadHistory' 또는 'download-history' 문자열 존재
    - sys11.ts MSW에 'researchYear', 'budget', 'progressStatus', 'download-history', '/stats' 문자열 존재
    - npx tsc --noEmit 통과
  </acceptance_criteria>
  <done>G11-G16 모든 GAP 해소. 메인 통계, 컬럼, 폼, 검색, 엑셀, 다운로드 이력 모두 동작.</done>
</task>

<task type="auto">
  <name>Task 2: SYS11 테스트 업데이트</name>
  <read_first>
    - navy-admin/src/__tests__/sys11/research.test.ts
    - navy-admin/src/pages/sys11-research/ResearchMainPage.tsx
    - navy-admin/src/pages/sys11-research/ResearchListPage.tsx
    - navy-admin/src/pages/sys11-research/ResearchAdminPage.tsx
  </read_first>
  <files>navy-admin/src/__tests__/sys11/research.test.ts</files>
  <action>
기존 readFileSync 테스트 패턴에 GAP 패치 검증 추가:

```typescript
describe('ResearchMainPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'ResearchMainPage.tsx'), 'utf-8')

  it('G11: 년도 선택이 존재한다', () => {
    expect(content).toMatch(/selectedYear|년도/)
  })

  it('G11: 카테고리별 통계가 존재한다', () => {
    expect(content).toMatch(/categoryStats|카테고리별/)
  })

  it('G11: 소개자료가 존재한다', () => {
    expect(content).toMatch(/INTRO_DATA|소개/)
  })
})

describe('ResearchListPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'ResearchListPage.tsx'), 'utf-8')

  it('G12: 연구년도 컬럼이 존재한다', () => {
    expect(content).toContain('researchYear')
  })

  it('G12: 연구예산 컬럼이 존재한다', () => {
    expect(content).toContain('budget')
  })

  it('G13: 진행상황 폼 필드가 존재한다', () => {
    expect(content).toContain('progressStatus')
  })

  it('G13: Upload 컴포넌트가 존재한다', () => {
    expect(content).toContain('Upload')
  })

  it('G14: 키워드 검색이 존재한다', () => {
    expect(content).toContain('keyword')
  })

  it('G15: 엑셀 다운로드가 존재한다', () => {
    expect(content).toMatch(/excel|csv|엑셀/i)
  })
})

describe('ResearchAdminPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'ResearchAdminPage.tsx'), 'utf-8')

  it('G16: 다운로드 이력 기능이 존재한다', () => {
    expect(content).toMatch(/downloadHistory|download-history|다운로드 이력/)
  })
})

describe('sys11 MSW 핸들러 GAP 패치', () => {
  const content = readFileSync(resolve(HANDLERS_BASE, 'sys11.ts'), 'utf-8')

  it('확장 필드가 존재한다', () => {
    expect(content).toContain('researchYear')
    expect(content).toContain('budget')
  })

  it('통계 API가 존재한다', () => {
    expect(content).toContain('/stats')
  })

  it('다운로드 이력 API가 존재한다', () => {
    expect(content).toContain('download-history')
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys11/research.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 모든 GAP 패치 테스트(G11~G16)가 PASS
    - 기존 테스트도 여전히 PASS
  </acceptance_criteria>
  <done>SYS11 GAP 패치 테스트 전체 통과. G11-G16 검증 완료.</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` — 타입 에러 없음
2. `cd navy-admin && npx vitest run src/__tests__/sys11/ --reporter=verbose` — 전체 통과
3. grep 확인:
   - `grep -c 'researchYear' navy-admin/src/pages/sys11-research/ResearchListPage.tsx` >= 2
   - `grep -c 'budget' navy-admin/src/pages/sys11-research/ResearchListPage.tsx` >= 2
   - `grep -c 'download-history' navy-admin/src/shared/api/mocks/handlers/sys11.ts` >= 1
</verification>

<success_criteria>
- G11: 메인화면에 년도 선택, 카테고리별 통계표, 소개자료 카드 표시
- G12: 연구자료 목록에 연구년도, 연구예산 컬럼 표시
- G13: 등록폼에 연구년도, 연구예산, 진행상황, 첨부파일 필드
- G14: 검색조건에 진행상황 Select + 키워드 검색
- G15: 엑셀 다운로드 버튼 동작
- G16: 관리자에서 다운로드 이력 조회 동작
- 빌드 에러 없음, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-09-SUMMARY.md`
</output>
