---
phase: 03-5
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx
  - navy-admin/src/pages/sys16-meeting-room/MeetingStatusPage.tsx
  - navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx
  - navy-admin/src/pages/sys16-meeting-room/index.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys16.ts
  - navy-admin/src/__tests__/sys16/meeting-room.test.ts
autonomous: true
gap_closure: true
requirements:
  - ROOM-01
  - ROOM-03
  - ROOM-05
  - ROOM-07

must_haves:
  truths:
    - "예약신청폼에 관리부대 선택, 참석인원, 참석자정보 필드가 표시된다"
    - "회의현황 검색조건에 관리부대 Select가 있다"
    - "회의예약관리에 엑셀 다운로드와 프린트 버튼이 있다"
    - "사용자별권한등록 라우트가 index.tsx에 존재한다"
  artifacts:
    - path: "navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx"
      provides: "관리부대 + 참석인원 + 참석자정보 필드"
      contains: "managingUnit"
    - path: "navy-admin/src/pages/sys16-meeting-room/MeetingStatusPage.tsx"
      provides: "관리부대 검색조건"
      contains: "managingUnit"
    - path: "navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx"
      provides: "엑셀 + 프린트 버튼"
      contains: "exportToExcel"
    - path: "navy-admin/src/pages/sys16-meeting-room/index.tsx"
      provides: "AuthGroupIndex 라우트"
      contains: "AuthGroupIndex"
  key_links:
    - from: "MeetingReservePage.tsx"
      to: "/api/sys16/reservations"
      via: "POST body에 managingUnit, attendeeCount, attendees 포함"
      pattern: "managingUnit"
    - from: "index.tsx"
      to: "common/auth-group"
      via: "lazy import + Route"
      pattern: "AuthGroupIndex"
---

<objective>
SYS16 회의실예약관리체계 GAP 패치 (G28-G31)

Purpose: 가장 완성도 높은(85%) 서브시스템의 나머지 GAP을 해소한다. 예약신청 폼 필드 추가, 검색조건 보강, 엑셀/프린트, 권한 라우트.
Output: 4개 파일 패치 + MSW + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-5/03-UI-SPEC.md

<interfaces>
<!-- MeetingReservePage 현재 폼: roomId, date, startTime, endTime, purpose -->
<!-- 추가 필요: managingUnit(부대선택), attendeeCount(참석인원), attendees(참석자정보) -->
<!-- MeetingStatusPage: 검색조건 roomId + dateRange, 추가 필요: managingUnit -->
<!-- ReservationMgmtPage: 승인/반려 구현됨, 엑셀/프린트 미구현 -->
<!-- index.tsx: 권한관리 라우트 없음 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SYS16 페이지 4개 GAP 패치 + MSW (G28-G31)</name>
  <read_first>
    - navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx
    - navy-admin/src/pages/sys16-meeting-room/MeetingStatusPage.tsx
    - navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx
    - navy-admin/src/pages/sys16-meeting-room/index.tsx
    - navy-admin/src/shared/api/mocks/handlers/sys16.ts
  </read_first>
  <files>
    navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx,
    navy-admin/src/pages/sys16-meeting-room/MeetingStatusPage.tsx,
    navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx,
    navy-admin/src/pages/sys16-meeting-room/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys16.ts
  </files>
  <action>
**G28 — MeetingReservePage.tsx 폼 필드 추가:**
현재 ReserveFormValues에 추가:
```typescript
interface ReserveFormValues {
  managingUnit: string  // 추가
  roomId: string
  date: import('dayjs').Dayjs
  startTime: import('dayjs').Dayjs
  endTime: import('dayjs').Dayjs
  purpose: string
  attendeeCount: number  // 추가
  attendees: string      // 추가
}
```

Form에 "회의실 선택" 앞에 "관리부대" 필드 추가:
```tsx
<Form.Item name="managingUnit" label="회의실 관리 부대" rules={[{required:true, message:'관리 부대를 선택하세요'}]}>
  <Select placeholder="부대 선택" onChange={handleUnitChange} options={[
    {label:'해병대사령부',value:'해병대사령부'},
    {label:'1사단',value:'1사단'},
    {label:'2사단',value:'2사단'},
  ]} />
</Form.Item>
```
handleUnitChange: 부대 선택 시 회의실 목록을 해당 부대로 필터링 (rooms.filter(r => r.managingUnit === value))

"회의 목적" 다음에 참석인원/참석자정보 필드 추가:
```tsx
<Form.Item name="attendeeCount" label="참석 인원" rules={[{required:true, message:'참석 인원을 입력하세요'}]}>
  <InputNumber min={1} max={200} placeholder="인원 수" style={{width:'100%'}} />
</Form.Item>
<Form.Item name="attendees" label="참석자 정보">
  <Input.TextArea rows={2} placeholder="참석자 목록 (성명, 소속 등)" />
</Form.Item>
```

reserveMutation payload에 managingUnit, attendeeCount, attendees 추가.

**G29 — MeetingStatusPage.tsx 검색조건 추가:**
현재 검색 영역에 "관리부대" Select 추가 (회의실 Select 앞에):
```tsx
<Select value={managingUnit} onChange={setManagingUnit} placeholder="관리 부대" allowClear style={{width:140}} options={[
  {label:'전체',value:''},{label:'해병대사령부',value:'해병대사령부'},{label:'1사단',value:'1사단'},{label:'2사단',value:'2사단'},
]} />
```
fetch URL에 managingUnit 쿼리 파라미터 추가.

**G30 — ReservationMgmtPage.tsx 엑셀/프린트:**
기존 toolBarRender (또는 적절한 위치)에 버튼 2개 추가:
```tsx
<Button key="excel" onClick={handleExcelExport}>엑셀 다운로드</Button>
<Button key="print" onClick={handlePrint}>프린트</Button>
```

handleExcelExport: 현재 목록 데이터를 CSV로 변환 → Blob 다운로드 (컬럼: 회의실, 예약일, 시간, 목적, 신청자, 상태)
handlePrint: `window.print()` 호출 (간단 구현. Print CSS는 추후 보완)

**G31 — index.tsx 사용자별권한등록 라우트 추가:**
현재 index.tsx에 공통코드관리(2/1)만 있고 권한관리 라우트가 없음. 추가:
```tsx
const AuthGroupIndex = lazy(() => import('@/pages/common/auth-group'))

// Routes 내 공통코드관리(2/1) 다음에:
<Route
  path="2/2"
  element={
    <Suspense fallback={<PageSpinner />}>
      <AuthGroupIndex />
    </Suspense>
  }
/>
```

**MSW sys16.ts 확장:**
MeetingRoom Mock 데이터에 `managingUnit` 필드 추가:
```typescript
managingUnit: faker.helpers.arrayElement(['해병대사령부', '1사단', '2사단'])
```

Reservation Mock 데이터에 추가:
```typescript
attendeeCount: faker.number.int({min:2, max:30})
attendees: faker.person.fullName() + ', ' + faker.person.fullName()
managingUnit: faker.helpers.arrayElement(['해병대사령부', '1사단', '2사단'])
```

GET /api/sys16/meeting-rooms에 managingUnit 쿼리 필터 추가.
GET /api/sys16/reservations (회의현황)에 managingUnit 쿼리 필터 추가.
POST /api/sys16/reservations body에서 managingUnit, attendeeCount, attendees 읽어서 저장.
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys16/meeting-room.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MeetingReservePage.tsx에 'managingUnit', 'attendeeCount', 'attendees' 문자열 존재
    - MeetingStatusPage.tsx에 'managingUnit' 문자열 존재
    - ReservationMgmtPage.tsx에 'exportToExcel' 또는 'excel' 또는 'csv' 문자열 존재
    - ReservationMgmtPage.tsx에 'print' 또는 '프린트' 문자열 존재
    - index.tsx에 'AuthGroupIndex' 문자열 존재
    - index.tsx에 'auth-group' 문자열 존재
    - sys16.ts MSW에 'managingUnit', 'attendeeCount' 문자열 존재
    - npx tsc --noEmit 통과
  </acceptance_criteria>
  <done>G28-G31 모든 GAP 해소. 예약 폼 확장, 검색 보강, 엑셀/프린트, 권한 라우트 완성.</done>
</task>

<task type="auto">
  <name>Task 2: SYS16 테스트 업데이트</name>
  <read_first>
    - navy-admin/src/__tests__/sys16/meeting-room.test.ts
    - navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx
    - navy-admin/src/pages/sys16-meeting-room/MeetingStatusPage.tsx
    - navy-admin/src/pages/sys16-meeting-room/ReservationMgmtPage.tsx
    - navy-admin/src/pages/sys16-meeting-room/index.tsx
  </read_first>
  <files>navy-admin/src/__tests__/sys16/meeting-room.test.ts</files>
  <action>
기존 readFileSync 테스트 패턴에 GAP 패치 검증 추가:

```typescript
describe('MeetingReservePage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'MeetingReservePage.tsx'), 'utf-8')

  it('G28: 관리부대 선택 필드가 존재한다', () => {
    expect(content).toContain('managingUnit')
  })

  it('G28: 참석인원 필드가 존재한다', () => {
    expect(content).toContain('attendeeCount')
  })

  it('G28: 참석자정보 필드가 존재한다', () => {
    expect(content).toContain('attendees')
  })
})

describe('MeetingStatusPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'MeetingStatusPage.tsx'), 'utf-8')

  it('G29: 관리부대 검색조건이 존재한다', () => {
    expect(content).toContain('managingUnit')
  })
})

describe('ReservationMgmtPage GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'ReservationMgmtPage.tsx'), 'utf-8')

  it('G30: 엑셀 다운로드가 존재한다', () => {
    expect(content).toMatch(/excel|csv|엑셀/i)
  })

  it('G30: 프린트가 존재한다', () => {
    expect(content).toMatch(/print|프린트/i)
  })
})

describe('Sys16 index.tsx GAP 패치', () => {
  const content = readFileSync(resolve(BASE, 'index.tsx'), 'utf-8')

  it('G31: 사용자별권한등록 라우트가 존재한다', () => {
    expect(content).toContain('AuthGroupIndex')
    expect(content).toContain('auth-group')
  })
})

describe('sys16 MSW 핸들러 GAP 패치', () => {
  const content = readFileSync(resolve(HANDLERS_BASE, 'sys16.ts'), 'utf-8')

  it('managingUnit 필드가 존재한다', () => {
    expect(content).toContain('managingUnit')
  })

  it('attendeeCount 필드가 존재한다', () => {
    expect(content).toContain('attendeeCount')
  })
})
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys16/meeting-room.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 모든 GAP 패치 테스트(G28~G31)가 PASS
    - 기존 테스트도 여전히 PASS
  </acceptance_criteria>
  <done>SYS16 GAP 패치 테스트 전체 통과. G28-G31 검증 완료.</done>
</task>

</tasks>

<verification>
1. `cd navy-admin && npx tsc --noEmit` — 타입 에러 없음
2. `cd navy-admin && npx vitest run src/__tests__/sys16/ --reporter=verbose` — 전체 통과
3. grep 확인:
   - `grep -c 'managingUnit' navy-admin/src/pages/sys16-meeting-room/MeetingReservePage.tsx` >= 2
   - `grep -c 'AuthGroupIndex' navy-admin/src/pages/sys16-meeting-room/index.tsx` >= 1
</verification>

<success_criteria>
- G28: 예약신청에 관리부대, 참석인원, 참석자정보 필드 표시
- G29: 회의현황에 관리부대 검색조건 추가
- G30: 회의예약관리에 엑셀 다운로드 + 프린트 버튼 동작
- G31: 사용자별권한등록 라우트 추가
- 빌드 에러 없음, 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-5/03-10-SUMMARY.md`
</output>
