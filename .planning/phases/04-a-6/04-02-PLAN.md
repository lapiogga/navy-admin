---
phase: 04-a-6
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - navy-admin/src/pages/sys17-inspection/index.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionPlanPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionUnitPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionResultPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionProgressPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionDataPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys17.ts
  - navy-admin/src/__tests__/sys17/inspection.test.ts
autonomous: true
requirements: [INSP-01, INSP-02, INSP-03, INSP-04, INSP-05, INSP-06, INSP-07, INSP-08, INSP-09, INSP-10, INSP-11, INSP-12]

must_haves:
  truths:
    - "검열계획을 등록/수정/삭제할 수 있다"
    - "검열부대를 지정할 수 있다"
    - "검열결과를 CRUD하고 통계 차트가 표시된다"
    - "결재대기/결재완료/반려 플로우가 Steps 컴포넌트로 시각화된다"
    - "추진현황이 Progress로 표시된다"
    - "공지사항/코드관리/권한관리/접속로그가 공통 재사용으로 동작한다"
  artifacts:
    - path: "navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx"
      provides: "결재 워크플로우 (Steps 시각화)"
      contains: "Steps"
    - path: "navy-admin/src/pages/sys17-inspection/InspectionResultPage.tsx"
      provides: "검열결과 CRUD + 통계"
      contains: "DataTable"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys17.ts"
      provides: "검열 MSW 핸들러"
      min_lines: 80
  key_links:
    - from: "navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx"
      to: "navy-admin/src/shared/api/mocks/handlers/sys17.ts"
      via: "결재 API (approve/reject)"
      pattern: "/api/sys17/approval"
    - from: "navy-admin/src/pages/sys17-inspection/InspectionResultPage.tsx"
      to: "@ant-design/charts"
      via: "Bar chart import"
      pattern: "import.*@ant-design/charts"
---

<objective>
sys17 검열결과관리체계 구현 - 결재 워크플로우 연동 패턴 확립

Purpose: Phase 4 핵심 신규 패턴인 결재 워크플로우(D-01~03)를 검열 서브시스템에서 확립한다. 결재선 선택 -> 순차 결재 -> 완료/반려를 antd Steps로 시각화하고, 검열결과에 @ant-design/charts 통계를 포함한다.
Output: sys17 12개 요구사항 페이지, 결재 연동 패턴, MSW 핸들러, 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-a-6/04-CONTEXT.md
@.planning/phases/04-a-6/04-RESEARCH.md
@navy-admin/src/pages/common/approval/
@navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx
@navy-admin/src/shared/ui/DataTable/DataTable.tsx
@navy-admin/src/shared/ui/CrudForm/CrudForm.tsx
@navy-admin/src/shared/api/mocks/handlers/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: sys17 검열계획/부대/결과/추진현황 페이지 구현</name>
  <files>
    navy-admin/src/pages/sys17-inspection/index.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionPlanPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionUnitPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionResultPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionProgressPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionDataPage.tsx
  </files>
  <read_first>
    navy-admin/src/pages/sys17-inspection/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts,
    navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx,
    navy-admin/src/pages/sys04-certificate/index.tsx,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx,
    navy-admin/src/pages/common/board/index.tsx,
    navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx,
    navy-admin/src/pages/common/auth-group/index.tsx
  </read_first>
  <action>
    1. sys17 index.tsx 라우트 구현 (per D-24, menus.ts sys17 구조):
       - Routes 구조:
         - `/sys17/1/1` → BoardIndex (INSP-06 공지사항 - lazy)
         - `/sys17/1/2` → InspectionUnitPage (INSP-02 검열부대 지정)
         - `/sys17/1/3` → InspectionPlanPage (INSP-01 검열계획)
         - `/sys17/1/4` → InspectionResultPage (INSP-03 검열결과)
         - `/sys17/1/5` → InspectionApprovalPage (INSP-05 결재) — Task 2에서 구현
         - `/sys17/1/6` → InspectionProgressPage (INSP-04 추진현황)
         - `/sys17/2/1` → CodeMgmtPage (INSP-07 공통코드관리 - lazy)
         - `/sys17/2/2` → InspectionUnitMgmtPage 또는 별도 부대관리 (INSP-08)
         - `/sys17/2/3` → AuthGroupIndex (INSP-09 권한관리 - lazy)
         - `/sys17/2/4` → AccessLogPage (INSP-10 접속로그 - lazy)
         - `/sys17/3/1` → InspectionDataPage (INSP-11, INSP-12 데이터 조회)
       - index → Navigate to `/sys17/1/3`
    
    2. InspectionPlanPage.tsx (INSP-01):
       - PageContainer + DataTable + CrudForm 기본 CRUD
       - 컬럼: 계획명, 검열유형, 검열기간(시작일~종료일), 검열부대수, 상태(StatusBadge), 등록일
       - CrudForm 필드: 계획명(Input), 검열유형(Select: 정기/특별/수시), 시작일(DatePicker), 종료일(DatePicker), 비고(TextArea)
       - 상태: draft(작성중) / submitted(제출) / approved(결재완료)
       - API: GET/POST/PUT/DELETE `/api/sys17/plans`
    
    3. InspectionUnitPage.tsx (INSP-02):
       - DataTable로 검열계획별 부대 지정
       - 상단: 검열계획 Select로 계획 선택
       - 하단: 부대 목록 DataTable + 다중선택(rowSelection)
       - '부대 추가' 버튼 → antd Transfer 또는 Modal로 부대 선택
       - API: GET `/api/sys17/plans/:planId/units`, POST `/api/sys17/plans/:planId/units`
    
    4. InspectionResultPage.tsx (INSP-03):
       - antd Tabs: '검열결과 목록' | '통계/차트'
       - 목록 탭: DataTable + CrudForm CRUD
         - 컬럼: 부대명, 검열일자, 검열관, 평가등급(A/B/C/D), 총점, 상태
         - CrudForm: 부대(Select), 검열일자(DatePicker), 검열관(Input), 평가항목별 점수(InputNumber), 소견(TextArea)
       - 통계 탭 (per D-08/09):
         - `import { Bar } from '@ant-design/charts'`
         - 부대별 평가등급 분포 Bar 차트
         - 검열유형별 평균점수 Bar 차트
         - MSW에서 집계 데이터 반환
       - 저장: CSV 다운로드
       - API: GET/POST/PUT/DELETE `/api/sys17/results`, GET `/api/sys17/results/stats`
    
    5. InspectionProgressPage.tsx (INSP-04):
       - DataTable로 추진현황 조회
       - 컬럼: 검열계획명, 총부대수, 완료부대수, 진행률(antd Progress 인라인), 상태
       - per D-17: 각 행에 antd Progress percent 표시
       - 저장: CSV 다운로드
       - API: GET `/api/sys17/progress`
    
    6. InspectionDataPage.tsx (INSP-11, INSP-12):
       - 조회 전용 DataTable 2개 탭 또는 2개 섹션
       - 검열계획 정보(INSP-11): 전체 계획 목록 조회 (수정 불가)
       - 검열결과 정보(INSP-12): 전체 결과 목록 조회 (수정 불가)
       - API: GET `/api/sys17/data/plans`, GET `/api/sys17/data/results`
    
    7. INSP-08 부대관리:
       - DataTable + CrudForm 기본 CRUD
       - 컬럼: 부대코드, 부대명, 상위부대, 사용여부
       - sys17/2/2 라우트에 연결
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys17 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sys17-inspection/index.tsx contains routes for path "1/1" through "3/1"
    - sys17-inspection/index.tsx contains "InspectionPlanPage" and "InspectionResultPage" and "InspectionApprovalPage"
    - sys17-inspection/index.tsx contains "import('@/pages/common/board')" and "import('@/pages/common/auth-group')"
    - InspectionPlanPage.tsx contains "DataTable" and "CrudForm" and "/api/sys17/plans"
    - InspectionUnitPage.tsx contains "DataTable" and "rowSelection" or "Transfer"
    - InspectionResultPage.tsx contains "import { Bar } from '@ant-design/charts'" and "Tabs" and "/api/sys17/results"
    - InspectionProgressPage.tsx contains "Progress" and "/api/sys17/progress"
    - InspectionDataPage.tsx contains "DataTable" and "/api/sys17/data"
  </acceptance_criteria>
  <done>sys17 검열계획/부대지정/결과(+차트)/추진현황/데이터 페이지 구현 완료.</done>
</task>

<task type="auto">
  <name>Task 2: sys17 결재 워크플로우 연동 + MSW 핸들러 + 테스트</name>
  <files>
    navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys17.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys17/inspection.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/common/approval/ApprovalLinePage.tsx,
    navy-admin/src/shared/api/mocks/handlers/common/approval.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys17-inspection/InspectionPlanPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionResultPage.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx
  </read_first>
  <action>
    1. InspectionApprovalPage.tsx (INSP-05 — per D-01, D-02, D-03):
       - antd Tabs: '결재대기' | '결재완료' | '반려'
       - 결재대기 탭:
         - DataTable: 검열계획명, 요청자, 요청일, 현재 결재 단계
         - 행 클릭 시 상세 Modal:
           - 검열계획 요약 정보
           - antd Steps로 결재 진행 시각화 (per D-02):
             ```
             const approvalSteps = approvers.map((a, idx) => ({
               title: `${a.userRank} ${a.userName}`,
               description: a.status === 'approved' ? '결재완료' : a.status === 'rejected' ? '반려' : '대기',
               status: a.status === 'approved' ? 'finish' : a.status === 'rejected' ? 'error' : idx === currentStep ? 'process' : 'wait',
             }))
             ```
           - '결재' 버튼 (현재 결재자인 경우만 활성): POST `/api/sys17/approval/approve` body: `{ approvalLineId, contextId: inspectionPlanId, action: 'approve' }`
           - '반려' 버튼 + 반려사유 Input: POST `/api/sys17/approval/reject` body: `{ approvalLineId, contextId: inspectionPlanId, action: 'reject', reason: string }`
       - 결재완료 탭: DataTable 조회 전용 (status=approved)
       - 반려 탭: DataTable 조회 전용 (status=rejected), 반려사유 표시
    
    2. sys17 MSW 핸들러 (shared/api/mocks/handlers/sys17.ts):
       - faker.js로 Mock 데이터 생성 (ko locale)
       - 검열계획 CRUD:
         - GET `/api/sys17/plans` — 페이징 목록
         - POST `/api/sys17/plans` — 등록
         - PUT `/api/sys17/plans/:id` — 수정
         - DELETE `/api/sys17/plans/:id` — 삭제
       - 검열부대:
         - GET `/api/sys17/plans/:planId/units` — 계획별 부대 목록
         - POST `/api/sys17/plans/:planId/units` — 부대 지정
       - 검열결과:
         - GET `/api/sys17/results` — 결과 목록
         - POST `/api/sys17/results` — 등록
         - PUT `/api/sys17/results/:id` — 수정
         - DELETE `/api/sys17/results/:id` — 삭제
         - GET `/api/sys17/results/stats` — 통계 (faker 집계 데이터)
       - 결재 (per D-03):
         - GET `/api/sys17/approval/pending` — 결재대기 목록
         - GET `/api/sys17/approval/completed` — 결재완료 목록
         - GET `/api/sys17/approval/rejected` — 반려 목록
         - POST `/api/sys17/approval/approve` — 결재 (body: { approvalLineId, contextId, action })
         - POST `/api/sys17/approval/reject` — 반려 (body: { approvalLineId, contextId, action, reason })
       - 추진현황:
         - GET `/api/sys17/progress` — 현황 목록
       - 데이터:
         - GET `/api/sys17/data/plans` — 조회 전용
         - GET `/api/sys17/data/results` — 조회 전용
       - 부대관리:
         - GET `/api/sys17/units` — CRUD
       - `export const sys17Handlers = [...]`
    
    3. handlers/index.ts 수정:
       - `import { sys17Handlers } from './sys17'` 추가
       - handlers 배열에 `...sys17Handlers` 추가
    
    4. 테스트 (sys17/inspection.test.ts):
       - readFileSync 기반 검증
       - InspectionApprovalPage.tsx에 `Steps`, `/api/sys17/approval/approve`, `/api/sys17/approval/reject` 확인
       - InspectionPlanPage.tsx에 `DataTable`, `CrudForm` 확인
       - InspectionResultPage.tsx에 `Bar`, `@ant-design/charts` 확인
       - sys17.ts 핸들러: `/api/sys17/plans`, `/api/sys17/results`, `/api/sys17/approval`, `/api/sys17/progress` 확인
       - index.tsx 라우트 경로 확인
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys17 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - InspectionApprovalPage.tsx contains "Steps" and "items={" or "items={"
    - InspectionApprovalPage.tsx contains "/api/sys17/approval/approve" and "/api/sys17/approval/reject"
    - InspectionApprovalPage.tsx contains "Tabs" and "결재대기" and "결재완료" and "반려"
    - sys17.ts contains "sys17Handlers" and at least 15 http.get/http.post/http.put/http.delete calls
    - sys17.ts contains "/api/sys17/approval/pending" and "/api/sys17/approval/approve"
    - sys17.ts contains "/api/sys17/results/stats"
    - handlers/index.ts contains "sys17Handlers"
    - vitest run sys17 tests exit 0
  </acceptance_criteria>
  <done>sys17 검열결과관리체계 12개 요구사항 완전 구현. 결재 워크플로우(Steps 시각화) 패턴 확립.</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npm run build` 빌드 성공
- `cd navy-admin && npx vitest run src/__tests__/sys17` 모든 테스트 통과
- InspectionApprovalPage에 antd Steps 결재 단계 시각화 존재
- 결재 API가 contextId(검열계획ID)를 포함하여 호출
</verification>

<success_criteria>
- 검열계획 CRUD, 부대 지정, 결과 CRUD+차트, 추진현황 Progress, 결재 Steps 모두 동작
- 공지사항/코드관리/권한관리/접속로그가 공통 페이지 재사용으로 연결
- 결재대기/완료/반려 3탭 구조로 결재 플로우 완성
- MSW 핸들러 15+ 엔드포인트
</success_criteria>

<output>
After completion, create `.planning/phases/04-a-6/04-02-SUMMARY.md`
</output>
