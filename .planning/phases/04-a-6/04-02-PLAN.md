---
phase: 04-a-6
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys17-inspection/index.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionUnitPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionPlanPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionResultPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionProgressPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionUnitMgmtPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionPlanDataPage.tsx
  - navy-admin/src/pages/sys17-inspection/InspectionResultDataPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys17.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys17/inspection.test.ts
autonomous: true
requirements:
  - INSP-01
  - INSP-02
  - INSP-03
  - INSP-04
  - INSP-05
  - INSP-06
  - INSP-07
  - INSP-08
  - INSP-09
  - INSP-10
  - INSP-11
  - INSP-12

must_haves:
  truths:
    - "검열부대를 조직도 트리에서 체크하여 지정하고 저장할 수 있다"
    - "검열계획을 등록/수정/삭제하고, 계획 목록에서 검열연도/대상부대/기간 등이 표시된다"
    - "조치과제를 등록하고 조치결과를 입력하면 진행상태가 StatusBadge로 표시된다"
    - "결재 접수대기 목록에서 과제를 선택하면 antd Steps로 결재 진행 단계가 시각화되고, 접수(승인)/반송(반려)이 동작한다. MVP에서는 Steps 시각화로 결재 플로우를 표현하며, Phase 1 common/approval/ 데이터 로딩은 /api/sys17/approval/:taskId/line 엔드포인트로 시연한다."
    - "추진현황에서 Card+Statistic 요약과 부대별 Stacked Bar 차트, 세부현황 DataTable이 표시된다"
    - "공지사항/코드관리/부대관리/권한관리/접속로그/데이터 페이지는 Phase 1 재사용 또는 신규 DataTable"
    - "INSP-11(검열계획 정보) 및 INSP-12(검열결과 정보)가 읽기전용 DataTable로 라우트에 매핑된다"
  artifacts:
    - path: "navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx"
      provides: "결재 플로우 (INSP-05)"
      contains: "Steps"
    - path: "navy-admin/src/pages/sys17-inspection/InspectionProgressPage.tsx"
      provides: "추진현황 통계 (INSP-03/04)"
      contains: "@ant-design/charts"
    - path: "navy-admin/src/pages/sys17-inspection/InspectionResultDataPage.tsx"
      provides: "검열결과 정보 읽기전용 (INSP-12)"
      contains: "DataTable"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys17.ts"
      provides: "MSW Mock API"
      exports: ["sys17Handlers"]
  key_links:
    - from: "InspectionApprovalPage.tsx"
      to: "antd Steps"
      via: "결재 단계 시각화"
      pattern: "Steps"
    - from: "InspectionProgressPage.tsx"
      to: "@ant-design/charts"
      via: "Bar chart import"
      pattern: "import.*@ant-design/charts"
---

<objective>
SYS17 검열결과관리체계 25개 프로세스 구현

Purpose: 결재 워크플로우 연동(antd Steps)과 추진현황 Stacked Bar 차트는 Phase 4의 핵심 신규 패턴. Phase 5~7에서도 동일 패턴이 재사용됨.
Output: 검열부대지정/검열계획/검열결과(조치과제)/결재/추진현황 5개 고유 페이지 + 데이터 페이지 2개 + 공통 재사용 + MSW + 테스트
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-a-6/04-CONTEXT.md
@.planning/phases/04-a-6/04-UI-SPEC.md
@.planning/phases/04-a-6/04-RESEARCH.md

@navy-admin/src/pages/sys04-certificate/index.tsx
@navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx
@navy-admin/src/shared/api/mocks/handlers/sys04.ts
@navy-admin/src/__tests__/sys04/certificate.test.ts
@navy-admin/src/entities/subsystem/menus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MSW 핸들러 + 테스트 + 검열부대지정 + 검열계획 (INSP-01, INSP-02)</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys17.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys17/inspection.test.ts,
    navy-admin/src/pages/sys17-inspection/InspectionUnitPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionPlanPage.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys04.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys04/certificate.test.ts,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**MSW 핸들러** (`sys17.ts`)

인터페이스 (`extends Record<string, unknown>` 필수):
```typescript
export type InspProgressStatus = 'notStarted' | 'inProgress' | 'completed' | 'received'
export type ApprovalStatus = 'pending' | 'inReview' | 'approved' | 'rejected'

export interface InspectionPlan extends Record<string, unknown> {
  id: string
  inspYear: string
  planName: string
  startDate: string
  endDate: string
  targetUnit: string
  taskCount: number
  fileCount: number
  remarks: string
}

export interface InspectionTask extends Record<string, unknown> {
  id: string
  taskNo: string
  taskName: string
  planId: string
  planName: string
  targetUnit: string
  managingDept: string
  inspField: string       // 검열분야
  taskContent: string
  dueDate: string
  progressStatus: InspProgressStatus
  approvalStatus: ApprovalStatus
  submittedAt?: string
  lastUpdated: string
  issues: string
  actionResult: string
}

export interface InspActionHistory extends Record<string, unknown> {
  id: string
  taskId: string
  date: string
  assignee: string
  status: string
  content: string
}
```

Mock 데이터: 검열계획 10건, 조치과제 30건, 이력 50건 (faker.js ko). 부대=['1사단','2사단','해병대사령부','교육훈련단','상륙기동단']. 검열분야=['전투준비태세','교육훈련','군수','인사','정보화'].

핸들러:
- GET /sys17/units -- 검열부대 목록 (트리 구조)
- PUT /sys17/units -- 검열부대 저장 (body: { unitIds: string[] })
- GET /sys17/plans -- 검열계획 목록 (page, size, inspYear, planName, targetUnit, inspPeriod)
- GET /sys17/plans/:id -- 검열계획 상세
- POST /sys17/plans -- 등록
- PUT /sys17/plans/:id -- 수정
- DELETE /sys17/plans/:id -- 삭제
- GET /sys17/tasks -- 조치과제 목록 (page, size, inspYear, targetUnit, taskName, progressStatus, inspField)
- GET /sys17/tasks/:id -- 조치과제 상세
- POST /sys17/tasks -- 등록
- PUT /sys17/tasks/:id -- 수정
- DELETE /sys17/tasks/:id -- 삭제
- PUT /sys17/tasks/:id/result -- 조치결과 입력 (body: { progressStatus, issues, actionResult })
- GET /sys17/tasks/:id/history -- 과제처리 이력 (Timeline용)
- GET /sys17/approval/pending -- 접수대기 목록
- PUT /sys17/approval/:taskId/approve -- 접수(승인)
- PUT /sys17/approval/:taskId/reject -- 반송(반려) (body: { reason })
- GET /sys17/approval/:taskId/line -- 결재선 데이터 (Steps 시각화용, per D-01 MVP: Phase 1 common/approval/ 데이터 로딩 시연)
- GET /sys17/stats/progress -- 추진현황 통계 (반환: {total, completed, inProgress, notStarted, unitStats: [{unitName, totalTasks, completed, inProgress, notStarted, completionRate}]})

**InspectionUnitPage.tsx** (INSP-02: 검열부대 지정)

per UI-SPEC 17-A:
- 좌측: antd Tree (checkable) -- 조직도 트리. GET /sys17/units에서 트리 데이터 로드
- 우측: antd Table -- 선택된 부대 목록 (부대명, 삭제 버튼)
- 체크 시 우측 추가, 우측에서 제거 가능
- 하단 [저장] 버튼 (PUT /sys17/units)

**InspectionPlanPage.tsx** (INSP-01: 검열계획)

PageContainer title="검열계획".

SearchForm fields (per UI-SPEC 17-B):
- inspYear (검열연도): Select, 연도 목록 (2020~2030)
- planName (검열계획명): Input text
- targetUnit (대상부대): Select, 부대 목록
- inspPeriod (검열기간): DateRange

DataTable 컬럼 (per UI-SPEC 17-B):
- planName (검열계획명): width 250, text 링크
- inspYear (검열연도): width 80, text, sortable
- targetUnit (대상부대): width 150, text, sortable
- startDate (시작일): width 120, date, sortable
- endDate (종료일): width 120, date, sortable
- taskCount (과제 수): width 80, number, sortable
- fileCount (첨부): width 60, number

CrudForm fields (per UI-SPEC 17-B):
- inspYear (검열연도): Select, required
- planName (검열계획명): text, required, maxLength 200
- startDate (시작일): date, required
- endDate (종료일): date, required (startDate 이후)
- targetUnit (대상부대): Select multiple, required (검열부대 목록에서)
- remarks (비고): TextArea
- attachments (첨부파일): Upload

toolBarRender: [검열계획 작성] 버튼 (primary)

**테스트 파일** (`inspection.test.ts`)

readFileSync 기반. 테스트 대상: InspectionUnitPage(Tree 포함), InspectionPlanPage(DataTable+CrudForm 포함), InspectionResultPage(조치과제+StatusBadge+Timeline), InspectionApprovalPage(Steps 포함), InspectionProgressPage(@ant-design/charts 포함), InspectionPlanDataPage(DataTable 읽기전용), InspectionResultDataPage(DataTable 읽기전용), sys17.ts(sys17Handlers, /sys17/plans, /sys17/tasks, /sys17/approval), index.tsx(라우트 매핑, "3/1" 및 "3/2" 포함)

**handlers/index.ts**: sys17Handlers import 및 spread 추가
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys17 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sys17.ts에 "extends Record<string, unknown>" 포함
    - sys17.ts에 "sys17Handlers" export 포함
    - sys17.ts에 "/sys17/plans" 포함
    - sys17.ts에 "/sys17/tasks" 포함
    - sys17.ts에 "/sys17/approval" 포함
    - sys17.ts에 "/sys17/stats/progress" 포함
    - sys17.ts에 "/sys17/approval/:taskId/line" 포함
    - InspectionUnitPage.tsx에 "Tree" 포함
    - InspectionPlanPage.tsx에 "DataTable" 포함
    - InspectionPlanPage.tsx에 "CrudForm" 포함
    - InspectionPlanPage.tsx에 "검열계획" 텍스트 포함
    - index.ts에 "sys17Handlers" import 포함
  </acceptance_criteria>
  <done>MSW 핸들러(19 엔드포인트, 결재선 line 포함), 테스트 스캐폴드, 검열부대지정(Tree), 검열계획(CRUD) 완성</done>
</task>

<task type="auto">
  <name>Task 2: 검열결과 + 결재 + 추진현황 + 데이터 페이지 + 라우트 (INSP-03~12)</name>
  <files>
    navy-admin/src/pages/sys17-inspection/InspectionResultPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionProgressPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionUnitMgmtPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionPlanDataPage.tsx,
    navy-admin/src/pages/sys17-inspection/InspectionResultDataPage.tsx,
    navy-admin/src/pages/sys17-inspection/index.tsx
  </files>
  <read_first>
    navy-admin/src/pages/sys04-certificate/index.tsx,
    navy-admin/src/pages/sys04-certificate/CertificateApprovalPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys17.ts,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**InspectionResultPage.tsx** (INSP-03: 검열결과 -- 조치과제)

PageContainer title="검열결과". antd Tabs로 2탭: 조치과제 목록 / 조치결과 목록.

Tab 1 -- 조치과제 목록:

SearchForm fields (per UI-SPEC 17-C):
- inspYear (연도): Select
- targetUnit (대상부대): Select
- taskName (과제명): Input text
- progressStatus (진행상태): Select, options=['미조치','진행중','조치완료','접수완료']
- inspField (검열분야): Select, options=['전투준비태세','교육훈련','군수','인사','정보화']

DataTable 컬럼 (per UI-SPEC 17-C):
- taskNo (과제번호): width 100, text, sortable
- taskName (과제명): width 250, text 링크
- planName (검열계획): width 150, text, sortable
- targetUnit (대상부대): width 120, text, sortable
- managingDept (주관부서): width 120, text, sortable
- inspField (검열분야): width 100, text, sortable
- progressStatus (진행상태): width 100, StatusBadge (colorMap: notStarted='default', inProgress='blue', completed='green', received='cyan')
- dueDate (완료기한): width 120, date, sortable

toolBarRender: [조치과제 작성] 버튼 (primary) + [엑셀 다운로드] 버튼 (default)

CrudForm fields (per UI-SPEC 17-C):
- planId (검열계획): Select, required
- targetUnit (대상부대): Select, required
- managingDept (주관부서): Select, required
- inspField (검열분야): Select, required, options=['전투준비태세','교육훈련','군수','인사','정보화']
- taskName (과제명): text, required, maxLength 200
- taskContent (과제내용): TextArea, required
- dueDate (완료기한): date, required
- remarks (비고): TextArea
- attachments (첨부파일): Upload

상세 Modal 내 조치결과 입력 폼 (per UI-SPEC 17-C):
- progressStatus (진행상태): Select, required, options=['미조치','진행중','조치완료']
- issues (문제점): TextArea
- actionResult (조치계획/결과): TextArea, required
- attachments (첨부파일): Upload

과제처리 이력: antd Timeline (per D-18, UI-SPEC 17-C)
- GET /sys17/tasks/:id/history
- 각 Timeline.Item: dot 색상(StatusBadge 색상과 동일), children에 "[날짜] 담당자 - 상태변경 / 내용"
- 최신순 정렬 (상단이 최신)

Tab 2 -- 조치결과 목록 (per UI-SPEC 17-C):
- 컬럼: taskNo(과제번호), taskName(과제명), managingDept(주관부서), progressStatus(진행상태 StatusBadge), lastUpdated(최종수정일)

**InspectionApprovalPage.tsx** (INSP-05: 결재)

PageContainer title="결재". antd Tabs로 2탭: 접수대기 / 접수완료.

접수대기 SearchForm (per UI-SPEC 17-D):
- inspYear (연도): Select
- targetUnit (대상부대): Select
- inspField (검열분야): Select
- actionUnit (조치부대): Select
- taskName (과제명): Input text

접수대기 DataTable 컬럼 (per UI-SPEC 17-D):
- taskNo (과제번호): width 100, text
- taskName (과제명): width 200, text 링크
- targetUnit (대상부대): width 120, text
- inspField (검열분야): width 100, text
- submittedAt (보고일): width 120, date
- approvalStatus (결재상태): width 120, StatusBadge (colorMap: pending='orange', inReview='blue', approved='green', rejected='red')

상세 조회 Modal 내 결재 플로우 시각화 (per D-01~03, UI-SPEC 17-D):

**D-01 MVP 결재 연동 방침:** Phase 1 common/approval/ 데이터 로딩은 GET /api/sys17/approval/:taskId/line 엔드포인트에서 결재선 데이터를 받아 Steps 컴포넌트로 시각화한다. 현재 MVP에서는 Steps 시각화가 결재 플로우를 표현하는 핵심이며, Phase 1 결재선 관리의 full integration(결재선 선택 UI 연동)은 v2 enhancement로 계획한다.

```typescript
import { Steps } from 'antd'

const APPROVAL_STEPS = [
  { title: '조치결과 보고', description: '담당자' },
  { title: '중간 결재', description: '결재자' },
  { title: '최종 접수', description: '승인권자' },
]

// 상태 매핑:
// pending -> current=0
// inReview -> current=1
// approved -> current=2, status='finish' (모든 step)
// rejected -> 해당 step에 status='error'
```

Steps horizontal 컴포넌트로 현재 단계 하이라이트. 완료 단계는 green checkmark, 반송 시 red error.

액션 버튼:
- [접수 (승인)] primary: PUT /sys17/approval/:taskId/approve, ConfirmDialog 확인
- [반송 (반려)] danger: PUT /sys17/approval/:taskId/reject, 반려 사유 입력 Modal

접수완료 탭: 동일 컬럼, approvalStatus='approved' 필터. 읽기 전용.

**InspectionProgressPage.tsx** (INSP-03/04: 추진현황)

PageContainer title="추진현황". antd Tabs로 2탭: 종합현황 / 세부현황.

Tab 1 -- 종합현황 (per UI-SPEC 17-E):
- SearchForm: 기간(DateRange), 대상부대(Select), 검열분야(Select)
- 상단: antd Row + Col, Card + Statistic 4개 (총 과제 수, 완료(green), 진행중(blue), 미조치(default))
- 중앙: @ant-design/charts Bar (isStack=true, 가로) -- 대상부대별 추진현황 (완료/진행중/미조치 스택)
  - `import { Bar } from '@ant-design/charts'`
  - config: { data: unitStats, xField: 'count', yField: 'unitName', seriesField: 'status', isStack: true, height: 300 }
- 하단: antd Progress -- 전체 추진율 (strokeColor: 0-30% red, 31-70% gold, 71-100% green)

Tab 2 -- 세부현황 (per UI-SPEC 17-E):
- SearchForm: 기간(DateRange), 검열분야(Select)
- DataTable 컬럼: unitName(부대명 150), totalTasks(총 과제 80), completed(완료 80 green), inProgress(진행중 80 blue), notStarted(미조치 80 default), completionRate(추진율 120 antd Progress percent)
- 각 행 expandedRowRender: 해당 부대의 과제 상세 목록 서브테이블
- 차트: @ant-design/charts Bar (스택) -- 부대별 추진현황 그래프

**InspectionUnitMgmtPage.tsx** (INSP-08: 부대관리)

PageContainer title="부대관리". DataTable + CrudForm 기본 패턴.
컬럼: unitCode(부대코드), unitName(부대명), parentUnit(상위부대), unitType(부대유형), sortOrder(정렬순서)
CrudForm: unitCode(text,required), unitName(text,required), parentUnit(Select), unitType(Select), sortOrder(number)

**InspectionPlanDataPage.tsx** (INSP-11: 검열계획 정보 -- 읽기전용)

PageContainer title="검열계획 정보". DataTable 읽기전용 (toolBarRender 없음, CrudForm 없음).
데이터: GET /sys17/plans (동일 엔드포인트, 읽기전용 표시).
컬럼: InspectionPlanPage와 동일 컬럼 구조. 클릭 시 상세 Modal 읽기전용.

**InspectionResultDataPage.tsx** (INSP-12: 검열결과 정보 -- 읽기전용)

PageContainer title="검열결과 정보". DataTable 읽기전용 (toolBarRender 없음, CrudForm 없음).
데이터: GET /sys17/tasks (동일 엔드포인트, 읽기전용 표시).
컬럼: InspectionResultPage Tab1과 동일 컬럼 구조 (taskNo, taskName, planName, targetUnit, managingDept, inspField, progressStatus StatusBadge, dueDate). 클릭 시 상세 Modal 읽기전용 (조치결과 입력 비활성화).

**index.tsx** (라우트 매핑)

menus.ts 기반 Route 매핑:
```
Route index -> Navigate to="/sys17/1/1" replace
Route "1/1" -> BoardIndex (lazy, INSP-06 공지사항)
Route "1/2" -> InspectionUnitPage (INSP-02)
Route "1/3" -> InspectionPlanPage (INSP-01)
Route "1/4" -> InspectionResultPage (INSP-03)
Route "1/5" -> InspectionApprovalPage (INSP-05)
Route "1/6" -> InspectionProgressPage (INSP-04)
Route "2/1" -> CodeMgmtIndex (lazy, INSP-07)
Route "2/2" -> InspectionUnitMgmtPage (INSP-08)
Route "2/3" -> AuthGroupIndex (lazy, INSP-09)
Route "2/4" -> AccessLogIndex (lazy, INSP-10)
Route "3/1" -> InspectionPlanDataPage (INSP-11: DataTable 조회 전용)
Route "3/2" -> InspectionResultDataPage (INSP-12: DataTable 조회 전용)
```

per D-26: 공지사항은 lazy(() => import('@/pages/common/board')) 재사용
per D-27: 코드관리/권한관리는 Phase 1 재사용
INSP-10: 접속로그는 Phase 1 system-mgr/AccessLogPage 재사용
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys17 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - InspectionResultPage.tsx에 "DataTable" 포함
    - InspectionResultPage.tsx에 "StatusBadge" 포함
    - InspectionResultPage.tsx에 "Timeline" 포함
    - InspectionResultPage.tsx에 "조치과제" 텍스트 포함
    - InspectionApprovalPage.tsx에 "Steps" import 포함
    - InspectionApprovalPage.tsx에 "접수" 텍스트 포함
    - InspectionApprovalPage.tsx에 "반송" 또는 "반려" 텍스트 포함
    - InspectionProgressPage.tsx에 "@ant-design/charts" import 포함
    - InspectionProgressPage.tsx에 "Bar" 포함
    - InspectionProgressPage.tsx에 "Statistic" 포함
    - InspectionProgressPage.tsx에 "Progress" 포함
    - InspectionPlanDataPage.tsx에 "DataTable" 포함
    - InspectionResultDataPage.tsx에 "DataTable" 포함
    - index.tsx에 "/sys17/" 경로 매핑 포함
    - index.tsx에 "InspectionApprovalPage" 포함
    - index.tsx에 "InspectionPlanDataPage" 포함
    - index.tsx에 "InspectionResultDataPage" 포함
    - index.tsx에 "3/1" 및 "3/2" 경로 포함
  </acceptance_criteria>
  <done>검열결과(조치과제 CRUD+Timeline), 결재(Steps 시각화+접수/반송+결재선 line API 시연), 추진현황(Stacked Bar+Statistic+Progress), 부대관리, 데이터 페이지 2종(INSP-11 검열계획 정보, INSP-12 검열결과 정보), 라우트 매핑 완성. 모든 INSP-01~12 커버</done>
</task>

</tasks>

<verification>
cd navy-admin && npx vitest run src/__tests__/sys17 --reporter=verbose
cd navy-admin && npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- SYS17 7개 고유 페이지(검열부대지정/검열계획/검열결과/결재/추진현황/부대관리) + 데이터 페이지 2개(INSP-11, INSP-12) + index.tsx 완성
- antd Steps 결재 플로우 시각화 구현 (결재선 line API 포함)
- @ant-design/charts Stacked Bar 차트 추진현황 구현
- antd Timeline 과제처리 이력 구현
- INSP-12 검열결과 정보 읽기전용 DataTable이 Route "3/2"에 매핑
- MSW 핸들러 19개 엔드포인트 동작 (결재선 line 포함)
- 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-a-6/04-02-SUMMARY.md`
</output>
