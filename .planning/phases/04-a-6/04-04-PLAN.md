---
phase: 04-a-6
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys02-survey/index.tsx
  - navy-admin/src/pages/sys02-survey/MySurveyPage.tsx
  - navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx
  - navy-admin/src/pages/sys02-survey/SurveyParticipationPage.tsx
  - navy-admin/src/pages/sys02-survey/SurveyFormPage.tsx
  - navy-admin/src/pages/sys02-survey/SurveyResultPage.tsx
  - navy-admin/src/pages/sys02-survey/PastSurveyPage.tsx
  - navy-admin/src/pages/sys02-survey/SurveyAdminPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys02.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys02/survey.test.ts
autonomous: true
requirements:
  - SURV-01
  - SURV-02
  - SURV-03
  - SURV-04
  - SURV-05
  - SURV-06
  - SURV-07

must_haves:
  truths:
    - "나의 설문관리에서 설문을 생성하고 목록에 상태(draft/submitted/approved/rejected/active/closed)가 StatusBadge로 표시"
    - "설문 문항 편집기에서 dnd-kit으로 문항 순서를 드래그 변경하고, 4가지 문항유형(radio/checkbox/textarea/rate)을 추가할 수 있다"
    - "설문참여 목록에서 활성 설문을 선택하면 응답 폼이 표시되고, 제출 후 응답 완료 상태로 변경"
    - "설문 결과분석에서 문항별 Bar 차트(객관식)/텍스트 목록(주관식)/Rate 분포(평점)가 표시"
    - "체계관리에서 관리자가 6개 탭(승인대기/전체설문관리/카테고리관리/통계/대상자관리/설문템플릿)을 통해 설문 전체를 관리한다 (SURV-04 13개 프로세스 커버)"
    - "지난 설문보기에서 마감된 설문의 결과를 조회"
  artifacts:
    - path: "navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx"
      provides: "설문 문항 편집기 (SURV-02 핵심)"
      contains: "@dnd-kit"
    - path: "navy-admin/src/pages/sys02-survey/SurveyResultPage.tsx"
      provides: "결과분석 차트 (SURV-02/04)"
      contains: "@ant-design/charts"
    - path: "navy-admin/src/pages/sys02-survey/SurveyAdminPage.tsx"
      provides: "체계관리 6탭 (SURV-04)"
      contains: "Tabs"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys02.ts"
      provides: "MSW Mock API"
      exports: ["sys02Handlers"]
  key_links:
    - from: "SurveyQuestionEditor.tsx"
      to: "@dnd-kit/sortable"
      via: "useSortable + DndContext"
      pattern: "dnd-kit"
    - from: "SurveyResultPage.tsx"
      to: "@ant-design/charts"
      via: "Bar chart"
      pattern: "import.*@ant-design/charts"
    - from: "SurveyAdminPage.tsx"
      to: "antd Tabs"
      via: "6탭 체계관리"
      pattern: "Tabs"
---

<objective>
SYS02 설문종합관리체계 31개 프로세스 구현 (Phase 4 최복잡 서브시스템)

Purpose: dnd-kit 기반 설문 문항 편집기는 Phase 4의 핵심 신규 패턴. 설문 생성->배포->응답->결과분석 전체 라이프사이클을 구현. SURV-04 체계관리는 13개 프로세스를 6개 탭으로 커버.
Output: 나의설문/문항편집기/설문참여/응답폼/결과분석/지난설문/체계관리 7개 고유 페이지 + MSW + 테스트
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-a-6/04-CONTEXT.md
@.planning/phases/04-a-6/04-UI-SPEC.md
@.planning/phases/04-a-6/04-RESEARCH.md

@navy-admin/src/pages/sys04-certificate/index.tsx
@navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx
@navy-admin/src/shared/api/mocks/handlers/sys04.ts
@navy-admin/src/__tests__/sys04/certificate.test.ts
@navy-admin/src/entities/subsystem/menus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MSW 핸들러 + 테스트 + 나의 설문관리 + 문항 편집기 (SURV-02)</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys02.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys02/survey.test.ts,
    navy-admin/src/pages/sys02-survey/MySurveyPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys04.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys04/certificate.test.ts,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**MSW 핸들러** (`sys02.ts`)

타입 정의 (`extends Record<string, unknown>` 필수):
```typescript
export type SurveyStatus = 'draft' | 'submitted' | 'approved' | 'rejected' | 'active' | 'closed'
export type QuestionType = 'radio' | 'checkbox' | 'textarea' | 'rate'

export interface Survey extends Record<string, unknown> {
  id: string
  surveyName: string
  description: string
  status: SurveyStatus
  startDate: string
  endDate: string
  targetType: string        // '부대' | '직급' | '전체'
  targetUnits: string[]
  targetRanks: string[]
  targetCount: number
  responseCount: number
  questionCount: number
  isPublicResult: boolean
  isAnonymous: boolean
  authorName: string
  createdAt: string
  submittedAt?: string
  categoryId?: string       // 설문 카테고리 ID
  templateId?: string       // 설문 템플릿 ID (복사 시)
}

export interface SurveyQuestion extends Record<string, unknown> {
  id: string
  surveyId: string
  questionText: string
  questionType: QuestionType
  isRequired: boolean
  options: string[]          // radio/checkbox 선택지
  orderIndex: number
}

export interface SurveyResponse extends Record<string, unknown> {
  id: string
  surveyId: string
  questionId: string
  respondentName: string
  answer: string | string[]  // 텍스트 또는 선택지 배열
  ratingValue?: number       // rate 유형일 때
}

export interface SurveyCategory extends Record<string, unknown> {
  id: string
  categoryName: string
  description: string
  sortOrder: number
  surveyCount: number
  createdAt: string
}

export interface SurveyTemplate extends Record<string, unknown> {
  id: string
  templateName: string
  description: string
  categoryId: string
  questionCount: number
  createdAt: string
  authorName: string
}

export interface SurveyTarget extends Record<string, unknown> {
  id: string
  surveyId: string
  targetName: string
  targetUnit: string
  targetRank: string
  isResponded: boolean
  respondedAt?: string
}
```

Mock 데이터: 설문 15건 (상태별 분포), 문항 각 3~5개, 응답 30건, 카테고리 5건, 템플릿 5건, 대상자 50건.

핸들러:
- GET /sys02/surveys -- 목록 (page, size, status)
- GET /sys02/surveys/:id -- 상세
- POST /sys02/surveys -- 생성
- PUT /sys02/surveys/:id -- 수정
- DELETE /sys02/surveys/:id -- 삭제
- PUT /sys02/surveys/:id/submit -- 승인 요청 (status: draft->submitted)
- PUT /sys02/surveys/:id/deploy -- 배포 (status: approved->active)
- PUT /sys02/surveys/:id/close -- 마감 (status: active->closed)
- GET /sys02/surveys/:id/questions -- 문항 목록
- PUT /sys02/surveys/:id/questions -- 문항 일괄 저장 (body: questions[])
- GET /sys02/surveys/participation -- 설문참여 목록 (활성 설문)
- POST /sys02/surveys/:id/respond -- 설문 응답 제출
- GET /sys02/surveys/:id/results -- 결과 분석 (문항별 통계)
- GET /sys02/surveys/past -- 지난 설문 (closed 상태)
- GET /sys02/surveys/pending -- 승인 대기 목록
- PUT /sys02/surveys/:id/approve -- 승인
- PUT /sys02/surveys/:id/reject -- 반려 (body: { reason })
- GET /sys02/surveys/all -- 전체 설문관리 목록 (관리자)
- GET /sys02/categories -- 카테고리 목록 (SURV-04 카테고리관리)
- POST /sys02/categories -- 카테고리 등록
- PUT /sys02/categories/:id -- 카테고리 수정
- DELETE /sys02/categories/:id -- 카테고리 삭제
- GET /sys02/stats -- 설문 통계 (SURV-04 통계: 월별 설문 수, 응답률, 카테고리별 분포)
- GET /sys02/templates -- 설문 템플릿 목록 (SURV-04 설문템플릿)
- POST /sys02/templates -- 템플릿 저장
- PUT /sys02/templates/:id -- 템플릿 수정
- DELETE /sys02/templates/:id -- 템플릿 삭제
- GET /sys02/surveys/:id/targets -- 대상자 목록 (SURV-04 대상자관리)
- PUT /sys02/surveys/:id/targets -- 대상자 일괄 수정

export const sys02Handlers = [...] 형태.

**handlers/index.ts**: sys02Handlers import 및 spread 추가.

**MySurveyPage.tsx** (SURV-02: 나의 설문관리 목록)

PageContainer title="나의 설문관리". DataTable + Modal(설문 설정 폼).

DataTable 컬럼 (per UI-SPEC 02-A):
- surveyName (설문명): width 250, text 링크 (클릭 시 설문 상세/문항 편집)
- status (상태): width 100, StatusBadge (colorMap: draft='default', submitted='orange', approved='blue', rejected='red', active='cyan', closed='gray'; labelMap: draft='작성중', submitted='제출됨', approved='승인', rejected='반려', active='진행중', closed='마감')
- targetCount (대상자 수): width 100, number, sortable
- responseCount (응답자 수): width 100, number, sortable
- createdAt (등록일): width 120, date, sortable
- startDate (시작일): width 120, date, sortable
- endDate (종료일): width 120, date, sortable

toolBarRender: [신규 등록] 버튼 (primary)

CrudForm fields -- 설문 설정 폼 (per UI-SPEC 02-B):
- surveyName (설문명): text, required, maxLength 100
- description (설문내용(개요)): TextArea, required, maxLength 2000
- startDate (시작일): date, required
- endDate (종료일): date, required (startDate 이후만 선택 가능)
- targetType (대상 구분): Select, required, options=['부대','직급','전체']
- targetUnits (대상 부대): Select multiple, 조건부 (targetType='부대' 시 표시)
- targetRanks (대상 직급): Select multiple, 조건부 (targetType='직급' 시 표시)
- isPublicResult (결과 공개): Select, required, options=[{label:'공개',value:true},{label:'비공개',value:false}]
- isAnonymous (익명 여부): Select, required, options=[{label:'익명',value:true},{label:'실명',value:false}]

설문 상세에서 [문항 편집] 버튼 -> SurveyQuestionEditor 페이지/모달 이동.
액션 드롭다운: [승인 요청] (draft -> submitted), [배포] (approved -> active), [마감] (active -> closed), [재작성 (복사)]

**SurveyQuestionEditor.tsx** (SURV-02: 문항 편집기 -- Phase 4 핵심 신규 패턴)

per UI-SPEC 02-C, D-04~05:

DnD 설정:
```typescript
import { DndContext, closestCenter, type DragEndEvent } from '@dnd-kit/core'
import { SortableContext, verticalListSortingStrategy, useSortable, arrayMove } from '@dnd-kit/sortable'
import { CSS } from '@dnd-kit/utilities'
```

레이아웃 (per UI-SPEC 02-C):
- 상단 도구 모음: [엑셀 양식 다운로드] + [엑셀 업로드] + [문항 추가] 버튼
- 좌측 (Col span={14}): 문항 목록 (SortableContext 내)
  - 각 문항 행: 드래그 핸들(HolderOutlined, 24x24px, cursor grab) + 문항번호 + 문항 미리보기 텍스트 + 유형 Tag + 필수 체크 + 삭제 버튼
  - 드래그 중: opacity 0.5, border 2px dashed #003366
  - 드롭 완료: arrayMove로 재정렬, orderIndex 자동 재계산
- 우측 (Col span={10}): 선택된 문항 편집 영역
  - questionText (문항 내용): TextArea, required, maxLength 500
  - questionType (문항 유형): Select, required, options=['radio','checkbox','textarea','rate'] (per D-05: 단일선택 Radio, 복수선택 Checkbox, 주관식 TextArea, 평점 Rate)
  - isRequired (필수 응답): Switch, default true
  - options (선택지 목록): Form.List 동적 -- questionType이 radio 또는 checkbox일 때만 표시. 각 선택지: Input + 삭제 버튼, [선택지 추가] 버튼
- 하단: [문항 추가] 버튼 (type="dashed", 전체 너비)

각 SortableItem 컴포넌트:
```typescript
function SortableQuestionItem({ question, isSelected, onSelect, onDelete }: Props) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: question.id })
  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1,
    border: isDragging ? '2px dashed #003366' : undefined,
  }
  // ...
}
```

문항 저장: PUT /sys02/surveys/:id/questions (body: 전체 문항 배열, orderIndex 포함)

**테스트 파일** (`survey.test.ts`)

readFileSync 기반:
- MySurveyPage: DataTable, StatusBadge, CrudForm, '설문명' 컬럼, '상태' 컬럼
- SurveyQuestionEditor: '@dnd-kit' import, 'DndContext', 'SortableContext', 'useSortable', 'HolderOutlined', '문항 추가'
- SurveyParticipationPage: DataTable, '설문참여'
- SurveyFormPage: Radio 또는 Checkbox, Rate, '제출'
- SurveyResultPage: '@ant-design/charts', 'Bar'
- PastSurveyPage: DataTable, 'closed'
- SurveyAdminPage: '승인', '반려', 'Tabs', '카테고리', '통계', '대상자', '템플릿' (6탭 검증)
- sys02.ts: sys02Handlers, '/sys02/surveys', '/sys02/surveys/participation', '/sys02/surveys/pending', '/sys02/categories', '/sys02/templates', '/sys02/stats'
- index.tsx: 라우트 매핑
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys02 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sys02.ts에 "sys02Handlers" export 포함
    - sys02.ts에 "/sys02/surveys" 포함
    - sys02.ts에 "/sys02/categories" 포함
    - sys02.ts에 "/sys02/templates" 포함
    - sys02.ts에 "/sys02/stats" 포함
    - sys02.ts에 "extends Record<string, unknown>" 포함
    - MySurveyPage.tsx에 "DataTable" 포함
    - MySurveyPage.tsx에 "StatusBadge" 포함
    - MySurveyPage.tsx에 "surveyName" 컬럼 포함
    - SurveyQuestionEditor.tsx에 "dnd-kit" 또는 "@dnd-kit" import 포함
    - SurveyQuestionEditor.tsx에 "DndContext" 포함
    - SurveyQuestionEditor.tsx에 "SortableContext" 포함
    - SurveyQuestionEditor.tsx에 "useSortable" 포함
    - SurveyQuestionEditor.tsx에 "HolderOutlined" 포함
    - SurveyQuestionEditor.tsx에 "문항 추가" 텍스트 포함
    - index.ts에 "sys02Handlers" import 포함
  </acceptance_criteria>
  <done>MSW 핸들러(29 엔드포인트, 카테고리/템플릿/대상자/통계 포함), 나의설문관리(CRUD+상태전환), 문항편집기(dnd-kit DnD+4유형) 완성</done>
</task>

<task type="auto">
  <name>Task 2: 설문참여 + 응답폼 + 결과분석 + 지난설문 + 체계관리(6탭) + 라우트 (SURV-01,03,04~07)</name>
  <files>
    navy-admin/src/pages/sys02-survey/SurveyParticipationPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyFormPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyResultPage.tsx,
    navy-admin/src/pages/sys02-survey/PastSurveyPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyAdminPage.tsx,
    navy-admin/src/pages/sys02-survey/index.tsx
  </files>
  <read_first>
    navy-admin/src/pages/sys04-certificate/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys02.ts,
    navy-admin/src/pages/sys02-survey/MySurveyPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**SurveyParticipationPage.tsx** (SURV-01: 설문참여 목록)

PageContainer title="설문참여".

DataTable 컬럼 (per UI-SPEC 02-D):
- surveyName (설문명): width 300, text 링크 (클릭 시 SurveyFormPage로 이동)
- authorName (등록자): width 100, text
- startDate (시작일): width 120, date
- endDate (종료일): width 120, date
- isParticipated (참여 여부): width 100, StatusBadge (colorMap: true='green'/label='참여', false='default'/label='미참여')

데이터: GET /sys02/surveys/participation (active 상태 설문만)

**SurveyFormPage.tsx** (SURV-01: 설문 응답 폼)

per UI-SPEC 02-E:

antd Card 내부에 문항별 Form.Item 동적 렌더링:
- 설문 제목 Typography.Title level={3}
- 설문 개요 Typography.Paragraph
- 각 문항: Card 안에 문항번호 + 문항내용 (Typography.Text strong) + 필수 표시 (빨간 *)
- 문항 유형별 렌더링:
  - radio: antd Radio.Group (선택지 목록)
  - checkbox: antd Checkbox.Group (선택지 목록)
  - textarea: antd TextArea (자유 입력)
  - rate: antd Rate (별점 1~5)
- 하단: [제출] 버튼 (primary) + [임시 저장] 버튼 (default)
- 대상자 체크: 설문 대상이 아닌 경우 antd Result status="warning" + "본 설문의 대상자가 아닙니다."
- 제출 완료: antd Result status="success" + "설문 응답이 완료되었습니다."

**SurveyResultPage.tsx** (SURV-02/04: 설문 결과 분석)

per UI-SPEC 02-F, D-06:

상단 통계 요약: antd Row + Col, Card + Statistic (총 대상자, 응답자, 응답률 %)

문항별 Card 나열:
- 각 문항 Card:
  - 문항 내용 (Heading)
  - 객관식 (radio/checkbox): @ant-design/charts Bar (가로 막대) -- 선택지별 응답 수/비율
    - `import { Bar } from '@ant-design/charts'`
    - config: { data: [{option, count, ratio}], xField: 'count', yField: 'option', height: 200 }
  - 주관식 (textarea): antd List -- 응답 텍스트 나열 (pagination)
  - 평점 (rate): antd Rate (평균 값, disabled) + @ant-design/charts Bar (점수 분포: 1점~5점)

[엑셀 다운로드] 버튼: 전체 결과 엑셀 출력

결과 공개 규칙: isPublicResult=true인 설문만 결과 조회 가능. 비공개 시 antd Result status="info" + "결과가 비공개 처리된 설문입니다."

**PastSurveyPage.tsx** (SURV-03: 지난 설문보기)

per UI-SPEC 02-I:
MySurveyPage와 동일 컬럼 구조. 데이터: GET /sys02/surveys/past (closed 상태만).
결과 조회 시 SurveyResultPage 재사용. 결과 공개 설문만 조회 가능.

**SurveyAdminPage.tsx** (SURV-04: 체계관리 -- 13개 프로세스를 6개 탭으로 커버)

antd Tabs로 6탭 구성:

Tab 1 -- 승인 대기 (per UI-SPEC 02-G):
DataTable 컬럼:
- surveyName (설문명): width 250, text 링크
- authorName (등록자): width 100, text
- submittedAt (제출일): width 120, date
- targetCount (대상자 수): width 100, number
- questionCount (문항 수): width 80, number

상세 조회: 설문 설정 + 문항 목록 읽기 전용 표시
액션: [승인] 버튼 (primary) + [반려] 버튼 (danger) -- 반려 시 사유 입력 Modal

Tab 2 -- 전체 설문관리 (per UI-SPEC 02-H):
DataTable 컬럼:
- surveyName (설문명): width 200, text 링크
- status (상태): width 100, StatusBadge, sortable
- authorName (등록자): width 100, text, sortable
- targetCount (대상자 수): width 100, number, sortable
- responseCount (응답자 수): width 100, number, sortable
- startDate (시작일): width 120, date, sortable
- endDate (종료일): width 120, date, sortable

액션 드롭다운: 설정 수정, 문항 수정, 삭제, 결과 조회

Tab 3 -- 카테고리 관리 (SURV-04 카테고리관리):
DataTable + CrudForm 패턴.
데이터: GET /sys02/categories
DataTable 컬럼:
- categoryName (카테고리명): width 200, text 링크
- description (설명): width 300, text
- surveyCount (설문 수): width 100, number
- sortOrder (정렬순서): width 80, number, sortable
- createdAt (등록일): width 120, date

toolBarRender: [카테고리 추가] 버튼 (primary)
CrudForm fields:
- categoryName (카테고리명): text, required, maxLength 50
- description (설명): TextArea, maxLength 200
- sortOrder (정렬순서): number, required, default 0

Tab 4 -- 통계 (SURV-04 통계):
@ant-design/charts Bar 차트: 월별 설문 수 + 응답률 추이.
데이터: GET /sys02/stats
- `import { Bar } from '@ant-design/charts'`
- 상단: Card + Statistic 4개 (총 설문 수, 진행중, 마감, 평균 응답률)
- 중앙: Bar 차트 (월별 설문 등록/마감/응답 수)
- 하단: DataTable -- 카테고리별 설문 수, 응답률 요약

Tab 5 -- 대상자 관리 (SURV-04 대상자관리):
DataTable로 설문별 대상자 목록 표시.
상단: 설문 선택 Select (활성/전체 설문 목록)
데이터: GET /sys02/surveys/:id/targets
DataTable 컬럼:
- targetName (대상자명): width 100, text
- targetUnit (소속부대): width 150, text
- targetRank (직급/계급): width 100, text
- isResponded (응답 여부): width 100, StatusBadge (true='green'/label='완료', false='default'/label='미응답')
- respondedAt (응답일시): width 150, date

액션: [대상자 추가] 버튼 (primary) -- 부대/직급 기반 대상자 일괄 추가 Modal
[미응답자 알림] 버튼 (default) -- 미응답자에게 알림 발송 (Mock)

Tab 6 -- 설문 템플릿 (SURV-04 설문템플릿):
DataTable + CrudForm 패턴.
데이터: GET /sys02/templates
DataTable 컬럼:
- templateName (템플릿명): width 200, text 링크
- description (설명): width 250, text
- categoryId (카테고리): width 120, text (카테고리명으로 표시)
- questionCount (문항 수): width 80, number
- authorName (작성자): width 100, text
- createdAt (등록일): width 120, date

toolBarRender: [템플릿 등록] 버튼 (primary)
CrudForm fields:
- templateName (템플릿명): text, required, maxLength 100
- description (설명): TextArea, maxLength 500
- categoryId (카테고리): Select, required, options=카테고리 목록에서 로드
- 저장 시 현재 선택된 설문의 문항을 템플릿으로 복사

상세 조회: 템플릿 문항 목록 읽기전용 표시
액션: [이 템플릿으로 설문 생성] 버튼 -- 템플릿 문항을 복사하여 새 설문 draft 생성

**index.tsx** (라우트 매핑)

menus.ts 기반:
```
Route index -> Navigate to="/sys02/1/2" replace
Route "1/1" -> BoardIndex (lazy, SURV-05 게시판)
Route "1/2" -> MySurveyPage (SURV-02 나의 설문관리)
Route "1/2/edit/:id" -> SurveyQuestionEditor (문항 편집)
Route "1/3" -> SurveyParticipationPage (SURV-01 설문참여)
Route "1/3/:id" -> SurveyFormPage (설문 응답)
Route "1/4" -> PastSurveyPage (SURV-03 지난 설문보기)
Route "1/5" -> SurveyAdminPage (SURV-04 체계관리)
Route "2/1" -> CodeMgmtIndex (lazy, SURV-06 공통코드관리)
Route "2/2" -> AuthGroupIndex (lazy, SURV-07 권한관리)
```

결과 분석은 MySurveyPage/PastSurveyPage/SurveyAdminPage에서 Modal 또는 서브라우트로 SurveyResultPage 호출.

per D-26: 게시판은 Phase 1 공통게시판 재사용
per D-27: 코드관리/권한관리는 Phase 1 재사용
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys02 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SurveyParticipationPage.tsx에 "DataTable" 포함
    - SurveyParticipationPage.tsx에 "설문참여" 또는 "surveyName" 포함
    - SurveyFormPage.tsx에 "Radio" 포함
    - SurveyFormPage.tsx에 "Checkbox" 포함
    - SurveyFormPage.tsx에 "Rate" 포함
    - SurveyFormPage.tsx에 "제출" 텍스트 포함
    - SurveyResultPage.tsx에 "@ant-design/charts" import 포함
    - SurveyResultPage.tsx에 "Bar" 포함
    - SurveyResultPage.tsx에 "Statistic" 포함
    - SurveyAdminPage.tsx에 "승인" 텍스트 포함
    - SurveyAdminPage.tsx에 "반려" 텍스트 포함
    - SurveyAdminPage.tsx에 "Tabs" import 포함
    - SurveyAdminPage.tsx에 "카테고리" 텍스트 포함
    - SurveyAdminPage.tsx에 "통계" 텍스트 포함
    - SurveyAdminPage.tsx에 "대상자" 텍스트 포함
    - SurveyAdminPage.tsx에 "템플릿" 텍스트 포함
    - PastSurveyPage.tsx에 "DataTable" 포함
    - index.tsx에 "/sys02/" 경로 매핑 포함
    - index.tsx에 "SurveyQuestionEditor" 포함
  </acceptance_criteria>
  <done>설문참여(목록+대상자체크), 응답폼(4유형 동적렌더링), 결과분석(Bar차트+텍스트+Rate분포), 지난설문, 체계관리(6탭: 승인대기/전체관리/카테고리/통계/대상자/템플릿), 라우트 매핑 완성. 모든 SURV-01~07 커버 (SURV-04 13개 프로세스 6탭으로 100% 커버)</done>
</task>

</tasks>

<verification>
cd navy-admin && npx vitest run src/__tests__/sys02 --reporter=verbose
cd navy-admin && npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- SYS02 7개 고유 페이지 + index.tsx 라우트 매핑 완성
- dnd-kit 문항 편집기: DndContext + SortableContext + useSortable 구현
- 4가지 문항 유형(radio/checkbox/textarea/rate) 지원
- 설문 상태 전환(draft->submitted->approved->active->closed) 동작
- @ant-design/charts Bar 결과분석 차트 구현
- SurveyAdminPage 6개 탭(승인대기/전체관리/카테고리/통계/대상자/템플릿) 구현
- MSW 핸들러 29개 엔드포인트 동작 (카테고리/템플릿/대상자/통계 포함)
- 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-a-6/04-04-SUMMARY.md`
</output>
