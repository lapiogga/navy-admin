---
phase: 04-a-6
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys12-directives/index.tsx
  - navy-admin/src/pages/sys12-directives/DirectiveProgressPage.tsx
  - navy-admin/src/pages/sys12-directives/DirectiveListPage.tsx
  - navy-admin/src/pages/sys12-directives/ProposalProgressPage.tsx
  - navy-admin/src/pages/sys12-directives/ProposalListPage.tsx
  - navy-admin/src/pages/sys12-directives/DirectiveAdminPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys12.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys12/directives.test.ts
autonomous: true
requirements:
  - DRCT-01
  - DRCT-02
  - DRCT-03
  - DRCT-04
  - DRCT-05
  - DRCT-06
  - DRCT-07

must_haves:
  truths:
    - "지시사항 추진현황에서 Card+Statistic 요약(총지시/완료/진행중/미착수/추진율)과 매트릭스 테이블(카테고리별 현황)이 표시"
    - "지시사항 목록에서 CRUD+검색이 동작하고, 조치사항 등록 서브 Modal과 이행 이력 Timeline이 표시"
    - "건의사항도 지시사항과 동일 패턴으로 추진현황/목록/CRUD/Timeline이 동작"
    - "대통령/국방부장관 지시사항은 Phase 1 공통게시판 읽기전용으로 재사용"
    - "관리자 페이지에서 지시/건의 관리, 카테고리, 통계(@ant-design/charts), 사용자/부대 설정이 동작"
  artifacts:
    - path: "navy-admin/src/pages/sys12-directives/DirectiveProgressPage.tsx"
      provides: "지시사항 추진현황 (DRCT-01)"
      contains: "Statistic"
    - path: "navy-admin/src/pages/sys12-directives/DirectiveListPage.tsx"
      provides: "지시사항 목록 (DRCT-01)"
      contains: "Timeline"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys12.ts"
      provides: "MSW Mock API"
      exports: ["sys12Handlers"]
  key_links:
    - from: "DirectiveProgressPage.tsx"
      to: "@ant-design/charts"
      via: "Bar chart (stacked)"
      pattern: "import.*@ant-design/charts"
    - from: "DirectiveListPage.tsx"
      to: "antd Timeline"
      via: "이행 이력 표시"
      pattern: "Timeline"
---

<objective>
SYS12 지시건의사항관리체계 32개 프로세스 구현

Purpose: Progress+Timeline+StatusBadge 조합 이행/처리현황 패턴과 Stacked Bar 통계 차트를 확립. 지시사항/건의사항 대칭 구조로 코드 재사용 극대화.
Output: 지시추진현황/지시목록/건의추진현황/건의목록/관리자 5개 고유 페이지 + 공통 재사용 + MSW + 테스트
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-a-6/04-CONTEXT.md
@.planning/phases/04-a-6/04-UI-SPEC.md
@.planning/phases/04-a-6/04-RESEARCH.md

@navy-admin/src/pages/sys04-certificate/index.tsx
@navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx
@navy-admin/src/shared/api/mocks/handlers/sys04.ts
@navy-admin/src/__tests__/sys04/certificate.test.ts
@navy-admin/src/entities/subsystem/menus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MSW 핸들러 + 테스트 + 지시사항 추진현황/목록 (DRCT-01)</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys12.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys12/directives.test.ts,
    navy-admin/src/pages/sys12-directives/DirectiveProgressPage.tsx,
    navy-admin/src/pages/sys12-directives/DirectiveListPage.tsx
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys04.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys04/certificate.test.ts,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**MSW 핸들러** (`sys12.ts`)

타입 정의 (`extends Record<string, unknown>` 필수):
```typescript
export type ProgressStatus = 'notStarted' | 'inProgress' | 'completed' | 'delayed'

export interface Directive extends Record<string, unknown> {
  id: string
  directiveNo: string       // 관리번호
  director: string          // 지시자
  directiveDate: string     // 지시일자
  targetUnit: string        // 수명부대
  content: string           // 지시내용
  progressStatus: ProgressStatus
  category: string          // 지시사항 종류
  attachments: string[]
  createdAt: string
}

export interface Proposal extends Record<string, unknown> {
  id: string
  proposalNo: string        // 관리번호
  proposer: string          // 건의자
  proposalDate: string      // 건의일자
  managingUnit: string      // 주관부대
  content: string           // 건의내용
  progressStatus: ProgressStatus
  category: string
  attachments: string[]
  createdAt: string
}

export interface ActionItem extends Record<string, unknown> {
  id: string
  parentId: string          // directive or proposal ID
  parentType: 'directive' | 'proposal'
  assignee: string          // 업무담당자
  progressStatus: ProgressStatus
  plan: string              // 추진계획
  result: string            // 추진내용
  attachments: string[]
  createdAt: string
}

export interface ActionHistory extends Record<string, unknown> {
  id: string
  parentId: string
  date: string
  assignee: string
  status: string
  content: string
}
```

Mock 데이터: 지시사항 20건, 건의사항 15건, 조치사항 30건, 이력 60건. 부대=['1사단','2사단','해병대사령부','교육훈련단','상륙기동단']. 카테고리=['작전','교육','군수','인사','정보화']. 지시자=['사령관','참모장','부사령관'].

핸들러:
- GET /sys12/directives — 지시사항 목록 (page, size, directiveDate, director, targetUnit, progressStatus, keyword)
- GET /sys12/directives/:id — 상세
- POST /sys12/directives — 등록
- PUT /sys12/directives/:id — 수정
- DELETE /sys12/directives/:id — 삭제
- GET /sys12/directives/:id/actions — 조치사항 목록
- POST /sys12/directives/:id/actions — 조치사항 등록
- GET /sys12/directives/:id/history — 이행 이력 (Timeline용)
- GET /sys12/directives/progress — 추진현황 통계 (반환: {total, completed, inProgress, notStarted, delayed, completionRate, categoryStats: [{category, totalCount, completedCount, inProgressCount, notStartedCount, delayedCount, completionRate}]})
- GET /sys12/proposals — 건의사항 목록
- GET /sys12/proposals/:id — 상세
- POST /sys12/proposals — 등록
- PUT /sys12/proposals/:id — 수정
- DELETE /sys12/proposals/:id — 삭제
- GET /sys12/proposals/:id/actions — 조치사항 목록
- POST /sys12/proposals/:id/actions — 조치사항 등록
- GET /sys12/proposals/:id/history — 처리 이력
- GET /sys12/proposals/progress — 처리현황 통계
- GET /sys12/admin/stats — 관리자 통계 (차트용)

export const sys12Handlers = [...] 형태.

**handlers/index.ts**: sys12Handlers import 및 spread 추가.

**DirectiveProgressPage.tsx** (DRCT-01: 지시사항 추진현황 매트릭스)

per UI-SPEC 12-A, D-17:

상단 통계 요약: antd Row + Col, Card + Statistic 5개:
- 총 지시 (total): 전체 수
- 완료 (completed): green valueStyle
- 진행중 (inProgress): blue valueStyle
- 미착수 (notStarted): default valueStyle
- 추진율 (completionRate): % 표시, antd Progress 포함

매트릭스 테이블 (per UI-SPEC 12-A):
DataTable 컬럼:
- category (구분(지시자)): width 150, text
- totalCount (지시 건수): width 100, number
- completedCount (완료): width 80, number (green color)
- inProgressCount (진행중): width 80, number (blue color)
- notStartedCount (미착수): width 80, number (default color)
- delayedCount (지연): width 80, number (red color)
- completionRate (추진율): width 120, antd Progress percent

카테고리 전환: antd Tabs 또는 Select로 지시자 구분별 통계 전환.
[엑셀 다운로드] 버튼.

**DirectiveListPage.tsx** (DRCT-01: 지시사항 목록/상세/등록/수정)

SearchForm fields (per UI-SPEC 12-B):
- directiveDate (지시일): DateRange
- director (지시자): Select, options=['사령관','참모장','부사령관']
- targetUnit (수명부대): Select, 부대 목록
- progressStatus (진행상황): Select, options=['미착수','진행중','완료','지연']
- keyword (지시내용): Input text

DataTable 컬럼 (per UI-SPEC 12-B):
- directiveNo (관리번호): width 100, text, sortable
- director (지시자): width 100, text, sortable
- directiveDate (지시일자): width 120, date, sortable
- targetUnit (수명부대): width 150, text, sortable
- content (지시내용): width 300, text (ellipsis), not sortable
- progressStatus (추진상태): width 100, StatusBadge (colorMap: notStarted='default', inProgress='blue', completed='green', delayed='red'; labelMap: notStarted='미착수', inProgress='진행중', completed='완료', delayed='지연')
- category (종류): width 100, text, sortable

toolBarRender: [지시사항 등록] 버튼 (primary)

CrudForm fields (per UI-SPEC 12-B):
- director (지시자): Select, required
- directiveDate (지시일자): date, required
- targetUnit (수명부대): Select multiple, required
- progressStatus (추진상태): Select, required, options=['미착수','진행중','완료','지연']
- content (지시내용): TextArea, required, maxLength 2000
- category (지시사항 종류): Select, required
- attachments (첨부파일): Upload, maxCount 5

상세 Modal 내:
1. 기본 정보 Descriptions
2. 조치사항 등록 서브 Modal (per UI-SPEC 12-B):
   - assignee (업무담당자): text, required
   - progressStatus (추진상태): Select, required, options=['미착수','진행중','완료']
   - plan (추진계획): TextArea, required
   - result (추진내용): TextArea
   - attachments (첨부파일): Upload
3. 이행 이력 Timeline (per D-18):
   - antd Timeline 컴포넌트
   - 각 항목: dot(StatusBadge color 동일 원형 아이콘) + [날짜] 담당자 - 상태변경 + 내용
   - 최신순 정렬 (상단이 최신)

**테스트 파일** (`directives.test.ts`)

readFileSync 기반:
- DirectiveProgressPage: Statistic, Progress, 'category' 컬럼, '추진율'
- DirectiveListPage: DataTable, CrudForm, StatusBadge, Timeline, '지시사항' 텍스트
- ProposalProgressPage: Statistic, Progress
- ProposalListPage: DataTable, CrudForm, StatusBadge, Timeline, '건의사항' 텍스트
- DirectiveAdminPage: '@ant-design/charts' 또는 통계
- sys12.ts: sys12Handlers, '/sys12/directives', '/sys12/proposals'
- index.tsx: 라우트 매핑
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys12 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sys12.ts에 "sys12Handlers" export 포함
    - sys12.ts에 "/sys12/directives" 포함
    - sys12.ts에 "/sys12/proposals" 포함
    - sys12.ts에 "extends Record<string, unknown>" 포함
    - DirectiveProgressPage.tsx에 "Statistic" 포함
    - DirectiveProgressPage.tsx에 "Progress" 포함
    - DirectiveProgressPage.tsx에 "category" 컬럼 포함
    - DirectiveListPage.tsx에 "DataTable" 포함
    - DirectiveListPage.tsx에 "StatusBadge" 포함
    - DirectiveListPage.tsx에 "Timeline" 포함
    - DirectiveListPage.tsx에 "지시사항" 텍스트 포함
    - index.ts에 "sys12Handlers" import 포함
  </acceptance_criteria>
  <done>MSW 핸들러(19 엔드포인트), 지시사항 추진현황(Statistic+매트릭스), 지시사항 목록(CRUD+조치+Timeline), 테스트 완성</done>
</task>

<task type="auto">
  <name>Task 2: 건의사항 + 관리자 + 라우트 + 공통 재사용 (DRCT-02~07)</name>
  <files>
    navy-admin/src/pages/sys12-directives/ProposalProgressPage.tsx,
    navy-admin/src/pages/sys12-directives/ProposalListPage.tsx,
    navy-admin/src/pages/sys12-directives/DirectiveAdminPage.tsx,
    navy-admin/src/pages/sys12-directives/index.tsx
  </files>
  <read_first>
    navy-admin/src/pages/sys12-directives/DirectiveProgressPage.tsx,
    navy-admin/src/pages/sys12-directives/DirectiveListPage.tsx,
    navy-admin/src/pages/sys04-certificate/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys12.ts,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**ProposalProgressPage.tsx** (DRCT-04: 건의사항 처리현황)

DirectiveProgressPage.tsx와 동일 패턴. 필드명만 변경 (per UI-SPEC 12-C):
- "지시" -> "건의"
- director -> proposer (건의자)
- directiveDate -> proposalDate (건의일자)
- targetUnit -> managingUnit (주관부대)
- API: GET /sys12/proposals/progress

Statistic 5개: 총 건의 / 완료 / 진행중 / 미착수 / 처리율

**ProposalListPage.tsx** (DRCT-04: 건의사항 목록/상세/등록/수정)

DirectiveListPage.tsx와 동일 패턴. 필드명 변경 (per UI-SPEC 12-C):

SearchForm: 건의일(proposalDate DateRange), 건의자(proposer Select), 주관부대(managingUnit Select), 추진상태(progressStatus Select), 건의내용(keyword text)

DataTable 컬럼: proposalNo(관리번호), proposer(건의자), proposalDate(건의일자), managingUnit(주관부대), content(건의내용 ellipsis), progressStatus(추진상태 StatusBadge), category(종류)

CrudForm: proposer(건의자 Select required), proposalDate(건의일자 date required), managingUnit(주관부대 Select multiple required), progressStatus(추진상태 Select required), content(건의내용 TextArea required maxLength 2000), category(종류 Select required), attachments(Upload maxCount 5)

조치사항 + Timeline: DirectiveListPage와 동일 패턴 (API: /sys12/proposals/:id/actions, /sys12/proposals/:id/history)

toolBarRender: [건의사항 등록] 버튼 (primary)

**DirectiveAdminPage.tsx** (DRCT-05: 관리자)

PageContainer title="관리자". antd Tabs로 여러 탭:

Tab 1 — 지시사항관리: DirectiveListPage와 동일한 DataTable. 관리자 권한으로 모든 지시 관리(수정/삭제) 가능.
Tab 2 — 건의사항관리: ProposalListPage와 동일한 DataTable. 관리자 권한.
Tab 3 — 카테고리 관리: DataTable + CrudForm (카테고리명, 정렬순서, 사용여부)
Tab 4 — 통계: @ant-design/charts Bar (스택) — 카테고리별 지시/건의 처리현황 그래프
  - `import { Bar } from '@ant-design/charts'`
  - data: [{category, count, status}], xField: 'count', yField: 'category', seriesField: 'status', isStack: true, height: 300
Tab 5 — 사용자 관리: DataTable (사용자 목록, 권한 설정)
Tab 6 — 부대 관리: DataTable (부대 목록)
Tab 7 — 기간 설정: 지시/건의 관리 기간 설정 폼
Tab 8 — 알림 설정: 알림 주기/대상 설정 폼
Tab 9 — 권한 관리: Phase 1 auth-group 재사용

(Tab 5~9는 간단한 DataTable 또는 설정 폼으로 구현. D-05 관리자 기능 9개 커버)

**index.tsx** (라우트 매핑)

menus.ts 기반:
```
Route index -> Navigate to="/sys12/2/3" replace
Route "1/1" -> BoardIndex (lazy, DRCT-06 공지사항)
Route "1/2" -> BoardIndex (lazy, DRCT-07 질의응답, boardType='qna')
Route "2/1" -> BoardIndex (lazy, DRCT-02 대통령 지시사항, boardType='president', 읽기전용)
Route "2/2" -> BoardIndex (lazy, DRCT-03 국방부장관 지시사항, boardType='minister', 읽기전용)
Route "2/3" -> DirectiveProgressPage + DirectiveListPage (DRCT-01 지시사항)
Route "3/1" -> ProposalProgressPage + ProposalListPage (DRCT-04 건의사항)
Route "4/1" -> DirectiveAdminPage (DRCT-05 관리자)
```

참고: "2/3" 경로에서 추진현황과 목록을 어떻게 배치할지 — Tabs로 통합하거나, 추진현황을 상단, 목록을 하단에 배치. 또는 별도 서브라우트 사용. menus.ts에 지시사항이 단일 메뉴이므로 Tabs(추진현황/목록) 통합 권장.

per D-26: 공지사항/질의응답/대통령/국방부장관 모두 Phase 1 공통게시판 재사용
per D-25: 메인화면 대시보드 패턴은 DirectiveProgressPage가 대신함 (Card+Statistic 이미 포함)
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys12 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ProposalProgressPage.tsx에 "Statistic" 포함
    - ProposalProgressPage.tsx에 "Progress" 포함
    - ProposalListPage.tsx에 "DataTable" 포함
    - ProposalListPage.tsx에 "StatusBadge" 포함
    - ProposalListPage.tsx에 "Timeline" 포함
    - ProposalListPage.tsx에 "건의사항" 텍스트 포함
    - DirectiveAdminPage.tsx에 "Tabs" 포함
    - DirectiveAdminPage.tsx에 "@ant-design/charts" 또는 "Bar" 포함
    - index.tsx에 "/sys12/" 경로 매핑 포함
    - index.tsx에 "common/board" lazy import 포함
  </acceptance_criteria>
  <done>건의사항(추진현황+목록+CRUD+Timeline), 관리자(9탭: 관리/카테고리/통계/사용자/부대/기간/알림/권한), 라우트 매핑 완성. 모든 DRCT-01~07 커버</done>
</task>

</tasks>

<verification>
cd navy-admin && npx vitest run src/__tests__/sys12 --reporter=verbose
cd navy-admin && npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- SYS12 5개 고유 페이지(지시추진/지시목록/건의추진/건의목록/관리자) + index.tsx 완성
- Progress + StatusBadge 이행/처리현황 패턴 구현
- Timeline 이행/처리 이력 구현
- Stacked Bar 통계 차트 구현
- 대통령/국방부장관 게시판 읽기전용 재사용
- MSW 핸들러 19개 엔드포인트 동작
- 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-a-6/04-05-SUMMARY.md`
</output>
