---
phase: 04-a-6
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - navy-admin/src/pages/sys02-survey/index.tsx
  - navy-admin/src/pages/sys02-survey/SurveyListPage.tsx
  - navy-admin/src/pages/sys02-survey/SurveyFormPage.tsx
  - navy-admin/src/pages/sys02-survey/MySurveyPage.tsx
  - navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx
  - navy-admin/src/pages/sys02-survey/PastSurveyPage.tsx
  - navy-admin/src/pages/sys02-survey/SurveyAdminPage.tsx
  - navy-admin/src/pages/sys02-survey/SurveyResultPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys02.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys02/survey.test.ts
autonomous: true
requirements: [SURV-01, SURV-02, SURV-03, SURV-04, SURV-05, SURV-06, SURV-07]

must_haves:
  truths:
    - "사용자가 설문 목록에서 참여하고 응답을 제출할 수 있다"
    - "관리자가 설문을 생성하고 문항을 편집(추가/삭제/재정렬)할 수 있다"
    - "설문 문항 유형 4종(단일선택/복수선택/주관식/평점)이 동작한다"
    - "설문 배포/마감 상태 전환(draft->active->closed)이 동작한다"
    - "결과분석에서 문항별 응답 통계가 차트로 표시된다"
    - "체계관리에서 설문/사용자/카테고리/통계/템플릿/대상자를 관리할 수 있다"
  artifacts:
    - path: "navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx"
      provides: "문항 편집기 (Form.List + dnd-kit)"
      contains: "DndContext"
    - path: "navy-admin/src/pages/sys02-survey/SurveyFormPage.tsx"
      provides: "설문 응답 폼"
      contains: "Radio"
    - path: "navy-admin/src/pages/sys02-survey/SurveyResultPage.tsx"
      provides: "결과 분석 (Bar 차트)"
      contains: "Bar"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys02.ts"
      provides: "설문 MSW 핸들러"
      min_lines: 100
  key_links:
    - from: "navy-admin/src/pages/sys02-survey/MySurveyPage.tsx"
      to: "navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx"
      via: "문항편집기 컴포넌트 import"
      pattern: "SurveyQuestionEditor"
    - from: "navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx"
      to: "@dnd-kit/core"
      via: "DndContext import"
      pattern: "import.*@dnd-kit/core"
    - from: "navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx"
      to: "@dnd-kit/sortable"
      via: "SortableContext, useSortable import"
      pattern: "import.*@dnd-kit/sortable"
---

<objective>
sys02 설문종합관리체계 구현 - 설문 문항 편집기(dnd-kit) + 결과분석 차트 패턴

Purpose: Phase 4에서 가장 복잡한 서브시스템. antd Form.List + @dnd-kit/sortable로 문항 편집기를 구현하고(D-04~07), 설문 참여/응답/결과분석, 체계관리를 포함한 31개 프로세스를 완성한다. Phase 5~7의 동적 폼 편집 패턴의 기반이 된다.
Output: sys02 7개 요구사항 페이지, SurveyQuestionEditor 컴포넌트, MSW 핸들러, 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-a-6/04-CONTEXT.md
@.planning/phases/04-a-6/04-RESEARCH.md
@navy-admin/src/shared/ui/DataTable/DataTable.tsx
@navy-admin/src/shared/ui/CrudForm/CrudForm.tsx
@navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx
@navy-admin/src/shared/api/mocks/handlers/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: sys02 설문참여/응답/결과 + 문항편집기 컴포넌트</name>
  <files>
    navy-admin/src/pages/sys02-survey/index.tsx,
    navy-admin/src/pages/sys02-survey/SurveyListPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyFormPage.tsx,
    navy-admin/src/pages/sys02-survey/MySurveyPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx,
    navy-admin/src/pages/sys02-survey/PastSurveyPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyResultPage.tsx
  </files>
  <read_first>
    navy-admin/src/pages/sys02-survey/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/DetailModal/DetailModal.tsx,
    navy-admin/src/pages/common/board/index.tsx,
    navy-admin/src/pages/common/code-mgmt/CodeManagementPage.tsx,
    navy-admin/src/pages/common/auth-group/index.tsx,
    navy-admin/package.json
  </read_first>
  <action>
    1. sys02 index.tsx 라우트 (per D-24, menus.ts sys02 구조):
       - Routes:
         - `/sys02/1/1` → BoardIndex (SURV-05 게시판 - lazy)
         - `/sys02/1/2` → MySurveyPage (SURV-02 나의 설문관리)
         - `/sys02/1/3` → SurveyListPage (SURV-01 설문참여)
         - `/sys02/1/4` → PastSurveyPage (SURV-03 지난 설문보기)
         - `/sys02/1/5` → SurveyAdminPage (SURV-04 체계관리) — Task 2에서 구현
         - `/sys02/2/1` → CodeMgmtPage (SURV-06 공통코드관리 - lazy)
         - `/sys02/2/2` → AuthGroupIndex (SURV-07 권한관리 - lazy)
       - index → Navigate to `/sys02/1/3`
    
    2. SurveyListPage.tsx (SURV-01 — 설문참여):
       - DataTable: 설문제목, 설문기간, 카테고리, 참여현황(N/M명), 상태(StatusBadge: active)
       - active 상태 설문만 표시
       - '참여' 버튼 클릭 시 SurveyFormPage를 Modal로 표시
       - '결과보기' 버튼 (closed 상태 설문): SurveyResultPage를 Modal로 표시
       - API: GET `/api/sys02/surveys?status=active`
    
    3. SurveyFormPage.tsx (SURV-01 설문응답):
       - Modal 내부 antd Form으로 구현
       - 서버에서 받은 문항(questions) 배열을 순회하여 동적 렌더링:
         - type='radio': antd Radio.Group (보기 목록)
         - type='checkbox': antd Checkbox.Group (보기 목록)
         - type='text': antd Input.TextArea
         - type='rate': antd Rate (per D-05)
       - 제출: POST `/api/sys02/surveys/:id/responses` body: `{ answers: [{questionId, value}] }`
       - 제출 후 Modal 닫기 + 목록 갱신
    
    4. SurveyQuestionEditor.tsx (SURV-02 문항편집 — per D-04, D-05):
       - Props: `{ surveyId: string, questions: Question[], onSave: (questions: Question[]) => void }`
       - antd Form.List name="questions" + @dnd-kit/sortable:
         ```tsx
         import { DndContext, closestCenter, PointerSensor, useSensor, useSensors } from '@dnd-kit/core'
         import { SortableContext, useSortable, verticalListSortingStrategy, arrayMove } from '@dnd-kit/sortable'
         import { CSS } from '@dnd-kit/utilities'
         ```
       - SortableQuestion 컴포넌트:
         - useSortable({ id: field.key }) 사용
         - 드래그 핸들 (cursor: grab)
         - 문항 유형 선택: Radio.Group (radio/checkbox/text/rate) — per D-05
         - 문항 텍스트: Input (질문 내용)
         - 보기 관리 (radio/checkbox일 때): Form.List nested로 보기 추가/삭제
         - 삭제 버튼: danger Button
       - onDragEnd: fields.findIndex 기반 key 탐색 → move(oldIndex, newIndex) — Pitfall 2 방어
       - '문항 추가' 버튼: add({ type: 'radio', text: '', options: [] })
       - '저장' 버튼: onSave(formValues.questions) → PUT `/api/sys02/surveys/:id/questions`
    
    5. MySurveyPage.tsx (SURV-02 — 8개 프로세스, per D-07):
       - DataTable: 설문제목, 생성일, 상태(StatusBadge: draft/active/closed), 응답수, 카테고리
       - CrudForm으로 설문 생성/수정:
         - 필드: 설문제목(Input), 카테고리(Select), 설문설명(TextArea), 시작일(DatePicker), 종료일(DatePicker)
       - 상태별 액션 (per D-07):
         - draft: '편집', '삭제', '배포' 버튼
         - active: '마감' 버튼, 문항 편집 비활성화 (Pitfall 5 방어)
         - closed: '결과분석' 버튼만
       - '편집' 클릭 시: Modal로 SurveyQuestionEditor 표시 (draft 상태만)
       - '배포' 클릭 시 (per D-07):
         - Popconfirm "배포하시겠습니까?"
         - 대상자 선택 Modal: 부대(Select multi), 직급(Select multi) 기반 다중 선택
         - PUT `/api/sys02/surveys/:id/deploy` body: `{ targetUnits: [], targetRanks: [] }`
         - status draft → active
       - '마감': PUT `/api/sys02/surveys/:id/close` → status active → closed
       - '결과분석' 클릭 시: Modal로 SurveyResultPage 표시
       - 저장: CSV
       - API: GET/POST/PUT/DELETE `/api/sys02/surveys`
    
    6. SurveyResultPage.tsx (SURV-02 결과분석, SURV-03 결과보기 — per D-06):
       - Props: `{ surveyId: string }`
       - Modal 또는 독립 페이지로 사용 가능
       - 설문 정보 요약: antd Descriptions
       - 문항별 응답 통계:
         - radio/checkbox: `import { Bar } from '@ant-design/charts'` — xField='option', yField='responseCount'
         - text: 응답 목록 DataTable
         - rate: 평균 점수 antd Statistic + Bar 분포
       - 전체 응답률: antd Progress
       - API: GET `/api/sys02/surveys/:id/results`
    
    7. PastSurveyPage.tsx (SURV-03):
       - DataTable: 설문제목, 참여기간, 카테고리, 나의응답상태
       - 상세 조회: DetailModal
       - '결과보기' 버튼: SurveyResultPage Modal
       - API: GET `/api/sys02/surveys?status=closed`
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys02 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sys02-survey/index.tsx contains routes "1/1" through "2/2"
    - sys02-survey/index.tsx contains "MySurveyPage" and "SurveyListPage" and "SurveyAdminPage"
    - SurveyQuestionEditor.tsx contains "import { DndContext" or "import { DndContext," from "@dnd-kit/core"
    - SurveyQuestionEditor.tsx contains "import { SortableContext" or "import { useSortable" from "@dnd-kit/sortable"
    - SurveyQuestionEditor.tsx contains "Form.List" and "Radio" and "Checkbox" and "Rate"
    - SurveyFormPage.tsx contains "Radio.Group" and "Checkbox.Group" and "Rate" and "TextArea"
    - MySurveyPage.tsx contains "SurveyQuestionEditor" and "deploy" and "close"
    - MySurveyPage.tsx contains "draft" and "active" and "closed"
    - SurveyResultPage.tsx contains "Bar" and "@ant-design/charts"
    - PastSurveyPage.tsx contains "DataTable"
  </acceptance_criteria>
  <done>설문 참여/응답/문항편집기(dnd-kit)/결과분석/지난설문 구현 완료. 문항 4종 유형 + 드래그 정렬 동작.</done>
</task>

<task type="auto">
  <name>Task 2: sys02 체계관리 + MSW 핸들러 + 테스트</name>
  <files>
    navy-admin/src/pages/sys02-survey/SurveyAdminPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys02.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys02/survey.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/api/mocks/handlers/sys14.ts,
    navy-admin/src/pages/sys02-survey/MySurveyPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx,
    navy-admin/src/pages/sys02-survey/SurveyFormPage.tsx,
    navy-admin/src/pages/sys02-survey/SurveyResultPage.tsx
  </read_first>
  <action>
    1. SurveyAdminPage.tsx (SURV-04 — 13개 프로세스):
       - PageContainer + antd Tabs: '설문관리' | '사용자관리' | '카테고리관리' | '통계' | '설문템플릿' | '대상자관리'
       
       a) 설문관리 탭:
         - DataTable: 전체 설문 목록 (관리자 뷰)
         - 설문 상태 변경(강제 마감, 삭제), 응답 데이터 초기화
       
       b) 사용자관리 탭:
         - DataTable: 사용자 목록, 역할(관리자/일반) 변경
       
       c) 카테고리관리 탭:
         - DataTable + CrudForm: 설문 카테고리 CRUD (업무/교육/만족도/기타)
       
       d) 통계 탭 (per D-08):
         - `import { Bar, Pie } from '@ant-design/charts'`
         - 카테고리별 설문 건수 Bar
         - 참여율 추이 Line
         - 응답 완료율 Pie
         - antd Statistic + Card: 총 설문수, 진행 중, 평균 참여율
       
       e) 설문템플릿 탭:
         - DataTable: 템플릿 목록
         - CrudForm: 템플릿 생성 (이름, 설명, 기본 문항 세트)
         - '템플릿에서 설문 생성' 버튼
       
       f) 대상자관리 탭:
         - DataTable: 대상 그룹(부대/직급 조합) 관리
         - CrudForm: 그룹명, 부대(multi Select), 직급(multi Select)
    
    2. sys02 MSW 핸들러 (shared/api/mocks/handlers/sys02.ts):
       - faker.js (ko locale) Mock 데이터
       - 문항 유형별 Mock 데이터 생성:
         - radio/checkbox: faker로 보기 4~6개
         - text: 빈 문자열
         - rate: min 1, max 5
       - 설문 CRUD:
         - GET `/api/sys02/surveys` — 페이징 목록 (status 필터)
         - POST `/api/sys02/surveys` — 생성
         - PUT `/api/sys02/surveys/:id` — 수정
         - DELETE `/api/sys02/surveys/:id` — 삭제
         - GET `/api/sys02/surveys/:id` — 상세 (문항 포함)
       - 문항:
         - GET `/api/sys02/surveys/:id/questions` — 문항 목록
         - PUT `/api/sys02/surveys/:id/questions` — 문항 일괄 저장
       - 응답:
         - POST `/api/sys02/surveys/:id/responses` — 응답 제출
         - GET `/api/sys02/surveys/:id/results` — 결과 분석 (문항별 집계)
       - 배포/마감:
         - PUT `/api/sys02/surveys/:id/deploy` — 배포 (status: draft→active)
         - PUT `/api/sys02/surveys/:id/close` — 마감 (status: active→closed)
       - 관리자:
         - GET `/api/sys02/admin/categories` — 카테고리 CRUD
         - POST/PUT/DELETE `/api/sys02/admin/categories`
         - GET `/api/sys02/admin/stats` — 종합 통계
         - GET `/api/sys02/admin/templates` — 템플릿 CRUD
         - POST/PUT/DELETE `/api/sys02/admin/templates`
         - GET `/api/sys02/admin/target-groups` — 대상자 그룹 CRUD
         - POST/PUT/DELETE `/api/sys02/admin/target-groups`
       - 상태 전환 방어 (Pitfall 5):
         - deploy: status !== 'draft' → 400 에러
         - close: status !== 'active' → 400 에러
         - 문항 수정: status !== 'draft' → 400 에러
       - `export const sys02Handlers = [...]`
    
    3. handlers/index.ts 수정:
       - `import { sys02Handlers } from './sys02'` 추가
       - handlers 배열에 `...sys02Handlers` 추가
    
    4. 테스트 (sys02/survey.test.ts):
       - readFileSync 기반 검증
       - index.tsx: 라우트 확인
       - SurveyQuestionEditor.tsx: DndContext, SortableContext, useSortable, Form.List, radio/checkbox/text/rate 확인
       - MySurveyPage.tsx: SurveyQuestionEditor, deploy, close, draft/active/closed 확인
       - SurveyFormPage.tsx: Radio.Group, Checkbox.Group, Rate, TextArea 확인
       - SurveyResultPage.tsx: Bar, @ant-design/charts 확인
       - SurveyAdminPage.tsx: Tabs 6개 탭, Bar, Pie, Statistic 확인
       - sys02.ts: /api/sys02/surveys, /api/sys02/surveys/:id/questions, /api/sys02/surveys/:id/deploy, /api/sys02/admin/ 확인
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys02 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SurveyAdminPage.tsx contains "Tabs" with at least 6 tab items
    - SurveyAdminPage.tsx contains "Bar" or "Pie" from "@ant-design/charts"
    - SurveyAdminPage.tsx contains "Statistic" and "Card"
    - sys02.ts contains "sys02Handlers"
    - sys02.ts contains "/api/sys02/surveys" and "/api/sys02/surveys/:id/questions"
    - sys02.ts contains "/api/sys02/surveys/:id/deploy" and "/api/sys02/surveys/:id/close"
    - sys02.ts contains "/api/sys02/admin/categories" and "/api/sys02/admin/stats"
    - sys02.ts contains status check for deploy (draft check) — "draft" and "400"
    - handlers/index.ts contains "sys02Handlers"
    - vitest run sys02 tests exit 0
  </acceptance_criteria>
  <done>sys02 설문종합관리체계 7개 요구사항 완전 구현. 문항편집기(dnd-kit) + 결과분석(차트) + 체계관리 6탭 동작.</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npm run build` 빌드 성공
- `cd navy-admin && npx vitest run src/__tests__/sys02` 모든 테스트 통과
- SurveyQuestionEditor에서 dnd-kit 드래그 정렬 코드 존재
- 4종 문항(radio/checkbox/text/rate) 렌더링 코드 존재
- 상태 전환(draft->active->closed) 방어 로직 존재
</verification>

<success_criteria>
- 설문 참여(active 목록 → 응답 폼 → 제출) 플로우 동작
- 문항 편집기: Form.List + dnd-kit 드래그 정렬, 4종 문항 유형
- 설문 생성 → 문항 편집 → 배포 → 마감 → 결과분석 전체 라이프사이클
- 결과분석: 문항별 응답 통계 Bar 차트
- 체계관리 6탭(설문/사용자/카테고리/통계/템플릿/대상자) 동작
- 상태 기반 편집 제한(active 설문 문항 수정 불가)
</success_criteria>

<output>
After completion, create `.planning/phases/04-a-6/04-05-SUMMARY.md`
</output>
