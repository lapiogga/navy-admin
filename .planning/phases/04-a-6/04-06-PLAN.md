---
phase: 04-a-6
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys09-memorial/index.tsx
  - navy-admin/src/pages/sys09-memorial/DeceasedPage.tsx
  - navy-admin/src/pages/sys09-memorial/InjuredPage.tsx
  - navy-admin/src/pages/sys09-memorial/ReviewPage.tsx
  - navy-admin/src/pages/sys09-memorial/StatsUnitPage.tsx
  - navy-admin/src/pages/sys09-memorial/StatsTypePage.tsx
  - navy-admin/src/pages/sys09-memorial/StatsMonthPage.tsx
  - navy-admin/src/pages/sys09-memorial/StatsYearPage.tsx
  - navy-admin/src/pages/sys09-memorial/StatsUnitListPage.tsx
  - navy-admin/src/pages/sys09-memorial/StatsAllListPage.tsx
  - navy-admin/src/pages/sys09-memorial/ReportDeceasedPage.tsx
  - navy-admin/src/pages/sys09-memorial/ReportInjuredPage.tsx
  - navy-admin/src/pages/sys09-memorial/CertDeathPage.tsx
  - navy-admin/src/pages/sys09-memorial/CertMeritDeathPage.tsx
  - navy-admin/src/pages/sys09-memorial/CertMeritInjuredPage.tsx
  - navy-admin/src/pages/sys09-memorial/CertReviewResultPage.tsx
  - navy-admin/src/pages/sys09-memorial/CertIssueLedgerPage.tsx
  - navy-admin/src/pages/sys09-memorial/PrintableReport.tsx
  - navy-admin/src/pages/sys09-memorial/print.css
  - navy-admin/src/shared/api/mocks/handlers/sys09.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys09/memorial.test.ts
autonomous: true
requirements:
  - HONOR-01
  - HONOR-02
  - HONOR-03
  - HONOR-04
  - HONOR-05
  - HONOR-06
  - HONOR-07
  - HONOR-08
  - HONOR-09
  - HONOR-10
  - HONOR-11
  - HONOR-12
  - HONOR-13
  - HONOR-14
  - HONOR-15
  - HONOR-16
  - HONOR-17

must_haves:
  truths:
    - "사망자/상이자/심사 데이터를 CRUD할 수 있고, SearchForm(군구분/군번/성명/생년월일/계급/소속)으로 검색 가능"
    - "부대별(Bar)/신분별(Pie)/연도별(Line)/월별(Bar) 현황 차트와 테이블이 표시"
    - "부대별 명부/전사망자 명부가 DataTable로 표시되고 엑셀 다운로드 가능"
    - "7종 보고서/확인서가 antd Descriptions로 화면 표시되고, Print CSS로 A4 출력 가능"
    - "게시판(HONOR-17)은 Phase 1 공통게시판을 sysCode='sys09' boardType='notice'로 lazy 재사용"
  artifacts:
    - path: "navy-admin/src/pages/sys09-memorial/DeceasedPage.tsx"
      provides: "사망자 관리 CRUD (HONOR-01)"
      contains: "DataTable"
    - path: "navy-admin/src/pages/sys09-memorial/PrintableReport.tsx"
      provides: "인쇄 래퍼 컴포넌트"
      contains: "window.print"
    - path: "navy-admin/src/pages/sys09-memorial/print.css"
      provides: "Print CSS A4 레이아웃"
      contains: "@media print"
    - path: "navy-admin/src/shared/api/mocks/handlers/sys09.ts"
      provides: "MSW Mock API"
      exports: ["sys09Handlers"]
  key_links:
    - from: "StatsUnitPage.tsx"
      to: "@ant-design/charts"
      via: "Bar chart"
      pattern: "import.*@ant-design/charts"
    - from: "PrintableReport.tsx"
      to: "print.css"
      via: "CSS import"
      pattern: "print.css"
    - from: "index.tsx"
      to: "common/board"
      via: "HONOR-17 게시판 lazy 재사용"
      pattern: "common/board"
---

<objective>
SYS09 영현보훈체계 35개 프로세스 구현

Purpose: 데이터 CRUD(사망자/상이자/심사) + 4종 통계 차트(Bar/Pie/Line/Column) + 7종 보고서/확인서(Print CSS) 패턴. 가장 많은 페이지 수(17개)를 가진 서브시스템.
Output: CRUD 3개 + 현황 6개 + 보고서/확인서 7개 + 인쇄래퍼 + Print CSS + MSW + 테스트
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-a-6/04-CONTEXT.md
@.planning/phases/04-a-6/04-UI-SPEC.md
@.planning/phases/04-a-6/04-RESEARCH.md

@navy-admin/src/pages/sys04-certificate/index.tsx
@navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx
@navy-admin/src/shared/api/mocks/handlers/sys04.ts
@navy-admin/src/__tests__/sys04/certificate.test.ts
@navy-admin/src/entities/subsystem/menus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1a: MSW 핸들러 + 테스트 + 타입 정의 (HONOR-01~17 API)</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys09.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys09/memorial.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/sys04.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/__tests__/sys04/certificate.test.ts,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**MSW 핸들러** (`sys09.ts`)

타입 정의 (`extends Record<string, unknown>` 필수):
```typescript
export type DeathType = 'combat' | 'duty' | 'disease' | 'accident'
export type InjuryType = 'combat' | 'duty'
export type ReviewResult = 'eligible' | 'ineligible'

export interface Deceased extends Record<string, unknown> {
  id: string
  serviceNumber: string
  name: string
  residentNumber: string     // 마스킹: 앞6-*******
  rank: string
  unit: string
  enlistDate: string
  phone: string
  deathType: DeathType
  deathDate: string
  deathPlace: string
  deathCause: string
  burialPlace: string
  familyContact: string
  remarks: string
  militaryType: string       // 해병/해군/육군/공군
}

export interface Injured extends Record<string, unknown> {
  id: string
  serviceNumber: string
  name: string
  residentNumber: string
  rank: string
  unit: string
  enlistDate: string
  phone: string
  address: string
  injuryType: InjuryType
  injuryGrade: string        // 1~7급
  injuryDate: string
  injuryPlace: string
  injuryCause: string
  hospitalName: string
  remarks: string
  militaryType: string
}

export interface CombatReview extends Record<string, unknown> {
  id: string
  reviewRound: string        // "제1차" etc
  reviewDate: string
  name: string
  serviceNumber: string
  birthDate: string
  enlistDate: string
  rank: string
  unit: string
  incidentType: string       // 전사/순직/전상/공상
  incidentDate: string
  incidentCause: string
  result: ReviewResult
  resultDetail: string
  remarks: string
}
```

Mock 데이터: 사망자 25건, 상이자 20건, 심사 15건. 군구분=['해병','해군','육군','공군']. 계급=['이병','일병','상병','병장','하사','중사','상사','원사','소위','중위','대위','소령','중령','대령']. 부대=['1사단','2사단','해병대사령부','교육훈련단','상륙기동단'].

핸들러:
- GET /sys09/deceased -- 사망자 목록 (page, size, militaryType, serviceNumber, name, birthDate, rank, unit)
- GET /sys09/deceased/:id -- 상세
- POST /sys09/deceased -- 등록
- PUT /sys09/deceased/:id -- 수정
- DELETE /sys09/deceased/:id -- 삭제
- GET /sys09/injured -- 상이자 목록
- GET /sys09/injured/:id -- 상세
- POST /sys09/injured -- 등록
- PUT /sys09/injured/:id -- 수정
- DELETE /sys09/injured/:id -- 삭제
- GET /sys09/reviews -- 심사 목록
- GET /sys09/reviews/:id -- 상세
- POST /sys09/reviews -- 등록
- PUT /sys09/reviews/:id -- 수정
- DELETE /sys09/reviews/:id -- 삭제
- GET /sys09/stats/unit -- 부대별 사망자 현황 (반환: [{unit, combat, duty, disease, accident, total}])
- GET /sys09/stats/unit-list -- 부대별 사망자 명부 (PageResponse)
- GET /sys09/stats/type -- 신분별 사망자 현황 (반환: [{type, combat, duty, disease, accident, total}])
- GET /sys09/stats/year -- 연도별 사망자 현황 (반환: [{year, combat, duty, disease, accident, total}])
- GET /sys09/stats/month -- 월별 사망자 현황 (반환: [{year, m1~m12, total}])
- GET /sys09/stats/all-list -- 전사망자 명부 (PageResponse)
- GET /sys09/reports/deceased -- 사망자 현황 보고서 데이터
- GET /sys09/reports/injured -- 상이자 현황 보고서 데이터
- GET /sys09/reports/death-cert/:id -- 순직/사망확인서 데이터
- GET /sys09/reports/merit-death/:id -- 국가유공자 확인서(사망자) 데이터
- GET /sys09/reports/merit-injured/:id -- 국가유공자 확인서(상이자) 데이터
- GET /sys09/reports/review-result/:id -- 전공사상심사결과 데이터
- GET /sys09/reports/issue-ledger -- 전사망자 확인증 발급대장 데이터

export const sys09Handlers = [...] 형태.

**handlers/index.ts**: sys09Handlers import 및 spread 추가.

**테스트 파일** (`memorial.test.ts`)

readFileSync 기반:
- DeceasedPage: DataTable, CrudForm, StatusBadge, '사망자', 'serviceNumber', 'deathType', 'deathDate'
- InjuredPage: DataTable, CrudForm, StatusBadge, '상이자', 'injuryType', 'injuryGrade'
- ReviewPage: DataTable, CrudForm, StatusBadge, '심사', 'reviewRound', 'result'
- PrintableReport: 'window.print', 'print-area', '인쇄'
- print.css: '@media print', '@page', 'A4', 'report-title'
- StatsUnitPage: '@ant-design/charts' 또는 'Bar'
- StatsTypePage: 'Pie'
- StatsYearPage: 'Line'
- ReportDeceasedPage: 'Descriptions', 'PrintableReport'
- CertDeathPage: 'Descriptions', 'PrintableReport', '확인서'
- sys09.ts: sys09Handlers, '/sys09/deceased', '/sys09/injured', '/sys09/reviews', '/sys09/stats/', '/sys09/reports/'
- index.tsx: 라우트 매핑, 13개 이상 Route, 'common/board' lazy import (HONOR-17)
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys09 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sys09.ts에 "sys09Handlers" export 포함
    - sys09.ts에 "/sys09/deceased" 포함
    - sys09.ts에 "/sys09/injured" 포함
    - sys09.ts에 "/sys09/reviews" 포함
    - sys09.ts에 "/sys09/stats/" 포함
    - sys09.ts에 "/sys09/reports/" 포함
    - sys09.ts에 "extends Record<string, unknown>" 포함
    - index.ts에 "sys09Handlers" import 포함
  </acceptance_criteria>
  <done>MSW 핸들러(27 엔드포인트), 타입 정의 3종(Deceased/Injured/CombatReview), 테스트 스캐폴드 생성</done>
</task>

<task type="auto">
  <name>Task 1b: CRUD 3종(사망자/상이자/심사) + PrintableReport + Print CSS (HONOR-01~03)</name>
  <files>
    navy-admin/src/pages/sys09-memorial/DeceasedPage.tsx,
    navy-admin/src/pages/sys09-memorial/InjuredPage.tsx,
    navy-admin/src/pages/sys09-memorial/ReviewPage.tsx,
    navy-admin/src/pages/sys09-memorial/PrintableReport.tsx,
    navy-admin/src/pages/sys09-memorial/print.css
  </files>
  <read_first>
    navy-admin/src/pages/sys04-certificate/CertificateApplyPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys09.ts,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**DeceasedPage.tsx** (HONOR-01: 사망자 관리)

PageContainer title="사망자 관리".

SearchForm fields (per UI-SPEC 09-A):
- militaryType (군구분): Select, options=['해병','해군','육군','공군']
- serviceNumber (군번): Input text
- name (성명): Input text
- birthDate (주민번호(생년월일)): Input text (6자리 또는 full)
- rank (계급): Select, 계급 목록
- unit (소속): Select, 부대 목록

DataTable 컬럼 (per UI-SPEC 09-A):
- serviceNumber (군번): width 120, text, sortable
- name (성명): width 100, text 링크, sortable
- rank (계급): width 80, text, sortable
- unit (소속): width 150, text, sortable
- deathType (사망구분): width 100, StatusBadge (colorMap: combat='red', duty='orange', disease='blue', accident='default'; labelMap: combat='전사', duty='순직', disease='병사', accident='사고사'), sortable
- deathDate (사망일자): width 120, date, sortable
- enlistDate (입대일자): width 120, date, sortable

toolBarRender: [신규 등록] (primary) + [엑셀 다운로드] (default)

CrudForm fields (per UI-SPEC 09-A):
- serviceNumber (군번): text, required
- name (성명): text, required
- residentNumber (주민등록번호): text, required (placeholder: "앞6-*******" 마스킹 표시)
- rank (계급): Select, required
- unit (소속): Select, required
- enlistDate (입대일자): date, required
- phone (전화번호): text
- deathType (사망구분): Select, required, options=['전사','순직','병사','사고사']
- deathDate (사망일자): date, required
- deathPlace (사망장소): text
- deathCause (사망원인): TextArea
- burialPlace (안장지): text
- familyContact (유족 연락처): text
- remarks (비고): TextArea

**InjuredPage.tsx** (HONOR-02: 상이자 관리)

DeceasedPage와 동일 패턴. 필드 변경 (per UI-SPEC 09-B):

SearchForm: DeceasedPage와 동일 (군구분, 군번, 성명, 생년월일, 계급, 소속)

DataTable 컬럼:
- serviceNumber (군번): width 120, text, sortable
- name (성명): width 100, text 링크, sortable
- rank (계급): width 80, text, sortable
- unit (소속): width 150, text, sortable
- injuryType (상이구분): width 100, StatusBadge (combat='red'/label='전상', duty='orange'/label='공상'), sortable
- injuryGrade (상이등급): width 80, text, sortable
- injuryDate (상이일자): width 120, date, sortable

CrudForm fields (per UI-SPEC 09-B):
- serviceNumber, name, residentNumber, rank, unit, enlistDate, phone (사망자와 동일)
- address (현주소): text
- injuryType (상이구분): Select, required, options=['전상','공상']
- injuryGrade (상이등급): Select, required, options=['1급','2급','3급','4급','5급','6급','7급']
- injuryDate (상이일자): date, required
- injuryPlace (상이장소): text
- injuryCause (상이원인): TextArea
- hospitalName (치료병원): text
- remarks (비고): TextArea

**ReviewPage.tsx** (HONOR-03: 전공사상심사 관리)

SearchForm: 군구분, 군번, 성명, 생년월일, 계급, 소속 (공통)

DataTable 컬럼 (per UI-SPEC 09-C):
- reviewRound (심사차수): width 80, text, sortable
- reviewDate (심사일자): width 120, date, sortable
- name (성명): width 100, text 링크, sortable
- serviceNumber (군번): width 120, text, sortable
- rank (계급): width 80, text, sortable
- unit (소속): width 150, text, sortable
- result (심사결과): width 100, StatusBadge (eligible='green'/label='해당', ineligible='red'/label='비해당'), sortable

CrudForm fields (per UI-SPEC 09-C):
- reviewRound (심사차수): text, required (예: "제1차")
- reviewDate (심사일자): date, required
- name (성명): text, required
- serviceNumber (군번): text, required
- birthDate (주민번호(생년월일)): text, required
- enlistDate (입대일자): date, required
- rank (계급): Select, required
- unit (소속): Select, required
- incidentType (사고유형): Select, required, options=['전사','순직','전상','공상']
- incidentDate (사고일자): date, required
- incidentCause (사고원인): TextArea, required
- result (심사결과): Select, required, options=['해당','비해당']
- resultDetail (결과 상세): TextArea
- remarks (비고): TextArea

**PrintableReport.tsx** (공통 인쇄 래퍼 -- D-12)

```typescript
import { useRef } from 'react'
import { Button } from 'antd'
import { PrinterOutlined } from '@ant-design/icons'
import './print.css'

interface PrintableReportProps {
  title: string
  children: React.ReactNode
}

export function PrintableReport({ title, children }: PrintableReportProps) {
  const printRef = useRef<HTMLDivElement>(null)
  
  const handlePrint = () => {
    window.print()
  }

  return (
    <>
      <div className="no-print" style={{ marginBottom: 16, textAlign: 'right' }}>
        <Button type="primary" icon={<PrinterOutlined />} onClick={handlePrint}>
          인쇄
        </Button>
      </div>
      <div ref={printRef} className="print-area">
        <div className="report-title">{title}</div>
        {children}
      </div>
    </>
  )
}
```

**print.css** (per UI-SPEC Print CSS 계약)

```css
@media print {
  @page { size: A4 portrait; margin: 20mm; }
  .ant-layout-sider, .ant-layout-header, .ant-breadcrumb, .ant-btn, .no-print { display: none !important; }
  .print-area { width: 100%; padding: 0; margin: 0; }
  .report-title { font-size: 24px; font-weight: 700; text-align: center; margin-bottom: 32px; }
  .signature-area { margin-top: 48px; text-align: right; }
  .ant-descriptions { border: 1px solid #000 !important; }
  .ant-descriptions-item-label { font-weight: 600 !important; background: #f0f0f0 !important; }
  .no-break { page-break-inside: avoid; }
}
```
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys09 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DeceasedPage.tsx에 "DataTable" 포함
    - DeceasedPage.tsx에 "deathType" 포함
    - InjuredPage.tsx에 "injuryType" 포함
    - ReviewPage.tsx에 "reviewRound" 포함
    - PrintableReport.tsx에 "window.print" 포함
    - print.css에 "@media print" 포함
    - print.css에 "A4" 포함
  </acceptance_criteria>
  <done>CRUD 3종(사망자/상이자/심사) + 인쇄래퍼(PrintableReport) + Print CSS 완성</done>
</task>

<task type="auto">
  <name>Task 2: 현황 6종 + 보고서/확인서 7종 + 라우트 (HONOR-04~17)</name>
  <files>
    navy-admin/src/pages/sys09-memorial/StatsUnitPage.tsx,
    navy-admin/src/pages/sys09-memorial/StatsTypePage.tsx,
    navy-admin/src/pages/sys09-memorial/StatsMonthPage.tsx,
    navy-admin/src/pages/sys09-memorial/StatsYearPage.tsx,
    navy-admin/src/pages/sys09-memorial/StatsUnitListPage.tsx,
    navy-admin/src/pages/sys09-memorial/StatsAllListPage.tsx,
    navy-admin/src/pages/sys09-memorial/ReportDeceasedPage.tsx,
    navy-admin/src/pages/sys09-memorial/ReportInjuredPage.tsx,
    navy-admin/src/pages/sys09-memorial/CertDeathPage.tsx,
    navy-admin/src/pages/sys09-memorial/CertMeritDeathPage.tsx,
    navy-admin/src/pages/sys09-memorial/CertMeritInjuredPage.tsx,
    navy-admin/src/pages/sys09-memorial/CertReviewResultPage.tsx,
    navy-admin/src/pages/sys09-memorial/CertIssueLedgerPage.tsx,
    navy-admin/src/pages/sys09-memorial/index.tsx
  </files>
  <read_first>
    navy-admin/src/pages/sys09-memorial/DeceasedPage.tsx,
    navy-admin/src/pages/sys09-memorial/PrintableReport.tsx,
    navy-admin/src/pages/sys09-memorial/print.css,
    navy-admin/src/shared/api/mocks/handlers/sys09.ts,
    navy-admin/src/pages/sys04-certificate/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts
  </read_first>
  <action>
**현황 페이지 6종** (HONOR-04~09)

모든 현황 페이지 공통 레이아웃 (per UI-SPEC 09-D): 상단 SearchForm + 중앙 차트(있는 경우) + 하단 DataTable. [엑셀 다운로드] 버튼 공통.

**StatsUnitPage.tsx** (HONOR-04: 부대별 사망자 현황)
- 차트: @ant-design/charts Bar (가로) -- 부대별 사망자 수
  - `import { Bar } from '@ant-design/charts'`
  - config: { data, xField: 'total', yField: 'unit', height: 300 }
- DataTable 컬럼: unit(부대명), combat(전사), duty(순직), disease(병사), accident(사고사), total(계)

**StatsUnitListPage.tsx** (HONOR-05: 부대별 사망자 명부)
- DataTable 전용 (차트 없음)
- 컬럼: index(연번), unit(부대명), serviceNumber(군번), name(성명), rank(계급), deathType(사망구분), deathDate(사망일자)

**StatsTypePage.tsx** (HONOR-06: 신분별 사망자 현황)
- 차트: @ant-design/charts Pie -- 신분별(장교/부사관/병사/군무원) 비율
  - `import { Pie } from '@ant-design/charts'`
  - config: { data, angleField: 'value', colorField: 'type', radius: 0.8, height: 300 }
- DataTable 컬럼: type(신분구분), combat(전사), duty(순직), disease(병사), accident(사고사), total(계)

**StatsYearPage.tsx** (HONOR-07: 연도별 사망자 현황)
- 차트: @ant-design/charts Line -- 연도별 추이
  - `import { Line } from '@ant-design/charts'`
  - config: { data, xField: 'year', yField: 'total', height: 300 }
- DataTable 컬럼: year(연도), combat(전사), duty(순직), disease(병사), accident(사고사), total(계)

**StatsMonthPage.tsx** (HONOR-08: 월별 사망자 현황)
- 차트: @ant-design/charts Column (세로 막대) -- 월별 사망자 수
  - `import { Column } from '@ant-design/charts'`
  - config: { data, xField: 'month', yField: 'count', height: 300 }
- DataTable 컬럼: year(연도), m1(1월)~m12(12월), total(계)

**StatsAllListPage.tsx** (HONOR-09: 전사망자 명부)
- DataTable 전용 (차트 없음)
- 컬럼: index(연번), serviceNumber(군번), name(성명), rank(계급), unit(소속), deathType(사망구분), deathDate(사망일자), deathPlace(사망장소)

**보고서/확인서 7종** (HONOR-10~16)

모든 보고서/확인서는 PrintableReport 래퍼 사용 (per D-11~13).

**ReportDeceasedPage.tsx** (HONOR-10: 사망자 현황 보고서)
per UI-SPEC 09-E:
- PrintableReport title="사망자 현황 보고서"
- 작성일자 (우측정렬)
- 사망자 현황 테이블: antd Table -- 부대별 x 사망구분 매트릭스 (행: 부대명, 열: 전사/순직/병사/사고사/계, 합계 행 bold)
- 확인자 서명란: `<div className="signature-area">확인자: _________________ (인)</div>`

**ReportInjuredPage.tsx** (HONOR-11: 상이자 현황 보고서)
사망자 보고서와 동일 레이아웃. 열: 상이구분(전상/공상)/상이등급으로 변경.

**CertDeathPage.tsx** (HONOR-12: 순직/사망확인서)
per UI-SPEC 09-E:
- PrintableReport title="순직/사망확인서"
- 문서번호 (상단 좌측)
- antd Descriptions bordered: 군번, 성명, 계급, 소속, 사망일자, 사망장소, 사망원인(full width)
- 확인 문구 paragraph
- 작성일자 (중앙정렬)
- 확인자 서명란

**CertMeritDeathPage.tsx** (HONOR-13: 국가유공자 요건 해당사실 확인서 - 사망자)
순직/사망확인서와 유사. 제목 변경 + 국가유공자 요건 항목 추가.

**CertMeritInjuredPage.tsx** (HONOR-14: 국가유공자 요건 해당사실 확인서 - 상이자)
사망자 확인서와 유사. 상이 관련 필드(상이구분, 상이등급, 상이일자)로 변경.

**CertReviewResultPage.tsx** (HONOR-15: 전공사상심사결과)
per UI-SPEC 09-E:
- PrintableReport title="전공사상심사결과"
- Descriptions 2열: 심사차수/심사일자
- 대상자 정보 Descriptions: 성명, 군번, 계급, 소속
- 사고 내용 Descriptions (full width): 사고유형, 사고일자, 사고원인
- 심사결과 Descriptions (bold 강조): 결과, 상세
- 위원 서명란 하단 테이블 (직책, 성명, 서명)

**CertIssueLedgerPage.tsx** (HONOR-16: 전사망자 확인증 발급대장)
per UI-SPEC 09-E:
- PrintableReport title="전사망자 확인증 발급대장"
- antd Table: 연번, 발급일자, 군번, 성명, 계급, 소속, 용도, 수령인
- 하단 서명란

**index.tsx** (라우트 매핑)

menus.ts 기반 (sys09 메뉴 구조 참조):
```
Route index -> Navigate to="/sys09/2/1" replace
Route "1/1" -> BoardIndex (lazy, HONOR-17 게시판: lazy(() => import('@/pages/common/board')), sysCode='sys09', boardType='notice')
Route "2/1" -> DeceasedPage (HONOR-01)
Route "2/2" -> InjuredPage (HONOR-02)
Route "2/3" -> ReviewPage (HONOR-03)
Route "3/1" -> CertMeritDeathPage (HONOR-13)
Route "3/2" -> CertMeritInjuredPage (HONOR-14)
Route "3/3" -> CertReviewResultPage (HONOR-15)
Route "3/4" -> CertDeathPage (HONOR-12)
Route "3/5" -> ReportDeceasedPage (HONOR-10)
Route "3/6" -> ReportInjuredPage (HONOR-11)
Route "3/7" -> StatsTypePage (HONOR-06)
Route "3/8" -> StatsMonthPage (HONOR-08)
Route "3/9" -> StatsYearPage (HONOR-07)
Route "3/10" -> StatsUnitPage (HONOR-04)
Route "3/11" -> StatsUnitListPage (HONOR-05)
Route "3/12" -> StatsAllListPage (HONOR-09)
Route "3/13" -> CertIssueLedgerPage (HONOR-16)
```

per D-26: 게시판은 Phase 1 공통게시판 재사용 (HONOR-17)
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys09 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - StatsUnitPage.tsx에 "@ant-design/charts" import 포함
    - StatsUnitPage.tsx에 "Bar" 포함
    - StatsTypePage.tsx에 "Pie" 포함
    - StatsYearPage.tsx에 "Line" 포함
    - StatsMonthPage.tsx에 "Column" 포함
    - ReportDeceasedPage.tsx에 "PrintableReport" 포함
    - ReportDeceasedPage.tsx에 "Descriptions" 또는 "Table" 포함
    - CertDeathPage.tsx에 "PrintableReport" 포함
    - CertDeathPage.tsx에 "Descriptions" 포함
    - CertDeathPage.tsx에 "확인서" 또는 "확인자" 텍스트 포함
    - CertReviewResultPage.tsx에 "PrintableReport" 포함
    - CertIssueLedgerPage.tsx에 "PrintableReport" 포함
    - index.tsx에 "/sys09/" 경로 매핑 포함
    - index.tsx에 17개 이상 Route (또는 path= 매핑)
    - index.tsx에 "common/board" lazy import 포함 (HONOR-17)
  </acceptance_criteria>
  <done>현황 6종(부대별Bar/신분별Pie/연도별Line/월별Column/명부2종), 보고서/확인서 7종(Print CSS), 라우트 매핑(17메뉴, HONOR-17 게시판 포함) 완성. 모든 HONOR-01~17 커버</done>
</task>

</tasks>

<verification>
cd navy-admin && npx vitest run src/__tests__/sys09 --reporter=verbose
cd navy-admin && npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- SYS09 16개 고유 페이지(CRUD 3 + 현황 6 + 보고서/확인서 7) + index.tsx 완성
- @ant-design/charts 4종(Bar, Pie, Line, Column) 차트 구현
- PrintableReport 래퍼 + Print CSS A4 인쇄 구현
- 7종 보고서/확인서 antd Descriptions 렌더링
- MSW 핸들러 27개 엔드포인트 동작
- 테스트 전체 통과
- 게시판(HONOR-17) Phase 1 공통게시판 lazy 재사용 확인
</success_criteria>

<output>
After completion, create `.planning/phases/04-a-6/04-06-SUMMARY.md`
</output>
