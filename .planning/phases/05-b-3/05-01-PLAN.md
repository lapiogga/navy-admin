---
phase: 05-b-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys07-mil-data/index.tsx
  - navy-admin/src/pages/sys07-mil-data/MilDataListPage.tsx
  - navy-admin/src/pages/sys07-mil-data/MilDataFormPage.tsx
  - navy-admin/src/pages/sys07-mil-data/MilDataDetailPage.tsx
  - navy-admin/src/pages/sys07-mil-data/MilDataUsagePage.tsx
  - navy-admin/src/pages/sys07-mil-data/MilDataStatsPage.tsx
  - navy-admin/src/pages/sys07-mil-data/MilDataEvalPage.tsx
  - navy-admin/src/pages/sys07-mil-data/HaegidanListPage.tsx
  - navy-admin/src/pages/sys07-mil-data/HaegidanFormPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys07.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys07/military-data.test.ts
autonomous: true
requirements:
  - MDATA-01
  - MDATA-02
  - MDATA-03
  - MDATA-04

must_haves:
  truths:
    - "군사자료 목록에서 보안등급(비밀/대외비/일반)이 Tag 색상으로 구분되어 표시된다"
    - "군사자료를 등록/수정/삭제(사유필수)할 수 있고, 엑셀 일괄등록으로 검증결과 Modal이 표시된다"
    - "열람신청->승인->대출->반납 Steps 워크플로우가 동작하고, 비밀등급 자료 열람 시 경고 Modal이 표시된다"
    - "통계자료에서 4종 차트(Column/Pie/Line/Bar)와 3종 DataTable이 표시되고 인쇄/엑셀 버튼이 동작한다"
    - "해기단자료를 CRUD할 수 있고 삭제 시 사유 입력이 필수이다"
    - "보존기간 만료 자료가 평가심의 목록에 자동 표시되고, 결과 업로드로 파기/보존연장이 처리된다"
  artifacts:
    - path: "navy-admin/src/pages/sys07-mil-data/MilDataListPage.tsx"
      provides: "군사자료 목록 + 보안등급 Tag + 검색 + 일괄등록 Upload"
      min_lines: 100
    - path: "navy-admin/src/pages/sys07-mil-data/MilDataUsagePage.tsx"
      provides: "대출/열람 Steps 워크플로우 + 비밀등급 경고 Modal"
      min_lines: 100
    - path: "navy-admin/src/pages/sys07-mil-data/MilDataStatsPage.tsx"
      provides: "@ant-design/charts 4종 차트 + 3종 DataTable"
      min_lines: 100
    - path: "navy-admin/src/shared/api/mocks/handlers/sys07.ts"
      provides: "군사자료/해기단/활용/통계/평가심의 MSW 핸들러"
      min_lines: 100
  key_links:
    - from: "navy-admin/src/pages/sys07-mil-data/MilDataListPage.tsx"
      to: "/api/sys07"
      via: "useQuery/useMutation in TanStack Query"
      pattern: "useQuery.*sys07"
    - from: "navy-admin/src/pages/sys07-mil-data/MilDataUsagePage.tsx"
      to: "antd Steps"
      via: "Steps current prop driven by status"
      pattern: "Steps.*current"
    - from: "navy-admin/src/shared/api/mocks/handlers/sys07.ts"
      to: "navy-admin/src/shared/api/mocks/handlers/index.ts"
      via: "sys07Handlers export + index.ts import"
      pattern: "sys07Handlers"
---

<objective>
SYS07 군사자료관리체계 전체 화면(40개 프로세스)을 구현한다. 보안등급별 Tag 분류, 열람/대출/반납 Steps 워크플로우, 평가심의/파기, 통계 차트 4종, 해기단자료 CRUD를 포함한다.

Purpose: 군사자료의 보안등급 기반 관리, 열람/대출 추적, 통계 시각화로 군사자료 운영의 전 생애주기를 완전 커버
Output: sys07-mil-data/ 9개 페이지 + sys07.ts MSW 핸들러 + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-b-3/05-CONTEXT.md
@.planning/phases/05-b-3/05-UI-SPEC.md (SYS07 섹션: 07-A ~ 07-J)
@.planning/phases/05-b-3/05-RESEARCH.md (Pattern 1: SeatGrid는 무시, Pattern 4: Upload+검증 Modal 참조)

@navy-admin/src/shared/ui/DataTable/DataTable.tsx
@navy-admin/src/shared/ui/CrudForm/CrudForm.tsx
@navy-admin/src/shared/ui/StatusBadge/
@navy-admin/src/shared/ui/DetailModal/
@navy-admin/src/shared/ui/SearchForm/
@navy-admin/src/shared/ui/ConfirmDialog/
@navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx (Steps 결재 패턴 참조)
@navy-admin/src/pages/sys13-knowledge/KnowledgeStatsPage.tsx (차트 패턴 참조)
@navy-admin/src/pages/sys09-memorial/PrintableReport.tsx (인쇄 래퍼 참조)
@navy-admin/src/shared/api/mocks/handlers/index.ts
@navy-admin/src/entities/subsystem/menus.ts (sys07 메뉴 구조)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SYS07 MSW 핸들러 + 라우트 분기 + 목록/등록/상세/평가심의/해기단 페이지</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys07.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys07-mil-data/index.tsx,
    navy-admin/src/pages/sys07-mil-data/MilDataListPage.tsx,
    navy-admin/src/pages/sys07-mil-data/MilDataFormPage.tsx,
    navy-admin/src/pages/sys07-mil-data/MilDataDetailPage.tsx,
    navy-admin/src/pages/sys07-mil-data/MilDataEvalPage.tsx,
    navy-admin/src/pages/sys07-mil-data/HaegidanListPage.tsx,
    navy-admin/src/pages/sys07-mil-data/HaegidanFormPage.tsx,
    navy-admin/src/__tests__/sys07/military-data.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/shared/api/mocks/handlers/sys17.ts,
    navy-admin/src/pages/sys07-mil-data/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx,
    navy-admin/src/shared/ui/ConfirmDialog/ConfirmDialog.tsx
  </read_first>
  <action>
**1. MSW 핸들러 (sys07.ts):**
- Faker.js(@faker-js/faker, ko locale)로 군사자료 Mock 데이터 30건 생성
- 각 자료 필드: id, securityLevel('secret'|'confidential'|'normal'), storageType, docNumber, docType, transferDate, title, author, retentionPeriod, retentionExpireDate, pages, status('active'|'on_loan'|'evaluation'|'disposed'), registeredAt, attachFile, remarks
- API 엔드포인트:
  - GET /api/sys07/documents (페이지네이션 + securityLevel/storageType/docType/keyword/status 필터)
  - POST /api/sys07/documents (등록)
  - PUT /api/sys07/documents/:id (수정)
  - DELETE /api/sys07/documents/:id (body에 deleteReason 필수)
  - GET /api/sys07/documents/:id (상세조회 + 변경이력)
  - POST /api/sys07/documents/bulk-validate (일괄등록 검증: valid[], errors[{row, column, errorMessage}] 응답)
  - POST /api/sys07/documents/bulk-save (일괄 저장)
- 평가심의:
  - GET /api/sys07/evaluations (보존기간 만료 자료 자동 목록, retentionExpireDate < today 필터)
  - POST /api/sys07/evaluations/bulk-upload (결과 업로드: 각 건에 result='dispose'|'extend', newRetentionDate)
- 해기단자료:
  - GET /api/sys07/haegidan (페이지네이션 + department/keyword 필터)
  - POST/PUT/DELETE /api/sys07/haegidan/:id (CRUD, 삭제 시 deleteReason 필수)
  - GET /api/sys07/haegidan/:id (상세)
- `export const sys07Handlers = [...]`
- handlers/index.ts에 `import { sys07Handlers } from './sys07'` 추가, handlers 배열에 `...sys07Handlers` 스프레드

**2. index.tsx 라우트 분기 (per D-33):**
- sys17 index.tsx 패턴 참조하여 SubsystemPage 대신 메뉴 path 기반 라우트 분기 구현
- `/sys07/1/1` → MilDataListPage, `/sys07/1/2` → MilDataUsagePage, `/sys07/1/3` → MilDataStatsPage
- `/sys07/2/1` → HaegidanListPage
- 하위 라우트(MilDataEvalPage 등)는 MilDataListPage 내부 탭 또는 별도 경로

**3. MilDataListPage.tsx (MDATA-01, per D-04/D-05/D-12):**
- DataTable로 군사자료 목록: UI-SPEC 07-A 컬럼 그대로 구현
  - securityLevel: Tag 렌더(red='비밀', orange='대외비', blue='일반') per D-04
  - status: StatusBadge (active='보존중'/green, on_loan='대출중'/blue, evaluation='평가심의'/gold, disposed='파기'/default)
  - title: 클릭 시 MilDataDetailPage Modal 오픈
- SearchForm: securityLevel(Select), storageType(Select), docType(Select), keyword(Input), transferDateRange(RangePicker), status(Select)
- toolBarRender: [신규 등록] [일괄 등록(엑셀)] [엑셀 다운로드] [인쇄]
- 신규 등록 → MilDataFormPage Modal 오픈
- 일괄 등록 → antd Upload.Dragger (.xlsx/.xls, maxCount:1) + beforeUpload에서 POST /api/sys07/documents/bulk-validate 호출
  - 검증 결과 Modal: 성공 건수(green text), 오류 테이블(행번호/오류컬럼/오류내용), [취소][저장] 버튼(오류 0건일 때만 저장 활성화)
- 엑셀 다운로드/인쇄: message.success Mock (per D-38)
- 평가심의 탭(Tabs): '군사자료 목록' | '평가심의 목록' 2탭 구성, 평가심의 탭이 MilDataEvalPage를 인라인 렌더

**4. MilDataFormPage.tsx (MDATA-01 등록/수정):**
- CrudForm 래퍼: UI-SPEC 07-B 필드 그대로 (securityLevel, storageLocation, docNumber, docType, transferDate, title, author, retentionPeriod, retentionExpireDate, pages, attachFile, remarks)
- 수정 모드 추가 필드: disposeFlag(Checkbox), retentionExtend(Checkbox), newRetentionDate(DatePicker, retentionExtend=true 시 노출)
- useMutation으로 POST/PUT

**5. MilDataDetailPage.tsx (MDATA-01 상세):**
- antd Descriptions (layout="vertical", column=3): 비밀등급, 보관형태, 관리번호, 문서구분, 이관일자, 보존기간, 자료명, 작성자, 쪽수, 보관장소, 등록일, 상태
- 관리자 전용: 변경이력 DataTable (변경일시/변경자/변경항목/변경전/변경후)

**6. MilDataEvalPage.tsx (MDATA-01 평가심의, per D-10/D-11):**
- DataTable: UI-SPEC 07-E 컬럼 (securityLevel, docNumber, title, retentionExpireDate, status, evaluationResult, newRetentionDate)
- SearchForm: securityLevel, keyword, expireDateRange
- toolBarRender: [평가심의 결과 업로드][엑셀 다운로드]
- 결과 업로드: 일괄등록과 동일 Upload+검증 Modal 패턴. 결과값: 파기/보존기간연장+연장기간

**7. HaegidanListPage.tsx + HaegidanFormPage.tsx (MDATA-04):**
- DataTable: UI-SPEC 07-I 컬럼 (department, managerPosition, fileFolder, title, dataType, publisher, securityLevel Tag, publishYear, storageLocation, registeredAt)
- SearchForm: department(Select), managerPosition(Input), fileFolder(Input), keyword(Input)
- toolBarRender: [신규 등록][엑셀 다운로드][인쇄]
- CrudForm: UI-SPEC 07-J 필드 (title, dataType, publisher, securityLevel, publishYear, storageLocation, pages, attachFile, remarks)
- 삭제: ConfirmDialog + deleteReason TextArea 필수

**8. 테스트 (readFileSync 기반, jsdom 패턴):**
- sys07.ts MSW 핸들러 export 검증
- MilDataListPage.tsx 파일에 'securityLevel', 'Tag', 'SearchForm', 'DataTable' 문자열 존재 검증
- MilDataUsagePage.tsx 파일에 'Steps', 'current' 문자열 존재 검증 (Task 2에서 생성되므로 stub)
- HaegidanListPage.tsx 파일에 'DataTable', 'deleteReason' 존재 검증
- handlers/index.ts에 'sys07Handlers' 존재 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys07 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/shared/api/mocks/handlers/sys07.ts exports `sys07Handlers`
    - navy-admin/src/shared/api/mocks/handlers/index.ts contains `sys07Handlers`
    - navy-admin/src/pages/sys07-mil-data/MilDataListPage.tsx contains `Tag` and `securityLevel` and `Upload.Dragger`
    - navy-admin/src/pages/sys07-mil-data/MilDataFormPage.tsx contains `CrudForm` and `retentionExtend`
    - navy-admin/src/pages/sys07-mil-data/MilDataDetailPage.tsx contains `Descriptions`
    - navy-admin/src/pages/sys07-mil-data/MilDataEvalPage.tsx contains `bulk-upload` and `evaluationResult`
    - navy-admin/src/pages/sys07-mil-data/HaegidanListPage.tsx contains `DataTable` and `deleteReason`
    - navy-admin/src/pages/sys07-mil-data/index.tsx contains route mapping for `/sys07/1/1` through `/sys07/2/1`
  </acceptance_criteria>
  <done>SYS07 MDATA-01/04 군사자료 목록/등록/수정/삭제/상세/일괄등록/평가심의/해기단자료 전체 CRUD 동작, 보안등급 Tag 표시, MSW 핸들러 연동</done>
</task>

<task type="auto">
  <name>Task 2: SYS07 대출/열람 Steps 워크플로우 + 통계 차트 4종</name>
  <files>
    navy-admin/src/pages/sys07-mil-data/MilDataUsagePage.tsx,
    navy-admin/src/pages/sys07-mil-data/MilDataStatsPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys07.ts,
    navy-admin/src/__tests__/sys07/military-data.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx,
    navy-admin/src/pages/sys13-knowledge/KnowledgeStatsPage.tsx,
    navy-admin/src/pages/sys09-memorial/PrintableReport.tsx,
    navy-admin/src/pages/sys09-memorial/print.css,
    navy-admin/src/shared/api/mocks/handlers/sys07.ts,
    navy-admin/src/pages/sys07-mil-data/index.tsx
  </read_first>
  <action>
**1. MilDataUsagePage.tsx (MDATA-02, per D-08/D-09/D-06):**
- DataTable: UI-SPEC 07-F 컬럼 (usageType Tag, docTitle 링크, securityLevel Tag, userName, militaryId, rank, position, unit, usageDate, returnDueDate, returnDate, status StatusBadge)
- StatusBadge colorMap: pending='신청대기'/gold, approved='승인완료'/blue, on_loan='대출중'/cyan, returned='반납완료'/green, rejected='반려'/red
- SearchForm: usageType(Select 전체/대출/열람/지출), status(Select), keyword(Input 성명/군번), usageDateRange(RangePicker)
- 상세 조회 시 antd Steps 워크플로우 시각화 (Phase 4 InspectionApprovalPage 패턴 재사용):
  - Steps direction="horizontal", items: [신청, 승인, 대출/열람, 반납완료]
  - step 0 = finish (항상), step 1 = finish/wait/error(반려), step 2 = finish/wait, step 3 = finish/wait
  - current prop은 status에 따라 계산: pending→0, approved→1, on_loan→2, returned→3
- 처리 폼(CrudForm Modal): UI-SPEC 07-G 필드 (usageType, militaryId, userName, rank, position, unit, phone, usagePurpose, returnDueDate)
- **비밀등급 경고 Modal (per D-06):** 열람신청 시 자료의 securityLevel이 'secret'이면 antd Modal 경고 "이 자료는 [비밀] 등급입니다. 열람 사유를 입력하세요." 표시, usagePurpose를 required로 전환. securityLevel='confidential'이면 "[대외비]" 경고.
- 관리자 액션 버튼: pending→[승인][반려], approved→[대출처리], on_loan→[반납처리]

**2. sys07.ts MSW 핸들러에 활용 API 추가:**
- GET /api/sys07/usages (페이지네이션 + usageType/status/keyword/usageDateRange 필터)
- POST /api/sys07/usages (열람/대출 신청)
- PUT /api/sys07/usages/:id/approve (승인)
- PUT /api/sys07/usages/:id/reject (반려, body: rejectReason)
- PUT /api/sys07/usages/:id/loan (대출처리)
- PUT /api/sys07/usages/:id/return (반납처리)
- Faker로 활용 Mock 데이터 20건: 다양한 status 분포, returnDueDate/returnDate 포함

**3. MilDataStatsPage.tsx (MDATA-03, per D-37):**
- Tabs로 4개 차트 + 3개 DataTable 배치
- **차트 1 (Column):** 문서별 보유현황 — x축: 문서구분(코드), y축: 보유 건수. @ant-design/charts Column 컴포넌트. height 300. color '#003366'. 필터: DatePicker.RangePicker
- **차트 2 (Pie):** 보안등급별 현황 — 비밀/대외비/일반 3분류. color: ['#ff4d4f','#fa8c16','#1890ff']. height 300. 필터: 기준년도 Select
- **차트 3 (Line):** 활용실적 추이 — x축: 월(1~12), y축: 건수, 3개 시리즈(대출/열람/지출). height 300. 필터: 기준년도 Select
- **차트 4:** 크로스탭 Table + 보안등급/상태별 Bar Chart. antd Table 커스텀 컬럼 그룹핑
- **접수용관리기록부 DataTable:** UI-SPEC 07-H 컬럼 (securityLevel Tag, docStatus, baseYear, totalCount, newCount, disposedCount, remainCount)
- **활용지원기록부 DataTable:** UI-SPEC 07-H 컬럼 (baseYear, securityLevel Tag, usageType, totalCount, unitCount)
- SearchForm 공통: dateRange(RangePicker), baseYear(Select 연도), securityLevel(Select), usageType(Select)
- toolBarRender: [엑셀 다운로드][인쇄] — message.success Mock

**4. sys07.ts MSW 핸들러에 통계 API 추가:**
- GET /api/sys07/stats/by-doc-type (문서별 현황)
- GET /api/sys07/stats/by-security-level (등급별 현황)
- GET /api/sys07/stats/usage-trend (활용실적 추이, 월별)
- GET /api/sys07/stats/cross-tab (등급별/상태별 연도별)
- GET /api/sys07/stats/receipt-records (접수용관리기록부)
- GET /api/sys07/stats/usage-records (활용지원기록부)
- Faker로 연도별/월별/등급별 통계 Mock 데이터 생성

**5. 테스트 업데이트:**
- MilDataUsagePage.tsx에 'Steps', 'current', '비밀', '열람 사유' 문자열 검증
- MilDataStatsPage.tsx에 'Column', 'Pie', 'Line', '@ant-design/charts' import 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys07 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/sys07-mil-data/MilDataUsagePage.tsx contains `Steps` and `current` and `비밀` and `열람 사유`
    - navy-admin/src/pages/sys07-mil-data/MilDataUsagePage.tsx contains `usagePurpose` and `approve` and `reject`
    - navy-admin/src/pages/sys07-mil-data/MilDataStatsPage.tsx contains `Column` and `Pie` and `Line` from `@ant-design/charts`
    - navy-admin/src/pages/sys07-mil-data/MilDataStatsPage.tsx contains `접수용관리기록부` or `receipt` pattern
    - navy-admin/src/shared/api/mocks/handlers/sys07.ts contains `/api/sys07/usages` and `/api/sys07/stats`
  </acceptance_criteria>
  <done>SYS07 대출/열람 Steps 워크플로우(신청→승인→대출→반납) + 비밀등급 경고 + 통계 차트 4종 + 테이블 3종 완전 동작. MDATA-02/03 100% 커버.</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx vitest run src/__tests__/sys07 --reporter=verbose` 전체 통과
- `cd navy-admin && npx tsc --noEmit` 타입 에러 없음
- sys07.ts MSW 핸들러가 handlers/index.ts에 등록되어 브라우저에서 /api/sys07/* 응답 확인 가능
</verification>

<success_criteria>
- SYS07 군사자료관리체계 40개 프로세스(MDATA-01~04) 전체 화면 구현 완료
- 보안등급 Tag(비밀/대외비/일반) 색상 구분 표시
- 열람/대출/반납 Steps 워크플로우 동작
- 일괄등록 Upload + 검증 Modal 동작
- 통계 차트 4종 + DataTable 3종 표시
- 해기단자료 CRUD + 삭제사유 필수 입력
- 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/05-b-3/05-01-SUMMARY.md`
</output>
