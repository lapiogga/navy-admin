---
phase: 05-b-3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys10-weekend-bus/index.tsx
  - navy-admin/src/pages/sys10-weekend-bus/SeatGrid.tsx
  - navy-admin/src/pages/sys10-weekend-bus/BusReservationPage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/BusReservationStatusPage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/BusDispatchPage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/BusSchedulePage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/BusUsagePage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/TicketPrint.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys10.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys10/weekend-bus.test.ts
autonomous: true
requirements:
  - BUS-01
  - BUS-02
  - BUS-03
  - BUS-04
  - BUS-06
  - BUS-09

must_haves:
  truths:
    - "사용자가 노선/일자/시간을 선택하면 4열 좌석 그리드가 표시되고, 빈좌석(파랑)을 클릭하여 내선택(초록)으로 토글할 수 있다"
    - "예약 완료 후 승차권 발급 버튼으로 PrintableReport 기반 승차권을 인쇄할 수 있다"
    - "예약현황에서 노선/일자/부대별 예약 목록을 조회하고 엑셀/인쇄할 수 있다"
    - "배차관리에서 노선/차량을 등록하고 좌석배치 확인(SeatGrid readOnly)이 가능하다"
    - "예약시간관리에서 구간/계급별 예약오픈시간/마감시간을 CRUD할 수 있다"
    - "게시판(공지/질의응답)은 Phase 1 공통게시판 sysCode=sys10으로 재사용된다"
  artifacts:
    - path: "navy-admin/src/pages/sys10-weekend-bus/SeatGrid.tsx"
      provides: "antd Row/Col 기반 좌석 그리드 공유 컴포넌트"
      exports: ["SeatGrid"]
      min_lines: 50
    - path: "navy-admin/src/pages/sys10-weekend-bus/BusReservationPage.tsx"
      provides: "노선선택 + SeatGrid 연동 예약 페이지"
      min_lines: 80
    - path: "navy-admin/src/shared/api/mocks/handlers/sys10.ts"
      provides: "주말버스 예약/배차/시간관리 MSW 핸들러"
      min_lines: 80
  key_links:
    - from: "navy-admin/src/pages/sys10-weekend-bus/BusReservationPage.tsx"
      to: "SeatGrid.tsx"
      via: "import SeatGrid, onSeatClick callback"
      pattern: "import.*SeatGrid"
    - from: "navy-admin/src/pages/sys10-weekend-bus/TicketPrint.tsx"
      to: "PrintableReport"
      via: "import PrintableReport from sys09-memorial"
      pattern: "PrintableReport"
    - from: "navy-admin/src/shared/api/mocks/handlers/sys10.ts"
      to: "navy-admin/src/shared/api/mocks/handlers/index.ts"
      via: "sys10Handlers export"
      pattern: "sys10Handlers"
---

<objective>
SYS10 주말버스예약관리체계 핵심 기능(예약/좌석선택/배차/시간관리/현황/게시판)을 구현한다. antd Row/Col 좌석 그리드(D-01~03)가 이 Plan의 핵심 신규 패턴이다.

Purpose: 좌석 선택 UI와 예약 핵심 플로우를 먼저 완성하여, Plan 03에서 대기자/위규자/타군 인증을 추가할 기반 마련
Output: SeatGrid 공유 컴포넌트 + 예약/배차/시간관리 6개 페이지 + MSW 핸들러 + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-b-3/05-CONTEXT.md
@.planning/phases/05-b-3/05-UI-SPEC.md (SYS10 섹션: 10-A ~ 10-J)
@.planning/phases/05-b-3/05-RESEARCH.md (Pattern 1: SeatGrid)

@navy-admin/src/shared/ui/DataTable/DataTable.tsx
@navy-admin/src/shared/ui/CrudForm/CrudForm.tsx
@navy-admin/src/shared/ui/StatusBadge/
@navy-admin/src/shared/ui/SearchForm/
@navy-admin/src/pages/sys09-memorial/PrintableReport.tsx
@navy-admin/src/pages/sys09-memorial/print.css
@navy-admin/src/pages/common/board/
@navy-admin/src/shared/api/mocks/handlers/index.ts
@navy-admin/src/entities/subsystem/menus.ts (sys10 메뉴 구조)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SeatGrid 컴포넌트 + MSW 핸들러 + 라우트 분기 + 예약 페이지</name>
  <files>
    navy-admin/src/pages/sys10-weekend-bus/SeatGrid.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys10.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys10-weekend-bus/index.tsx,
    navy-admin/src/pages/sys10-weekend-bus/BusReservationPage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/TicketPrint.tsx,
    navy-admin/src/__tests__/sys10/weekend-bus.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/sys10-weekend-bus/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/entities/subsystem/menus.ts,
    navy-admin/src/pages/sys09-memorial/PrintableReport.tsx,
    navy-admin/src/pages/sys09-memorial/print.css,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/pages/common/board/BoardListPage.tsx
  </read_first>
  <action>
**1. SeatGrid.tsx (per D-01/D-02/D-03, UI-SPEC Pattern A):**
- Props: `interface SeatGridProps { seats: Seat[]; onSeatClick: (seatId: string) => void; readOnly?: boolean }`
- Seat 타입: `type SeatStatus = 'available' | 'reserved' | 'selected' | 'unavailable'`, `interface Seat { id: string; seatNo: string; status: SeatStatus }`
- STATUS_COLOR 상수: available='#1677ff', reserved='#8c8c8c', selected='#52c41a', unavailable='#ff4d4f'
- 레이아웃:
  - 상단: Row justify="end" — 운전석 Button (disabled, UserOutlined icon, size="small")
  - 좌석 그리드: 행수 = Math.ceil(seats.length / 4), 4열 배치
  - 각 Row gutter=[4,4] style={{ marginBottom: 4 }}
  - 열배치: [Col span=5][Col span=5][Col span=2 통로][Col span=5][Col span=5]
  - 좌석 Button: size="small", block, width 40px, height 40px
  - disabled 조건: readOnly || status === 'reserved' || status === 'unavailable'
  - style: background=STATUS_COLOR[status], color='#fff', borderColor=STATUS_COLOR[status]
  - onClick: onSeatClick(seat.id)
  - 하단 범례: Row gutter=8, marginTop 12px — 각 상태별 예시 Button + 한글 라벨(빈좌석/예약됨/내선택/불가)

**2. MSW 핸들러 (sys10.ts):**
- 노선 Mock 데이터 5개 (서울→포항, 서울→청주, 서울→대전, 서울→광주, 서울→부산). 각 노선에 경유지, 시간표(08:00/10:00/14:00)
- 배차 Mock 데이터 10건: 노선+운행일자+출발시간+좌석수(40)+차량번호+배정여부
- 좌석 Mock: 배차당 40석, seatNo='1A'~'10D', status 랜덤(available 70%, reserved 20%, unavailable 10%)
- 예약 Mock: 15건 (다양한 노선/일자/상태)
- API 엔드포인트:
  - GET /api/sys10/routes (전체 노선 목록)
  - GET /api/sys10/routes/:id/seats?date=&time= (좌석 현황)
  - POST /api/sys10/reservations (예약 신청, body: routeId, operationDate, departureTime, seatId)
  - PUT /api/sys10/reservations/:id/cancel (예약 취소)
  - GET /api/sys10/reservations (예약현황 목록, 페이지네이션+필터)
  - GET /api/sys10/dispatches (배차 목록, 페이지네이션)
  - POST/PUT/DELETE /api/sys10/dispatches/:id (배차 CRUD)
  - GET /api/sys10/dispatches/:id/seats (배차별 좌석 현황, SeatGrid readOnly용)
  - GET /api/sys10/schedule (예약시간 목록)
  - POST/PUT/DELETE /api/sys10/schedule/:id (시간 CRUD)
  - GET /api/sys10/usage (사용현황)
- `export const sys10Handlers = [...]`
- handlers/index.ts에 `import { sys10Handlers } from './sys10'` 추가, handlers 배열에 스프레드

**3. index.tsx 라우트 분기:**
- `/sys10/1/1` → BoardListPage(sysCode='sys10', boardType='notice') + BoardListPage(boardType='qna') lazy import (per D-34, BUS-09)
- `/sys10/1/2` → BusReservationPage
- `/sys10/1/3` → BusWaitlistPage (Plan 03에서 구현, 여기서는 placeholder '대기자관리 준비중')
- `/sys10/1/4` → BusReservationStatusPage
- `/sys10/1/5` → BusDispatchPage
- `/sys10/1/6` → BusSchedulePage
- `/sys10/1/7` → BusUsagePage
- `/sys10/1/8` → BusViolatorPage (Plan 03에서 구현, placeholder)
- `/sys10/1/9` → ExternalUserPage (Plan 03에서 구현, placeholder)
- NOTE: /sys10/login 별도 경로는 Plan 03 Task에서 router.tsx에 추가

**4. BusReservationPage.tsx (BUS-01, per D-01~03):**
- 레이아웃: antd Row gutter=[16,16]
  - 좌측 Col(span=12): 버스 정보 선택 폼
    - operationDate: DatePicker (required)
    - routeId: Select (노선 목록, required) — GET /api/sys10/routes
    - departureTime: Select (노선 선택 후 동적 로드, required)
    - stopover: text 읽기전용 (경유지 표시)
  - 우측 Col(span=12): SeatGrid 컴포넌트
    - 노선+일자+시간 선택 후 GET /api/sys10/routes/:id/seats 호출
    - onSeatClick: available↔selected 토글 (로컬 state로 관리)
- 하단: [예약 신청] 버튼 — POST /api/sys10/reservations (routeId, date, time, seatId)
  - 성공 시 message.success('예약이 완료되었습니다') + [승차권 발급] 버튼 노출

**5. TicketPrint.tsx (BUS-01, per D-32):**
- PrintableReport(sys09-memorial) import 재사용
- antd Descriptions layout="vertical": 노선, 좌석, 운행일자, 출발시간, 출발지, 도착지, 경유지, 예약자 성명/계급, 군번, 소속
- QR코드 자리: placeholder img (120x120 회색 박스 + 'QR' 텍스트)
- 하단 문구: "본 승차권은 탑승 시 제시하여야 합니다."
- [인쇄] 버튼 → window.print()

**6. 테스트 (readFileSync 기반):**
- SeatGrid.tsx에 'STATUS_COLOR', 'available', 'reserved', 'selected', 'unavailable', 'Row', 'Col' 존재 검증
- BusReservationPage.tsx에 'SeatGrid', 'onSeatClick', 'routeId' 존재 검증
- sys10.ts에 'sys10Handlers' export 검증
- handlers/index.ts에 'sys10Handlers' 존재 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys10 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/sys10-weekend-bus/SeatGrid.tsx contains `STATUS_COLOR` and `available` and `reserved` and `selected` and `unavailable`
    - navy-admin/src/pages/sys10-weekend-bus/SeatGrid.tsx contains `Row` and `Col` and `onSeatClick`
    - navy-admin/src/pages/sys10-weekend-bus/BusReservationPage.tsx contains `SeatGrid` and `routeId` and `operationDate`
    - navy-admin/src/pages/sys10-weekend-bus/TicketPrint.tsx contains `PrintableReport` and `승차권`
    - navy-admin/src/shared/api/mocks/handlers/sys10.ts exports `sys10Handlers`
    - navy-admin/src/shared/api/mocks/handlers/index.ts contains `sys10Handlers`
  </acceptance_criteria>
  <done>SeatGrid 좌석 그리드 컴포넌트 완성, 좌석 선택 예약 페이지 동작, 승차권 발급 가능, 게시판(sysCode=sys10) 재사용 연동</done>
</task>

<task type="auto">
  <name>Task 2: 예약현황 + 배차관리 + 예약시간관리 + 사용현황 페이지</name>
  <files>
    navy-admin/src/pages/sys10-weekend-bus/BusReservationStatusPage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/BusDispatchPage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/BusSchedulePage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/BusUsagePage.tsx,
    navy-admin/src/__tests__/sys10/weekend-bus.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/sys10-weekend-bus/index.tsx,
    navy-admin/src/pages/sys10-weekend-bus/SeatGrid.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys10.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/SearchForm/SearchForm.tsx
  </read_first>
  <action>
**1. BusReservationStatusPage.tsx (BUS-02):**
- DataTable: UI-SPEC 10-D 컬럼 (operationDate, route, direction, departureTime, totalSeats, reservedCount, unit, userName, seatNo, status StatusBadge)
- StatusBadge colorMap: reserved='예약됨'/green, cancelled='취소됨'/default, waiting='대기중'/gold
- SearchForm: operationDate(RangePicker), routeId(Select), unit(Input), keyword(Input 예약자 성명/군번)
- toolBarRender: [엑셀 다운로드][인쇄] — message.success Mock
- 행 액션: [승차권 발급] → TicketPrint Modal, [예약 취소] → ConfirmDialog + PUT /api/sys10/reservations/:id/cancel

**2. BusDispatchPage.tsx (BUS-03):**
- DataTable: UI-SPEC 10-E 컬럼 (direction, operationDate, departureTime, departure, destination, totalSeats, assignStatus StatusBadge, reservedCount)
- StatusBadge: assigned='배정'/green, unassigned='미배정'/default
- toolBarRender: [신규 등록]
- 행 액션: [배정][배정취소][좌석배치 확인]
  - 좌석배치 확인: Modal 내 SeatGrid readOnly=true, GET /api/sys10/dispatches/:id/seats
- CrudForm Modal(등록/수정): UI-SPEC 10-E 폼 (direction Select 상행/하행, operationDate DatePicker, departureTime TimePicker HH:mm, departure Input, destination Input, stopover Input, totalSeats InputNumber min:1 max:50, vehicleNo Input)

**3. BusSchedulePage.tsx (BUS-04, per D-27):**
- DataTable: UI-SPEC 10-F 컬럼 (routeId, rank, operationDate, reservationRank, openTime, closeTime)
- toolBarRender: [신규 등록]
- CrudForm Modal(등록/수정): routeId(Select), rank(Select 계급 목록), operationDate(DatePicker), reservationRank(InputNumber), openTime(TimePicker), closeTime(TimePicker)

**4. BusUsagePage.tsx (BUS-06):**
- DataTable: UI-SPEC 10-J 컬럼 (operationDate, route, unit, totalSeats, usedSeats, usageRate %)
- SearchForm: dateRange(RangePicker), routeId(Select), unit(Input)
- toolBarRender: [인쇄] — message.success Mock

**5. 테스트 업데이트:**
- BusReservationStatusPage.tsx에 'DataTable', 'reservedCount', 'StatusBadge' 존재 검증
- BusDispatchPage.tsx에 'SeatGrid', 'readOnly', 'CrudForm' 존재 검증
- BusSchedulePage.tsx에 'openTime', 'closeTime', 'reservationRank' 존재 검증
- BusUsagePage.tsx에 'usageRate', 'usedSeats' 존재 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys10 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/sys10-weekend-bus/BusReservationStatusPage.tsx contains `DataTable` and `reservedCount` and `승차권`
    - navy-admin/src/pages/sys10-weekend-bus/BusDispatchPage.tsx contains `SeatGrid` and `readOnly` and `CrudForm`
    - navy-admin/src/pages/sys10-weekend-bus/BusSchedulePage.tsx contains `openTime` and `closeTime` and `reservationRank`
    - navy-admin/src/pages/sys10-weekend-bus/BusUsagePage.tsx contains `usageRate` and `usedSeats`
  </acceptance_criteria>
  <done>예약현황/배차관리/예약시간관리/사용현황 4개 페이지 완성. BUS-02/03/04/06 커버. SeatGrid readOnly 배차 확인 동작.</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx vitest run src/__tests__/sys10 --reporter=verbose` 전체 통과
- `cd navy-admin && npx tsc --noEmit` 타입 에러 없음
- SeatGrid 컴포넌트가 BusReservationPage와 BusDispatchPage 양쪽에서 재사용됨
</verification>

<success_criteria>
- SeatGrid 좌석 그리드 4열+통로 레이아웃 + 범례 표시
- 예약 신청 → 좌석 선택 → 승차권 발급 플로우 동작
- 배차관리에서 좌석배치 확인(readOnly) 동작
- 예약시간관리 계급별 CRUD 동작
- 사용현황 조회 동작
- 게시판 sysCode=sys10 재사용
</success_criteria>

<output>
After completion, create `.planning/phases/05-b-3/05-02-SUMMARY.md`
</output>
