---
phase: 05-b-3
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - navy-admin/src/pages/sys10-weekend-bus/BusWaitlistPage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/BusViolatorPage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/ExternalLoginPage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/ExternalUserPage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/ExternalRegisterPage.tsx
  - navy-admin/src/pages/sys10-weekend-bus/index.tsx
  - navy-admin/src/app/router.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys10.ts
  - navy-admin/src/__tests__/sys10/weekend-bus.test.ts
autonomous: true
requirements:
  - BUS-05
  - BUS-07
  - BUS-08

must_haves:
  truths:
    - "대기자 목록에서 자동배정 버튼을 클릭하면 FIFO 순서대로 빈좌석에 자동 배정된다"
    - "수동배정으로 대기자를 선택하고 빈좌석 드롭다운에서 좌석을 개별 배정할 수 있다"
    - "위규자를 등록하면 제재기간 동안 예약 시도 시 차단된다"
    - "타군 사용자가 /sys10/login에서 별도 로그인하고, 승인 후 주말버스 예약만 접근 가능하다"
    - "타군 회원등록 신청 후 관리자가 승인/반려할 수 있다"
  artifacts:
    - path: "navy-admin/src/pages/sys10-weekend-bus/BusWaitlistPage.tsx"
      provides: "대기자 목록 + 자동배정 + 수동배정"
      min_lines: 80
    - path: "navy-admin/src/pages/sys10-weekend-bus/BusViolatorPage.tsx"
      provides: "위규자 CRUD + 제재기간 RangePicker"
      min_lines: 60
    - path: "navy-admin/src/pages/sys10-weekend-bus/ExternalLoginPage.tsx"
      provides: "타군 전용 로그인 페이지"
      min_lines: 60
    - path: "navy-admin/src/pages/sys10-weekend-bus/ExternalUserPage.tsx"
      provides: "타군 사용자 관리 + 승인/반려"
      min_lines: 60
  key_links:
    - from: "navy-admin/src/app/router.tsx"
      to: "ExternalLoginPage.tsx"
      via: "path: '/sys10/login' route outside RequireAuth"
      pattern: "sys10/login"
    - from: "navy-admin/src/pages/sys10-weekend-bus/BusWaitlistPage.tsx"
      to: "/api/sys10/waitlist/auto-assign"
      via: "useMutation POST"
      pattern: "auto-assign"
---

<objective>
SYS10 주말버스예약관리체계 나머지 기능(대기자/위규자/타군 인증)을 구현한다. /sys10/login 별도 경로와 타군 전용 레이아웃이 이 프로젝트 유일의 별도 인증 체계이다.

Purpose: 대기자 자동배정, 위규자 제재, 타군 사용자 별도 인증이라는 주말버스 고유 비즈니스 로직을 완성
Output: 대기자/위규자/타군 4개 페이지 + router.tsx 경로 추가 + MSW 핸들러 확장
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-b-3/05-CONTEXT.md
@.planning/phases/05-b-3/05-UI-SPEC.md (SYS10 섹션: 10-A, 10-G, 10-H, 10-I)
@.planning/phases/05-b-3/05-02-SUMMARY.md (Plan 02 산출물 — SeatGrid, sys10.ts 참조)

@navy-admin/src/app/router.tsx
@navy-admin/src/pages/sys10-weekend-bus/index.tsx
@navy-admin/src/pages/sys10-weekend-bus/SeatGrid.tsx
@navy-admin/src/shared/api/mocks/handlers/sys10.ts
@navy-admin/src/shared/ui/DataTable/DataTable.tsx
@navy-admin/src/shared/ui/CrudForm/CrudForm.tsx
@navy-admin/src/shared/ui/StatusBadge/
@navy-admin/src/shared/ui/ConfirmDialog/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 대기자관리(자동/수동배정) + 위규자관리(제재) + MSW 확장</name>
  <files>
    navy-admin/src/pages/sys10-weekend-bus/BusWaitlistPage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/BusViolatorPage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys10.ts,
    navy-admin/src/__tests__/sys10/weekend-bus.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/sys10-weekend-bus/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys10.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx
  </read_first>
  <action>
**1. BusWaitlistPage.tsx (BUS-05, per D-25/D-26):**
- DataTable: UI-SPEC 10-G 컬럼 (waitingNo, operationDate, route, userName, rank, unit, waitingDate, assignedSeat, status StatusBadge)
- StatusBadge colorMap: waiting='대기중'/gold, assigned='배정완료'/green, cancelled='취소됨'/default
- toolBarRender:
  - [자동 배정] (primary) — POST /api/sys10/waitlist/auto-assign. FIFO 순번대로 빈좌석에 배정. 결과: message.success('총 {n}명이 자동 배정되었습니다.') 또는 message.warning('배정 가능한 빈좌석이 없습니다.')
  - [엑셀 다운로드] — message.success Mock
- 행 액션: [수동 배정] — Modal 열기: seatId Select(빈좌석 목록 GET /api/sys10/waitlist/:waitingId/available-seats), 선택 후 POST /api/sys10/waitlist/:waitingId/manual-assign

**2. sys10.ts MSW에 대기자 API 추가:**
- GET /api/sys10/waitlist (대기자 목록, 페이지네이션, 노선/일자 필터)
- POST /api/sys10/waitlist/auto-assign — Mock: waiting 상태 중 순번 상위 N명을 assigned로 변경, 응답 { assignedCount: number }
- GET /api/sys10/waitlist/:id/available-seats — 해당 노선/일자의 빈좌석 id+seatNo 배열
- POST /api/sys10/waitlist/:id/manual-assign — body: { seatId }, 해당 대기자를 assigned로 변경
- Faker로 대기자 Mock 10건: waitingNo 1~10, 다양한 status

**3. BusViolatorPage.tsx (BUS-07, per D-28/D-29):**
- DataTable: UI-SPEC 10-H 컬럼 (userName, militaryId, rank, unit, violationType, sanctionStart, sanctionEnd, sanctionStatus StatusBadge, registeredAt)
- StatusBadge: sanctioned='제재중'/red (today >= sanctionStart && today <= sanctionEnd), sanction_expired='제재만료'/default (today > sanctionEnd)
  - 프론트엔드 dayjs 비교로 자동 계산 (per D-31 패턴과 동일)
- toolBarRender: [신규 등록][인쇄]
- CrudForm Modal (per D-28): militaryId(Input, required, max 10), userName(Input, required, max 20), rank(Select, required), unit(Input, required, max 100), violationType(Select required: 무단취소/탑승거부/기타), violationReason(TextArea, required, max 500), sanctionPeriod(DatePicker.RangePicker, required)

**4. sys10.ts MSW에 위규자 API 추가:**
- GET /api/sys10/violators (목록, 페이지네이션)
- POST /api/sys10/violators (등록)
- PUT /api/sys10/violators/:id (수정)
- DELETE /api/sys10/violators/:id (삭제)
- Faker로 위규자 Mock 5건: 제재중 3건 + 만료 2건

**5. index.tsx 라우트 업데이트:**
- `/sys10/1/3` placeholder를 실제 BusWaitlistPage로 교체
- `/sys10/1/8` placeholder를 실제 BusViolatorPage로 교체

**6. 테스트 업데이트:**
- BusWaitlistPage.tsx에 'auto-assign', 'manual-assign', 'waitingNo', 'StatusBadge' 존재 검증
- BusViolatorPage.tsx에 'sanctionPeriod', 'RangePicker', 'violationType', 'CrudForm' 존재 검증
- sys10.ts에 '/api/sys10/waitlist' 와 '/api/sys10/violators' 존재 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys10 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/sys10-weekend-bus/BusWaitlistPage.tsx contains `auto-assign` and `manual-assign` and `waitingNo`
    - navy-admin/src/pages/sys10-weekend-bus/BusViolatorPage.tsx contains `sanctionPeriod` and `RangePicker` and `violationType`
    - navy-admin/src/shared/api/mocks/handlers/sys10.ts contains `/api/sys10/waitlist` and `/api/sys10/violators`
    - navy-admin/src/pages/sys10-weekend-bus/index.tsx contains `BusWaitlistPage` and `BusViolatorPage` (no longer placeholder)
  </acceptance_criteria>
  <done>대기자 자동/수동 배정 동작, 위규자 CRUD + 제재기간 RangePicker + 상태 자동계산 동작. BUS-05/07 100% 커버.</done>
</task>

<task type="auto">
  <name>Task 2: 타군 전용 로그인 + 회원등록 + 타군 사용자 관리 + router.tsx 경로</name>
  <files>
    navy-admin/src/pages/sys10-weekend-bus/ExternalLoginPage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/ExternalRegisterPage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/ExternalUserPage.tsx,
    navy-admin/src/pages/sys10-weekend-bus/index.tsx,
    navy-admin/src/app/router.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys10.ts,
    navy-admin/src/__tests__/sys10/weekend-bus.test.ts
  </files>
  <read_first>
    navy-admin/src/app/router.tsx,
    navy-admin/src/pages/sys10-weekend-bus/index.tsx,
    navy-admin/src/pages/login/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys10.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx
  </read_first>
  <action>
**1. ExternalLoginPage.tsx (BUS-08, per D-21~D-24):**
- 페이지 중앙 정렬 레이아웃: 배경 #f5f5f5, Card 너비 400px, 상단에 해병대 로고/제목
- 제목: "주말버스 예약 시스템", 부제목: "타군 사용자 로그인"
- antd Form:
  - militaryId(Input, required, max 10, label='군번')
  - password(Input.Password, required, min 8, label='비밀번호')
- [로그인] Button (primary, block) — POST /api/sys10/external-auth/login. 성공 시 주말버스 예약 화면만 접근 가능한 경로로 이동
- [회원등록 신청] Button (default, block) — ExternalRegisterPage로 이동 (같은 페이지 Tabs 또는 별도 뷰)
- "패스워드를 잊으셨나요?" Link → 패스워드 초기화 Modal: militaryId + email 입력, 확인 시 message.success('임시 패스워드가 등록된 메일로 발송되었습니다.') per D-23

**2. ExternalRegisterPage.tsx (BUS-08, per D-22):**
- CrudForm 또는 standalone antd Form:
  - militaryBranch(Select required: 육군/공군/해군/해병대)
  - unitName(Input required, max 100)
  - militaryId(Input required, max 10)
  - rank(Select required, 계급 코드)
  - name(Input required, max 20)
  - position(Input required, max 50)
  - phone(Input, format 010-XXXX-XXXX)
  - email(Input required, email validation)
  - password(Input.Password required, min 8)
  - passwordConfirm(Input.Password required, password와 일치 검증)
- [등록 신청] Button → POST /api/sys10/external-users/register
- 성공 시: message.success('회원등록 신청이 완료되었습니다. 관리자 승인 후 로그인이 가능합니다.') + 로그인 페이지로 이동

**3. ExternalUserPage.tsx (BUS-08 관리자):**
- DataTable: UI-SPEC 10-I 컬럼 (militaryBranch, unitName, militaryId, rank, name, position, phone, email, status StatusBadge, registeredAt)
- StatusBadge: pending='신청대기'/gold, approved='승인'/green, rejected='반려'/red
- toolBarRender: [신규 등록][패스워드 초기화(선택 행 활성화)][엑셀 다운로드]
- 행 액션: [승인][반려] (status='pending' 시만 노출)
  - 승인: PUT /api/sys10/external-users/:id/approve → message.success
  - 반려: PUT /api/sys10/external-users/:id/reject + rejectReason TextArea ConfirmDialog
- [패스워드 초기화]: 선택된 행의 사용자에 대해 message.success('임시 패스워드가 메일로 발송되었습니다.') Mock

**4. sys10.ts MSW에 타군 API 추가:**
- POST /api/sys10/external-auth/login (militaryId + password → Mock 인증, 성공시 {token, user} 응답)
- POST /api/sys10/external-users/register (회원등록)
- GET /api/sys10/external-users (목록, 페이지네이션+상태 필터)
- PUT /api/sys10/external-users/:id/approve
- PUT /api/sys10/external-users/:id/reject (body: rejectReason)
- POST /api/sys10/external-users/:id/reset-password (Mock)
- Faker로 타군 사용자 Mock 8건: pending 3, approved 4, rejected 1

**5. router.tsx 수정 (per D-21):**
- RequireAuth 바깥에 새 경로 추가: `{ path: '/sys10/login', element: <ExternalLoginPage /> }`
- ExternalLoginPage는 lazy import 추가: `const ExternalLoginPage = lazy(() => import('@/pages/sys10-weekend-bus/ExternalLoginPage'))`
- withSuspense 래핑
- NOTE: 타군 사용자 로그인 후 접근 가능한 경로는 /sys10/1/2(예약)만. 현재 MVP에서는 메인 포탈 접근 불가 라우트 가드는 별도 구현 불요 (per D-24, 제한된 레이아웃은 UI 수준 가이드)

**6. index.tsx 라우트 업데이트:**
- `/sys10/1/9` placeholder를 실제 ExternalUserPage로 교체

**7. 테스트 업데이트:**
- ExternalLoginPage.tsx에 '타군 사용자 로그인', 'militaryId', 'password', '패스워드 초기화' 존재 검증
- ExternalRegisterPage.tsx에 'militaryBranch', 'passwordConfirm', '회원등록 신청' 존재 검증
- ExternalUserPage.tsx에 'approve', 'reject', 'StatusBadge', 'external-users' 존재 검증
- router.tsx에 'sys10/login' 존재 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys10 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/sys10-weekend-bus/ExternalLoginPage.tsx contains `타군 사용자 로그인` and `militaryId` and `패스워드 초기화`
    - navy-admin/src/pages/sys10-weekend-bus/ExternalRegisterPage.tsx contains `militaryBranch` and `passwordConfirm`
    - navy-admin/src/pages/sys10-weekend-bus/ExternalUserPage.tsx contains `approve` and `reject` and `external-users`
    - navy-admin/src/app/router.tsx contains `sys10/login`
    - navy-admin/src/shared/api/mocks/handlers/sys10.ts contains `/api/sys10/external-auth` and `/api/sys10/external-users`
  </acceptance_criteria>
  <done>타군 전용 로그인 /sys10/login 동작, 회원등록 신청 폼, 관리자 승인/반려, 패스워드 초기화 Mock 동작. BUS-08 100% 커버. SYS10 전체 완성.</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx vitest run src/__tests__/sys10 --reporter=verbose` 전체 통과
- `cd navy-admin && npx tsc --noEmit` 타입 에러 없음
- router.tsx에 /sys10/login 경로가 RequireAuth 바깥에 존재
- SYS10 모든 메뉴(1/1~1/9)에 대응하는 페이지 컴포넌트 존재
</verification>

<success_criteria>
- 대기자 자동배정(FIFO) 및 수동배정(좌석 Select) 동작
- 위규자 CRUD + 제재기간 RangePicker + 제재상태 자동계산 동작
- /sys10/login 타군 전용 로그인 페이지 동작
- 회원등록 신청 → 관리자 승인/반려 플로우 동작
- SYS10 주말버스예약관리체계 44개 프로세스(BUS-01~09) 전체 완성
</success_criteria>

<output>
After completion, create `.planning/phases/05-b-3/05-03-SUMMARY.md`
</output>
