---
phase: 05-b-3
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - navy-admin/src/pages/sys18-job-desc/index.tsx
  - navy-admin/src/pages/sys18-job-desc/JobDescFormPage.tsx
  - navy-admin/src/pages/sys18-job-desc/JobDescListPage.tsx
  - navy-admin/src/pages/sys18-job-desc/JobDescApprovalPage.tsx
  - navy-admin/src/pages/sys18-job-desc/OrgDiagnosisPage.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys18.ts
  - navy-admin/src/shared/api/mocks/handlers/index.ts
  - navy-admin/src/__tests__/sys18/job-desc.test.ts
autonomous: true
requirements:
  - JOB-01
  - JOB-03
  - JOB-04

must_haves:
  truths:
    - "직무기술서 5단계 Steps 폼(기본정보→업무분류→시간배분→역량→완료)으로 작성하고 임시저장/다음 단계가 동작한다"
    - "업무분류 단계에서 Form.List로 업무를 동적 추가/삭제하고, 비율 합계가 100% 실시간 검증된다"
    - "Tabs로 나의개인JD/직책JD/부서JD 3탭이 구분되고, 동일 폼 컴포넌트를 type prop으로 재사용한다"
    - "결재 요청 후 작성→1차결재→2차결재→완료 Steps 워크플로우가 동작한다"
    - "조직진단 대상을 등록하고, 진단기간 이후에는 수정/삭제가 비활성화된다"
  artifacts:
    - path: "navy-admin/src/pages/sys18-job-desc/JobDescFormPage.tsx"
      provides: "5단계 Steps + Form.List 비율 검증 + Tabs 3종 + 임시저장"
      min_lines: 150
    - path: "navy-admin/src/pages/sys18-job-desc/JobDescApprovalPage.tsx"
      provides: "결재 Steps 워크플로우 + 결재자 CRUD"
      min_lines: 100
    - path: "navy-admin/src/pages/sys18-job-desc/OrgDiagnosisPage.tsx"
      provides: "조직진단 대상 CRUD + 진단기간 이후 수정/삭제 비활성화"
      min_lines: 60
    - path: "navy-admin/src/shared/api/mocks/handlers/sys18.ts"
      provides: "직무기술서/결재/조직진단 MSW 핸들러"
      min_lines: 100
  key_links:
    - from: "navy-admin/src/pages/sys18-job-desc/JobDescFormPage.tsx"
      to: "antd Steps + Form.List"
      via: "Steps current state + Form.List dynamic fields"
      pattern: "Form\\.List"
    - from: "navy-admin/src/pages/sys18-job-desc/JobDescApprovalPage.tsx"
      to: "Phase 4 Steps 결재 패턴"
      via: "Steps direction=horizontal, current by approvalStep"
      pattern: "Steps.*current"
    - from: "navy-admin/src/shared/api/mocks/handlers/sys18.ts"
      to: "navy-admin/src/shared/api/mocks/handlers/index.ts"
      via: "sys18Handlers export"
      pattern: "sys18Handlers"
---

<objective>
SYS18 직무기술서관리체계 핵심 기능(5단계 작성 폼, 결재 워크플로우, 조직진단 대상)을 구현한다. 멀티스텝 폼 + Form.List 비율 검증이 이 Plan의 핵심 패턴이다.

Purpose: 직무기술서 작성과 결재라는 핵심 비즈니스 로직을 먼저 완성하여, Plan 05에서 관리자/통계/재사용 기능을 추가할 기반 마련
Output: 직무기술서 작성 5단계 폼 + 결재 Steps + 조직진단 CRUD + MSW 핸들러 + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-b-3/05-CONTEXT.md
@.planning/phases/05-b-3/05-UI-SPEC.md (SYS18 섹션: 18-A ~ 18-L)
@.planning/phases/05-b-3/05-RESEARCH.md (Pattern 2: Steps+Form, Pattern 3: 비율 검증)

@navy-admin/src/shared/ui/DataTable/DataTable.tsx
@navy-admin/src/shared/ui/CrudForm/CrudForm.tsx
@navy-admin/src/shared/ui/StatusBadge/
@navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx (Steps 결재 패턴)
@navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx (Form.List 패턴)
@navy-admin/src/shared/api/mocks/handlers/index.ts
@navy-admin/src/entities/subsystem/menus.ts (sys18 메뉴 구조)
</context>

<tasks>

<task type="auto">
  <name>Task 1: MSW 핸들러 + 라우트 분기 + 직무기술서 목록/작성(5단계 Steps 폼)</name>
  <files>
    navy-admin/src/shared/api/mocks/handlers/sys18.ts,
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys18-job-desc/index.tsx,
    navy-admin/src/pages/sys18-job-desc/JobDescListPage.tsx,
    navy-admin/src/pages/sys18-job-desc/JobDescFormPage.tsx,
    navy-admin/src/__tests__/sys18/job-desc.test.ts
  </files>
  <read_first>
    navy-admin/src/shared/api/mocks/handlers/index.ts,
    navy-admin/src/pages/sys18-job-desc/index.tsx,
    navy-admin/src/entities/subsystem/menus.ts,
    navy-admin/src/pages/sys02-survey/SurveyQuestionEditor.tsx,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx
  </read_first>
  <action>
**1. MSW 핸들러 (sys18.ts):**
- Faker.js(ko locale)로 직무기술서 Mock 데이터 20건 생성
- 각 JD 필드: id, type('personal'|'position'|'department'), diagnosisId, diagnosisName, writerId, writerName, writerMilitaryId, rank, position, department, unit, phone, status('draft'|'submitted'|'approved_1st'|'completed'|'rejected'), tasks[](taskType, taskName, taskDetail, ratio), timeAllocation[](taskName, weeklyHours, percentage), competency{requiredCompetency, requiredQualification, educationRequired, experienceRequired, specialNote}, createdAt, submittedAt
- 조직진단 Mock 5건: diagnosisName, diagnosisUnit, writePeriodStart/End, diagnosisPeriodStart/End, targetUsers[], targetCount, progressStatus
- API 엔드포인트:
  - GET /api/sys18/job-descs (페이지네이션 + type/status/diagnosisId 필터)
  - POST /api/sys18/job-descs (등록)
  - PUT /api/sys18/job-descs/:id (수정)
  - DELETE /api/sys18/job-descs/:id
  - GET /api/sys18/job-descs/:id (상세)
  - PUT /api/sys18/job-descs/:id/draft (임시저장)
  - PUT /api/sys18/job-descs/:id/submit (결재 요청: status → submitted)
  - POST /api/sys18/job-descs/:id/copy (복사)
  - GET /api/sys18/org-diagnosis (조직진단 목록)
  - POST /api/sys18/org-diagnosis (등록)
  - PUT /api/sys18/org-diagnosis/:id (수정)
  - DELETE /api/sys18/org-diagnosis/:id
  - GET /api/sys18/standard-hours (표준업무시간 — Plan 05에서 사용)
- `export const sys18Handlers = [...]`
- handlers/index.ts에 `import { sys18Handlers } from './sys18'` 추가, handlers 배열에 스프레드

**2. index.tsx 라우트 분기 (per D-33):**
- `/sys18/1/1` → BoardListPage (sysCode='sys18') lazy import (per D-34, JOB-05) — 공지/질의응답/자료실 3개 게시판
- `/sys18/1/2` → OrgDiagnosisPage
- `/sys18/1/3` → JobDescListPage (Tabs: 나의개인JD/직책JD/부서JD)
- `/sys18/1/4` → JobDescApprovalPage
- `/sys18/1/5` → JobDescAdminPage (Plan 05에서 구현, placeholder)
- `/sys18/2/1` → CodeMgmtPage lazy import (per D-35, JOB-06)
- `/sys18/2/2` → StandardWorkTimePage (Plan 05에서 구현, placeholder)
- `/sys18/2/3` → AuthGroupPage lazy import (per D-36, JOB-07)

**3. JobDescListPage.tsx (JOB-01 목록, per D-16):**
- antd Tabs items 3개: 나의 개인직무기술서(type='personal'), 직책 개인직무기술서(type='position'), 부서직무기술서(type='department')
- 각 탭 내 DataTable: UI-SPEC 18-A 컬럼 (diagnosisName, writerName(개인JD만), department, position, status StatusBadge, createdAt, submittedAt)
- StatusBadge: draft='작성중'/default, submitted='제출됨'/orange, completed='완료'/green, rejected='반려'/red
- toolBarRender: [직무기술서 작성][인쇄]
- 행 액션: [상세][수정][복사][삭제][인쇄]
- [직무기술서 작성] → JobDescFormPage로 이동 (같은 탭의 type 전달)

**4. JobDescFormPage.tsx (JOB-01 5단계 폼, per D-13~D-16, D-19):**
- Props: `type: 'personal' | 'position' | 'department'`, `id?: string` (수정 시)
- antd Steps direction="horizontal", items: [{title:'기본정보'}, {title:'업무분류/비율'}, {title:'시간배분'}, {title:'역량/자격요건'}, {title:'완료/제출'}]
- state: `const [current, setCurrent] = useState(0)`
- antd Form (form instance) 단일 인스턴스로 5단계 공유

**Step 1 (기본정보, UI-SPEC 18-C):**
- diagnosisId(Select required, 조직진단 목록에서 선택)
- writerMilitaryId(Input 읽기전용, 로그인 사용자 자동)
- writerName(Input 읽기전용), rank(Input 읽기전용), unit(Input 읽기전용)
- position(Input required, max 50), department(Input required, max 100)
- phone(Input), submissionPeriod(text 읽기전용, 조직진단에서 자동)
- 대리작성 모드 (per D-19): 부서관리자 권한일 때 상단에 "작성자 선택" Select 드롭다운 추가 (사용자 목록)

**Step 2 (업무분류/비율, UI-SPEC 18-D, per D-14):**
- Form.List name="tasks" (Phase 4 SurveyQuestionEditor Form.List 패턴 재사용)
- 각 항목 필드: taskType(Select required: 정책/관리/지원/기타), taskName(Input required, max 100), taskDetail(TextArea, max 300), ratio(InputNumber required, min 0, max 100, addonAfter='%')
- [업무 추가] Button으로 항목 추가, 각 항목 우측 [삭제] MinusCircleOutlined 아이콘
- 비율 합계 실시간 표시: `Form.useWatch('tasks', form)` → reduce sum → Typography.Text type={sum===100?'success':'danger'} 텍스트 "합계: {sum}% (100%가 되어야 합니다)"
- '다음 단계' 클릭 시 합계 100% 아니면 message.error('업무 비율 합계가 100%가 되어야 합니다. 현재: {n}%')

**Step 3 (시간배분, UI-SPEC 18-E):**
- standardHours(InputNumber 읽기전용, GET /api/sys18/standard-hours에서 로드)
- Form.List name="taskTimeList": Step 2의 tasks 연동 — taskName(text 읽기전용), weeklyHours(InputNumber required), percentage(text 자동계산 = weeklyHours/standardHours*100%)

**Step 4 (역량/자격요건, UI-SPEC 18-F):**
- requiredCompetency(TextArea, max 500)
- requiredQualification(TextArea, max 500)
- educationRequired(TextArea, max 300)
- experienceRequired(TextArea, max 300)
- specialNote(TextArea, max 300)

**Step 5 (완료/제출, UI-SPEC 18-G):**
- antd Descriptions (layout="vertical", column=2): 기본정보 요약(성명, 직책, 부서), 업무분류 건수, 시간배분 요약, 상태
- 결재선 확인 영역: 1차 결재자/2차 결재자 (부서별 설정값 GET /api/sys18/approvers?department= 조회)
- [결재 요청] → PUT /api/sys18/job-descs/:id/submit → message.success('결재 요청이 완료되었습니다.')

**각 단계 하단 버튼 (Space):**
- Step 1: [임시저장][다음 단계]
- Step 2~4: [이전 단계][임시저장][다음 단계]
- Step 5: [이전 단계][결재 요청]
- 임시저장: PUT /api/sys18/job-descs/:id/draft → message.success('임시저장되었습니다.')
- 다음 단계: form.validateFields() 현재 단계 필드만 → 성공 시 setCurrent(prev+1)

**5. 테스트 (readFileSync 기반):**
- sys18.ts에 'sys18Handlers' export 검증
- handlers/index.ts에 'sys18Handlers' 존재 검증
- JobDescFormPage.tsx에 'Steps', 'Form.List', 'ratio', '임시저장', '결재 요청', 'current' 존재 검증
- JobDescListPage.tsx에 'Tabs', 'personal', 'department', 'DataTable' 존재 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys18 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/shared/api/mocks/handlers/sys18.ts exports `sys18Handlers`
    - navy-admin/src/shared/api/mocks/handlers/index.ts contains `sys18Handlers`
    - navy-admin/src/pages/sys18-job-desc/JobDescFormPage.tsx contains `Steps` and `Form.List` and `ratio` and `임시저장` and `결재 요청`
    - navy-admin/src/pages/sys18-job-desc/JobDescFormPage.tsx contains `taskType` and `taskName` and `weeklyHours`
    - navy-admin/src/pages/sys18-job-desc/JobDescListPage.tsx contains `Tabs` and `personal` and `department`
    - navy-admin/src/pages/sys18-job-desc/index.tsx contains route mappings for `/sys18/1/1` through `/sys18/2/3`
  </acceptance_criteria>
  <done>직무기술서 5단계 작성 폼(기본정보/업무분류 비율검증/시간배분/역량/제출) + Tabs 3탭(개인/직책/부서) + 임시저장 + MSW 핸들러 완성. JOB-01 100% 커버.</done>
</task>

<task type="auto">
  <name>Task 2: 결재 Steps 워크플로우 + 결재자 CRUD + 조직진단 대상 관리</name>
  <files>
    navy-admin/src/pages/sys18-job-desc/JobDescApprovalPage.tsx,
    navy-admin/src/pages/sys18-job-desc/OrgDiagnosisPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys18.ts,
    navy-admin/src/__tests__/sys18/job-desc.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/sys17-inspection/InspectionApprovalPage.tsx,
    navy-admin/src/pages/sys18-job-desc/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys18.ts,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/ConfirmDialog/ConfirmDialog.tsx
  </read_first>
  <action>
**1. JobDescApprovalPage.tsx (JOB-03, per D-17/D-18):**
- Tabs 2개: 결재대기 | 결재자관리

**결재대기 탭:**
- DataTable: UI-SPEC 18-K 결재대기 컬럼 (writerName, department, jobDescType(개인/부서), submittedAt, approvalStep(1차/2차), status StatusBadge)
- StatusBadge: submitted='결재대기'/gold, approved_1st='1차완료'/blue, completed='결재완료'/green, rejected='반려'/red
- 행 클릭 시 상세 Modal:
  - antd Steps direction="horizontal", items: [작성, 1차 결재, 2차 결재, 완료]
  - step 0=finish(항상), step 1: submitted→process, approved_1st→finish, rejected→error, step 2: approved_1st→process, completed→finish, step 3: completed→finish
  - current: submitted→1, approved_1st→2, completed→3, rejected→1(error)
  - 상세 Descriptions: 작성자 정보 + 업무분류 목록 + 시간배분 + 역량
- 결재 액션 버튼:
  - [승인] (primary, green) → PUT /api/sys18/approvals/:id/approve
  - [반려] (danger) → rejectReason TextArea ConfirmDialog → PUT /api/sys18/approvals/:id/reject (body: rejectReason)
- 반려 시 재결재요청: 반려된 JD 목록에서 [재결재 요청] 버튼 → status를 submitted로 복원

**결재자관리 탭 (per D-17):**
- DataTable: UI-SPEC 18-K 결재자 컬럼 (department, firstApprover, secondApprover)
- CrudForm Modal: department(Select required), firstApproverId(Select required, 사용자 목록), secondApproverId(Select required, 사용자 목록)
- CRUD: GET/POST/PUT/DELETE /api/sys18/approvers

**2. OrgDiagnosisPage.tsx (JOB-04):**
- DataTable: UI-SPEC 18-L 컬럼 (diagnosisName 링크, diagnosisUnit, writePeriodStart, writePeriodEnd, diagnosisPeriodStart, diagnosisPeriodEnd, targetCount, progressStatus StatusBadge)
- toolBarRender: [신규 등록]
- 행 액션: 진단기간(diagnosisPeriodStart) 이전이면 [수정][삭제], 이후이면 비활성화 + antd Tooltip "진단기간 이후에는 수정/삭제할 수 없습니다."
  - dayjs 비교: dayjs().isBefore(dayjs(record.diagnosisPeriodStart)) → 수정/삭제 활성화
- CrudForm Modal: diagnosisName(Input required, max 100), diagnosisUnit(Input required, max 100), writePeriod(DatePicker.RangePicker required), diagnosisPeriod(DatePicker.RangePicker required), targetUsers(Select mode="multiple", 사용자 목록)

**3. sys18.ts MSW에 결재/조직진단 API 추가:**
- GET /api/sys18/approvals (결재대기 목록, status 필터)
- PUT /api/sys18/approvals/:id/approve
- PUT /api/sys18/approvals/:id/reject (body: rejectReason)
- PUT /api/sys18/approvals/:id/resubmit (재결재요청)
- GET /api/sys18/approvers (결재자 목록)
- POST/PUT/DELETE /api/sys18/approvers/:id (결재자 CRUD)
- org-diagnosis CRUD API는 Task 1에서 이미 추가됨
- Faker로 결재대기 Mock 8건 (submitted 3, approved_1st 2, completed 2, rejected 1)
- Faker로 결재자 Mock 5건 (부서별 1차/2차 결재자)

**4. 테스트 업데이트:**
- JobDescApprovalPage.tsx에 'Steps', 'approve', 'reject', 'rejectReason', '재결재' 존재 검증
- JobDescApprovalPage.tsx에 'firstApprover', 'secondApprover', 'CrudForm' 존재 검증
- OrgDiagnosisPage.tsx에 'diagnosisPeriod', 'targetUsers', 'isBefore' 존재 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys18 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/sys18-job-desc/JobDescApprovalPage.tsx contains `Steps` and `approve` and `reject` and `rejectReason`
    - navy-admin/src/pages/sys18-job-desc/JobDescApprovalPage.tsx contains `firstApprover` and `secondApprover`
    - navy-admin/src/pages/sys18-job-desc/OrgDiagnosisPage.tsx contains `diagnosisPeriod` and `targetUsers`
    - navy-admin/src/pages/sys18-job-desc/OrgDiagnosisPage.tsx contains `isBefore` or dayjs comparison for edit/delete disable
    - navy-admin/src/shared/api/mocks/handlers/sys18.ts contains `/api/sys18/approvals` and `/api/sys18/approvers`
  </acceptance_criteria>
  <done>결재 Steps(작성→1차→2차→완료) + 반려/재결재 동작, 결재자 부서별 CRUD, 조직진단 CRUD + 진단기간 이후 수정 비활성화. JOB-03/04 100% 커버.</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx vitest run src/__tests__/sys18 --reporter=verbose` 전체 통과
- `cd navy-admin && npx tsc --noEmit` 타입 에러 없음
- JobDescFormPage 5단계가 Steps로 시각화되고 Form.List 비율 합계 검증 동작
</verification>

<success_criteria>
- 직무기술서 5단계 Steps 폼 동작 (기본정보/업무분류 비율 100%/시간배분/역량/완료)
- Tabs 3탭 (나의개인JD/직책JD/부서JD) 구분 동작
- 임시저장 + 결재요청 플로우 동작
- 결재 작성→1차→2차→완료 Steps 시각화 + 승인/반려/재결재 동작
- 결재자 부서별 CRUD 동작
- 조직진단 CRUD + 진단기간 이후 수정/삭제 비활성화
</success_criteria>

<output>
After completion, create `.planning/phases/05-b-3/05-04-SUMMARY.md`
</output>
