---
phase: 05-b-3
plan: 05
type: execute
wave: 2
depends_on: ["05-04"]
files_modified:
  - navy-admin/src/pages/sys18-job-desc/JobDescAdminPage.tsx
  - navy-admin/src/pages/sys18-job-desc/StandardWorkTimePage.tsx
  - navy-admin/src/pages/sys18-job-desc/index.tsx
  - navy-admin/src/shared/api/mocks/handlers/sys18.ts
  - navy-admin/src/__tests__/sys18/job-desc.test.ts
autonomous: true
requirements:
  - JOB-02
  - JOB-05
  - JOB-06
  - JOB-07
  - JOB-08

must_haves:
  truths:
    - "관리자가 개인/부서 직무기술서를 조회하고 검토결과 입력/의견보내기/반송을 할 수 있다"
    - "관리자 통계에서 부대별 작성현황 Bar차트, 직급별 Column차트, 업무분류 Pie차트가 표시된다"
    - "표준업무시간을 신분별로 CRUD하고, 적용상태(적용중/적용예정/적용만료)가 dayjs로 자동계산된다"
    - "게시판(공지/질의응답/자료실)은 Phase 1 공통게시판 sysCode=sys18로 재사용된다"
    - "코드관리는 Phase 1 common/code-mgmt/ lazy 재사용된다"
    - "사용자권한관리는 Phase 1 common/auth-group/ lazy 재사용된다"
  artifacts:
    - path: "navy-admin/src/pages/sys18-job-desc/JobDescAdminPage.tsx"
      provides: "관리자 조회 목록 + 검토결과/의견/반송 Modal + 통계 차트 3종"
      min_lines: 120
    - path: "navy-admin/src/pages/sys18-job-desc/StandardWorkTimePage.tsx"
      provides: "표준업무시간 CRUD + 적용상태 자동계산"
      min_lines: 60
  key_links:
    - from: "navy-admin/src/pages/sys18-job-desc/JobDescAdminPage.tsx"
      to: "@ant-design/charts"
      via: "import Bar, Column, Pie from @ant-design/charts"
      pattern: "@ant-design/charts"
    - from: "navy-admin/src/pages/sys18-job-desc/StandardWorkTimePage.tsx"
      to: "dayjs"
      via: "dayjs comparison for active/upcoming/expired status"
      pattern: "dayjs"
    - from: "navy-admin/src/pages/sys18-job-desc/index.tsx"
      to: "Phase 1 공통 페이지"
      via: "lazy import BoardListPage, CodeMgmtPage, AuthGroupPage"
      pattern: "lazy.*board|code-mgmt|auth-group"
---

<objective>
SYS18 직무기술서관리체계 나머지 기능(관리자 조회/통계, 표준업무시간, 재사용 컴포넌트)을 구현한다. Phase 1 공통 기능(게시판/코드/권한) 재사용으로 JOB-05~07을 최소 코드로 완성한다.

Purpose: 관리자 검토/통계와 시스템 관리 기능을 완성하여 SYS18 전체 47개 프로세스를 100% 커버
Output: 관리자 조회/통계 + 표준업무시간 + 재사용 연결 + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-b-3/05-CONTEXT.md
@.planning/phases/05-b-3/05-UI-SPEC.md (SYS18 섹션: 18-H ~ 18-M)
@.planning/phases/05-b-3/05-04-SUMMARY.md (Plan 04 산출물 — sys18.ts, index.tsx 참조)

@navy-admin/src/shared/ui/DataTable/DataTable.tsx
@navy-admin/src/shared/ui/StatusBadge/
@navy-admin/src/pages/sys13-knowledge/KnowledgeStatsPage.tsx (차트 패턴)
@navy-admin/src/pages/common/board/BoardListPage.tsx
@navy-admin/src/pages/common/code-mgmt/
@navy-admin/src/pages/common/auth-group/
@navy-admin/src/shared/api/mocks/handlers/sys18.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 관리자 조회/검토/통계 페이지</name>
  <files>
    navy-admin/src/pages/sys18-job-desc/JobDescAdminPage.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys18.ts,
    navy-admin/src/__tests__/sys18/job-desc.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/sys18-job-desc/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys18.ts,
    navy-admin/src/pages/sys13-knowledge/KnowledgeStatsPage.tsx,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/SearchForm/SearchForm.tsx,
    navy-admin/src/shared/ui/ConfirmDialog/ConfirmDialog.tsx
  </read_first>
  <action>
**1. JobDescAdminPage.tsx (JOB-02, per D-20):**
- Tabs 3개: 개인직무기술서 | 부서직무기술서 | 통계

**개인직무기술서 탭 (UI-SPEC 18-H):**
- DataTable 컬럼: diagnosisName, writerName, department, position, rank, status StatusBadge, submittedAt, reviewResult
- StatusBadge: draft/submitted/completed/rejected (Plan 04와 동일 colorMap)
- SearchForm: diagnosisName(Select), dateRange(RangePicker), unit(Input), position(Input)
- toolBarRender: [인쇄][엑셀 다운로드] — message.success Mock
- 행 액션: [상세][검토결과 입력][의견 보내기][반송]

**부서직무기술서 탭:** 동일 구조, type='department' 필터

**검토결과 입력 Modal (UI-SPEC 18-I):**
- antd Modal, title: "검토결과 입력"
- reviewResult(Select required: 적합/수정필요/부적합)
- reviewComment(TextArea required, max 500)
- [확인] → PUT /api/sys18/job-descs/:id/review (body: {reviewResult, reviewComment})

**의견보내기 Modal (UI-SPEC 18-I):**
- antd Modal, title: "의견 보내기"
- opinionContent(TextArea required, max 500)
- [보내기] → POST /api/sys18/job-descs/:id/opinion (body: {opinionContent})

**반송 ConfirmDialog (UI-SPEC 18-I):**
- title: "직무기술서 반송", 추가 입력: returnReason TextArea (필수, max 500)
- [확인] → PUT /api/sys18/job-descs/:id/return (body: {returnReason})

**통계 탭 (UI-SPEC 18-J, per D-37):**
- **차트 1 (Bar, 수평):** 부대별 작성현황 — y축: 부대 목록, x축: 작성완료/작성중/미작성 건수 스택. color: ['#52c41a','#1890ff','#d9d9d9']. height 300
- **차트 2 (Column):** 직급별 현황 — x축: 계급 구분, y축: 완료 건수. color '#003366'. height 300
- **차트 3 (Pie):** 업무분류별 분포 — 정책/관리/지원/기타 4분류. 4-색 팔레트. height 300
- SearchForm: diagnosisName(Select), dateRange(RangePicker), unit(Input)
- @ant-design/charts: `import { Bar, Column, Pie } from '@ant-design/charts'`

**2. sys18.ts MSW에 관리자 API 추가:**
- GET /api/sys18/job-descs/admin (관리자용 목록, type/status/keyword 필터)
- PUT /api/sys18/job-descs/:id/review (검토결과)
- POST /api/sys18/job-descs/:id/opinion (의견보내기)
- PUT /api/sys18/job-descs/:id/return (반송)
- GET /api/sys18/stats/by-unit (부대별 작성현황: unit, completed, inProgress, notStarted)
- GET /api/sys18/stats/by-rank (직급별 현황: rank, count)
- GET /api/sys18/stats/by-task-type (업무분류별: taskType, count)
- Faker로 통계 Mock 데이터 생성

**3. 테스트 업데이트:**
- JobDescAdminPage.tsx에 'Bar', 'Column', 'Pie', '@ant-design/charts' import 검증
- JobDescAdminPage.tsx에 'reviewResult', 'opinionContent', 'returnReason' 존재 검증
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys18 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/sys18-job-desc/JobDescAdminPage.tsx contains `Bar` and `Column` and `Pie` from `@ant-design/charts`
    - navy-admin/src/pages/sys18-job-desc/JobDescAdminPage.tsx contains `reviewResult` and `opinionContent` and `returnReason`
    - navy-admin/src/pages/sys18-job-desc/JobDescAdminPage.tsx contains `검토결과 입력` and `의견 보내기` and `반송`
    - navy-admin/src/shared/api/mocks/handlers/sys18.ts contains `/api/sys18/stats` and `/api/sys18/job-descs/admin`
  </acceptance_criteria>
  <done>관리자 조회(개인/부서 Tabs) + 검토결과 입력/의견보내기/반송 Modal + 통계 차트 3종 완성. JOB-02 100% 커버.</done>
</task>

<task type="auto">
  <name>Task 2: 표준업무시간 관리 + Phase 1 재사용 연결(게시판/코드/권한) + index.tsx 완성</name>
  <files>
    navy-admin/src/pages/sys18-job-desc/StandardWorkTimePage.tsx,
    navy-admin/src/pages/sys18-job-desc/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys18.ts,
    navy-admin/src/__tests__/sys18/job-desc.test.ts
  </files>
  <read_first>
    navy-admin/src/pages/sys18-job-desc/index.tsx,
    navy-admin/src/shared/api/mocks/handlers/sys18.ts,
    navy-admin/src/pages/common/board/BoardListPage.tsx,
    navy-admin/src/pages/common/code-mgmt/CodeGroupPage.tsx,
    navy-admin/src/pages/common/auth-group/AuthGroupPage.tsx,
    navy-admin/src/shared/ui/DataTable/DataTable.tsx,
    navy-admin/src/shared/ui/CrudForm/CrudForm.tsx,
    navy-admin/src/shared/ui/StatusBadge/StatusBadge.tsx
  </read_first>
  <action>
**1. StandardWorkTimePage.tsx (JOB-08, per D-30/D-31):**
- DataTable: UI-SPEC 18-M 컬럼 (rankCategory, standardHours(주간, 시간), periodStart, periodEnd, applyStatus StatusBadge)
- StatusBadge 자동계산 로직 (프론트엔드 dayjs 비교, per D-31):
  ```
  import dayjs from 'dayjs'
  const today = dayjs()
  if (today.isAfter(dayjs(record.periodEnd))) → 'expired' (적용만료, default)
  else if (today.isBefore(dayjs(record.periodStart))) → 'upcoming' (적용예정, blue)
  else → 'active' (적용중, green)
  ```
- colorMap: active='적용중'/green, upcoming='적용예정'/blue, expired='적용만료'/default
- toolBarRender: [신규 등록]
- CrudForm Modal: rankCategory(Select required: 장관/영관/부사관/원사/병), standardHours(InputNumber required, min 1, max 60, label='표준업무시간 (주간, 시간)'), applyPeriod(DatePicker.RangePicker required, label='적용기간')

**2. sys18.ts MSW에 표준업무시간 API 추가:**
- GET /api/sys18/standard-hours (목록, 페이지네이션)
- POST /api/sys18/standard-hours (등록)
- PUT /api/sys18/standard-hours/:id (수정)
- DELETE /api/sys18/standard-hours/:id (삭제)
- Faker로 표준업무시간 Mock 5건: 장관 40h, 영관 42h, 부사관 44h, 원사 44h, 병 40h. 적용기간 다양 (만료 1, 적용중 3, 예정 1)

**3. index.tsx 라우트 완성 (Phase 1 재사용 연결):**
- `/sys18/1/1` → Phase 1 공통게시판 lazy import 재사용 (per D-34, JOB-05)
  ```typescript
  const BoardListPage = lazy(() => import('@/pages/common/board/BoardListPage'))
  ```
  - 게시판 Tabs 3개: 공지사항(boardType='notice'), 질의응답(boardType='qna'), 자료실(boardType='library')
  - sysCode='sys18' 전달
- `/sys18/2/1` → Phase 1 코드관리 lazy import 재사용 (per D-35, JOB-06)
  ```typescript
  const CodeGroupPage = lazy(() => import('@/pages/common/code-mgmt/CodeGroupPage'))
  ```
- `/sys18/2/2` → StandardWorkTimePage (placeholder 교체)
- `/sys18/2/3` → Phase 1 권한관리 lazy import 재사용 (per D-36, JOB-07)
  ```typescript
  const AuthGroupPage = lazy(() => import('@/pages/common/auth-group/AuthGroupPage'))
  ```
- `/sys18/1/5` → JobDescAdminPage (placeholder 교체)

**4. 테스트 업데이트:**
- StandardWorkTimePage.tsx에 'dayjs', 'isAfter', 'isBefore', 'rankCategory', 'standardHours', 'CrudForm' 존재 검증
- index.tsx에 'BoardListPage', 'code-mgmt', 'auth-group', 'StandardWorkTimePage', 'JobDescAdminPage' 존재 검증 (no more placeholders)
  </action>
  <verify>
    <automated>cd navy-admin && npx vitest run src/__tests__/sys18 --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - navy-admin/src/pages/sys18-job-desc/StandardWorkTimePage.tsx contains `dayjs` and (`isAfter` or `isBefore`) and `rankCategory` and `standardHours`
    - navy-admin/src/pages/sys18-job-desc/StandardWorkTimePage.tsx contains `CrudForm` and `RangePicker`
    - navy-admin/src/pages/sys18-job-desc/index.tsx contains `BoardListPage` or `board` import
    - navy-admin/src/pages/sys18-job-desc/index.tsx contains `code-mgmt` or `CodeGroupPage`
    - navy-admin/src/pages/sys18-job-desc/index.tsx contains `auth-group` or `AuthGroupPage`
    - navy-admin/src/pages/sys18-job-desc/index.tsx contains `StandardWorkTimePage` and `JobDescAdminPage` (no placeholder text)
    - navy-admin/src/shared/api/mocks/handlers/sys18.ts contains `/api/sys18/standard-hours`
  </acceptance_criteria>
  <done>표준업무시간 CRUD + 적용상태 dayjs 자동계산, 게시판/코드관리/권한관리 Phase 1 재사용 연결, SYS18 전체 라우트 완성. JOB-05~08 100% 커버. SYS18 전체 완성.</done>
</task>

</tasks>

<verification>
- `cd navy-admin && npx vitest run src/__tests__/sys18 --reporter=verbose` 전체 통과
- `cd navy-admin && npx tsc --noEmit` 타입 에러 없음
- SYS18 모든 메뉴(1/1~1/5, 2/1~2/3)에 대응하는 페이지 컴포넌트 존재 (placeholder 없음)
</verification>

<success_criteria>
- 관리자 조회(개인/부서 Tabs) + 검토결과/의견/반송 동작
- 통계 차트 3종(Bar/Column/Pie) 표시
- 표준업무시간 CRUD + 적용상태 자동계산 동작
- 게시판(공지/질의응답/자료실) sysCode=sys18 재사용 동작
- 코드관리/권한관리 Phase 1 재사용 동작
- SYS18 직무기술서관리체계 47개 프로세스(JOB-01~08) 전체 완성
</success_criteria>

<output>
After completion, create `.planning/phases/05-b-3/05-05-SUMMARY.md`
</output>
